// Auto-generated by Manual/generate_help_data.py
// Do not edit manually
#pragma once
#include <string>
#include <map>
#include <vector>
#include <algorithm>

struct HelpEntry {
    std::string en;
    std::string zh;
    std::string ja;
    const std::string& get(const std::string& lang) const {
        if (lang == "zh") return zh;
        if (lang == "ja") return ja;
        return en;
    }
};

struct ModuleHelpData {
    std::string name;
    HelpEntry description;
    std::vector<std::pair<std::string, HelpEntry>> entries;
};

inline std::map<std::string, ModuleHelpData> initHelpData() {
    std::map<std::string, ModuleHelpData> data;
    // ADGenerator (35 entries)
    {
        ModuleHelpData m;
        m.name = "AD Generator";
        m.description = {"Triple AD envelope generator with dual processing modes, global parameter controls, and per-track band pass filtering", "三軌 AD Envelope 生成器，具備雙處理模式、Global 參數控制與各軌 Band Pass Filter", "デュアル処理モード、グローバルパラメータ制御、トラック別バンドパスフィルターを備えた3トラックADエンベロープ生成器"};
        m.entries.push_back({"Attack All", {"Global attack time offset (-1.0 to +1.0). Adds up to ±0.5s offset to all track attack times. Useful for simultaneously shifting all envelopes faster or slower.", "Global Attack 時間偏移（-1.0 至 +1.0）。對所有軌道 Attack 時間加上最多 +-0.5s 偏移。", "グローバルアタック時間オフセット（-1.0〜+1.0）。全トラックに最大+-0.5sのオフセットを追加。"}});
        m.entries.push_back({"Decay All", {"Global decay time offset (-1.0 to +1.0). Same behavior as Attack All but for decay times.", "Global Decay 時間偏移（-1.0 至 +1.0）。與 Attack All 相同但作用於 Decay 時間。", "グローバルディケイ時間オフセット（-1.0〜+1.0）。Attack Allと同様だがディケイ時間に適用。"}});
        m.entries.push_back({"Auto Route", {"Auto-route toggle (default: ON). When enabled, Track 1 input is internally routed to all three tracks, allowing single-source multi-band envelope extraction when combined with different BPF frequencies.", "Auto-route 開關（預設: ON）。啟用時 Track 1 輸入路由至所有三軌。", "Auto-routeトグル（デフォルト: ON）。有効時、Track 1入力を全3トラックにルーティング。"}});
        m.entries.push_back({"Track 1 Trigger", {"Trigger or audio input for Track 1. In Trigger-only mode, uses Schmitt trigger detection. In Hybrid mode, requires >9.5V for trigger while following audio dynamics.", "軌道 1 Trigger 或 Audio 輸入。", "トラック1トリガーまたはオーディオ入力。"}});
        m.entries.push_back({"Track 2 Trigger", {"Trigger or audio input for Track 2. Same behavior as Track 1.", "軌道 2 Trigger 或 Audio 輸入。", "トラック2トリガーまたはオーディオ入力。"}});
        m.entries.push_back({"Track 3 Trigger", {"Trigger or audio input for Track 3. Same behavior as Track 1.", "軌道 3 Trigger 或 Audio 輸入。", "トラック3トリガーまたはオーディオ入力。"}});
        m.entries.push_back({"Track 1 Attack", {"Attack time with exponential scaling. Formula: 10^((param-0.5)*6). At 0%: ~1ms, at 50%: ~1s. Default: 0.002.", "軌道 1 Attack 時間，指數縮放。預設: 0.002。", "トラック1アタック時間、指数スケーリング。デフォルト: 0.002。"}});
        m.entries.push_back({"Track 2 Attack", {"Attack time for Track 2. Same scaling. Default: 0.", "軌道 2 Attack 時間。預設: 0。", "トラック2アタック時間。デフォルト: 0。"}});
        m.entries.push_back({"Track 3 Attack", {"Attack time for Track 3. Same scaling. Default: 0.", "軌道 3 Attack 時間。預設: 0。", "トラック3アタック時間。デフォルト: 0。"}});
        m.entries.push_back({"Track 1 Decay", {"Decay time with identical exponential scaling. Default: 0.3 (~63ms actual time).", "軌道 1 Decay 時間，指數縮放。預設: 0.3。", "トラック1ディケイ時間、指数スケーリング。デフォルト: 0.3。"}});
        m.entries.push_back({"Track 2 Decay", {"Decay time for Track 2. Default: 0.3.", "軌道 2 Decay 時間。預設: 0.3。", "トラック2ディケイ時間。デフォルト: 0.3。"}});
        m.entries.push_back({"Track 3 Decay", {"Decay time for Track 3. Default: 0.3.", "軌道 3 Decay 時間。預設: 0.3。", "トラック3ディケイ時間。デフォルト: 0.3。"}});
        m.entries.push_back({"Track 1 Curve", {"S-curve shaping (-0.99 to +0.99). Negative = exponential, positive = logarithmic. Default: -0.748.", "軌道 1 S-curve 形狀（-0.99 至 +0.99）。預設: -0.748。", "トラック1 S-curveシェイプ（-0.99〜+0.99）。デフォルト: -0.748。"}});
        m.entries.push_back({"Track 2 Curve", {"S-curve shaping for Track 2. Default: -0.832.", "軌道 2 S-curve 形狀。預設: -0.832。", "トラック2 S-curveシェイプ。デフォルト: -0.832。"}});
        m.entries.push_back({"Track 3 Curve", {"S-curve shaping for Track 3. Default: -0.731.", "軌道 3 S-curve 形狀。預設: -0.731。", "トラック3 S-curveシェイプ。デフォルト: -0.731。"}});
        m.entries.push_back({"Track 1 BPF Enable", {"Toggle between Trigger-only (OFF) and Hybrid (ON) modes for Track 1. Blue LED indicates active state.", "軌道 1 Trigger-only/Hybrid 模式切換。", "トラック1 Trigger-only/Hybridモード切替。"}});
        m.entries.push_back({"Track 2 BPF Enable", {"Toggle Hybrid mode for Track 2.", "軌道 2 Trigger-only/Hybrid 模式切換。", "トラック2 Trigger-only/Hybridモード切替。"}});
        m.entries.push_back({"Track 3 BPF Enable", {"Toggle Hybrid mode for Track 3.", "軌道 3 Trigger-only/Hybrid 模式切換。", "トラック3 Trigger-only/Hybridモード切替。"}});
        m.entries.push_back({"Track 1 BPF Frequency", {"4-stage cascaded bandpass filter center frequency (20Hz-8kHz). Default: 200Hz (bass).", "軌道 1 BPF 中心頻率（20Hz-8kHz）。預設: 200Hz。", "トラック1 BPF中心周波数（20Hz-8kHz）。デフォルト: 200Hz。"}});
        m.entries.push_back({"Track 2 BPF Frequency", {"BPF center frequency for Track 2. Default: 1kHz (mid).", "軌道 2 BPF 中心頻率。預設: 1kHz。", "トラック2 BPF中心周波数。デフォルト: 1kHz。"}});
        m.entries.push_back({"Track 3 BPF Frequency", {"BPF center frequency for Track 3. Default: 5kHz (high).", "軌道 3 BPF 中心頻率。預設: 5kHz。", "トラック3 BPF中心周波数。デフォルト: 5kHz。"}});
        m.entries.push_back({"Track 1 BPF Gain", {"Post-filter output gain (0.1x-100x). Default: 3.0x.", "軌道 1 Filter 後增益（0.1x-100x）。預設: 3.0x。", "トラック1フィルター後ゲイン（0.1x-100x）。デフォルト: 3.0x。"}});
        m.entries.push_back({"Track 2 BPF Gain", {"Post-filter gain for Track 2. Default: 3.0x.", "軌道 2 Filter 後增益。預設: 3.0x。", "トラック2フィルター後ゲイン。デフォルト: 3.0x。"}});
        m.entries.push_back({"Track 3 BPF Gain", {"Post-filter gain for Track 3. Default: 3.0x.", "軌道 3 Filter 後增益。預設: 3.0x。", "トラック3フィルター後ゲイン。デフォルト: 3.0x。"}});
        m.entries.push_back({"Trigger-Only (Follower OFF)", {"Classic AD envelope behavior. Envelope triggers on rising edge using standard Schmitt trigger detection. Attack phase ramps from 0 to peak, then Decay phase returns to 0. Ignores audio content between triggers.", "Classic AD envelope behavior. Envelope triggers on rising edge using standard Schmitt trigger detection. Attack phase ramps from 0 to peak, then Decay phase returns to 0. Ignores audio content between triggers.", "Classic AD envelope behavior. Envelope triggers on rising edge using standard Schmitt trigger detection. Attack phase ramps from 0 to peak, then Decay phase returns to 0. Ignores audio content between triggers."}});
        m.entries.push_back({"Hybrid (Follower ON)", {"Advanced dual-engine processing. Runs BOTH trigger envelope AND amplitude follower simultaneously, outputting the MAXIMUM of both. The follower uses rectified input with configurable attack/release coefficients. This mode excels at extracting dynamics from complex audio while still responding to explicit triggers.", "Advanced dual-engine processing. Runs BOTH trigger envelope AND amplitude follower simultaneously, outputting the MAXIMUM of both. The follower uses rectified input with configurable attack/release coefficients. This mode excels at extracting dynamics from complex audio while still responding to explicit triggers.", "Advanced dual-engine processing. Runs BOTH trigger envelope AND amplitude follower simultaneously, outputting the MAXIMUM of both. The follower uses rectified input with configurable attack/release coefficients. This mode excels at extracting dynamics from complex audio while still responding to explicit triggers."}});
        m.entries.push_back({"Track 1 Envelope", {"Track 1 envelope output (0-10V). When Follower is ON, output is multiplied by GAIN setting.", "軌道 1 Envelope 輸出（0-10V）。Follower ON 時乘以 GAIN。", "トラック1エンベロープ出力（0-10V）。Follower ON時はGAIN設定で乗算。"}});
        m.entries.push_back({"Track 2 Envelope", {"Track 2 envelope output (0-10V). Same behavior.", "軌道 2 Envelope 輸出（0-10V）。", "トラック2エンベロープ出力（0-10V）。"}});
        m.entries.push_back({"Track 3 Envelope", {"Track 3 envelope output (0-10V). Same behavior.", "軌道 3 Envelope 輸出（0-10V）。", "トラック3エンベロープ出力（0-10V）。"}});
        m.entries.push_back({"Sum", {"Mixed output of all three tracks with 0.33x scaling per track to prevent clipping. Final output clamped to 0-10V.", "三軌混合輸出，每軌 0.33x 縮放。限制於 0-10V。", "3トラック混合出力、各トラック0.33xスケーリング。0-10Vにクランプ。"}});
        m.entries.push_back({"Multi-band Dynamics", {"Enable Auto-Route, set different BPF frequencies (e.g., 100Hz/800Hz/4kHz), and use outputs to control frequency-specific VCAs or filters.", "Enable Auto-Route, set different BPF frequencies (e.g., 100Hz/800Hz/4kHz), and use outputs to control frequency-specific VCAs or filters.", "Enable Auto-Route, set different BPF frequencies (e.g., 100Hz/800Hz/4kHz), and use outputs to control frequency-specific VCAs or filters."}});
        m.entries.push_back({"Drum Envelope Extraction", {"Feed a drum loop, enable Follower mode, tune FREQ to isolate kick (low), snare (mid), or hi-hat (high) dynamics.", "Feed a drum loop, enable Follower mode, tune FREQ to isolate kick (low), snare (mid), or hi-hat (high) dynamics.", "Feed a drum loop, enable Follower mode, tune FREQ to isolate kick (low), snare (mid), or hi-hat (high) dynamics."}});
        m.entries.push_back({"Sidechain Ducking", {"Use individual outputs to modulate other module parameters based on specific frequency content.", "使用各軌輸出根據特定頻率內容調變其他模組參數。", "Use individual outputs to modulate other module parameters based on specific frequency content."}});
        m.entries.push_back({"Polyrhythmic Triggers", {"With Auto-Route OFF, feed different clock divisions to each track for complex envelope patterns.", "With Auto-Route OFF, feed different clock divisions to each track for complex envelope patterns.", "With Auto-Route OFF, feed different clock divisions to each track for complex envelope patterns."}});
        m.entries.push_back({"Panel Theme", {"Light / Dark panel appearance", "Light / Dark 面板外觀", "Light / Dark パネル外観"}});
        data["ADGenerator"] = std::move(m);
    }
    // ALEXANDERPLATZ (39 entries)
    {
        ModuleHelpData m;
        m.name = "ALEXANDERPLATZ";
        m.description = {"4-track stereo mixer with 8-band master EQ, sidechain ducking, and cross-module solo, Berlin U8 themed", "4 軌 Stereo Mixer，具備 8-band Master EQ、Sidechain Ducking 與跨模組 Solo，柏林 U8 主題", "8バンドマスターEQ、サイドチェインダッキング、クロスモジュールソロを備えた4トラックステレオミキサー、ベルリンU8テーマ"};
        m.entries.push_back({"Bands", {"8 frequency bands", "8 frequency bands", "8 frequency bands"}});
        m.entries.push_back({"Frequencies", {"63Hz, 125Hz, 250Hz, 500Hz, 1kHz, 2kHz, 4kHz, 8kHz", "63Hz, 125Hz, 250Hz, 500Hz, 1kHz, 2kHz, 4kHz, 8kHz", "63Hz, 125Hz, 250Hz, 500Hz, 1kHz, 2kHz, 4kHz, 8kHz"}});
        m.entries.push_back({"Gain Range", {"-12dB to +12dB per band", "-12dB to +12dB per band", "-12dB to +12dB per band"}});
        m.entries.push_back({"Filter Type", {"Biquad peak EQ (Audio EQ Cookbook)", "Biquad peak EQ (Audio EQ Cookbook)", "Biquad peak EQ (Audio EQ Cookbook)"}});
        m.entries.push_back({"Q Factor", {"Fixed at 1.41 (approximately 1 octave bandwidth)", "Fixed at 1.41 (approximately 1 octave bandwidth)", "Fixed at 1.41 (approximately 1 octave bandwidth)"}});
        m.entries.push_back({"Controls", {"Vertical faders with center detent at 0dB", "Vertical faders with center detent at 0dB", "Vertical faders with center detent at 0dB"}});
        m.entries.push_back({"Processing", {"Applied to summed stereo output before final limiting", "Applied to summed stereo output before final limiting", "Applied to summed stereo output before final limiting"}});
        m.entries.push_back({"INPUT L/R", {"Stereo audio input (polyphonic). If only L connected, a 20ms delayed copy is sent to R (Haas effect stereo widening).", "Stereo Audio 輸入（Polyphonic）。若僅連接 L，以 20ms 延遲複製至 R（Haas Effect Stereo Widening）。", "ステレオオーディオ入力（ポリフォニック）。Lのみ接続時、20ms遅延コピーをRに送出（Haasエフェクトステレオワイドニング）。"}});
        m.entries.push_back({"LEVEL Knob", {"Track gain 0-200% (default: 100%)", "軌道 Gain 0-200%（預設: 100%）", "Track gain 0-200% (default: 100%)"}});
        m.entries.push_back({"LEVEL CV", {"Gain modulation input (\\u00b110V adds \\u00b1100% to knob value, final gain clamped 0.0-2.0)", "聲道音量 CV 輸入。", "チャンネル音量のCV入力。"}});
        m.entries.push_back({"DUCK Knob", {"Sidechain ducking depth 0-100%", "Sidechain Ducking 深度 0-100%", "Sidechain ducking depth 0-100%"}});
        m.entries.push_back({"DUCK Input", {"Sidechain control input (expects envelope 0-10V)", "Sidechain 控制輸入（預期 Envelope 0-10V）", "Sidechain control input (expects envelope 0-10V)"}});
        m.entries.push_back({"MUTE Button", {"Toggle track mute with red LED indicator", "切換軌道 Mute，紅色 LED 指示", "Toggle track mute with red LED indicator"}});
        m.entries.push_back({"SOLO Button", {"Toggle track solo with green LED indicator", "切換軌道 Solo，綠色 LED 指示", "Toggle track solo with green LED indicator"}});
        m.entries.push_back({"MUTE/SOLO Triggers", {"External trigger inputs for mute/solo toggle", "Mute/Solo 切換的外部 Trigger 輸入", "External trigger inputs for mute/solo toggle"}});
        m.entries.push_back({"Chain Detection", {"Solo state is shared across adjacent U8, ALEXANDERPLATZ, and SHINJUKU modules", "Solo state is shared across adjacent U8, ALEXANDERPLATZ, and SHINJUKU modules", "Solo state is shared across adjacent U8, ALEXANDERPLATZ, and SHINJUKU modules"}});
        m.entries.push_back({"Exclusive Solo", {"Hold solo button for 400ms to activate exclusive mode (cancels all other solos in chain)", "長按 Solo 按鈕 400ms 啟用 Exclusive 模式（取消 Chain 中所有其他 Solo）", "Hold solo button for 400ms to activate exclusive mode (cancels all other solos in chain)"}});
        m.entries.push_back({"Visual Feedback", {"Mute LED lights up when track is silenced by another track's solo", "Mute LED lights up when track is silenced by another track's solo", "Mute LED lights up when track is silenced by another track's solo"}});
        m.entries.push_back({"CHAIN L/R", {"Input from previous mixer module in chain", "來自 Chain 中前一個 Mixer 模組的輸入", "チェーン内の前のミキサーモジュールからの入力"}});
        m.entries.push_back({"MIX L/R", {"Final stereo output (track sum + chain + EQ processed)", "最終 Stereo 輸出（軌道加總 + Chain + EQ 處理）", "最終ステレオ出力（トラック合計 + チェーン + EQ処理）"}});
        m.entries.push_back({"Auto-Patch", {"Automatically connects to adjacent U8, YAMANOTE, ALEXANDERPLATZ, or SHINJUKU", "自動連接至相鄰的 U8、YAMANOTE、ALEXANDERPLATZ 或 SHINJUKU", "Automatically connects to adjacent U8, YAMANOTE, ALEXANDERPLATZ, or SHINJUKU"}});
        m.entries.push_back({"Cable Color", {"Deep blue (#004F7C) matching Berlin U8 line", "Deep blue (#004F7C) matching Berlin U8 line", "Deep blue (#004F7C) matching Berlin U8 line"}});
        m.entries.push_back({"Display", {"Horizontal stereo VU meter per track", "Horizontal stereo VU meter per track", "Horizontal stereo VU meter per track"}});
        m.entries.push_back({"Range", {"-36dB to +6dB", "-36dB to +6dB", "-36dB to +6dB"}});
        m.entries.push_back({"Color", {"Green to red gradient, red zone above 0dB", "Green to red gradient, red zone above 0dB", "Green to red gradient, red zone above 0dB"}});
        m.entries.push_back({"Chain Left", {"Left channel chain input for daisy-chaining multiple mixer modules.", "左聲道 Chain 輸入，用於串接多個混音器模組。", "複数のミキサーモジュールをデイジーチェーン接続するための左チャンネルチェイン入力。"}});
        m.entries.push_back({"Chain Right", {"Right channel chain input for daisy-chaining multiple mixer modules.", "右聲道 Chain 輸入，用於串接多個混音器模組。", "複数のミキサーモジュールをデイジーチェーン接続するための右チャンネルチェイン入力。"}});
        m.entries.push_back({"Mix Left", {"Main stereo mix left output. Sum of all tracks, EQ, and chain input.", "主立體聲混音左輸出。所有軌道、EQ 與 Chain 輸入的總和。", "メインステレオミックス左出力。全トラック、EQ、チェイン入力の合計。"}});
        m.entries.push_back({"Mix Right", {"Main stereo mix right output. Sum of all tracks, EQ, and chain input.", "主立體聲混音右輸出。所有軌道、EQ 與 Chain 輸入的總和。", "メインステレオミックス右出力。全トラック、EQ、チェイン入力の合計。"}});
        m.entries.push_back({"Left", {"Per-channel stereo audio input (left channel).", "各聲道立體聲音訊輸入（左聲道）。", "チャンネル別ステレオオーディオ入力（左チャンネル）。"}});
        m.entries.push_back({"Right", {"Per-channel stereo audio input (right channel).", "各聲道立體聲音訊輸入（右聲道）。", "チャンネル別ステレオオーディオ入力（右チャンネル）。"}});
        m.entries.push_back({"Level", {"Per-channel volume control (0-200%, default 100%).", "各聲道音量控制（0-200%，預設 100%）。", "チャンネル別音量コントロール（0-200%、デフォルト100%）。"}});
        m.entries.push_back({"Duck", {"Sidechain duck amount (0-100%).", "Sidechain Ducking 量（0-100%）。", "サイドチェインダック量（0-100%）。"}});
        m.entries.push_back({"Mute", {"Toggle channel mute.", "切換聲道靜音。", "チャンネルミュート切替。"}});
        m.entries.push_back({"Solo", {"Toggle channel solo (exclusive mode available in right-click menu).", "切換聲道 Solo（右鍵選單可啟用 Exclusive 模式）。", "チャンネルソロ切替（右クリックメニューでエクスクルーシブモード利用可能）。"}});
        m.entries.push_back({"Level CV", {"CV input for channel volume control.", "聲道音量 CV 輸入。", "チャンネル音量のCV入力。"}});
        m.entries.push_back({"Mute Trigger", {"Trigger input to toggle mute state.", "切換靜音狀態的 Trigger 輸入。", "ミュート状態を切り替えるトリガー入力。"}});
        m.entries.push_back({"Solo Trigger", {"Trigger input to toggle solo state.", "切換 Solo 狀態的 Trigger 輸入。", "ソロ状態を切り替えるトリガー入力。"}});
        m.entries.push_back({"Master EQ", {"8-band parametric EQ (63/125/250/500/1K/2K/4K/8K Hz), \\u00b112dB per band.", "8 頻段參數型 EQ（63/125/250/500/1K/2K/4K/8K Hz），每頻段 \\u00b112dB。", "8バンドパラメトリックEQ（63/125/250/500/1K/2K/4K/8K Hz）、バンドあたり\\u00b112dB。"}});
        data["ALEXANDERPLATZ"] = std::move(m);
    }
    // DECAPyramid (47 entries)
    {
        ModuleHelpData m;
        m.name = "DECAPyramid";
        m.description = {"8-channel 3D mixing console with per-channel spatial positioning, dual send/return buses for complete spatial solution", "8 聲道 3D Mixing Console，具備各聲道空間定位、雙 Send/Return Bus，提供完整空間解決方案", "チャンネル別空間定位、デュアルセンド/リターンバスを備えた8チャンネル3Dミキシングコンソール、完全な空間ソリューションを提供"};
        m.entries.push_back({"Speaker Positions (X,Y,Z)", {"8 corners at coordinates (±1.0, ±1.0, ±1.0)", "8 corners at coordinates (±1.0, ±1.0, ±1.0)", "8 corners at coordinates (±1.0, ±1.0, ±1.0)"}});
        m.entries.push_back({"Speaker Layout", {"Upper layer (FLU, FRU, BLU, BRU), Lower layer (FLD, FRD, BLD, BRD)", "Upper layer (FLU, FRU, BLU, BRU), Lower layer (FLD, FRD, BLD, BRD)", "Upper layer (FLU, FRU, BLU, BRU), Lower layer (FLD, FRD, BLD, BRD)"}});
        m.entries.push_back({"Distance Calculation", {"Speaker gain is based on 3D distance from source to each speaker position", "Speaker gain is based on 3D distance from source to each speaker position", "Speaker gain is based on 3D distance from source to each speaker position"}});
        m.entries.push_back({"Gain Response", {"Closer speakers receive more signal; gain decreases non-linearly with distance for natural spatial perception", "Closer speakers receive more signal; gain decreases non-linearly with distance for natural spatial perception", "Closer speakers receive more signal; gain decreases non-linearly with distance for natural spatial perception"}});
        m.entries.push_back({"Normalization", {"All 8 speaker gains are summed and divided to maintain unity gain", "All 8 speaker gains are summed and divided to maintain unity gain", "All 8 speaker gains are summed and divided to maintain unity gain"}});
        m.entries.push_back({"Smoothing", {"Position changes are smoothed with coefficient 0.005 per sample to prevent zipper noise", "Position changes are smoothed with coefficient 0.005 per sample to prevent zipper noise", "Position changes are smoothed with coefficient 0.005 per sample to prevent zipper noise"}});
        m.entries.push_back({"X Position", {"Left-right positioning (-1.0 to +1.0, default: -1.0). CV modulation adds ±10V × 0.2 = ±2.0 range, clamped to ±1.0.", "左右定位（-1.0 至 +1.0，預設: -1.0）。CV 增加 ±10V × 0.2 = ±2.0 範圍，限制在 ±1.0。", "左右定位（-1.0〜+1.0、デフォルト: -1.0）。CV変調は±10V × 0.2 = ±2.0の範囲を追加、±1.0でクランプ。"}});
        m.entries.push_back({"Y Position", {"Front-back positioning (-1.0 to +1.0, default: -1.0). Front speakers are at -Y, back at +Y.", "前後定位（-1.0 至 +1.0，預設: -1.0）。前方 Speaker 在 -Y，後方在 +Y。", "前後定位（-1.0〜+1.0、デフォルト: -1.0）。フロントスピーカーは-Y、リアは+Y。"}});
        m.entries.push_back({"Z Position", {"Up-down positioning (-1.0 to +1.0, default: -1.0). Lower speakers at -Z, upper at +Z.", "上下定位（-1.0 至 +1.0，預設: -1.0）。下方 Speaker 在 -Z，上方在 +Z。", "上下定位（-1.0〜+1.0、デフォルト: -1.0）。下層スピーカーは-Z、上層は+Z。"}});
        m.entries.push_back({"Level", {"Channel volume (0.0 to 1.0, default: 0.7, display: %). Applied before filtering.", "聲道音量（0.0 至 1.0，預設: 0.7，顯示: %）。Level 後套用 Filter。", "チャンネル音量（0.0〜1.0、デフォルト: 0.7、表示: %）。レベル後にフィルター適用。"}});
        m.entries.push_back({"Filter", {"Bipolar biquad filter control (-1.0 to +1.0, default: 0). Negative = lowpass (20Hz at -1.0, 22000Hz at 0), Positive = highpass (10Hz at 0, 8kHz at +1.0).", "雙極 Biquad Filter 控制（-1.0 至 +1.0，預設: 0）。負值 = Lowpass（-1.0 時 20Hz，0 時 22000Hz），正值 = Highpass（0 時 10Hz，+1.0 時 8kHz）。", "バイポーラBiquadフィルター（-1.0〜+1.0、デフォルト: 0）。負=ローパス（-1.0で20Hz、0で22000Hz）、正=ハイパス（0で10Hz、+1.0で8kHz）。"}});
        m.entries.push_back({"Send A", {"Post-fader aux send A level (0.0 to 1.0, default: 0, display: %). Can be switched to pre-fader via right-click menu.", "Mono Aux Send A 至外部效果器", "ポストフェーダーAuxセンドAレベル（0.0〜1.0、デフォルト: 0、表示: %）。右クリックメニューでプリフェーダーに切替可能。"}});
        m.entries.push_back({"Send B", {"Post-fader aux send B level (0.0 to 1.0, default: 0, display: %). Can be switched to pre-fader via right-click menu.", "Mono Aux Send B 至外部效果器", "ポストフェーダーAuxセンドBレベル（0.0〜1.0、デフォルト: 0、表示: %）。右クリックメニューでプリフェーダーに切替可能。"}});
        m.entries.push_back({"Insert Send", {"Per-track insert send output for external processing. Taps pre-fader signal.", "各軌 Pre-fader 外部處理用 Tap", "トラック別インサートセンド出力。プリフェーダー信号をタップ。"}});
        m.entries.push_back({"Insert Return", {"Per-track insert return input. When unconnected, signal passes through directly.", "各軌外部處理後 Return（未接線時訊號直接通過）", "トラック別インサートリターン入力。未接続時は信号が直接通過。"}});
        m.entries.push_back({"Topology", {"2-stage cascaded biquad (second-order sections) for 24dB/octave slope", "2-stage cascaded biquad (second-order sections) for 24dB/octave slope", "2-stage cascaded biquad (second-order sections) for 24dB/octave slope"}});
        m.entries.push_back({"Q Factor", {"Fixed at 0.707 (Butterworth response)", "Fixed at 0.707 (Butterworth response)", "Fixed at 0.707 (Butterworth response)"}});
        m.entries.push_back({"Lowpass Range", {"Frequency mapped from 20Hz (filter = -1.0) to 22000Hz (filter = 0)", "Frequency mapped from 20Hz (filter = -1.0) to 22000Hz (filter = 0)", "Frequency mapped from 20Hz (filter = -1.0) to 22000Hz (filter = 0)"}});
        m.entries.push_back({"Highpass Range", {"Frequency mapped from 10Hz (filter = 0) to 8000Hz (filter = +1.0)", "Frequency mapped from 10Hz (filter = 0) to 8000Hz (filter = +1.0)", "Frequency mapped from 10Hz (filter = 0) to 8000Hz (filter = +1.0)"}});
        m.entries.push_back({"Coefficient Update", {"Recalculated when filter parameter changes by more than 0.001", "Recalculated when filter parameter changes by more than 0.001", "Recalculated when filter parameter changes by more than 0.001"}});
        m.entries.push_back({"Return A Level", {"Return A volume (0.0 to 1.0, default: 0.7, display: %). Applied after return filter.", "Return A 音量（0.0 至 1.0，預設: 0.7，顯示: %）。在 Return Filter 後套用。", "リターンA音量（0.0〜1.0、デフォルト: 0.7、表示: %）。リターンフィルター後に適用。"}});
        m.entries.push_back({"Return A Filter", {"Return A bipolar filter (-1.0 to +1.0, default: 0). Same lowpass/highpass behavior as track filters.", "Return A 雙極 Filter（-1.0 至 +1.0，預設: 0）。與軌道 Filter 相同的 Lowpass/Highpass 行為。", "リターンAバイポーラフィルター（-1.0〜+1.0、デフォルト: 0）。トラックフィルターと同じローパス/ハイパス動作。"}});
        m.entries.push_back({"Return B Level", {"Return B volume (0.0 to 1.0, default: 0.7, display: %). Applied after return filter.", "Return B 音量（0.0 至 1.0，預設: 0.7，顯示: %）。在 Return Filter 後套用。", "リターンB音量（0.0〜1.0、デフォルト: 0.7、表示: %）。リターンフィルター後に適用。"}});
        m.entries.push_back({"Return B Filter", {"Return B bipolar filter (-1.0 to +1.0, default: 0). Same lowpass/highpass behavior as track filters.", "Return B 雙極 Filter（-1.0 至 +1.0，預設: 0）。與軌道 Filter 相同的 Lowpass/Highpass 行為。", "リターンBバイポーラフィルター（-1.0〜+1.0、デフォルト: 0）。トラックフィルターと同じローパス/ハイパス動作。"}});
        m.entries.push_back({"Output 1-4 Level", {"Sub-group level for speakers 1-4 (0.0 to 1.0, default: 0.7, display: %)", "Speaker 1-4 的 Sub-group Level（0.0 至 1.0，預設: 0.7，顯示: %）", "スピーカー1-4のサブグループレベル（0.0〜1.0、デフォルト: 0.7、表示: %）"}});
        m.entries.push_back({"Output 5-8 Level", {"Sub-group level for speakers 5-8 (0.0 to 1.0, default: 0.7, display: %)", "Speaker 5-8 的 Sub-group Level（0.0 至 1.0，預設: 0.7，顯示: %）", "スピーカー5-8のサブグループレベル（0.0〜1.0、デフォルト: 0.7、表示: %）"}});
        m.entries.push_back({"Master Output Level", {"Master output level affecting all 8 speaker outputs (0.0 to 1.0, default: 0.7, display: %)", "影響所有 8 個 Speaker Output 的 Master Level（0.0 至 1.0，預設: 0.7，顯示: %）", "全8スピーカー出力に影響するマスターレベル（0.0〜1.0、デフォルト: 0.7、表示: %）"}});
        m.entries.push_back({"View", {"Isometric projection of the 3D cube showing all 8 speaker positions", "Isometric projection of the 3D cube showing all 8 speaker positions", "Isometric projection of the 3D cube showing all 8 speaker positions"}});
        m.entries.push_back({"Source Indicators", {"Each channel shown as colored dot at its current XYZ position", "Each channel shown as colored dot at its current XYZ position", "Each channel shown as colored dot at its current XYZ position"}});
        m.entries.push_back({"Color Coding", {"Channel numbers 1-8 with distinct colors for identification", "Channel numbers 1-8 with distinct colors for identification", "Channel numbers 1-8 with distinct colors for identification"}});
        m.entries.push_back({"Real-time Update", {"Position updates every frame following smoothed parameter values", "Position updates every frame following smoothed parameter values", "Position updates every frame following smoothed parameter values"}});
        m.entries.push_back({"Audio", {"Individual mono track inputs (±10V audio), named \"Track N Audio\"", "各軌 Mono 輸入（±10V Audio），名稱為「Track N Audio」", "Individual mono track inputs (±10V audio), named \"Track N Audio\""}});
        m.entries.push_back({"X CV", {"Per-track X position CV modulation (±10V × 0.2 = ±2.0 offset, clamped to ±1.0)", "各軌 X 位置 CV 調變（±10V × 0.2 = ±2.0 偏移，限制在 ±1.0）", "Per-track X position CV modulation (±10V × 0.2 = ±2.0 offset, clamped to ±1.0)"}});
        m.entries.push_back({"Y CV", {"Per-track Y position CV modulation (±10V × 0.2 = ±2.0 offset, clamped to ±1.0)", "各軌 Y 位置 CV 調變（±10V × 0.2 = ±2.0 偏移，限制在 ±1.0）", "Per-track Y position CV modulation (±10V × 0.2 = ±2.0 offset, clamped to ±1.0)"}});
        m.entries.push_back({"Z CV", {"Per-track Z position CV modulation (±10V × 0.2 = ±2.0 offset, clamped to ±1.0)", "各軌 Z 位置 CV 調變（±10V × 0.2 = ±2.0 偏移，限制在 ±1.0）", "Per-track Z position CV modulation (±10V × 0.2 = ±2.0 offset, clamped to ±1.0)"}});
        m.entries.push_back({"Insert Send", {"Pre-fader tap for external processing per track", "各軌 Pre-fader 外部處理用 Tap", "トラック別インサートセンド出力。プリフェーダー信号をタップ。"}});
        m.entries.push_back({"Insert Return", {"Return from external processing per track (passes through when unpatched)", "各軌外部處理後 Return（未接線時訊號直接通過）", "トラック別インサートリターン入力。未接続時は信号が直接通過。"}});
        m.entries.push_back({"Send A", {"Mono aux send A output to external effects", "Mono Aux Send A 至外部效果器", "ポストフェーダーAuxセンドAレベル（0.0〜1.0、デフォルト: 0、表示: %）。右クリックメニューでプリフェーダーに切替可能。"}});
        m.entries.push_back({"Send B", {"Mono aux send B output to external effects", "Mono Aux Send B 至外部效果器", "ポストフェーダーAuxセンドBレベル（0.0〜1.0、デフォルト: 0、表示: %）。右クリックメニューでプリフェーダーに切替可能。"}});
        m.entries.push_back({"Return A L", {"Stereo return A left input", "Stereo Return A Left 輸入", "ステレオリターンA左入力"}});
        m.entries.push_back({"Return A R", {"Stereo return A right input", "Stereo Return A Right 輸入", "ステレオリターンA右入力"}});
        m.entries.push_back({"Return B L", {"Stereo return B left input", "Stereo Return B Left 輸入", "ステレオリターンB左入力"}});
        m.entries.push_back({"Return B R", {"Stereo return B right input", "Stereo Return B Right 輸入", "ステレオリターンB右入力"}});
        m.entries.push_back({"Master", {"8 speaker outputs (Master 1-8), upper layer (1-4) and lower layer (5-8)", "8 個 Speaker 輸出（Master 1-8），上層（1-4）與下層（5-8）", "8 speaker outputs (Master 1-8), upper layer (1-4) and lower layer (5-8)"}});
        m.entries.push_back({"Pyramid", {"3D spatial panner for single source (companion module)", "單一來源的 3D Spatial Panner（配套模組）", "単一ソース用3D空間パンナー（コンパニオンモジュール）"}});
        m.entries.push_back({"KEN", {"8-to-2 binaural processor (converts 8-channel output to headphones)", "8-to-2 Binaural Processor（Pyramid Series）", "8-to-2バイノーラルプロセッサー（Pyramid Series）"}});
        m.entries.push_back({"Send Pre-Level (per track)", {"When enabled, aux sends tap signal before level fader for consistent send level regardless of channel volume. Default: OFF (post-fader).", "When enabled, aux sends tap signal before level fader for consistent send level regardless of channel volume. Default: OFF (post-fader).", "When enabled, aux sends tap signal before level fader for consistent send level regardless of channel volume. Default: OFF (post-fader)."}});
        data["DECAPyramid"] = std::move(m);
    }
    // Drummmmmmer (30 entries)
    {
        ModuleHelpData m;
        m.name = "Drummmmmmer";
        m.description = {"4-voice world drum synthesizer with 10 cultural style presets, 8-voice engine, per-role frequency and decay controls, voice variation probability, and stereo spread", "4 聲部世界鼓合成器，具備 10 種文化風格預設、8 聲引擎、各角色頻率與衰減控制、Voice 變化機率與 Stereo Spread", "10種の文化スタイルプリセット、8ボイスエンジン、ロール別周波数・ディケイコントロール、ボイスバリエーション確率、ステレオスプレッドを備えた4ボイスワールドドラムシンセサイザー"};
        m.entries.push_back({"0 - West African", {"Gankogui, Dununba, Sangban, Djembe Slap/Tone", "Gankogui、Dununba、Sangban、Djembe Slap/Tone", "Gankogui、Dununba、Sangban、Djembe Slap/Tone"}});
        m.entries.push_back({"1 - Afro-Cuban", {"Clave, Tumba, Conga, Timbales, Quinto", "Clave、Tumba、Conga、Timbales、Quinto", "Clave、Tumba、Conga、Timbales、Quinto"}});
        m.entries.push_back({"2 - Brazilian", {"Agogo, Surdo, Tamborim, Caixa, Ganza", "Agogo、Surdo、Tamborim、Caixa、Ganza", "Agogo、Surdo、Tamborim、Caixa、Ganza"}});
        m.entries.push_back({"3 - Balkan", {"Rim, Tapan Bass/Mid, Tarabuka Doum/Tek", "Rim、Tapan Bass/Mid、Tarabuka Doum/Tek", "Rim、Tapan Bass/Mid、Tarabuka Doum/Tek"}});
        m.entries.push_back({"4 - Indian", {"Manjira, Baya Ge/Ka, Daya Na/Tin/Ti/Re", "Manjira、Baya Ge/Ka、Daya Na/Tin/Ti/Re", "Manjira、Baya Ge/Ka、Daya Na/Tin/Ti/Re"}});
        m.entries.push_back({"5 - Gamelan", {"Kenong, Kethuk, Gong, Kempul, Bonang Po, Bonang Sa, Gender, Saron", "Kenong、Kethuk、Gong、Kempul、Bonang Po、Bonang Sa、Gender、Saron", "Kenong、Kethuk、Gong、Kempul、Bonang Po、Bonang Sa、Gender、Saron"}});
        m.entries.push_back({"6 - Jazz", {"Ride/Bell, Kick, Snare, HiHat Cl/Op", "Ride/Bell、Kick、Snare、HiHat Cl/Op", "Ride/Bell、Kick、Snare、HiHat Cl/Op"}});
        m.entries.push_back({"7 - Electronic", {"HiHat, 808 Kick, Clap, Snare, Open HH", "HiHat、808 Kick、Clap、Snare、Open HH", "HiHat、808 Kick、Clap、Snare、Open HH"}});
        m.entries.push_back({"8 - Breakbeat", {"HiHat, Kick, Snare, Ghost, Open HH", "HiHat、Kick、Snare、Ghost、Open HH", "HiHat、Kick、Snare、Ghost、Open HH"}});
        m.entries.push_back({"9 - Techno", {"HiHat, 909 Kick, Clap, Rim, Open HH, Tom", "HiHat、909 Kick、Clap、Rim、Open HH、Tom", "HiHat、909 Kick、Clap、Rim、Open HH、Tom"}});
        m.entries.push_back({"VOICE Knob", {"Controls probability of triggering voice 2 instead of voice 1 (0-100%). At 0%, always triggers primary voice. At 100%, always triggers secondary voice.", "Controls probability of triggering voice 2 instead of voice 1 (0-100%). At 0%, always triggers primary voice. At 100%, always triggers secondary voice.", "Controls probability of triggering voice 2 instead of voice 1 (0-100%). At 0%, always triggers primary voice. At 100%, always triggers secondary voice."}});
        m.entries.push_back({"Voice Name Display", {"Dynamic labels on the right side show the name of the last triggered voice per role, with style-colored glow effect.", "Dynamic labels on the right side show the name of the last triggered voice per role, with style-colored glow effect.", "Dynamic labels on the right side show the name of the last triggered voice per role, with style-colored glow effect."}});
        m.entries.push_back({"Both voices process simultaneously", {"One may be decaying while the other is freshly triggered, allowing natural overlapping tails.", "One may be decaying while the other is freshly triggered, allowing natural overlapping tails.", "One may be decaying while the other is freshly triggered, allowing natural overlapping tails."}});
        m.entries.push_back({"TRIG", {"Trigger input per role. Schmitt trigger detection (0.1V low, 2V high).", "各角色 Trigger 輸入。Schmitt Trigger 偵測（0.1V 低、2V 高）。", "ロール別トリガー入力。シュミットトリガー検出（0.1V低、2V高）。"}});
        m.entries.push_back({"VEL", {"Velocity CV input per role (0-10V, normalized to 0-1). Default velocity is 1.0 when disconnected.", "各角色 Velocity CV 輸入（0-10V，正規化至 0-1）。未連接時預設 Velocity 為 1.0。", "ロール別ベロシティCV入力（0-10V、0-1に正規化）。未接続時のデフォルトベロシティは1.0。"}});
        m.entries.push_back({"FREQ", {"Frequency modifier (-1 to +1 octave). Applies to both voices via pow(2, freqParam). CV: ±5V = ±1 octave.", "頻率修正（-1 至 +1 八度）。透過 pow(2, freqParam) 套用至兩個 Voice。CV: ±5V = ±1 八度。", "周波数修正（-1～+1オクターブ）。pow(2, freqParam)で両ボイスに適用。CV: ±5V = ±1オクターブ。"}});
        m.entries.push_back({"DECAY", {"Decay multiplier (0.2x to 2.0x). Applies to both voices. CV: ±1V = ±0.18x (±5V = ±0.9x).", "Decay 倍數（0.2x 至 2.0x）。套用至兩個 Voice。CV: ±1V = ±0.18x（±5V = ±0.9x）。", "ディケイ倍率（0.2x～2.0x）。両ボイスに適用。CV: ±1V = ±0.18x（±5V = ±0.9x）。"}});
        m.entries.push_back({"OUT", {"Individual audio output per role (sum of both voices, scaled to ±5V).", "各角色獨立 Audio 輸出（兩個 Voice 總和，±5V 縮放）。", "ロール別オーディオ出力（両ボイス合計、±5Vスケール）。"}});
        m.entries.push_back({"STYLE", {"Style selection knob (0-9, snapped to integers). 30px TechnoSnapKnob with CV modulation ring display.", "風格選擇旋鈕（0-9，整數對齊）。30px TechnoSnapKnob，帶 CV 調變環顯示。", "スタイル選択ノブ（0-9、整数スナップ）。30px TechnoSnapKnob、CV変調リング表示付き。"}});
        m.entries.push_back({"STYLE CV", {"CV input for style selection. ±10V range, added to knob value.", "風格選擇調變（加到旋鈕值，限制 0-9）。", "スタイル選択変調（ノブ値に加算、0-9にクランプ）。"}});
        m.entries.push_back({"SPREAD", {"Stereo spread amount (0-100%, default 50%). Pan positions: Timeline -0.5, Foundation 0, Groove +0.3, Lead +0.7.", "Stereo Spread 量（0-100%，預設 50%）。Pan 位置：Timeline -0.5、Foundation 0、Groove +0.3、Lead +0.7。", "ステレオスプレッド量（0-100%、デフォルト50%）。パン位置：Timeline -0.5、Foundation 0、Groove +0.3、Lead +0.7。"}});
        m.entries.push_back({"VOICE", {"Voice variation probability (0-100%, default 0%). Controls which of the 2 voices per role gets triggered.", "Voice 變化機率（0-100%，預設 0%）。控制各角色觸發哪個 Voice。", "ボイスバリエーション確率（0-100%、デフォルト0%）。各ロールでどちらのボイスがトリガーされるかを制御。"}});
        m.entries.push_back({"STYLE CV", {"Style selection modulation (added to knob value, clamped 0-9).", "風格選擇調變（加到旋鈕值，限制 0-9）。", "スタイル選択変調（ノブ値に加算、0-9にクランプ）。"}});
        m.entries.push_back({"TRIG (x4)", {"Trigger input per role (Lead, Groove, Timeline, Foundation).", "各角色 Trigger 輸入（Lead、Groove、Timeline、Foundation）。", "ロール別トリガー入力（Lead、Groove、Timeline、Foundation）。"}});
        m.entries.push_back({"VEL (x4)", {"Velocity CV per role (0-10V).", "各角色 Velocity CV（0-10V）。", "ロール別ベロシティCV（0-10V）。"}});
        m.entries.push_back({"FREQ CV (x4)", {"Frequency modulation per role (±5V = ±1 octave).", "各角色頻率調變（±5V = ±1 八度）。", "ロール別周波数変調（±5V = ±1オクターブ）。"}});
        m.entries.push_back({"DECAY CV (x4)", {"Decay modulation per role (±1V = ±0.18x, ±5V = ±0.9x).", "各角色 Decay 調變（±1V = ±0.18x，±5V = ±0.9x）。", "ロール別ディケイ変調（±1V = ±0.18x、±5V = ±0.9x）。"}});
        m.entries.push_back({"OUT (x4)", {"Individual audio output per role (±5V, sum of 2 voices).", "各角色獨立 Audio 輸出（±5V，2 Voice 總和）。", "ロール別オーディオ出力（±5V、2ボイス合計）。"}});
        m.entries.push_back({"MIX L", {"Stereo left mix output with spread panning and soft limiting (tanh).", "Stereo 左聲道 Mix 輸出，含 Spread Panning 與 Soft Limiting（tanh）。", "ステレオ左ミックス出力、スプレッドパンニングとソフトリミッティング（tanh）適用済み。"}});
        m.entries.push_back({"MIX R", {"Stereo right mix output with spread panning and soft limiting (tanh).", "Stereo 右聲道 Mix 輸出，含 Spread Panning 與 Soft Limiting（tanh）。", "ステレオ右ミックス出力、スプレッドパンニングとソフトリミッティング（tanh）適用済み。"}});
        data["Drummmmmmer"] = std::move(m);
    }
    // EllenRipley (77 entries)
    {
        ModuleHelpData m;
        m.name = "Ellen Ripley";
        m.description = {"Serial effects chain combining stereo delay, 16-grain granular processor, and Freeverb reverb with modified Lorenz attractor chaos modulation", "串聯 Effect Chain，結合 Stereo Delay、16-Grain Granular Processor 與 Freeverb Reverb，具備修改版 Lorenz Attractor Chaos 調變", "ステレオディレイ、16グレイン・グラニュラープロセッサー、Freeverbリバーブを組み合わせたシリアルエフェクトチェーン、修正版ローレンツアトラクターカオス変調搭載"};
        m.entries.push_back({"Type", {"Modified Lorenz attractor (sigma=7.5, rho=30.9, beta=1.02)", "Modified Lorenz attractor (sigma=7.5, rho=30.9, beta=1.02)", "Modified Lorenz attractor (sigma=7.5, rho=30.9, beta=1.02)"}});
        m.entries.push_back({"Behavior", {"Three coupled variables create unpredictable yet deterministic modulation", "Three coupled variables create unpredictable yet deterministic modulation", "Three coupled variables create unpredictable yet deterministic modulation"}});
        m.entries.push_back({"Output", {"X component scaled and clamped to ±1 range", "X component scaled and clamped to ±1 range", "X component scaled and clamped to ±1 range"}});
        m.entries.push_back({"Stability Check", {"Auto-resets if values exceed ±100 or become NaN", "Auto-resets if values exceed ±100 or become NaN", "Auto-resets if values exceed ±100 or become NaN"}});
        m.entries.push_back({"Per-Channel", {"Each polyphonic channel has independent chaos generator", "Each polyphonic channel has independent chaos generator", "Each polyphonic channel has independent chaos generator"}});
        m.entries.push_back({"Buffer Size", {"96,000 samples per channel (~2 seconds at 48kHz)", "96,000 samples per channel (~2 seconds at 48kHz)", "96,000 samples per channel (~2 seconds at 48kHz)"}});
        m.entries.push_back({"TIME L/R", {"Independent delay times 1ms-2s (default: 250ms). CV adds ±200ms.", "獨立 Delay 時間 1ms-2s（預設: 250ms）。CV 增加 ±200ms。", "独立ディレイタイム1ms-2s（デフォルト: 250ms）。CVで±200ms追加。"}});
        m.entries.push_back({"FDBK", {"Feedback amount 0-95% (default: 30%). CV adds ±10%.", "Feedback 量 0-95%（預設: 30%）。CV 增加 ±10%。", "フィードバック量0-95%（デフォルト: 30%）。CVで±10%追加。"}});
        m.entries.push_back({"C Button (Chaos)", {"When enabled, modulates delay times (±100ms) and feedback (±10%) based on chaos output", "When enabled, modulates delay times (±100ms) and feedback (±10%) based on chaos output", "When enabled, modulates delay times (±100ms) and feedback (±10%) based on chaos output"}});
        m.entries.push_back({"Cross-Feedback", {"Each channel reads from its own buffer (no ping-pong)", "各聲道從自己的 Buffer 讀取（無 Ping-Pong）", "Each channel reads from its own buffer (no ping-pong)"}});
        m.entries.push_back({"Buffer Size", {"8,192 samples (~170ms at 48kHz)", "8,192 samples (~170ms at 48kHz)", "8,192 samples (~170ms at 48kHz)"}});
        m.entries.push_back({"Max Grains", {"16 simultaneous grains", "16 simultaneous grains", "16 simultaneous grains"}});
        m.entries.push_back({"SIZE", {"Grain duration 1-100ms. Smaller creates glitchy textures, larger creates smooth time-stretch.", "Grain 長度 1-100ms。較小產生 Glitchy 質感，較大產生平滑 Time-Stretch。", "グレイン長1-100ms。小=グリッチーなテクスチャ、大=スムースなタイムストレッチ。"}});
        m.entries.push_back({"BREAK", {"Grain trigger rate (density). 0 = few grains, 1 = up to 51 grains/second.", "Grain 觸發速率（密度）。0 = 少數 Grain，1 = 最多 51 Grain/秒。", "グレイントリガーレート（密度）。0=少数グレイン、1=最大51グレイン/秒。"}});
        m.entries.push_back({"SHIFT", {"Buffer read position 0-100%. Determines where in buffer grains start.", "Buffer 讀取位置 0-100%。決定 Grain 從 Buffer 何處開始。", "バッファ読み取り位置0-100%。グレインがバッファのどこから開始するかを決定。"}});
        m.entries.push_back({"Envelope", {"Raised cosine (Hanning) window for smooth grain crossfades", "Raised Cosine（Hanning）視窗，平滑 Grain 交叉淡化", "Raised cosine (Hanning) window for smooth grain crossfades"}});
        m.entries.push_back({"Chaos Effects", {"Position offset ±20 in normalized space (clamped 0-1), 30% chance reverse playback, 20% chance octave shift (0.5× or 2×, only when density > 0.7)", "Position offset ±20 in normalized space (clamped 0-1), 30% chance reverse playback, 20% chance octave shift (0.5× or 2×, only when density > 0.7)", "Position offset ±20 in normalized space (clamped 0-1), 30% chance reverse playback, 20% chance octave shift (0.5× or 2×, only when density > 0.7)"}});
        m.entries.push_back({"Output Normalization", {"Amplitude scales with active grain count to prevent clipping", "Amplitude scales with active grain count to prevent clipping", "Amplitude scales with active grain count to prevent clipping"}});
        m.entries.push_back({"Architecture", {"8 parallel comb filters + 4 series allpass diffusers", "8 parallel comb filters + 4 series allpass diffusers", "8 parallel comb filters + 4 series allpass diffusers"}});
        m.entries.push_back({"Comb Filter Sizes (L)", {"1557, 1617, 1491, 1422 samples (~30-34ms at 48kHz)", "1557, 1617, 1491, 1422 samples (~30-34ms at 48kHz)", "1557, 1617, 1491, 1422 samples (~30-34ms at 48kHz)"}});
        m.entries.push_back({"Comb Filter Sizes (R)", {"1277, 1356, 1188, 1116 samples (~23-28ms at 48kHz)", "1277, 1356, 1188, 1116 samples (~23-28ms at 48kHz)", "1277, 1356, 1188, 1116 samples (~23-28ms at 48kHz)"}});
        m.entries.push_back({"Allpass Sizes", {"556, 441, 341, 225 samples", "556, 441, 341, 225 samples", "556, 441, 341, 225 samples"}});
        m.entries.push_back({"ROOM", {"Room size 0-100% affects early reflection tap positions. Scale: 0.3-1.7×", "空間大小 0-100% 影響早期反射 Tap 位置。縮放: 0.3-1.7×", "ルームサイズ0-100%が初期反射タップ位置に影響。スケール: 0.3-1.7×"}});
        m.entries.push_back({"TONE", {"Damping coefficient 0.05-0.95. Low = dark, high = bright.", "Damping 係數 0.05-0.95。低 = 暗、高 = 亮。", "ダンピング係数0.05-0.95。低=暗い、高=明るい。"}});
        m.entries.push_back({"DECAY", {"Feedback 0.5-0.985. At max, near-infinite reverb tails (10+ seconds).", "Reverb Decay 0-1（預設: 0.6）。Feedback 0.5-0.985。最大時，近乎無限 Reverb 尾部（10+ 秒）。", "リバーブディケイ 0-1（デフォルト: 0.6）。フィードバック 0.5-0.985。最大時、ほぼ無限のリバーブテール（10秒以上）。"}});
        m.entries.push_back({"Highpass Filter", {"100Hz cutoff removes sub-bass buildup", "100Hz 截止頻率移除低頻累積", "100Hz cutoff removes sub-bass buildup"}});
        m.entries.push_back({"Chaos Effects", {"Feedback ±0.5, room offset ±50-70 samples", "Feedback ±0.5, room offset ±50-70 samples", "Feedback ±0.5, room offset ±50-70 samples"}});
        m.entries.push_back({"RATE", {"Chaos speed. Shape OFF: rate 0.01-1.0 (smooth continuous). Shape ON: rate 1.0-10.0, step update at rate×10 Hz (stepped S&H).", "Chaos 速度。Shape OFF: 速率 0.01-1.0（平滑連續）。Shape ON: 速率 1.0-10.0，Step 更新頻率 rate×10 Hz（S&H 階梯）。", "カオススピード。Shape OFF: レート0.01-1.0（スムース連続）。Shape ON: レート1.0-10.0、ステップ更新rate×10 Hz（S&Hステップ）。"}});
        m.entries.push_back({"SHAPE Button", {"OFF = smooth continuous chaos, ON = sample-and-hold stepped chaos", "OFF = smooth continuous chaos, ON = sample-and-hold stepped chaos", "OFF = smooth continuous chaos, ON = sample-and-hold stepped chaos"}});
        m.entries.push_back({"AMOUNT", {"Global chaos intensity multiplier 0-100%", "全域 Chaos 強度乘數 0-100%", "グローバルカオス強度乗数0-100%"}});
        m.entries.push_back({"CHAOS OUT", {"CV output ±5V for external modulation", "Chaos CV 輸出 ±5V（Polyphonic）", "カオスCV出力±5V（ポリフォニック）"}});
        m.entries.push_back({"Per-Stage Enable", {"C buttons on each section enable/disable chaos for that stage", "C buttons on each section enable/disable chaos for that stage", "C buttons on each section enable/disable chaos for that stage"}});
        m.entries.push_back({"DELAY", {"Wet/dry mix after delay stage", "Delay 階段後的 Wet/Dry Mix", "ディレイステージ後のウェット/ドライミックス"}});
        m.entries.push_back({"GRATCH", {"Wet/dry mix after granular stage", "Granular 階段後的 Wet/Dry Mix", "グラニュラーステージ後のウェット/ドライミックス"}});
        m.entries.push_back({"REVERB", {"Wet/dry mix after reverb stage", "Reverb 階段後的 Wet/Dry Mix", "リバーブステージ後のウェット/ドライミックス"}});
        m.entries.push_back({"Behavior", {"Linear crossfade between dry and wet signals", "Linear crossfade between dry and wet signals", "Linear crossfade between dry and wet signals"}});
        m.entries.push_back({"Channel Count", {"Up to 16 polyphonic channels", "Up to 16 polyphonic channels", "Up to 16 polyphonic channels"}});
        m.entries.push_back({"Per-Channel Instances", {"Delay buffers, grain processors, reverb processors, chaos generators", "Delay buffers, grain processors, reverb processors, chaos generators", "Delay buffers, grain processors, reverb processors, chaos generators"}});
        m.entries.push_back({"Memory Usage", {"~12MB total delay buffers (96k × 16 × 2 channels)", "~12MB total delay buffers (96k × 16 × 2 channels)", "~12MB total delay buffers (96k × 16 × 2 channels)"}});
        m.entries.push_back({"CV Handling", {"CV inputs use channel 0 if polyphony doesn't match", "CV inputs use channel 0 if polyphony doesn't match", "CV inputs use channel 0 if polyphony doesn't match"}});
        m.entries.push_back({"L/R Audio", {"Stereo input (R normalizes to L if disconnected)", "Stereo 處理輸出（Polyphonic）", "ステレオ処理出力（ポリフォニック）"}});
        m.entries.push_back({"TIME L/R CV", {"Delay time modulation", "Delay 時間調變", "ディレイタイム変調"}});
        m.entries.push_back({"FDBK CV", {"Delay feedback modulation", "Delay Feedback 調變", "ディレイフィードバック変調"}});
        m.entries.push_back({"SIZE/BREAK/SHIFT CV", {"Granular parameter modulation", "Granular 參數調變", "グラニュラーパラメータ変調"}});
        m.entries.push_back({"ROOM/TONE/DECAY CV", {"Reverb parameter modulation", "Reverb 參數調變", "リバーブパラメータ変調"}});
        m.entries.push_back({"L/R Audio", {"Stereo processed output (polyphonic)", "Stereo 處理輸出（Polyphonic）", "ステレオ処理出力（ポリフォニック）"}});
        m.entries.push_back({"CHAOS OUT", {"Chaos CV output ±5V (polyphonic)", "Chaos CV 輸出 ±5V（Polyphonic）", "カオスCV出力±5V（ポリフォニック）"}});
        m.entries.push_back({"Delay Time L", {"Left delay time 0.001-2.0s (default: 0.25s). CV: ±0.2s per volt.", "左聲道 Delay 時間 0.001-2.0s（預設: 0.25s）。CV: 每伏特 ±0.2s。", "左チャンネルディレイタイム 0.001-2.0s（デフォルト: 0.25s）。CV: ±0.2s/V。"}});
        m.entries.push_back({"Delay Time R", {"Right delay time 0.001-2.0s (default: 0.25s). CV: ±0.2s per volt.", "右聲道 Delay 時間 0.001-2.0s（預設: 0.25s）。CV: 每伏特 ±0.2s。", "右チャンネルディレイタイム 0.001-2.0s（デフォルト: 0.25s）。CV: ±0.2s/V。"}});
        m.entries.push_back({"Feedback", {"Delay feedback 0-95% (default: 30%). CV: ±10% per volt.", "Delay Feedback 0-95%（預設: 30%）。CV: 每伏特 ±10%。", "ディレイフィードバック 0-95%（デフォルト: 30%）。CV: ±10%/V。"}});
        m.entries.push_back({"Delay Chaos", {"Toggle chaos modulation on delay (threshold: 0.5). Modulates delay time ±0.1s and feedback ±0.1.", "切換 Delay 的 Chaos 調變（閾值: 0.5）。調變 Delay 時間 ±0.1s 和 Feedback ±0.1。", "ディレイのカオス変調切替（閾値: 0.5）。ディレイタイム ±0.1s とフィードバック ±0.1 を変調。"}});
        m.entries.push_back({"Delay Wet/Dry", {"Wet/dry mix after delay stage 0-100% (default: 0%).", "Delay 階段後的 Wet/Dry Mix 0-100%（預設: 0%）。", "ディレイステージ後のウェット/ドライミックス 0-100%（デフォルト: 0%）。"}});
        m.entries.push_back({"Chaos Rate", {"Chaos speed. Shape OFF: 0.01-1.0 (smooth). Shape ON: 1.0-10.0 (stepped S&H at rate x10 Hz).", "Chaos 速度。Shape OFF: 0.01-1.0（平滑）。Shape ON: 1.0-10.0（S&H 階梯，rate x10 Hz）。", "カオススピード。Shape OFF: 0.01-1.0（スムース）。Shape ON: 1.0-10.0（S&Hステップ、rate x10 Hz）。"}});
        m.entries.push_back({"Grain Size", {"Grain duration 0-1 (maps to 1-100ms). Default: 0.3.", "Grain 長度 0-1（映射至 1-100ms）。預設: 0.3。", "グレイン長 0-1（1-100msにマップ）。デフォルト: 0.3。"}});
        m.entries.push_back({"Grain Density/Glitch", {"Grain trigger density 0-1 (maps to 1-51 grains/sec). Default: 0.4.", "Grain 觸發密度 0-1（映射至 1-51 grains/sec）。預設: 0.4。", "グレイントリガー密度 0-1（1-51 grains/secにマップ）。デフォルト: 0.4。"}});
        m.entries.push_back({"Grain Position/Chaos", {"Buffer read position 0-1 (0-100%). Default: 0.5.", "Buffer 讀取位置 0-1（0-100%）。預設: 0.5。", "バッファ読み取り位置 0-1（0-100%）。デフォルト: 0.5。"}});
        m.entries.push_back({"Grain Chaos", {"Toggle chaos modulation on grains (threshold: 0.5). Enables reverse playback, octave shift, position offset.", "切換 Grain 的 Chaos 調變（閾值: 0.5）。啟用反向播放、Octave Shift、位置偏移。", "グレインのカオス変調切替（閾値: 0.5）。逆再生、オクターブシフト、位置オフセットを有効化。"}});
        m.entries.push_back({"Gratch Wet/Dry", {"Wet/dry mix after granular stage 0-100% (default: 0%).", "Granular 階段後的 Wet/Dry Mix 0-100%（預設: 0%）。", "グラニュラーステージ後のウェット/ドライミックス 0-100%（デフォルト: 0%）。"}});
        m.entries.push_back({"Room Size", {"Reverb room size 0-1 (default: 0.5). Affects early reflection tap positions, scale 0.3-1.7x.", "Reverb 空間大小 0-1（預設: 0.5）。影響早期反射 Tap 位置，縮放 0.3-1.7x。", "リバーブルームサイズ 0-1（デフォルト: 0.5）。初期反射タップ位置に影響、スケール 0.3-1.7x。"}});
        m.entries.push_back({"Damping", {"Reverb damping 0-1 (default: 0.4). Low = dark, high = bright. Coefficient 0.05-0.95.", "Reverb Damping 0-1（預設: 0.4）。低 = 暗、高 = 亮。係數 0.05-0.95。", "リバーブダンピング 0-1（デフォルト: 0.4）。低=暗い、高=明るい。係数 0.05-0.95。"}});
        m.entries.push_back({"Decay", {"Reverb decay 0-1 (default: 0.6). Feedback 0.5-0.985. At max, near-infinite reverb tails (10+ seconds).", "Reverb Decay 0-1（預設: 0.6）。Feedback 0.5-0.985。最大時，近乎無限 Reverb 尾部（10+ 秒）。", "リバーブディケイ 0-1（デフォルト: 0.6）。フィードバック 0.5-0.985。最大時、ほぼ無限のリバーブテール（10秒以上）。"}});
        m.entries.push_back({"Reverb Chaos", {"Toggle chaos modulation on reverb (threshold: 0.5). Modulates feedback ±0.5, room offset ±50-70 samples.", "切換 Reverb 的 Chaos 調變（閾值: 0.5）。調變 Feedback ±0.5、Room 偏移 ±50-70 Sample。", "リバーブのカオス変調切替（閾値: 0.5）。フィードバック ±0.5、ルームオフセット ±50-70サンプルを変調。"}});
        m.entries.push_back({"Reverb Wet/Dry", {"Wet/dry mix after reverb stage 0-100% (default: 0%).", "Reverb 階段後的 Wet/Dry Mix 0-100%（預設: 0%）。", "リバーブステージ後のウェット/ドライミックス 0-100%（デフォルト: 0%）。"}});
        m.entries.push_back({"Chaos Amount", {"Global chaos intensity multiplier 0-1 (default: 1.0).", "全域 Chaos 強度乘數 0-1（預設: 1.0）。", "グローバルカオス強度乗数 0-1（デフォルト: 1.0）。"}});
        m.entries.push_back({"Chaos Shape", {"OFF (below 0.5) = smooth continuous chaos. ON (above 0.5) = sample-and-hold stepped chaos.", "OFF（低於 0.5）= 平滑連續 Chaos。ON（高於 0.5）= Sample-and-Hold 階梯 Chaos。", "OFF（0.5未満）= スムース連続カオス。ON（0.5以上）= サンプル&ホールドステップカオス。"}});
        m.entries.push_back({"Left Audio", {"Stereo left audio input/output. Right normalizes to Left if disconnected. Polyphonic.", "Stereo 左聲道 Audio 輸入/輸出。R 未連接時正規化至 L。Polyphonic。", "ステレオ左オーディオ入出力。R未接続時はLに正規化。ポリフォニック。"}});
        m.entries.push_back({"Right Audio", {"Stereo right audio input/output. Polyphonic.", "Stereo 右聲道 Audio 輸入/輸出。Polyphonic。", "ステレオ右オーディオ入出力。ポリフォニック。"}});
        m.entries.push_back({"Delay Time L CV", {"CV modulation for left delay time. ±0.2s per volt.", "左聲道 Delay 時間 CV 調變。每伏特 ±0.2s。", "左ディレイタイムCV変調。±0.2s/V。"}});
        m.entries.push_back({"Delay Time R CV", {"CV modulation for right delay time. ±0.2s per volt.", "右聲道 Delay 時間 CV 調變。每伏特 ±0.2s。", "右ディレイタイムCV変調。±0.2s/V。"}});
        m.entries.push_back({"Feedback CV", {"CV modulation for delay feedback. ±0.1 per volt.", "Delay Feedback CV 調變。每伏特 ±0.1。", "ディレイフィードバックCV変調。±0.1/V。"}});
        m.entries.push_back({"Grain Size CV", {"CV modulation for grain size. ±0.1 per volt.", "Grain Size CV 調變。每伏特 ±0.1。", "グレインサイズCV変調。±0.1/V。"}});
        m.entries.push_back({"Grain Density CV", {"CV modulation for grain density. ±0.1 per volt.", "Grain Density CV 調變。每伏特 ±0.1。", "グレイン密度CV変調。±0.1/V。"}});
        m.entries.push_back({"Grain Position CV", {"CV modulation for grain position. ±0.1 per volt.", "Grain Position CV 調變。每伏特 ±0.1。", "グレイン位置CV変調。±0.1/V。"}});
        m.entries.push_back({"Reverb Room Size CV", {"CV modulation for reverb room size. ±0.1 per volt.", "Reverb Room Size CV 調變。每伏特 ±0.1。", "リバーブルームサイズCV変調。±0.1/V。"}});
        m.entries.push_back({"Reverb Damping CV", {"CV modulation for reverb damping. ±0.1 per volt.", "Reverb Damping CV 調變。每伏特 ±0.1。", "リバーブダンピングCV変調。±0.1/V。"}});
        m.entries.push_back({"Reverb Decay CV", {"CV modulation for reverb decay. ±0.1 per volt.", "Reverb Decay CV 調變。每伏特 ±0.1。", "リバーブディケイCV変調。±0.1/V。"}});
        m.entries.push_back({"Chaos CV", {"Chaos CV output ±5V. Polyphonic, one per channel.", "Chaos CV 輸出 ±5V。Polyphonic，各聲道獨立。", "カオスCV出力 ±5V。ポリフォニック、チャンネル毎独立。"}});
        data["EllenRipley"] = std::move(m);
    }
    // EnvVCA6 (115 entries)
    {
        ModuleHelpData m;
        m.name = "Env VCA 6";
        m.description = {"Six stereo envelope-controlled VCAs with independent attack/release, three gate output modes, and sum bus routing (collab: offthesky)", "6 聲道 Envelope 與 VCA 組合模組", "6チャンネルエンベロープとVCAの複合モジュール"};
        m.entries.push_back({"Time Range", {"Exponential scaling from ~1ms (knob at 0) to ~1000s (knob at 1.0). Center position: ~1s.", "Exponential scaling from ~1ms (knob at 0) to ~1000s (knob at 1.0). Center position: ~1s.", "Exponential scaling from ~1ms (knob at 0) to ~1000s (knob at 1.0). Center position: ~1s."}});
        m.entries.push_back({"Minimum Time", {"Clamped to 1ms to prevent clicks", "Clamped to 1ms to prevent clicks", "Clamped to 1ms to prevent clicks"}});
        m.entries.push_back({"Curve Shape", {"S-curve for natural-sounding transitions", "S-curve for natural-sounding transitions", "S-curve for natural-sounding transitions"}});
        m.entries.push_back({"Attack Phase", {"Ramps from 0 to 1 over attack time with curve applied", "Ramps from 0 to 1 over attack time with curve applied", "Ramps from 0 to 1 over attack time with curve applied"}});
        m.entries.push_back({"Decay/Release Phase", {"Ramps from 1 to 0 over decay time with inverted curve", "Ramps from 1 to 0 over decay time with inverted curve", "Ramps from 1 to 0 over decay time with inverted curve"}});
        m.entries.push_back({"AD Mode (HOLD off)", {"Attack → Decay. Envelope starts on rising edge, completes full cycle regardless of gate length.", "Attack → Decay. Envelope starts on rising edge, completes full cycle regardless of gate length.", "Attack → Decay. Envelope starts on rising edge, completes full cycle regardless of gate length."}});
        m.entries.push_back({"AHR Mode (HOLD on)", {"Attack → Hold → Release. Attack phase on rising edge, holds at peak while gate is high, release begins on gate falling edge.", "Attack → Hold → Release. Attack phase on rising edge, holds at peak while gate is high, release begins on gate falling edge.", "Attack → Hold → Release. Attack phase on rising edge, holds at peak while gate is high, release begins on gate falling edge."}});
        m.entries.push_back({"Trigger Detection", {"Schmitt trigger with 0.1V low / 2.0V high thresholds", "Schmitt trigger with 0.1V low / 2.0V high thresholds", "Schmitt trigger with 0.1V low / 2.0V high thresholds"}});
        m.entries.push_back({"Attack", {"Attack time (0.0-1.0, exponential scaling ~1ms to ~1000s)", "Attack 時間（0.0-1.0，指數縮放 ~1ms 至 ~1000s）", "アタック時間（0.0-1.0、指数スケーリング ~1ms〜~1000s）"}});
        m.entries.push_back({"Release", {"Release/Decay time (0.0-1.0, same scaling)", "Release/Decay 時間（0.0-1.0，相同縮放）", "リリース/ディケイ時間（0.0-1.0、同スケーリング）"}});
        m.entries.push_back({"Out Vol", {"Output volume multiplier (0.0-1.0, default: 0.8)", "Output 音量乘數（0.0-1.0，預設: 0.8）", "出力音量乗数（0.0-1.0、デフォルト: 0.8）"}});
        m.entries.push_back({"Manual Button (pink)", {"Momentary gate - generates 10V while pressed", "Momentary gate - generates 10V while pressed", "Momentary gate - generates 10V while pressed"}});
        m.entries.push_back({"HOLD Button (pink)", {"Toggles between AD and AHR envelope modes", "Toggles between AD and AHR envelope modes", "Toggles between AD and AHR envelope modes"}});
        m.entries.push_back({"Sum Button (pink)", {"Routes this channel's output to Channel 6 sum bus (CH1-5 only)", "Routes this channel's output to Channel 6 sum bus (CH1-5 only)", "Routes this channel's output to Channel 6 sum bus (CH1-5 only)"}});
        m.entries.push_back({"In L", {"Left/mono audio input. If R is unconnected, L is copied to both channels.", "Left/Mono Audio 輸入。若 R 未連接，L 複製至兩聲道。", "左/モノオーディオ入力。Rが未接続の場合、Lが両チャンネルにコピーされる。"}});
        m.entries.push_back({"In R", {"Right audio input (optional)", "Right Audio 輸入（選用）", "右オーディオ入力（オプション）"}});
        m.entries.push_back({"Gate", {"Trigger/gate input. Combined with manual button (max of both).", "Trigger/Gate 輸入。與手動按鈕結合（兩者取最大值）。", "トリガー/ゲート入力。手動ボタンと結合（両方の最大値）。"}});
        m.entries.push_back({"Vol Ctrl", {"Volume CV (0-10V maps to 0-1 multiplier). Defaults to 1.0 if unconnected.", "音量 CV（0-10V 映射至 0-1 乘數）。未連接時預設為 1.0。", "音量CV（0-10Vを0-1乗数にマップ）。未接続時は1.0がデフォルト。"}});
        m.entries.push_back({"Gate Out", {"Configurable gate/trigger output (see Gate Output Modes)", "可配置的 Gate/Trigger Output（見 Gate Output 模式）", "構成可能なゲート/トリガー出力（ゲート出力モード参照）"}});
        m.entries.push_back({"Env Out", {"Raw envelope output (0-10V)", "原始 Envelope Output（0-10V）", "生エンベロープ出力（0-10V）"}});
        m.entries.push_back({"Out L/R", {"Stereo VCA output = input × envelope × volCV × outVol", "Stereo VCA Output = input × envelope × volCV × outVol", "ステレオVCA出力 = input × envelope × volCV × outVol"}});
        m.entries.push_back({"Full Cycle Gate", {"Output stays high (10V) from gate rising edge until envelope returns to zero. Useful for chaining sequences.", "從 Gate 上升邊緣至 Envelope 回到零期間 Output 維持高電位（10V）。適用於串接序列。", "ゲート立ち上がりエッジからエンベロープがゼロに戻るまで出力がハイ（10V）を維持。シーケンス連鎖に有用。"}});
        m.entries.push_back({"End of Cycle Trigger", {"1ms pulse when envelope completes (falls below 0.001). Useful for triggering next stage.", "Envelope 完成時（降至 0.001 以下）產生 1ms 脈衝。適用於觸發下一階段。", "エンベロープ完了時（0.001以下に低下）に1msパルス。次ステージトリガーに有用。"}});
        m.entries.push_back({"Start + End Triggers", {"1ms pulses at both gate rising edge AND envelope completion. Useful for clock multiplication.", "在 Gate 上升邊緣與 Envelope 完成時皆產生 1ms 脈衝。適用於 Clock 倍增。", "ゲート立ち上がりエッジとエンベロープ完了の両方で1msパルス。クロック倍増に有用。"}});
        m.entries.push_back({"Routing", {"CH1-5 can route to CH6 via Sum buttons (CH6 has no sum button)", "CH1-5 can route to CH6 via Sum buttons (CH6 has no sum button)", "CH1-5 can route to CH6 via Sum buttons (CH6 has no sum button)"}});
        m.entries.push_back({"Audio Summing", {"Each channel is attenuated before summing to prevent overload", "Each channel is attenuated before summing to prevent overload", "Each channel is attenuated before summing to prevent overload"}});
        m.entries.push_back({"Envelope Summing", {"RMS-based calculation of summed channel envelopes; CH6 envelope output uses max(CH6 own envelope, RMS envelope) to preserve the larger value", "RMS-based calculation of summed channel envelopes; CH6 envelope output uses max(CH6 own envelope, RMS envelope) to preserve the larger value", "RMS-based calculation of summed channel envelopes; CH6 envelope output uses max(CH6 own envelope, RMS envelope) to preserve the larger value"}});
        m.entries.push_back({"Combination", {"Audio sum is ADDED to CH6's own output; envelope uses max() not add", "Audio sum is ADDED to CH6's own output; envelope uses max() not add", "Audio sum is ADDED to CH6's own output; envelope uses max() not add"}});
        m.entries.push_back({"Use Case", {"Submix multiple channels through CH6 for unified processing", "Submix multiple channels through CH6 for unified processing", "Submix multiple channels through CH6 for unified processing"}});
        m.entries.push_back({"Green LED", {"Shows current VCA gain level (envelope × volCV × outVol)", "Shows current VCA gain level (envelope × volCV × outVol)", "Shows current VCA gain level (envelope × volCV × outVol)"}});
        m.entries.push_back({"Brightness", {"Proportional to final gain multiplier", "Proportional to final gain multiplier", "Proportional to final gain multiplier"}});
        m.entries.push_back({"Ch 1 Attack", {"Channel 1 attack time (0.0-1.0, exponential ~1ms to ~1000s). Default: 0.1.", "Channel 1 Attack 時間（0.0-1.0，指數縮放 ~1ms 至 ~1000s）。預設: 0.1。", "チャンネル1アタック時間（0.0-1.0、指数スケーリング ~1ms〜~1000s）。デフォルト: 0.1。"}});
        m.entries.push_back({"Ch 1 Release", {"Channel 1 release/decay time (0.0-1.0, same scaling). Default: 0.5.", "Channel 1 Release/Decay 時間（0.0-1.0，相同縮放）。預設: 0.5。", "チャンネル1リリース/ディケイ時間（0.0-1.0、同スケーリング）。デフォルト: 0.5。"}});
        m.entries.push_back({"Ch 1 Out Volume", {"Channel 1 output volume multiplier 0-1 (default: 0.8).", "Channel 1 Output 音量乘數 0-1（預設: 0.8）。", "チャンネル1出力音量乗数 0-1（デフォルト: 0.8）。"}});
        m.entries.push_back({"Ch 1 Manual Gate (Momentary)", {"Channel 1 momentary gate button. Generates 10V while pressed.", "Channel 1 瞬時 Gate 按鈕。按住時產生 10V。", "チャンネル1モーメンタリーゲートボタン。押下中10V生成。"}});
        m.entries.push_back({"Ch 1 Sum to Ch6", {"Channel 1 sum bus routing toggle. Routes output to Channel 6.", "Channel 1 Sum Bus 路由切換。將 Output 路由至 Channel 6。", "チャンネル1サムバスルーティング切替。出力をチャンネル6にルーティング。"}});
        m.entries.push_back({"Ch 1 Env Mode (AD/AHR)", {"Channel 1 envelope mode. Off = AD, On = AHR (Attack-Hold-Release).", "Channel 1 Envelope 模式。Off = AD，On = AHR（Attack-Hold-Release）。", "チャンネル1エンベロープモード。Off = AD、On = AHR（アタック-ホールド-リリース）。"}});
        m.entries.push_back({"Ch 2 Attack", {"Channel 2 attack time.", "Channel 2 Attack 時間。", "チャンネル2アタック時間。"}});
        m.entries.push_back({"Ch 2 Release", {"Channel 2 release/decay time.", "Channel 2 Release/Decay 時間。", "チャンネル2リリース/ディケイ時間。"}});
        m.entries.push_back({"Ch 2 Out Volume", {"Channel 2 output volume multiplier.", "Channel 2 Output 音量乘數。", "チャンネル2出力音量乗数。"}});
        m.entries.push_back({"Ch 2 Manual Gate (Momentary)", {"Channel 2 momentary gate button.", "Channel 2 瞬時 Gate 按鈕。", "チャンネル2モーメンタリーゲートボタン。"}});
        m.entries.push_back({"Ch 2 Sum to Ch6", {"Channel 2 sum bus routing toggle.", "Channel 2 Sum Bus 路由切換。", "チャンネル2サムバスルーティング切替。"}});
        m.entries.push_back({"Ch 2 Env Mode (AD/AHR)", {"Channel 2 envelope mode.", "Channel 2 Envelope 模式。", "チャンネル2エンベロープモード。"}});
        m.entries.push_back({"Ch 3 Attack", {"Channel 3 attack time.", "Channel 3 Attack 時間。", "チャンネル3アタック時間。"}});
        m.entries.push_back({"Ch 3 Release", {"Channel 3 release/decay time.", "Channel 3 Release/Decay 時間。", "チャンネル3リリース/ディケイ時間。"}});
        m.entries.push_back({"Ch 3 Out Volume", {"Channel 3 output volume multiplier.", "Channel 3 Output 音量乘數。", "チャンネル3出力音量乗数。"}});
        m.entries.push_back({"Ch 3 Manual Gate (Momentary)", {"Channel 3 momentary gate button.", "Channel 3 瞬時 Gate 按鈕。", "チャンネル3モーメンタリーゲートボタン。"}});
        m.entries.push_back({"Ch 3 Sum to Ch6", {"Channel 3 sum bus routing toggle.", "Channel 3 Sum Bus 路由切換。", "チャンネル3サムバスルーティング切替。"}});
        m.entries.push_back({"Ch 3 Env Mode (AD/AHR)", {"Channel 3 envelope mode.", "Channel 3 Envelope 模式。", "チャンネル3エンベロープモード。"}});
        m.entries.push_back({"Ch 4 Attack", {"Channel 4 attack time.", "Channel 4 Attack 時間。", "チャンネル4アタック時間。"}});
        m.entries.push_back({"Ch 4 Release", {"Channel 4 release/decay time.", "Channel 4 Release/Decay 時間。", "チャンネル4リリース/ディケイ時間。"}});
        m.entries.push_back({"Ch 4 Out Volume", {"Channel 4 output volume multiplier.", "Channel 4 Output 音量乘數。", "チャンネル4出力音量乗数。"}});
        m.entries.push_back({"Ch 4 Manual Gate (Momentary)", {"Channel 4 momentary gate button.", "Channel 4 瞬時 Gate 按鈕。", "チャンネル4モーメンタリーゲートボタン。"}});
        m.entries.push_back({"Ch 4 Sum to Ch6", {"Channel 4 sum bus routing toggle.", "Channel 4 Sum Bus 路由切換。", "チャンネル4サムバスルーティング切替。"}});
        m.entries.push_back({"Ch 4 Env Mode (AD/AHR)", {"Channel 4 envelope mode.", "Channel 4 Envelope 模式。", "チャンネル4エンベロープモード。"}});
        m.entries.push_back({"Ch 5 Attack", {"Channel 5 attack time.", "Channel 5 Attack 時間。", "チャンネル5アタック時間。"}});
        m.entries.push_back({"Ch 5 Release", {"Channel 5 release/decay time.", "Channel 5 Release/Decay 時間。", "チャンネル5リリース/ディケイ時間。"}});
        m.entries.push_back({"Ch 5 Out Volume", {"Channel 5 output volume multiplier.", "Channel 5 Output 音量乘數。", "チャンネル5出力音量乗数。"}});
        m.entries.push_back({"Ch 5 Manual Gate (Momentary)", {"Channel 5 momentary gate button.", "Channel 5 瞬時 Gate 按鈕。", "チャンネル5モーメンタリーゲートボタン。"}});
        m.entries.push_back({"Ch 5 Sum to Ch6", {"Channel 5 sum bus routing toggle.", "Channel 5 Sum Bus 路由切換。", "チャンネル5サムバスルーティング切替。"}});
        m.entries.push_back({"Ch 5 Env Mode (AD/AHR)", {"Channel 5 envelope mode.", "Channel 5 Envelope 模式。", "チャンネル5エンベロープモード。"}});
        m.entries.push_back({"Ch 6 Attack", {"Channel 6 attack time.", "Channel 6 Attack 時間。", "チャンネル6アタック時間。"}});
        m.entries.push_back({"Ch 6 Release", {"Channel 6 release/decay time.", "Channel 6 Release/Decay 時間。", "チャンネル6リリース/ディケイ時間。"}});
        m.entries.push_back({"Ch 6 Out Volume", {"Channel 6 output volume multiplier.", "Channel 6 Output 音量乘數。", "チャンネル6出力音量乗数。"}});
        m.entries.push_back({"Ch 6 Manual Gate (Momentary)", {"Channel 6 momentary gate button.", "Channel 6 瞬時 Gate 按鈕。", "チャンネル6モーメンタリーゲートボタン。"}});
        m.entries.push_back({"Ch 6 Env Mode (AD/AHR)", {"Channel 6 envelope mode.", "Channel 6 Envelope 模式。", "チャンネル6エンベロープモード。"}});
        m.entries.push_back({"Gate Output Mode", {"Gate output behavior (right-click menu). 0=Full Cycle Gate, 1=End Trigger, 2=Start+End Triggers.", "Gate Output 行為（右鍵選單）。0=Full Cycle Gate，1=End Trigger，2=Start+End Triggers。", "ゲート出力動作（右クリックメニュー）。0=Full Cycle Gate、1=End Trigger、2=Start+End Triggers。"}});
        m.entries.push_back({"Ch 1 In L", {"Channel 1 left/mono audio input.", "Channel 1 Left/Mono Audio 輸入。", "チャンネル1左/モノオーディオ入力。"}});
        m.entries.push_back({"Ch 1 In R", {"Channel 1 right audio input (optional, L copied if unconnected).", "Channel 1 Right Audio 輸入（選用，未連接時複製 L）。", "チャンネル1右オーディオ入力（オプション、未接続時Lをコピー）。"}});
        m.entries.push_back({"Ch 1 Gate", {"Channel 1 gate/trigger input.", "Channel 1 Gate/Trigger Output（依模式而定）。", "チャンネル1ゲート/トリガー出力（モード依存）。"}});
        m.entries.push_back({"Ch 1 Vol Ctrl", {"Channel 1 volume CV (0-10V).", "Channel 1 音量 CV（0-10V）。", "チャンネル1音量CV（0-10V）。"}});
        m.entries.push_back({"Ch 2 In L", {"Channel 2 left/mono audio input.", "Channel 2 Left/Mono Audio 輸入。", "チャンネル2左/モノオーディオ入力。"}});
        m.entries.push_back({"Ch 2 In R", {"Channel 2 right audio input.", "Channel 2 Right Audio 輸入。", "チャンネル2右オーディオ入力。"}});
        m.entries.push_back({"Ch 2 Gate", {"Channel 2 gate/trigger input.", "Channel 2 Gate/Trigger Output。", "チャンネル2ゲート/トリガー出力。"}});
        m.entries.push_back({"Ch 2 Vol Ctrl", {"Channel 2 volume CV.", "Channel 2 音量 CV。", "チャンネル2音量CV。"}});
        m.entries.push_back({"Ch 3 In L", {"Channel 3 left/mono audio input.", "Channel 3 Left/Mono Audio 輸入。", "チャンネル3左/モノオーディオ入力。"}});
        m.entries.push_back({"Ch 3 In R", {"Channel 3 right audio input.", "Channel 3 Right Audio 輸入。", "チャンネル3右オーディオ入力。"}});
        m.entries.push_back({"Ch 3 Gate", {"Channel 3 gate/trigger input.", "Channel 3 Gate/Trigger Output。", "チャンネル3ゲート/トリガー出力。"}});
        m.entries.push_back({"Ch 3 Vol Ctrl", {"Channel 3 volume CV.", "Channel 3 音量 CV。", "チャンネル3音量CV。"}});
        m.entries.push_back({"Ch 4 In L", {"Channel 4 left/mono audio input.", "Channel 4 Left/Mono Audio 輸入。", "チャンネル4左/モノオーディオ入力。"}});
        m.entries.push_back({"Ch 4 In R", {"Channel 4 right audio input.", "Channel 4 Right Audio 輸入。", "チャンネル4右オーディオ入力。"}});
        m.entries.push_back({"Ch 4 Gate", {"Channel 4 gate/trigger input.", "Channel 4 Gate/Trigger Output。", "チャンネル4ゲート/トリガー出力。"}});
        m.entries.push_back({"Ch 4 Vol Ctrl", {"Channel 4 volume CV.", "Channel 4 音量 CV。", "チャンネル4音量CV。"}});
        m.entries.push_back({"Ch 5 In L", {"Channel 5 left/mono audio input.", "Channel 5 Left/Mono Audio 輸入。", "チャンネル5左/モノオーディオ入力。"}});
        m.entries.push_back({"Ch 5 In R", {"Channel 5 right audio input.", "Channel 5 Right Audio 輸入。", "チャンネル5右オーディオ入力。"}});
        m.entries.push_back({"Ch 5 Gate", {"Channel 5 gate/trigger input.", "Channel 5 Gate/Trigger Output。", "チャンネル5ゲート/トリガー出力。"}});
        m.entries.push_back({"Ch 5 Vol Ctrl", {"Channel 5 volume CV.", "Channel 5 音量 CV。", "チャンネル5音量CV。"}});
        m.entries.push_back({"Ch 6 In L", {"Channel 6 left/mono audio input.", "Channel 6 Left/Mono Audio 輸入。", "チャンネル6左/モノオーディオ入力。"}});
        m.entries.push_back({"Ch 6 In R", {"Channel 6 right audio input.", "Channel 6 Right Audio 輸入。", "チャンネル6右オーディオ入力。"}});
        m.entries.push_back({"Ch 6 Gate", {"Channel 6 gate/trigger input.", "Channel 6 Gate/Trigger Output。", "チャンネル6ゲート/トリガー出力。"}});
        m.entries.push_back({"Ch 6 Vol Ctrl", {"Channel 6 volume CV.", "Channel 6 音量 CV。", "チャンネル6音量CV。"}});
        m.entries.push_back({"Ch 1 Gate", {"Channel 1 gate/trigger output (mode-dependent).", "Channel 1 Gate/Trigger Output（依模式而定）。", "チャンネル1ゲート/トリガー出力（モード依存）。"}});
        m.entries.push_back({"Ch 1 Envelope", {"Channel 1 envelope output (0-10V).", "Channel 1 Envelope Output（0-10V）。", "チャンネル1エンベロープ出力（0-10V）。"}});
        m.entries.push_back({"Ch 1 Out L", {"Channel 1 left audio output.", "Channel 1 Left Audio Output。", "チャンネル1左オーディオ出力。"}});
        m.entries.push_back({"Ch 1 Out R", {"Channel 1 right audio output.", "Channel 1 Right Audio Output。", "チャンネル1右オーディオ出力。"}});
        m.entries.push_back({"Ch 2 Gate", {"Channel 2 gate/trigger output.", "Channel 2 Gate/Trigger Output。", "チャンネル2ゲート/トリガー出力。"}});
        m.entries.push_back({"Ch 2 Envelope", {"Channel 2 envelope output.", "Channel 2 Envelope Output。", "チャンネル2エンベロープ出力。"}});
        m.entries.push_back({"Ch 2 Out L", {"Channel 2 left audio output.", "Channel 2 Left Audio Output。", "チャンネル2左オーディオ出力。"}});
        m.entries.push_back({"Ch 2 Out R", {"Channel 2 right audio output.", "Channel 2 Right Audio Output。", "チャンネル2右オーディオ出力。"}});
        m.entries.push_back({"Ch 3 Gate", {"Channel 3 gate/trigger output.", "Channel 3 Gate/Trigger Output。", "チャンネル3ゲート/トリガー出力。"}});
        m.entries.push_back({"Ch 3 Envelope", {"Channel 3 envelope output.", "Channel 3 Envelope Output。", "チャンネル3エンベロープ出力。"}});
        m.entries.push_back({"Ch 3 Out L", {"Channel 3 left audio output.", "Channel 3 Left Audio Output。", "チャンネル3左オーディオ出力。"}});
        m.entries.push_back({"Ch 3 Out R", {"Channel 3 right audio output.", "Channel 3 Right Audio Output。", "チャンネル3右オーディオ出力。"}});
        m.entries.push_back({"Ch 4 Gate", {"Channel 4 gate/trigger output.", "Channel 4 Gate/Trigger Output。", "チャンネル4ゲート/トリガー出力。"}});
        m.entries.push_back({"Ch 4 Envelope", {"Channel 4 envelope output.", "Channel 4 Envelope Output。", "チャンネル4エンベロープ出力。"}});
        m.entries.push_back({"Ch 4 Out L", {"Channel 4 left audio output.", "Channel 4 Left Audio Output。", "チャンネル4左オーディオ出力。"}});
        m.entries.push_back({"Ch 4 Out R", {"Channel 4 right audio output.", "Channel 4 Right Audio Output。", "チャンネル4右オーディオ出力。"}});
        m.entries.push_back({"Ch 5 Gate", {"Channel 5 gate/trigger output.", "Channel 5 Gate/Trigger Output。", "チャンネル5ゲート/トリガー出力。"}});
        m.entries.push_back({"Ch 5 Envelope", {"Channel 5 envelope output.", "Channel 5 Envelope Output。", "チャンネル5エンベロープ出力。"}});
        m.entries.push_back({"Ch 5 Out L", {"Channel 5 left audio output.", "Channel 5 Left Audio Output。", "チャンネル5左オーディオ出力。"}});
        m.entries.push_back({"Ch 5 Out R", {"Channel 5 right audio output.", "Channel 5 Right Audio Output。", "チャンネル5右オーディオ出力。"}});
        m.entries.push_back({"Ch 6 Gate", {"Channel 6 gate/trigger output.", "Channel 6 Gate/Trigger Output。", "チャンネル6ゲート/トリガー出力。"}});
        m.entries.push_back({"Ch 6 Envelope / Sum Envelope", {"Channel 6 envelope output / sum envelope.", "Channel 6 Envelope Output / Sum Envelope。", "チャンネル6エンベロープ出力 / サムエンベロープ。"}});
        m.entries.push_back({"Ch 6 Out L / Sum L", {"Channel 6 left audio output / sum left.", "Channel 6 Left Audio Output / Sum Left。", "チャンネル6左オーディオ出力 / サム左。"}});
        m.entries.push_back({"Ch 6 Out R / Sum R", {"Channel 6 right audio output / sum right.", "Channel 6 Right Audio Output / Sum Right。", "チャンネル6右オーディオ出力 / サム右。"}});
        data["EnvVCA6"] = std::move(m);
    }
    // EuclideanRhythm (62 entries)
    {
        ModuleHelpData m;
        m.name = "Euclidean Rhythm";
        m.description = {"Three independent Euclidean rhythm tracks with per-track clock division/multiplication and chain outputs for sequential switching", "三軌獨立 Euclidean Rhythm，具備各軌 Clock Division/Multiplication 與 Chain 輸出，用於順序切換", "トラック別クロック分割/倍増とシーケンシャル切替用チェーン出力を備えた3トラック独立ユークリッドリズム"};
        m.entries.push_back({"Principle", {"Distributes hits as evenly as possible across the pattern length", "Distributes hits as evenly as possible across the pattern length", "Distributes hits as evenly as possible across the pattern length"}});
        m.entries.push_back({"FILL", {"Determines the number of hits in the pattern. Higher fill = more hits.", "活躍步進百分比（0-100%）。預設: T1=100%，T2=50%，T3=25%。CV 加 Volts × Attenuverter × 10。", "アクティブステップの割合（0-100%）。デフォルト: T1=100%、T2=50%、T3=25%。CVはボルト × アッテヌバーター × 10を加算。"}});
        m.entries.push_back({"SHIFT", {"Rotates the pattern starting point without changing rhythm density", "旋轉 Pattern 起始點，不改變節奏密度", "リズム密度を変えずにパターンの開始点を回転"}});
        m.entries.push_back({"Recalculation", {"Pattern updates immediately when FILL, LEN, or SHIFT changes", "Pattern updates immediately when FILL, LEN, or SHIFT changes", "Pattern updates immediately when FILL, LEN, or SHIFT changes"}});
        m.entries.push_back({"Example", {"E(5,8) = [xx.xx.x.] distributes 5 hits evenly across 8 steps", "E(5,8) = [xx.xx.x.] distributes 5 hits evenly across 8 steps", "E(5,8) = [xx.xx.x.] distributes 5 hits evenly across 8 steps"}});
        m.entries.push_back({"Range", {"-3 to +3 (snapped to integers)", "-3 to +3 (snapped to integers)", "-3 to +3 (snapped to integers)"}});
        m.entries.push_back({"Negative Values", {"Division. -1 = /2x, -2 = /3x, -3 = /4x", "Division. -1 = /2x, -2 = /3x, -3 = /4x", "Division. -1 = /2x, -2 = /3x, -3 = /4x"}});
        m.entries.push_back({"Positive Values", {"Multiplication. +1 = 2x, +2 = 3x, +3 = 4x", "Multiplication. +1 = 2x, +2 = 3x, +3 = 4x", "Multiplication. +1 = 2x, +2 = 3x, +3 = 4x"}});
        m.entries.push_back({"Zero", {"1:1 ratio (no change)", "1:1 ratio (no change)", "1:1 ratio (no change)"}});
        m.entries.push_back({"Implementation", {"Tracks elapsed time since last global clock, generates internal triggers at multiplied rate or counts down for division", "Tracks elapsed time since last global clock, generates internal triggers at multiplied rate or counts down for division", "Tracks elapsed time since last global clock, generates internal triggers at multiplied rate or counts down for division"}});
        m.entries.push_back({"Gate Length", {"50% of the effective clock period, minimum 1ms", "50% of the effective clock period, minimum 1ms", "50% of the effective clock period, minimum 1ms"}});
        m.entries.push_back({"CLK", {"Global clock input. Period is measured between rising edges (Schmitt trigger). Valid range: 10ms to 10s.", "全域 Clock 輸入。週期以上升緣間測量（Schmitt Trigger）。有效範圍: 10ms 至 10s。", "グローバルクロック入力。周期は立ち上がりエッジ間で測定（シュミットトリガー）。有効範囲: 10ms〜10s。"}});
        m.entries.push_back({"RST", {"Reset input + manual button. Rising edge resets all tracks to step 0 and clears all state.", "Reset 輸入 + 手動按鈕。上升緣重置所有軌道至步進 0 並清除所有狀態。", "リセット入力 + 手動ボタン。立ち上がりエッジで全トラックをステップ0にリセットし、全ての状態をクリア。"}});
        m.entries.push_back({"LEN", {"Sequence length (1-32 steps, snapped). Default: 16. CV adds volts × attenuverter to base value.", "序列長度（1-32 步，對齊）。預設: 16。CV 加 Volts × Attenuverter 至基礎值。", "シーケンス長（1-32ステップ、スナップ）。デフォルト: 16。CVはボルト × アッテヌバーターをベース値に加算。"}});
        m.entries.push_back({"FILL", {"Active steps as percentage (0-100%). Default: T1=100%, T2=50%, T3=25%. CV adds volts × attenuverter × 10.", "活躍步進百分比（0-100%）。預設: T1=100%，T2=50%，T3=25%。CV 加 Volts × Attenuverter × 10。", "アクティブステップの割合（0-100%）。デフォルト: T1=100%、T2=50%、T3=25%。CVはボルト × アッテヌバーター × 10を加算。"}});
        m.entries.push_back({"SHFT", {"Pattern rotation (0 to 31 steps, snapped; clamped to length-1 in processing). Rotates pattern left by shift amount.", "Pattern 旋轉（0 至 31 步，對齊；處理時 Clamp 至 Length-1）。向左旋轉 Pattern Shift 量。", "パターンローテーション（0〜31ステップ、スナップ；処理時にLength-1にクランプ）。パターンを左にShift量だけローテーション。"}});
        m.entries.push_back({"D/M", {"Clock division/multiplication (-3 to +3). Default: T1=0, T2=+1 (2x), T3=+2 (3x).", "Clock Division/Multiplication（-3 至 +3）。預設: T1=0，T2=+1 (2x)，T3=+2 (3x)。", "クロック分割/倍増（-3〜+3）。デフォルト: T1=0、T2=+1 (2x)、T3=+2 (3x)。"}});
        m.entries.push_back({"Attenuverters", {"±1.0 range for each CV input (Length, Fill, Shift).", "±1.0 range for each CV input (Length, Fill, Shift).", "±1.0 range for each CV input (Length, Fill, Shift)."}});
        m.entries.push_back({"1+2", {"Alternates T1 → T2 → T1 → T2...", "軌道 1 與 2 的 Chain 輸出", "トラック1と2のチェーン出力"}});
        m.entries.push_back({"2+3", {"Alternates T2 → T3 → T2 → T3...", "軌道 2 與 3 的 Chain 輸出", "トラック2と3のチェーン出力"}});
        m.entries.push_back({"1213 (1+2+3)", {"Sequence T1 → T2 → T1 → T3 → repeat. This creates an A-B-A-C pattern.", "序列 T1 → T2 → T1 → T3 → 重複。創造 A-B-A-C Pattern。", "T1 → T2 → T1 → T3 → 繰り返しのシーケンス。A-B-A-Cパターンを作成。"}});
        m.entries.push_back({"Cycle Length", {"Each track completes one cycle based on its length and clock division/multiplication settings", "Each track completes one cycle based on its length and clock division/multiplication settings", "Each track completes one cycle based on its length and clock division/multiplication settings"}});
        m.entries.push_back({"LED Indicators", {"Show which track is currently active in each chain", "Show which track is currently active in each chain", "Show which track is currently active in each chain"}});
        m.entries.push_back({"Function", {"Outputs 10V when ANY of the three tracks fires", "Outputs 10V when ANY of the three tracks fires", "Outputs 10V when ANY of the three tracks fires"}});
        m.entries.push_back({"RGB LED", {"Red = T1 active, Green = T2 active, Blue = T3 active (30ms hold)", "紅 = T1 活躍，綠 = T2 活躍，藍 = T3 活躍（30ms 保持）", "赤 = T1アクティブ、緑 = T2アクティブ、青 = T3アクティブ（30ms保持）"}});
        m.entries.push_back({"Use Case", {"Composite trigger for driving external modules", "Composite trigger for driving external modules", "Composite trigger for driving external modules"}});
        m.entries.push_back({"OUT 1/2/3", {"Individual track triggers (0V or 10V, 10ms pulse)", "獨立軌道 Trigger（0V 或 10V，10ms Pulse）", "個別トラックトリガー（0Vまたは10V、10msパルス）"}});
        m.entries.push_back({"1+2", {"Chain output for tracks 1 and 2", "軌道 1 與 2 的 Chain 輸出", "トラック1と2のチェーン出力"}});
        m.entries.push_back({"2+3", {"Chain output for tracks 2 and 3", "軌道 2 與 3 的 Chain 輸出", "トラック2と3のチェーン出力"}});
        m.entries.push_back({"1213", {"Chain output following T1→T2→T1→T3 pattern", "遵循 T1→T2→T1→T3 Pattern 的 Chain 輸出", "T1→T2→T1→T3パターンに従うチェーン出力"}});
        m.entries.push_back({"OR", {"Logical OR of all three track outputs", "所有三軌輸出的邏輯 OR", "全3トラック出力の論理OR"}});
        m.entries.push_back({"Global Clock", {"Global clock input. Period measured between rising edges. Valid range 10ms-10s.", "全域 Clock 輸入。週期以上升緣間測量。有效範圍 10ms-10s。", "グローバルクロック入力。立ち上がりエッジ間で周期測定。有効範囲10ms-10s。"}});
        m.entries.push_back({"Global Reset", {"Reset input. Rising edge resets all tracks to step 0.", "Reset 輸入。上升緣重置所有軌道至步進 0。", "リセット入力。立ち上がりエッジで全トラックをステップ0にリセット。"}});
        m.entries.push_back({"Manual Reset", {"Manual reset button. Same function as Global Reset input.", "手動 Reset 按鈕。功能與 Global Reset 輸入相同。", "手動リセットボタン。Global Reset入力と同じ機能。"}});
        m.entries.push_back({"T1 Length", {"Track 1 sequence length (1-32 steps, snapped). Default: 16.", "軌道 1 序列長度（1-32 步，對齊）。預設: 16。", "トラック1シーケンス長（1-32ステップ、スナップ）。デフォルト: 16。"}});
        m.entries.push_back({"T1 Fill", {"Track 1 active steps percentage (0-100%). Default: 100%.", "軌道 1 活躍步進百分比（0-100%）。預設: 100%。", "トラック1アクティブステップ割合（0-100%）。デフォルト: 100%。"}});
        m.entries.push_back({"T1 Shift", {"Track 1 pattern rotation (0-31 steps, snapped). Default: 0.", "軌道 1 Pattern 旋轉（0-31 步，對齊）。預設: 0。", "トラック1パターンローテーション（0-31ステップ、スナップ）。デフォルト: 0。"}});
        m.entries.push_back({"T1 Div/Mult", {"Track 1 clock division/multiplication (-3 to +3). Default: 0 (1x).", "軌道 1 Clock Division/Multiplication（-3 至 +3）。預設: 0 (1x)。", "トラック1クロック分割/倍増（-3〜+3）。デフォルト: 0 (1x)。"}});
        m.entries.push_back({"T1 Length CV", {"Track 1 length CV attenuverter/input (-1.0 to +1.0).", "軌道 1 Length CV Attenuverter/輸入（-1.0 至 +1.0）。", "トラック1 Length CVアッテヌバーター/入力（-1.0〜+1.0）。"}});
        m.entries.push_back({"T1 Fill CV", {"Track 1 fill CV attenuverter/input (-1.0 to +1.0).", "軌道 1 Fill CV Attenuverter/輸入（-1.0 至 +1.0）。", "トラック1 Fill CVアッテヌバーター/入力（-1.0〜+1.0）。"}});
        m.entries.push_back({"T1 Shift CV", {"Track 1 shift CV attenuverter/input (-1.0 to +1.0).", "軌道 1 Shift CV Attenuverter/輸入（-1.0 至 +1.0）。", "トラック1 Shift CVアッテヌバーター/入力（-1.0〜+1.0）。"}});
        m.entries.push_back({"T2 Length", {"Track 2 sequence length (1-32 steps). Default: 16.", "軌道 2 序列長度（1-32 步）。預設: 16。", "トラック2シーケンス長（1-32ステップ）。デフォルト: 16。"}});
        m.entries.push_back({"T2 Fill", {"Track 2 active steps percentage (0-100%). Default: 50%.", "軌道 2 活躍步進百分比（0-100%）。預設: 50%。", "トラック2アクティブステップ割合（0-100%）。デフォルト: 50%。"}});
        m.entries.push_back({"T2 Shift", {"Track 2 pattern rotation (0-31 steps). Default: 0.", "軌道 2 Pattern 旋轉（0-31 步）。預設: 0。", "トラック2パターンローテーション（0-31ステップ）。デフォルト: 0。"}});
        m.entries.push_back({"T2 Div/Mult", {"Track 2 clock division/multiplication. Default: +1 (2x).", "軌道 2 Clock Division/Multiplication。預設: +1 (2x)。", "トラック2クロック分割/倍増。デフォルト: +1 (2x)。"}});
        m.entries.push_back({"T2 Length CV", {"Track 2 length CV attenuverter/input.", "軌道 2 Length CV Attenuverter/輸入。", "トラック2 Length CVアッテヌバーター/入力。"}});
        m.entries.push_back({"T2 Fill CV", {"Track 2 fill CV attenuverter/input.", "軌道 2 Fill CV Attenuverter/輸入。", "トラック2 Fill CVアッテヌバーター/入力。"}});
        m.entries.push_back({"T2 Shift CV", {"Track 2 shift CV attenuverter/input.", "軌道 2 Shift CV Attenuverter/輸入。", "トラック2 Shift CVアッテヌバーター/入力。"}});
        m.entries.push_back({"T3 Length", {"Track 3 sequence length (1-32 steps). Default: 16.", "軌道 3 序列長度（1-32 步）。預設: 16。", "トラック3シーケンス長（1-32ステップ）。デフォルト: 16。"}});
        m.entries.push_back({"T3 Fill", {"Track 3 active steps percentage (0-100%). Default: 25%.", "軌道 3 活躍步進百分比（0-100%）。預設: 25%。", "トラック3アクティブステップ割合（0-100%）。デフォルト: 25%。"}});
        m.entries.push_back({"T3 Shift", {"Track 3 pattern rotation (0-31 steps). Default: 0.", "軌道 3 Pattern 旋轉（0-31 步）。預設: 0。", "トラック3パターンローテーション（0-31ステップ）。デフォルト: 0。"}});
        m.entries.push_back({"T3 Div/Mult", {"Track 3 clock division/multiplication. Default: +2 (3x).", "軌道 3 Clock Division/Multiplication。預設: +2 (3x)。", "トラック3クロック分割/倍増。デフォルト: +2 (3x)。"}});
        m.entries.push_back({"T3 Length CV", {"Track 3 length CV attenuverter/input.", "軌道 3 Length CV Attenuverter/輸入。", "トラック3 Length CVアッテヌバーター/入力。"}});
        m.entries.push_back({"T3 Fill CV", {"Track 3 fill CV attenuverter/input.", "軌道 3 Fill CV Attenuverter/輸入。", "トラック3 Fill CVアッテヌバーター/入力。"}});
        m.entries.push_back({"T3 Shift CV", {"Track 3 shift CV attenuverter/input.", "軌道 3 Shift CV Attenuverter/輸入。", "トラック3 Shift CVアッテヌバーター/入力。"}});
        m.entries.push_back({"T1 Trigger", {"Track 1 trigger output (0V or 10V, 10ms pulse).", "軌道 1 Trigger 輸出（0V 或 10V，10ms Pulse）。", "トラック1トリガー出力（0Vまたは10V、10msパルス）。"}});
        m.entries.push_back({"T2 Trigger", {"Track 2 trigger output (0V or 10V, 10ms pulse).", "軌道 2 Trigger 輸出（0V 或 10V，10ms Pulse）。", "トラック2トリガー出力（0Vまたは10V、10msパルス）。"}});
        m.entries.push_back({"T3 Trigger", {"Track 3 trigger output (0V or 10V, 10ms pulse).", "軌道 3 Trigger 輸出（0V 或 10V，10ms Pulse）。", "トラック3トリガー出力（0Vまたは10V、10msパルス）。"}});
        m.entries.push_back({"Master Trigger Sum", {"OR output, 10V when any track fires.", "OR 輸出，任一軌道觸發時輸出 10V。", "OR出力、いずれかのトラックが発火すると10V。"}});
        m.entries.push_back({"Chain 1+2", {"Chain output alternating T1 and T2.", "交替 T1 與 T2 的 Chain 輸出。", "T1とT2を交互に切替えるチェーン出力。"}});
        m.entries.push_back({"Chain 2+3", {"Chain output alternating T2 and T3.", "交替 T2 與 T3 的 Chain 輸出。", "T2とT3を交互に切替えるチェーン出力。"}});
        m.entries.push_back({"Chain 1+2+3", {"Chain output following T1-T2-T1-T3 pattern.", "遵循 T1-T2-T1-T3 Pattern 的 Chain 輸出。", "T1-T2-T1-T3パターンに従うチェーン出力。"}});
        data["EuclideanRhythm"] = std::move(m);
    }
    // Facehugger (31 entries)
    {
        ModuleHelpData m;
        m.name = "Facehugger";
        m.description = {"Chaos-modulated granular processor with Lorenz attractor modulation and sample-and-hold output", "Chaos 調變 Granular Processor，具備 Lorenz Attractor 調變與 Sample-and-Hold 輸出", "ローレンツアトラクター変調とサンプル&ホールド出力を備えたカオス変調グラニュラープロセッサー"};
        m.entries.push_back({"Size", {"Grain duration (1-100ms, default: 30%). Small = glitchy textures, large = smooth time-stretch.", "Grain 長度（1-100ms，預設：30%）。小 = Glitchy 質感，大 = 平滑 Time-Stretch。", "グレイン長（1-100ms、デフォルト：30%）。小=グリッチーなテクスチャ、大=スムースなタイムストレッチ。"}});
        m.entries.push_back({"Break", {"Grain density/trigger rate (0-100%, default: 40%). 0 = sparse grains, 100% = up to 51 grains/second.", "Grain 密度/觸發速率（0-100%，預設：40%）。0 = 稀疏 Grain，100% = 最多 51 Grain/秒。", "グレイン密度/トリガーレート（0-100%、デフォルト：40%）。0=まばらなグレイン、100%=最大51グレイン/秒。"}});
        m.entries.push_back({"Shift", {"Buffer read position offset (0-100%, default: 50%). Controls where in the buffer grains start playback.", "Buffer 讀取位置偏移（0-100%，預設：50%）。控制 Grain 從 Buffer 何處開始播放。", "バッファ読み取り位置オフセット（0-100%、デフォルト：50%）。グレインがバッファのどこから再生を開始するかを制御。"}});
        m.entries.push_back({"Mix", {"Wet/dry balance (0-100%, default: 50%). CV adds ±10%.", "Wet/Dry 平衡（0-100%，預設：50%）。CV 增加 ±10%。", "ウェット/ドライバランス（0-100%、デフォルト：50%）。CVで±10%追加。"}});
        m.entries.push_back({"Max Grains", {"16 simultaneous grains", "16 simultaneous grains", "16 simultaneous grains"}});
        m.entries.push_back({"Buffer Size", {"8,192 samples (~170ms at 48kHz)", "8,192 samples (~170ms at 48kHz)", "8,192 samples (~170ms at 48kHz)"}});
        m.entries.push_back({"Envelope", {"Raised cosine (Hanning) window for smooth crossfades", "Raised Cosine（Hanning）視窗，平滑交叉淡化", "Raised cosine (Hanning) window for smooth crossfades"}});
        m.entries.push_back({"Output Normalization", {"Amplitude scales with active grain count to prevent clipping", "Amplitude scales with active grain count to prevent clipping", "Amplitude scales with active grain count to prevent clipping"}});
        m.entries.push_back({"Algorithm", {"Lorenz strange attractor differential equations", "Lorenz strange attractor differential equations", "Lorenz strange attractor differential equations"}});
        m.entries.push_back({"Chaos", {"Modulation depth (0-100%, default: 0%). Controls chaos effects on granular parameters.", "調變深度（0-100%，預設：0%）。控制 Chaos 對 Granular 參數的影響。", "変調深度（0-100%、デフォルト：0%）。カオスがグラニュラーパラメータに与える影響を制御。"}});
        m.entries.push_back({"Rate", {"Chaos evolution speed (0.01-2.0x, default: 0.5x). Higher = faster parameter changes.", "Chaos 演化速度（0.01-2.0x，預設：0.5x）。越高 = 參數變化越快。", "カオス進化速度（0.01-2.0x、デフォルト：0.5x）。高いほどパラメータ変化が速い。"}});
        m.entries.push_back({"Chaos Effects", {"Position ±20 samples, 30% chance reverse playback, 20% chance octave shift (0.5× or 2×)", "Position ±20 samples, 30% chance reverse playback, 20% chance octave shift (0.5× or 2×)", "Position ±20 samples, 30% chance reverse playback, 20% chance octave shift (0.5× or 2×)"}});
        m.entries.push_back({"S&H Output", {"Stepped chaos signal sampled at RATE × 10 Hz", "Stepped chaos signal sampled at RATE × 10 Hz", "Stepped chaos signal sampled at RATE × 10 Hz"}});
        m.entries.push_back({"Range", {"0-5V CV output", "0-5V CV output", "0-5V CV output"}});
        m.entries.push_back({"Use", {"External modulation source with quantized, stepped behavior", "External modulation source with quantized, stepped behavior", "External modulation source with quantized, stepped behavior"}});
        m.entries.push_back({"Detection", {"When placed directly right of YAMANOTE, auto-connects to send/return", "When placed directly right of YAMANOTE, auto-connects to send/return", "When placed directly right of YAMANOTE, auto-connects to send/return"}});
        m.entries.push_back({"Channel Selection", {"Uses Channel A if single effect, Channel B if part of chain", "Uses Channel A if single effect, Channel B if part of chain", "Uses Channel A if single effect, Channel B if part of chain"}});
        m.entries.push_back({"Chain Support", {"Multiple effects (Runner, Facehugger, Ovomorph) auto-distribute across channels", "Multiple effects (Runner, Facehugger, Ovomorph) auto-distribute across channels", "Multiple effects (Runner, Facehugger, Ovomorph) auto-distribute across channels"}});
        m.entries.push_back({"Left Audio", {"Left stereo input", "左聲道處理輸出（Polyphonic）", "左処理出力（ポリフォニック）"}});
        m.entries.push_back({"Right Audio", {"Right stereo input (normalizes to Left if disconnected)", "右聲道處理輸出（Polyphonic）", "右処理出力（ポリフォニック）"}});
        m.entries.push_back({"Size CV", {"Grain size modulation", "Grain 大小調變", "グレインサイズ変調"}});
        m.entries.push_back({"Break CV", {"Grain density modulation", "Grain 密度調變", "グレイン密度変調"}});
        m.entries.push_back({"Shift CV", {"Buffer position modulation", "Buffer 位置調變", "バッファ位置変調"}});
        m.entries.push_back({"Mix CV", {"Wet/dry modulation", "Wet/Dry 調變", "ウェット/ドライ変調"}});
        m.entries.push_back({"Chaos CV", {"Chaos depth modulation", "原始 Chaos 訊號 0-5V（Polyphonic）", "生カオス信号0-5V（ポリフォニック）"}});
        m.entries.push_back({"Rate CV", {"Chaos speed modulation", "Chaos 速度調變", "カオス速度変調"}});
        m.entries.push_back({"Left Audio", {"Left processed output (polyphonic)", "左聲道處理輸出（Polyphonic）", "左処理出力（ポリフォニック）"}});
        m.entries.push_back({"Right Audio", {"Right processed output (polyphonic)", "右聲道處理輸出（Polyphonic）", "右処理出力（ポリフォニック）"}});
        m.entries.push_back({"Chaos CV", {"Raw chaos signal 0-5V (polyphonic)", "原始 Chaos 訊號 0-5V（Polyphonic）", "生カオス信号0-5V（ポリフォニック）"}});
        m.entries.push_back({"Sample &amp; Hold CV", {"Sample-and-hold chaos 0-5V (polyphonic)", "Sample-and-Hold Chaos 0-5V（Polyphonic）", "サンプル&ホールドカオス0-5V（ポリフォニック）"}});
        m.entries.push_back({"Sample & Hold CV", {"Sample & Hold output from Chaos section. Samples random values at Chaos Rate, with slew limiting.", "Chaos 區段的 Sample & Hold 輸出。依 Chaos Rate 取樣隨機值，帶有 Slew 限制。", "Chaosセクションのサンプル＆ホールド出力。Chaos Rateでランダム値をサンプリングし、スルーリミッティング付き。"}});
        data["Facehugger"] = std::move(m);
    }
    // KEN (47 entries)
    {
        ModuleHelpData m;
        m.name = "KEN";
        m.description = {"8-channel binaural processor using HRTF with independent azimuth/elevation per channel and ILD/ITD modeling", "使用 HRTF 處理的 8 聲道 Binaural Processor，具備各聲道獨立 Azimuth/Elevation 與 ILD/ITD 模擬", "HRTF処理を使用した8チャンネルバイノーラルプロセッサー、チャンネル別独立方位角・仰角とILD/ITDモデリング"};
        m.entries.push_back({"Head Width", {"0.18m (18cm anthropometric model)", "0.18m (18cm anthropometric model)", "0.18m (18cm anthropometric model)"}});
        m.entries.push_back({"Sound Speed", {"343 m/s (at sea level, 20°C)", "343 m/s (at sea level, 20°C)", "343 m/s (at sea level, 20°C)"}});
        m.entries.push_back({"ITD Calculation", {"Delay based on head width divided by sound speed, scaled by azimuth sine", "Delay based on head width divided by sound speed, scaled by azimuth sine", "Delay based on head width divided by sound speed, scaled by azimuth sine"}});
        m.entries.push_back({"Sample Delay", {"ITD converted to samples at current sample rate", "ITD 轉換為當前 Sample Rate 的 Samples", "ITD converted to samples at current sample rate"}});
        m.entries.push_back({"Delay Buffer", {"128 samples per ear per channel (circular buffer)", "每耳每聲道 128 Samples（環形 Buffer）", "128 samples per ear per channel (circular buffer)"}});
        m.entries.push_back({"Application", {"Positive azimuth delays left ear, negative azimuth delays right ear", "Positive azimuth delays left ear, negative azimuth delays right ear", "Positive azimuth delays left ear, negative azimuth delays right ear"}});
        m.entries.push_back({"Azimuth Effect", {"Sources on opposite side attenuated up to 80%", "Sources on opposite side attenuated up to 80%", "Sources on opposite side attenuated up to 80%"}});
        m.entries.push_back({"Same-Side Boost", {"Sources on same side boosted up to 30%", "Sources on same side boosted up to 30%", "Sources on same side boosted up to 30%"}});
        m.entries.push_back({"Head Shadow", {"Rear sources (|azimuth| > 90°) attenuated by 40%", "Rear sources (|azimuth| > 90°) attenuated by 40%", "Rear sources (|azimuth| > 90°) attenuated by 40%"}});
        m.entries.push_back({"Elevation Effect", {"Above horizon boosted up to 80%, below horizon attenuated up to 70%", "Above horizon boosted up to 80%, below horizon attenuated up to 70%", "Above horizon boosted up to 80%, below horizon attenuated up to 70%"}});
        m.entries.push_back({"Distance Attenuation", {"Inverse-square law with offset for smooth falloff", "Inverse-square law with offset for smooth falloff", "Inverse-square law with offset for smooth falloff"}});
        m.entries.push_back({"Final Gain", {"Clamped to range 0.1 to 1.5", "Clamped to range 0.1 to 1.5", "Clamped to range 0.1 to 1.5"}});
        m.entries.push_back({"Type", {"Lowpass biquad filter per channel per ear", "Lowpass biquad filter per channel per ear", "Lowpass biquad filter per channel per ear"}});
        m.entries.push_back({"Cutoff Behavior", {"Higher frequencies attenuated with increasing distance", "Higher frequencies attenuated with increasing distance", "Higher frequencies attenuated with increasing distance"}});
        m.entries.push_back({"Range", {"1kHz to 20kHz (clamped)", "1kHz to 20kHz (clamped)", "1kHz to 20kHz (clamped)"}});
        m.entries.push_back({"Q Factor", {"0.8", "0.8", "0.8"}});
        m.entries.push_back({"Purpose", {"Simulates high-frequency attenuation over distance (air absorption)", "Simulates high-frequency attenuation over distance (air absorption)", "Simulates high-frequency attenuation over distance (air absorption)"}});
        m.entries.push_back({"Above Horizon (elevation > 0)", {"Peak filter at 8kHz + elevation×40Hz, gain 2.0, Q=1.5. Boosts high frequencies for \"above\" perception.", "Peak filter at 8kHz + elevation×40Hz, gain 2.0, Q=1.5. Boosts high frequencies for \"above\" perception.", "Peak filter at 8kHz + elevation×40Hz, gain 2.0, Q=1.5. Boosts high frequencies for \"above\" perception."}});
        m.entries.push_back({"Purpose", {"Simulates pinna filtering based on vertical angle", "Simulates pinna filtering based on vertical angle", "Simulates pinna filtering based on vertical angle"}});
        m.entries.push_back({"Type", {"Highpass filter at 3kHz, Q=0.7", "Highpass filter at 3kHz, Q=0.7", "Highpass filter at 3kHz, Q=0.7"}});
        m.entries.push_back({"Gain", {"Increases with distance (base 0.1 plus distance scaling)", "隨距離增加（基值 0.1 加上距離縮放）", "Increases with distance (base 0.1 plus distance scaling)"}});
        m.entries.push_back({"Mix", {"Proportional to distance (distant sources have more reverb)", "與距離成比例（遠距離來源有更多 Reverb）", "Proportional to distance (distant sources have more reverb)"}});
        m.entries.push_back({"Purpose", {"Distant sources have more high-frequency reverb content", "Distant sources have more high-frequency reverb content", "Distant sources have more high-frequency reverb content"}});
        m.entries.push_back({"Input 1", {"FL Upper (azimuth -45°, elevation +30°, distance 0.5)", "FL Upper（方位角 -45°，仰角 +30°，距離 0.5）", "FL Upper（方位角-45°、仰角+30°、距離0.5）"}});
        m.entries.push_back({"Input 2", {"FR Upper (azimuth +45°, elevation +30°, distance 0.5)", "FR Upper（方位角 +45°，仰角 +30°，距離 0.5）", "FR Upper（方位角+45°、仰角+30°、距離0.5）"}});
        m.entries.push_back({"Input 3", {"BL Upper (azimuth -135°, elevation +30°, distance 2.0)", "BL Upper（方位角 -135°，仰角 +30°，距離 2.0）", "BL Upper（方位角-135°、仰角+30°、距離2.0）"}});
        m.entries.push_back({"Input 4", {"BR Upper (azimuth +135°, elevation +30°, distance 2.0)", "BR Upper（方位角 +135°，仰角 +30°，距離 2.0）", "BR Upper（方位角+135°、仰角+30°、距離2.0）"}});
        m.entries.push_back({"Input 5", {"FL Lower (azimuth -45°, elevation -30°, distance 0.5)", "FL Lower（方位角 -45°，仰角 -30°，距離 0.5）", "FL Lower（方位角-45°、仰角-30°、距離0.5）"}});
        m.entries.push_back({"Input 6", {"FR Lower (azimuth +45°, elevation -30°, distance 0.5)", "FR Lower（方位角 +45°，仰角 -30°，距離 0.5）", "FR Lower（方位角+45°、仰角-30°、距離0.5）"}});
        m.entries.push_back({"Input 7", {"BL Lower (azimuth -135°, elevation -30°, distance 2.0)", "BL Lower（方位角 -135°，仰角 -30°，距離 2.0）", "BL Lower（方位角-135°、仰角-30°、距離2.0）"}});
        m.entries.push_back({"Input 8", {"BR Lower (azimuth +135°, elevation -30°, distance 2.0)", "BR Lower（方位角 +135°，仰角 -30°，距離 2.0）", "BR Lower（方位角+135°、仰角-30°、距離2.0）"}});
        m.entries.push_back({"Level", {"Master output level (0.0 to 1.0, default: 0.7). Applied after all HRTF processing.", "Master Output Level（0.0 至 1.0，預設：0.7）。在所有 HRTF 處理後套用。", "マスター出力レベル（0.0〜1.0、デフォルト：0.7）。全HRTF処理後に適用。"}});
        m.entries.push_back({"Coefficient", {"0.001 per sample", "0.001 per sample", "0.001 per sample"}});
        m.entries.push_back({"Behavior", {"Exponential smoothing with 0.001 coefficient per sample", "Exponential smoothing with 0.001 coefficient per sample", "Exponential smoothing with 0.001 coefficient per sample"}});
        m.entries.push_back({"Purpose", {"Prevents clicks when input connections change", "Prevents clicks when input connections change", "Prevents clicks when input connections change"}});
        m.entries.push_back({"1", {"Input 1 - FL Upper spatial audio from Pyramid or DECAPyramid", "Input 1 - FL Upper 空間 Audio（來自 Pyramid 或 DECAPyramid）", "Input 1 - FL Upper 空間オーディオ（PyramidまたはDECAPyramidから）"}});
        m.entries.push_back({"2", {"Input 2 - FR Upper spatial audio", "Input 2 - FR Upper 空間 Audio", "Input 2 - FR Upper 空間オーディオ"}});
        m.entries.push_back({"3", {"Input 3 - BL Upper spatial audio", "Input 3 - BL Upper 空間 Audio", "Input 3 - BL Upper 空間オーディオ"}});
        m.entries.push_back({"4", {"Input 4 - BR Upper spatial audio", "Input 4 - BR Upper 空間 Audio", "Input 4 - BR Upper 空間オーディオ"}});
        m.entries.push_back({"5", {"Input 5 - FL Lower spatial audio", "Input 5 - FL Lower 空間 Audio", "Input 5 - FL Lower 空間オーディオ"}});
        m.entries.push_back({"6", {"Input 6 - FR Lower spatial audio", "Input 6 - FR Lower 空間 Audio", "Input 6 - FR Lower 空間オーディオ"}});
        m.entries.push_back({"7", {"Input 7 - BL Lower spatial audio", "Input 7 - BL Lower 空間 Audio", "Input 7 - BL Lower 空間オーディオ"}});
        m.entries.push_back({"8", {"Input 8 - BR Lower spatial audio", "Input 8 - BR Lower 空間 Audio", "Input 8 - BR Lower 空間オーディオ"}});
        m.entries.push_back({"Left", {"Left ear binaural output", "左耳 Binaural 輸出", "左耳バイノーラル出力"}});
        m.entries.push_back({"Right", {"Right ear binaural output", "右耳 Binaural 輸出", "右耳バイノーラル出力"}});
        m.entries.push_back({"Pyramid", {"Single-source 3D panner with 8 outputs", "單一來源 3D Panner，8 輸出", "単一ソース3Dパンナー、8出力"}});
        m.entries.push_back({"DECAPyramid", {"8-channel 3D mixer with 8 outputs", "8 聲道 3D Mixer，8 輸出", "8チャンネル3Dミキサー、8出力"}});
        data["KEN"] = std::move(m);
    }
    // KIMO (53 entries)
    {
        ModuleHelpData m;
        m.name = "KIMO";
        m.description = {"Single-track kick synthesizer with FM modulation, randomized parameter variation per trigger, and dynamic decay envelope", "單軌 Kick Synthesizer，具備 FM Modulation、每次 Trigger 隨機參數變化與 動態衰減包絡", "FM変調、トリガー毎のランダムパラメータバリエーション、ダイナミックディケイエンベロープ搭載シングルトラックキック合成器"};
        m.entries.push_back({"Pattern Length", {"Fixed 16 steps", "Fixed 16 steps", "Fixed 16 steps"}});
        m.entries.push_back({"Fill Calculation", {"Fill percentage converted to 0-16 hit count", "Fill percentage converted to 0-16 hit count", "Fill percentage converted to 0-16 hit count"}});
        m.entries.push_back({"Hit Distribution", {"Bjorklund algorithm distributes hits evenly across pattern length", "Bjorklund algorithm distributes hits evenly across pattern length", "Bjorklund algorithm distributes hits evenly across pattern length"}});
        m.entries.push_back({"Step Counter", {"Increments on each clock rising edge (2.0V threshold)", "每個 Clock 上升邊緣（2.0V 閾值）遞增", "Increments on each clock rising edge (2.0V threshold)"}});
        m.entries.push_back({"Pattern Wrap", {"Returns to step 0 after step 15", "步進 15 後返回步進 0", "Returns to step 0 after step 15"}});
        m.entries.push_back({"Accent Clock", {"Triggers every 4 steps (quarter-note in 16th-note pattern)", "每 4 步 Trigger（16 分音符 Pattern 中的 Quarter-note）", "Triggers every 4 steps (quarter-note in 16th-note pattern)"}});
        m.entries.push_back({"Shift Range", {"1-7 steps offset from downbeat", "1-7 steps offset from downbeat", "1-7 steps offset from downbeat"}});
        m.entries.push_back({"Accent Trigger", {"Fires when step plus shift value is divisible by 4", "Fires when step plus shift value is divisible by 4", "Fires when step plus shift value is divisible by 4"}});
        m.entries.push_back({"Accent Envelope", {"UnifiedEnvelope class with S-curve decay and separate decay time", "Accent Envelope 輸出（0-10V）。Quarter-note Pulse 用於 Groove 同步。", "アクセントエンベロープ出力（0-10V）。グルーブ同期用4分音符パルス。"}});
        m.entries.push_back({"Gain Modulation", {"Accent envelope multiplies final VCA output for dynamic emphasis", "Accent Envelope 乘以最終 VCA 輸出以產生動態強調", "Accent envelope multiplies final VCA output for dynamic emphasis"}});
        m.entries.push_back({"Oscillator", {"BasicSineVCO class generating pure sine wave", "BasicSineVCO 類別生成純 Sine Wave", "BasicSineVCO class generating pure sine wave"}});
        m.entries.push_back({"Frequency", {"Base frequency modulated by FM envelope, up to 20x multiplier at full depth", "Base frequency modulated by FM envelope, up to 20x multiplier at full depth", "Base frequency modulated by FM envelope, up to 20x multiplier at full depth"}});
        m.entries.push_back({"FM Envelope", {"UnifiedEnvelope class, fast attack (1ms), variable decay", "FM Envelope 輸出（0-10V）。適用於調變其他參數。", "FMエンベロープ出力（0-10V）。他パラメータ変調に有用。"}});
        m.entries.push_back({"Pitch Sweep", {"FM envelope creates characteristic kick \"thump\" by sweeping from high to low frequency", "FM Envelope 透過從高至低頻率掃描產生特徵性 Kick「重擊」", "FM envelope creates characteristic kick \"thump\" by sweeping from high to low frequency"}});
        m.entries.push_back({"Saturation", {"Tanh waveshaping with punch-controlled drive adds harmonics", "Tanh Waveshaping 以 Punch 控制 Drive 加入 Harmonic", "Tanh waveshaping with punch-controlled drive adds harmonics"}});
        m.entries.push_back({"At Punch 0", {"Clean sine output", "Clean sine output", "Clean sine output"}});
        m.entries.push_back({"At Punch 1", {"Heavy saturation with odd harmonics", "Heavy saturation with odd harmonics", "Heavy saturation with odd harmonics"}});
        m.entries.push_back({"UnifiedEnvelope", {"S-curve shaped decay with configurable attack (1ms default)", "S-curve 形狀 Decay 含可設定 Attack（預設 1ms）", "設定可能アタック付きS-curve形状ディケイ（デフォルト1ms）"}});
        m.entries.push_back({"LinearEnvelope", {"Simple linear decay for accent (no attack phase)", "Accent 用簡單線性 Decay（無 Attack 階段）", "アクセント用シンプルリニアディケイ（アタックフェーズなし）"}});
        m.entries.push_back({"VCA Envelope", {"Controls overall amplitude, uses LinearEnvelope (simple linear decay)", "VCA Envelope 輸出（0-10V）。用於觸發外部模組。", "VCAエンベロープ出力（0-10V）。外部モジュールトリガー用。"}});
        m.entries.push_back({"FM Envelope", {"Controls pitch sweep depth, uses UnifiedEnvelope (S-curve shaped decay)", "FM Envelope 輸出（0-10V）。適用於調變其他參數。", "FMエンベロープ出力（0-10V）。他パラメータ変調に有用。"}});
        m.entries.push_back({"Accent Envelope", {"Multiplies VCA output, uses UnifiedEnvelope (S-curve shaped decay with configurable shape)", "Accent Envelope 輸出（0-10V）。Quarter-note Pulse 用於 Groove 同步。", "アクセントエンベロープ出力（0-10V）。グルーブ同期用4分音符パルス。"}});
        m.entries.push_back({"CLK", {"Clock input (2.0V rising edge threshold)", "Clock 輸入（2.0V 上升邊緣閾值）", "クロック入力（2.0V立ち上がりエッジ閾値）"}});
        m.entries.push_back({"FILL", {"Euclidean fill density 0-100% (0-16 hits). CV input adds to knob value.", "Euclidean Fill 密度 0-100%（16 步 Pattern 中 0-16 Hit）。", "ユークリッドフィル密度0-100%（16ステップパターンで0-16ヒット）。"}});
        m.entries.push_back({"ACCENT", {"Quarter-note shift 1-7 steps. Determines which beat gets accent emphasis.", "Quarter-note Accent Shift 1-7 步（Snap）。決定哪個 Beat 獲得強調。", "4分音符アクセントシフト1-7ステップ（スナップ）。どのビートが強調されるか決定。"}});
        m.entries.push_back({"DELAY", {"Accent envelope decay 0.01-2.0s (linear scaling)", "Accent Envelope Decay 0.01-2.0s（線性縮放）", "アクセントエンベロープディケイ0.01-2.0s（リニアスケーリング）"}});
        m.entries.push_back({"TUNE", {"Base frequency 24-500Hz. Logarithmic scaling across range. CV input adds ±2 octaves.", "基礎 Oscillator 頻率 24-500Hz（對數縮放）。", "基本オシレーター周波数24-500Hz（対数スケーリング）。"}});
        m.entries.push_back({"FM", {"FM depth 0-100%. Higher values create more aggressive pitch sweep.", "FM 深度 0-100%。較高值產生更激進 Pitch Sweep。", "FM深度0-100%。高値はより激しいピッチスイープを生成。"}});
        m.entries.push_back({"PUNCH", {"Saturation amount 0-100%. Adds harmonic content via tanh waveshaping.", "Saturation 量 0-100%。透過 Tanh Waveshaping 加入 Harmonic 內容。", "サチュレーション量0-100%。tanhウェーブシェイピング経由でハーモニックコンテンツ追加。"}});
        m.entries.push_back({"DECAY", {"VCA envelope decay 0.01-2.0s (logarithmic). Controls kick body length.", "VCA Envelope Decay 時間 0.01-2.0 秒（對數）。控制 Kick Body 長度。", "VCAエンベロープディケイ時間0.01-2.0秒（対数）。キックボディ長を制御。"}});
        m.entries.push_back({"SHAPE", {"Envelope curve 0-0.99. Higher values create longer tail with faster initial decay.", "Envelope 曲線形狀 0-0.99。較高值產生較快初始 Decay 的較長尾。", "エンベロープカーブ形状0-0.99。高値は速い初期ディケイのロングテールを生成。"}});
        m.entries.push_back({"VCA ENV", {"VCA envelope output (0-10V). Use to trigger external modules.", "VCA Envelope 輸出（0-10V）。用於觸發外部模組。", "VCAエンベロープ出力（0-10V）。外部モジュールトリガー用。"}});
        m.entries.push_back({"FM ENV", {"FM envelope output (0-10V). Useful for modulating other parameters.", "FM Envelope 輸出（0-10V）。適用於調變其他參數。", "FMエンベロープ出力（0-10V）。他パラメータ変調に有用。"}});
        m.entries.push_back({"ACCENT ENV", {"Accent envelope output (0-10V). Quarter-note pulse for groove sync.", "Accent Envelope 輸出（0-10V）。Quarter-note Pulse 用於 Groove 同步。", "アクセントエンベロープ出力（0-10V）。グルーブ同期用4分音符パルス。"}});
        m.entries.push_back({"AUDIO", {"Final kick drum audio (±10V peak)", "最終 Kick Drum Audio 輸出（\\u00b110V Peak）。", "最終キックドラムオーディオ出力（\\u00b110Vピーク）。"}});
        m.entries.push_back({"Fill", {"Euclidean fill density 0-100% (0-16 hits in 16-step pattern).", "Euclidean Fill 密度 0-100%（16 步 Pattern 中 0-16 Hit）。", "ユークリッドフィル密度0-100%（16ステップパターンで0-16ヒット）。"}});
        m.entries.push_back({"Accent", {"Quarter-note accent shift 1-7 steps (snap). Determines which beat gets emphasis.", "Quarter-note Accent Shift 1-7 步（Snap）。決定哪個 Beat 獲得強調。", "4分音符アクセントシフト1-7ステップ（スナップ）。どのビートが強調されるか決定。"}});
        m.entries.push_back({"Accent Delay", {"Accent envelope decay time 0.01-2.0 seconds.", "Accent Envelope Decay 時間 0.01-2.0 秒。", "アクセントエンベロープディケイ時間0.01-2.0秒。"}});
        m.entries.push_back({"Tune", {"Base oscillator frequency 24-500Hz (logarithmic scaling).", "基礎 Oscillator 頻率 24-500Hz（對數縮放）。", "基本オシレーター周波数24-500Hz（対数スケーリング）。"}});
        m.entries.push_back({"FM Amount", {"FM modulation depth 0-100%. Higher values create more aggressive pitch sweep.", "FM Modulation 深度 0-100%。較高值產生更激進的 Pitch Sweep。", "FM変調深度0-100%。高値はより激しいピッチスイープを生成。"}});
        m.entries.push_back({"Punch Amount", {"Tanh saturation amount 0-100%. Adds harmonic content.", "Tanh Saturation 量 0-100%。加入 Harmonic 內容。", "tanhサチュレーション量0-100%。ハーモニックコンテンツを追加。"}});
        m.entries.push_back({"Decay", {"VCA envelope decay time 0.01-2.0 seconds (logarithmic). Controls kick body length.", "VCA Envelope Decay 時間 0.01-2.0 秒（對數）。控制 Kick Body 長度。", "VCAエンベロープディケイ時間0.01-2.0秒（対数）。キックボディ長を制御。"}});
        m.entries.push_back({"Shape", {"Envelope curve shape 0-0.99. Higher values create faster initial decay with longer tail.", "Envelope 曲線形狀 0-0.99。較高值產生較快初始 Decay 的較長尾。", "エンベロープカーブ形状0-0.99。高値は速い初期ディケイのロングテールを生成。"}});
        m.entries.push_back({"Clock", {"Clock input. Rising edge (2.0V threshold) advances the Euclidean step counter.", "Clock 輸入。上升邊緣（2.0V 閾值）推進 Euclidean Step Counter。", "クロック入力。立ち上がりエッジ（2.0V閾値）でユークリッドステップカウンターを進行。"}});
        m.entries.push_back({"Tune CV", {"Tune modulation CV input. Adds to base frequency in log2 space.", "Tune 調變 CV 輸入。在 log2 空間加至基礎頻率。", "チューン変調CV入力。log2空間で基本周波数に加算。"}});
        m.entries.push_back({"FM CV", {"FM amount modulation CV input (\\u00b110V).", "FM Amount 調變 CV 輸入（\\u00b110V）。", "FM量変調CV入力（\\u00b110V）。"}});
        m.entries.push_back({"Punch CV", {"Punch amount modulation CV input (\\u00b110V).", "Punch Amount 調變 CV 輸入（\\u00b110V）。", "パンチ量変調CV入力（\\u00b110V）。"}});
        m.entries.push_back({"Decay CV", {"Decay time modulation CV input (\\u00b110V).", "Decay 時間調變 CV 輸入（\\u00b110V）。", "ディケイ時間変調CV入力（\\u00b110V）。"}});
        m.entries.push_back({"Fill CV", {"Fill density modulation CV input. Adds to knob value.", "Fill 密度調變 CV 輸入。加至 Knob 值。", "フィル密度変調CV入力。ノブ値に加算。"}});
        m.entries.push_back({"VCA Envelope", {"VCA envelope output (0-10V). Use to trigger external modules.", "VCA Envelope 輸出（0-10V）。用於觸發外部模組。", "VCAエンベロープ出力（0-10V）。外部モジュールトリガー用。"}});
        m.entries.push_back({"FM Envelope", {"FM envelope output (0-10V). Useful for modulating other parameters.", "FM Envelope 輸出（0-10V）。適用於調變其他參數。", "FMエンベロープ出力（0-10V）。他パラメータ変調に有用。"}});
        m.entries.push_back({"Accent Envelope", {"Accent envelope output (0-10V). Quarter-note pulse for groove sync.", "Accent Envelope 輸出（0-10V）。Quarter-note Pulse 用於 Groove 同步。", "アクセントエンベロープ出力（0-10V）。グルーブ同期用4分音符パルス。"}});
        m.entries.push_back({"Audio", {"Final kick drum audio output (\\u00b110V peak).", "最終 Kick Drum Audio 輸出（\\u00b110V Peak）。", "最終キックドラムオーディオ出力（\\u00b110Vピーク）。"}});
        data["KIMO"] = std::move(m);
    }
    // Launchpad (52 entries)
    {
        ModuleHelpData m;
        m.name = "Launchpad";
        m.description = {"8x8 grid audio/CV looper with session-style clip launching, scene triggers, and send/return routing", "8x8 Grid Audio/CV Looper，具備 Session 風格 Clip 啟動、Scene 觸發與 Send/Return Routing", "8x8グリッドAudio/CVルーパー、セッションスタイルクリップランチ、シーントリガー、センド/リターンルーティング対応"};
        m.entries.push_back({"Recording", {"Click empty cell to start recording from row input. Click again or wait for max length to stop.", "Click empty cell to start recording from row input. Click again or wait for max length to stop.", "Click empty cell to start recording from row input. Click again or wait for max length to stop."}});
        m.entries.push_back({"Playback", {"Click recorded cell to start playback. In session mode, only one cell per row plays at a time.", "Click recorded cell to start playback. In session mode, only one cell per row plays at a time.", "Click recorded cell to start playback. In session mode, only one cell per row plays at a time."}});
        m.entries.push_back({"Stop", {"Click playing cell to stop. Playback stops at quantize boundary if enabled.", "Click playing cell to stop. Playback stops at quantize boundary if enabled.", "Click playing cell to stop. Playback stops at quantize boundary if enabled."}});
        m.entries.push_back({"Clear", {"Hold cell for 1 second to clear recording", "Hold cell for 1 second to clear recording", "Hold cell for 1 second to clear recording"}});
        m.entries.push_back({"Move", {"Drag cell to another location to move content", "Drag cell to another location to move content", "Drag cell to another location to move content"}});
        m.entries.push_back({"Copy", {"Shift+drag to copy content to new location", "Shift+drag to copy content to new location", "Shift+drag to copy content to new location"}});
        m.entries.push_back({"Empty", {"Dark gray, no content", "Dark gray, no content", "Dark gray, no content"}});
        m.entries.push_back({"Has Content", {"Brown, stopped with recording", "Brown, stopped with recording", "Brown, stopped with recording"}});
        m.entries.push_back({"Playing", {"Teal/cyan, currently playing with waveform and playhead", "Teal/cyan, currently playing with waveform and playhead", "Teal/cyan, currently playing with waveform and playhead"}});
        m.entries.push_back({"Recording", {"Red-brown with progress bar at bottom", "Red-brown with progress bar at bottom", "Red-brown with progress bar at bottom"}});
        m.entries.push_back({"Queued", {"Gold/yellow, waiting for quantize boundary to start", "Gold/yellow, waiting for quantize boundary to start", "Gold/yellow, waiting for quantize boundary to start"}});
        m.entries.push_back({"Stop Queued", {"Dark brown, waiting to stop", "Dark brown, waiting to stop", "Dark brown, waiting to stop"}});
        m.entries.push_back({"Free", {"Immediate launch/stop, no quantization", "立即 Launch/Stop，無量化", "即時ランチ/ストップ、クオンタイズなし"}});
        m.entries.push_back({"1", {"Launch on next clock", "在下一個 Clock 啟動", "次のクロックでランチ"}});
        m.entries.push_back({"8", {"Launch on next 8-clock boundary", "在下一個 8-Clock 邊界啟動", "次の8クロック境界でランチ"}});
        m.entries.push_back({"16", {"Launch on next 16-clock boundary", "在下一個 16-Clock 邊界啟動", "次の16クロック境界でランチ"}});
        m.entries.push_back({"32", {"Launch on next 32-clock boundary", "在下一個 32-Clock 邊界啟動", "次の32クロック境界でランチ"}});
        m.entries.push_back({"64", {"Launch on next 64-clock boundary", "在下一個 64-Clock 邊界啟動", "次の64クロック境界でランチ"}});
        m.entries.push_back({"SCENE 1-8", {"Trigger all cells in corresponding column simultaneously", "同時觸發對應欄中的所有格子", "対応する列の全セルを同時トリガー"}});
        m.entries.push_back({"Behavior", {"Launches one cell per row from the selected column", "Launches one cell per row from the selected column", "Launches one cell per row from the selected column"}});
        m.entries.push_back({"CV Trigger", {"Each scene has trigger input for external control", "Each scene has trigger input for external control", "Each scene has trigger input for external control"}});
        m.entries.push_back({"SEND A", {"Send level to effect chain A (0-100%)", "發送至效果鏈 A 的音量（0-100%）", "エフェクトチェーンAへのセンドレベル（0-100%）"}});
        m.entries.push_back({"SEND B", {"Send level to effect chain B (0-100%)", "發送至效果鏈 B 的音量（0-100%）", "エフェクトチェーンBへのセンドレベル（0-100%）"}});
        m.entries.push_back({"PAN", {"Stereo panning (-1 = full left, 0 = center, +1 = full right)", "Stereo Panning（-1 = 全左，0 = 中央，+1 = 全右）", "ステレオパンニング（-1 = 全左、0 = 中央、+1 = 全右）"}});
        m.entries.push_back({"LEVEL", {"Output level for this row (0-100%)", "該列的輸出音量（0-100%）", "この行の出力レベル（0-100%）"}});
        m.entries.push_back({"Automatic", {"Last 96 samples crossfade with first 96 samples", "Last 96 samples crossfade with first 96 samples", "Last 96 samples crossfade with first 96 samples"}});
        m.entries.push_back({"Minimum Length", {"Crossfade only applies if recording exceeds 192 samples", "Crossfade only applies if recording exceeds 192 samples", "Crossfade only applies if recording exceeds 192 samples"}});
        m.entries.push_back({"Purpose", {"Eliminates clicks at loop boundaries", "Eliminates clicks at loop boundaries", "Eliminates clicks at loop boundaries"}});
        m.entries.push_back({"CLOCK", {"Sync clock for quantized launching", "用於量化 Launch 的同步 Clock", "クオンタイズランチ用同期クロック"}});
        m.entries.push_back({"RESET", {"Reset clock counter", "重置 Clock 計數器", "クロックカウンターリセット"}});
        m.entries.push_back({"ROW 1-8", {"Audio/CV input for each row (recording source)", "每列的直接輸出（pre-mixer）", "各行のダイレクト出力（プリミキサー）"}});
        m.entries.push_back({"SCENE 1-8 TRIG", {"CV trigger for scene launching", "Scene Launch 的 CV Trigger", "シーンランチ用CVトリガー"}});
        m.entries.push_back({"STOP ALL TRIG", {"Stop all playing cells", "停止所有播放中的格子", "全再生セル停止"}});
        m.entries.push_back({"RETURN A L/R", {"Stereo return from effect chain A", "來自效果鏈 A 的 Stereo Return", "エフェクトチェーンAからのステレオリターン"}});
        m.entries.push_back({"RETURN B L/R", {"Stereo return from effect chain B", "來自效果鏈 B 的 Stereo Return", "エフェクトチェーンBからのステレオリターン"}});
        m.entries.push_back({"ROW 1-8", {"Direct output from each row (pre-mixer)", "每列的直接輸出（pre-mixer）", "各行のダイレクト出力（プリミキサー）"}});
        m.entries.push_back({"SEND A L/R", {"Stereo send to effect chain A", "發送至效果鏈 A 的 Stereo 輸出", "エフェクトチェーンAへのステレオセンド"}});
        m.entries.push_back({"SEND B L/R", {"Stereo send to effect chain B", "發送至效果鏈 B 的 Stereo 輸出", "エフェクトチェーンBへのステレオセンド"}});
        m.entries.push_back({"MIX L/R", {"Main stereo output (all rows mixed with pan and level)", "主 Stereo 輸出（所有列混合並應用 Pan 與 Level）", "メインステレオ出力（全行ミックス、PanとLevel適用）"}});
        m.entries.push_back({"Waveform", {"Downsampled waveform display in recorded cells", "Downsampled waveform display in recorded cells", "Downsampled waveform display in recorded cells"}});
        m.entries.push_back({"Playhead", {"White vertical line showing current playback position", "White vertical line showing current playback position", "White vertical line showing current playback position"}});
        m.entries.push_back({"Clock Counter", {"Bottom-right corner shows recording length in clocks", "Bottom-right corner shows recording length in clocks", "Bottom-right corner shows recording length in clocks"}});
        m.entries.push_back({"Progress Bar", {"Red bar at bottom during recording", "Red bar at bottom during recording", "Red bar at bottom during recording"}});
        m.entries.push_back({"Quantize", {"Scene trigger quantize mode. Options: Free (immediate), 1, 8, 16, 32, 64 (steps). Controls when scene changes take effect relative to clock.", "Scene 觸發量化模式。選項：Free（立即）、1、8、16、32、64（步）。控制場景變更相對於 Clock 何時生效。", "シーントリガークオンタイズモード。オプション: Free（即時）、1、8、16、32、64（ステップ）。クロックに対するシーン変更のタイミングを制御。"}});
        m.entries.push_back({"Return A Left", {"Left channel return input for Send/Return bus A.", "Send/Return Bus A 的左聲道 Return 輸入。", "Send/Return Bus Aの左チャンネルリターン入力。"}});
        m.entries.push_back({"Return A Right", {"Right channel return input for Send/Return bus A.", "Send/Return Bus A 的右聲道 Return 輸入。", "Send/Return Bus Aの右チャンネルリターン入力。"}});
        m.entries.push_back({"Return B Left", {"Left channel return input for Send/Return bus B.", "Send/Return Bus B 的左聲道 Return 輸入。", "Send/Return Bus Bの左チャンネルリターン入力。"}});
        m.entries.push_back({"Return B Right", {"Right channel return input for Send/Return bus B.", "Send/Return Bus B 的右聲道 Return 輸入。", "Send/Return Bus Bの右チャンネルリターン入力。"}});
        m.entries.push_back({"Mix Left", {"Main stereo mix left output.", "主立體聲混音左輸出。", "メインステレオミックス左出力。"}});
        m.entries.push_back({"Mix Right", {"Main stereo mix right output.", "主立體聲混音右輸出。", "メインステレオミックス右出力。"}});
        m.entries.push_back({"Row", {"Audio input/output per row. Each row is an independent loop recorder/player.", "每列的 Audio 輸入/輸出。每列為獨立的 Loop 錄音/播放器。", "各行のAudio入出力。各行は独立したループレコーダー/プレーヤー。"}});
        m.entries.push_back({"Scene", {"Scene buttons trigger different scene configurations. Can be CV-triggered.", "Scene 按鈕觸發不同的場景配置。可由 CV 觸發。", "シーンボタンで異なるシーン構成をトリガー。CVトリガー対応。"}});
        data["Launchpad"] = std::move(m);
    }
    // MADDY (49 entries)
    {
        ModuleHelpData m;
        m.name = "MADDY";
        m.description = {"All-in-one rhythm workstation combining internal swing clock, 3-track Euclidean sequencer, and pattern-based CV generation with custom pattern support", "整合 Swing Clock、3 軌 Euclidean Sequencer 與 Pattern CV 生成的節奏工作站，支援自訂 Pattern", "スウィングクロック、3トラックユークリッドシーケンサー、パターンCV生成を統合したリズムワークステーション、カスタムパターン対応"};
        m.entries.push_back({"Principle", {"Distributes hits as evenly as possible across the pattern length", "Distributes hits as evenly as possible across the pattern length", "Distributes hits as evenly as possible across the pattern length"}});
        m.entries.push_back({"FILL", {"Determines how many hits in the pattern (0-100%). Higher fill = more hits.", "決定 Pattern 中的 Hit 數量（0-100%）。Fill 越高 = Hit 越多。", "パターン内のヒット数を決定（0-100%）。Fillが高いほどヒットが多い。"}});
        m.entries.push_back({"SHIFT", {"Rotates the pattern start point without changing rhythm density (configurable via right-click menu, 0-4 steps)", "旋轉 Pattern 起始點，不改變節奏密度（透過右鍵選單設定，0-4 步）", "リズム密度を変えずにパターン開始点を回転（右クリックメニューで設定、0-4ステップ）"}});
        m.entries.push_back({"Recalculation", {"Pattern updates immediately when FILL, LEN, or SHIFT changes", "Pattern updates immediately when FILL, LEN, or SHIFT changes", "Pattern updates immediately when FILL, LEN, or SHIFT changes"}});
        m.entries.push_back({"Frequency", {"Internal clock frequency (0.125-128 Hz, exponential, default: ~7 Hz). Parameter range -3 to 7, displayed as 2^param Hz.", "內部 Clock 頻率（0.125-128 Hz，指數，預設：~7 Hz）。參數範圍 -3 到 7，顯示為 2^param Hz。", "内部クロック周波数（0.125-128 Hz、指数、デフォルト：~7 Hz）。パラメータ範囲-3から7、2^param Hzとして表示。"}});
        m.entries.push_back({"Swing", {"Swing amount via phase offset (0-1, default: 0). Display shows degrees: 180 to 90. Higher values create stronger shuffle feel by shifting the second pulse closer to the next beat.", "透過相位偏移的 Swing 量（0-1，預設：0）。顯示為角度：180 到 90。數值越高，透過將第二脈衝移近下一拍來產生更強的 Shuffle 感。", "位相オフセットによるスウィング量（0-1、デフォルト：0）。角度で表示：180から90。値が高いほど2番目のパルスを次のビートに近づけてシャッフル感を強化。"}});
        m.entries.push_back({"Clock Source", {"Selects what advances the CV sequencer. 7 options: LFO (internal, default), T1/T2/T3 (track triggers), 12/23/1213 (chain outputs).", "預設為 LFO（內部 Clock）。可透過 CLK SRC 按鈕選擇。", "CVシーケンサーの進行ソースを選択。7オプション：LFO（内部、デフォルト）、T1/T2/T3（トラックトリガー）、12/23/1213（チェーン出力）。"}});
        m.entries.push_back({"T3 Fill", {"Rhythm density per track (0-100%, default: 100% for all tracks). Converted to integer hit count based on pattern length.", "各軌節奏密度（0-100%，預設：100%）。根據 Pattern 長度轉換為整數 Hit 數。", "トラック別リズム密度（0-100%、デフォルト：100%）。パターン長に基づいて整数ヒット数に変換。"}});
        m.entries.push_back({"T3 Div/Mult", {"Clock division/multiplication per track (-3 to +3, default: 0 = 1x). Values: -3:1/4x, -2:1/3x, -1:1/2x, 0:1x, +1:2x, +2:3x, +3:4x. Snap-enabled.", "Track 3 Clock Division/Multiplication。範圍：1/4x 至 4x（-3 到 +3）。對 Track 3 的輸入 Clock 進行分頻或倍頻。", "トラック3クロック分周/逓倍。範囲: 1/4x〜4x（-3から+3）。トラック3の入力クロックを分周または逓倍。"}});
        m.entries.push_back({"Length", {"Global pattern length (1-48 steps, default: 16, snap). Shared across all tracks. Clamped to 1-32 in process.", "與全域 Pattern 長度相同（1-48 步）。CV Sequencer 在此長度時回繞。", "グローバルパターン長と同一（1-48ステップ）。CVシーケンサーはこの長さで折り返し。"}});
        m.entries.push_back({"Decay", {"Envelope decay parameter (0-1, default: 0.3). Non-linear mapping using power curve for natural-sounding decay.", "Envelope Decay 參數（0-1，預設：0.3）。使用 Power Curve 的非線性映射，產生自然的 Decay 音色。", "エンベロープディケイパラメータ（0-1、デフォルト：0.3）。自然なディケイ音色のための非線形パワーカーブマッピング。"}});
        m.entries.push_back({"Type", {"AD (Attack-Decay) envelope triggered on each Euclidean hit", "AD (Attack-Decay) envelope triggered on each Euclidean hit", "AD (Attack-Decay) envelope triggered on each Euclidean hit"}});
        m.entries.push_back({"Attack Time", {"Configurable via right-click menu (0.5-20ms, default: 6ms)", "每模組 Envelope Attack 時間（0.5-20ms，預設：6ms）。影響所有三軌。", "モジュール別エンベロープアタック時間（0.5-20ms、デフォルト：6ms）。全3トラックに影響。"}});
        m.entries.push_back({"Decay Time", {"Controlled by DECAY knob (0-1). Uses non-linear power mapping: sqrtDecay = decay^0.33, mapped to 0-0.8, then 10^((mapped-0.8)*5) seconds. Range approximately 0.01-1s.", "由 DECAY 旋鈕控制（0-1）。使用非線性 Power 映射：sqrtDecay = decay^0.33，映射至 0-0.8，然後 10^((mapped-0.8)*5) 秒。範圍約 0.01-1 秒。", "DECAYノブで制御（0-1）。非線形パワーマッピング使用：sqrtDecay = decay^0.33、0-0.8にマッピング後、10^((mapped-0.8)*5)秒。範囲約0.01-1秒。"}});
        m.entries.push_back({"Curve", {"Decay curve shape derived from DECAY parameter, ranging -0.8 to -0.45", "Decay curve shape derived from DECAY parameter, ranging -0.8 to -0.45", "Decay curve shape derived from DECAY parameter, ranging -0.8 to -0.45"}});
        m.entries.push_back({"Output Voltage", {"0V to 10V unipolar envelope", "0V to 10V unipolar envelope", "0V to 10V unipolar envelope"}});
        m.entries.push_back({"Retrigger", {"New trigger restarts envelope from attack phase", "新 Trigger 從 Attack 階段重新啟動 Envelope", "新トリガーでアタックフェーズからエンベロープを再スタート"}});
        m.entries.push_back({"K1", {"Voltage knob 1 (-10V to +10V, default: 0V)", "電壓旋鈕 1（-10V 至 +10V，預設：0V）", "電圧ノブ1（-10Vから+10V、デフォルト：0V）"}});
        m.entries.push_back({"K2", {"Voltage knob 2 (-10V to +10V, default: 2V)", "電壓旋鈕 2（-10V 至 +10V，預設：2V）", "電圧ノブ2（-10Vから+10V、デフォルト：2V）"}});
        m.entries.push_back({"K3", {"Voltage knob 3 (-10V to +10V, default: 4V)", "電壓旋鈕 3（-10V 至 +10V，預設：4V）", "電圧ノブ3（-10Vから+10V、デフォルト：4V）"}});
        m.entries.push_back({"K4", {"Voltage knob 4 (-10V to +10V, default: 6V)", "電壓旋鈕 4（-10V 至 +10V，預設：6V）", "電圧ノブ4（-10Vから+10V、デフォルト：6V）"}});
        m.entries.push_back({"K5", {"Voltage knob 5 (-10V to +10V, default: 8V)", "電壓旋鈕 5（-10V 至 +10V，預設：8V）", "電圧ノブ5（-10Vから+10V、デフォルト：8V）"}});
        m.entries.push_back({"Density", {"Controls how many knobs are active (2-5 knobs). 0-0.2: 2 knobs, 0.2-0.4: 3, 0.4-0.6: 4, 0.6-1.0: 5. Default: 0.5 (4 knobs).", "控制啟用的旋鈕數量（2-5 個）。0-0.2：2 個、0.2-0.4：3 個、0.4-0.6：4 個、0.6-1.0：5 個。預設：0.5（4 個）。", "有効なノブ数を制御（2-5個）。0-0.2：2個、0.2-0.4：3個、0.4-0.6：4個、0.6-1.0：5個。デフォルト：0.5（4個）。"}});
        m.entries.push_back({"Length", {"Same as global pattern length (1-48 steps). The CV sequencer wraps at this length.", "與全域 Pattern 長度相同（1-48 步）。CV Sequencer 在此長度時回繞。", "グローバルパターン長と同一（1-48ステップ）。CVシーケンサーはこの長さで折り返し。"}});
        m.entries.push_back({"Clock Source", {"Default is LFO (internal clock). Selectable via CLK SRC button.", "預設為 LFO（內部 Clock）。可透過 CLK SRC 按鈕選擇。", "CVシーケンサーの進行ソースを選択。7オプション：LFO（内部、デフォルト）、T1/T2/T3（トラックトリガー）、12/23/1213（チェーン出力）。"}});
        m.entries.push_back({"Chaos", {"Randomization amount (0-100%, default: 0%)", "隨機化量（0-100%，預設：0%）", "ランダム化量（0-100%、デフォルト：0%）"}});
        m.entries.push_back({"CHAIN 12", {"Chained envelope sequence of T1 and T2. Plays T1 envelope for one cycle, then T2, repeating.", "T1 和 T2 的 Chain Envelope 序列。播放 T1 Envelope 一個循環，然後 T2，重複。", "T1とT2のチェーンエンベロープシーケンス。T1エンベロープを1サイクル再生後、T2を再生、繰り返し。"}});
        m.entries.push_back({"CHAIN 23", {"Chained envelope sequence of T2 and T3. Plays T2 for one cycle, then T3, repeating.", "T2 和 T3 的 Chain Envelope 序列。播放 T2 一個循環，然後 T3，重複。", "T2とT3のチェーンエンベロープシーケンス。T2を1サイクル再生後、T3を再生、繰り返し。"}});
        m.entries.push_back({"CHAIN 123", {"Chained envelope sequence following pattern T1->T2->T1->T3, creating a weighted rotation.", "Chain Envelope 序列依循 T1->T2->T1->T3 Pattern，產生加權旋轉。", "T1->T2->T1->T3パターンに従うチェーンエンベロープシーケンス、加重ローテーションを生成。"}});
        m.entries.push_back({"Output", {"Outputs the current active track's envelope value (0-10V)", "Outputs the current active track's envelope value (0-10V)", "Outputs the current active track's envelope value (0-10V)"}});
        m.entries.push_back({"Clock", {"Master clock output (1ms pulse, post-swing)", "主 Clock 輸出（1ms Pulse，Swing 後）", "マスタークロック出力（1msパルス、スウィング後）"}});
        m.entries.push_back({"T1 Trigger", {"Track 1 envelope output (0-10V)", "軌道 1 Envelope 輸出（0-10V）", "トラック1エンベロープ出力（0-10V）"}});
        m.entries.push_back({"T2 Trigger", {"Track 2 envelope output (0-10V)", "軌道 2 Envelope 輸出（0-10V）", "トラック2エンベロープ出力（0-10V）"}});
        m.entries.push_back({"T3 Trigger", {"Track 3 envelope output (0-10V)", "軌道 3 Envelope 輸出（0-10V）", "トラック3エンベロープ出力（0-10V）"}});
        m.entries.push_back({"Chain 1+2", {"Chain 1+2 envelope output (0-10V)", "Chain 1+2 Envelope 輸出（0-10V）", "Chain 1+2エンベロープ出力（0-10V）"}});
        m.entries.push_back({"Chain 2+3", {"Chain 2+3 envelope output (0-10V)", "Chain 2+3 Envelope 輸出（0-10V）", "Chain 2+3エンベロープ出力（0-10V）"}});
        m.entries.push_back({"Chain 1+2+3", {"Chain 1+2+1+3 envelope output (0-10V)", "Chain 1+2+1+3 Envelope 輸出（0-10V）", "Chain 1+2+1+3エンベロープ出力（0-10V）"}});
        m.entries.push_back({"CV", {"CV sequencer voltage output (-10V to +10V)", "CV Sequencer 電壓輸出（-10V 至 +10V）", "CVシーケンサー電圧出力（-10Vから+10V）"}});
        m.entries.push_back({"Trigger", {"CV sequencer trigger output (10ms pulse, fires when CV value changes)", "CV Sequencer Trigger 輸出（10ms Pulse，當 CV 值變更時觸發）", "CVシーケンサートリガー出力（10msパルス、CV値変更時に発火）"}});
        m.entries.push_back({"Custom Pattern", {"Define the knob sequence for Custom mode. Enter a string of digits 1-5 (e.g., \"12312345\"). Each digit selects which K knob to use at that step.", "定義 Custom 模式的旋鈕序列。輸入 1-5 的數字字串（例如 \"12312345\"）。每個數字選擇該步進要使用的 K 旋鈕。", "Customモード用のノブシーケンスを定義。1-5の数字文字列を入力（例：「12312345」）。各数字はそのステップで使用するKノブを選択。"}});
        m.entries.push_back({"Attack Time", {"Per-module envelope attack time (0.5-20ms, default: 6ms). Affects all three tracks.", "每模組 Envelope Attack 時間（0.5-20ms，預設：6ms）。影響所有三軌。", "モジュール別エンベロープアタック時間（0.5-20ms、デフォルト：6ms）。全3トラックに影響。"}});
        m.entries.push_back({"Track 1 Shift", {"Euclidean pattern rotation for T1 track (0-4 steps)", "T1 軌道 Euclidean Pattern 旋轉（0-4 步）", "T1トラックユークリッドパターン回転（0-4ステップ）"}});
        m.entries.push_back({"Track 2 Shift", {"Euclidean pattern rotation for T2 track (0-4 steps)", "T2 軌道 Euclidean Pattern 旋轉（0-4 步）", "T2トラックユークリッドパターン回転（0-4ステップ）"}});
        m.entries.push_back({"Track 3 Shift", {"Euclidean pattern rotation for T3 track (0-4 steps)", "T3 軌道 Euclidean Pattern 旋轉（0-4 步）", "T3トラックユークリッドパターン回転（0-4ステップ）"}});
        m.entries.push_back({"Mode", {"Pattern style selector. Click to cycle: Sequential (Red LED), Custom (Green LED), Jump (Blue LED).", "Pattern 風格選擇器。點擊循環：Sequential（紅 LED）、Custom（綠 LED）、Jump（藍 LED）。", "パターンスタイルセレクター。クリックで切替: Sequential（赤LED）、Custom（緑LED）、Jump（青LED）。"}});
        m.entries.push_back({"T1 Div/Mult", {"Track 1 clock division/multiplication. Range: 1/4x to 4x (-3 to +3). Divides or multiplies the incoming clock for Track 1.", "Track 1 Clock Division/Multiplication。範圍：1/4x 至 4x（-3 到 +3）。對 Track 1 的輸入 Clock 進行分頻或倍頻。", "トラック1クロック分周/逓倍。範囲: 1/4x〜4x（-3から+3）。トラック1の入力クロックを分周または逓倍。"}});
        m.entries.push_back({"T2 Div/Mult", {"Track 2 clock division/multiplication. Range: 1/4x to 4x (-3 to +3). Divides or multiplies the incoming clock for Track 2.", "Track 2 Clock Division/Multiplication。範圍：1/4x 至 4x（-3 到 +3）。對 Track 2 的輸入 Clock 進行分頻或倍頻。", "トラック2クロック分周/逓倍。範囲: 1/4x〜4x（-3から+3）。トラック2の入力クロックを分周または逓倍。"}});
        m.entries.push_back({"T3 Div/Mult", {"Track 3 clock division/multiplication. Range: 1/4x to 4x (-3 to +3). Divides or multiplies the incoming clock for Track 3.", "Track 3 Clock Division/Multiplication。範圍：1/4x 至 4x（-3 到 +3）。對 Track 3 的輸入 Clock 進行分頻或倍頻。", "トラック3クロック分周/逓倍。範囲: 1/4x〜4x（-3から+3）。トラック3の入力クロックを分周または逓倍。"}});
        m.entries.push_back({"Reset", {"Reset trigger input. Resets all track step counters to 0 on rising edge.", "Reset Trigger 輸入。上升邊緣時將所有軌道的 Step Counter 重置為 0。", "リセットトリガー入力。立ち上がりエッジで全トラックのステップカウンターを0にリセット。"}});
        data["MADDY"] = std::move(m);
    }
    // MADDYPlus (49 entries)
    {
        ModuleHelpData m;
        m.name = "MADDY+";
        m.description = {"Extended MADDY with three independent CV sequencers featuring custom pattern support via context menu", "MADDY 進階版，具備三組獨立 CV Sequencer，支援右鍵選單自訂 Pattern", "3系統の独立CVシーケンサーを搭載したMADDY拡張版、コンテキストメニューでカスタムパターン対応"};
        m.entries.push_back({"Frequency", {"Internal clock frequency (0.125-128 Hz, exponential). Range -3 to 7, displayed as 2^param Hz. Default: ~7 Hz.", "內部 Clock 頻率（0.125-128 Hz，指數）。參數範圍 -3 到 7，顯示為 2^param Hz。預設：~7 Hz。", "内部クロック周波数（0.125-128 Hz、指数）。パラメータ範囲-3から7、2^param Hzとして表示。デフォルト：~7 Hz。"}});
        m.entries.push_back({"Swing", {"Swing amount via phase offset (0-1, default: 0). Display shows degrees: 180 to 90. Higher values create stronger shuffle feel.", "透過相位偏移的 Swing 量（0-1，預設：0）。顯示角度：180 到 90。數值越高 Shuffle 感越強。", "位相オフセットによるスウィング量（0-1、デフォルト：0）。角度で表示：180から90。値が高いほどシャッフル感が強化。"}});
        m.entries.push_back({"Clock CV Attenuverter", {"Attenuverter for Clock CV input (-1 to +1, default: 0). Scales the incoming CV before modulating the clock frequency.", "Clock CV 輸入的 Attenuverter（-1 至 +1，預設：0）。在調變 Clock 頻率前縮放輸入 CV。", "Clock CV入力のアッテニュバーター（-1から+1、デフォルト：0）。クロック周波数変調前に入力CVをスケール。"}});
        m.entries.push_back({"Manual Reset", {"Button to reset all tracks and sequencers to initial state.", "按鈕，重置所有軌道與 Sequencer 至初始狀態。", "全トラックとシーケンサーを初期状態にリセットするボタン。"}});
        m.entries.push_back({"Ch1 Clock Source", {"Selects what advances the main CV sequencer (Ch1). 7 options: LFO, T1, T2, T3, 12, 23, 1213. Default: 1 (T1).", "選擇推進主 CV Sequencer（Ch1）的來源。7 種選項：LFO、T1、T2、T3、12、23、1213。預設：1（T1）。", "メインCVシーケンサー（Ch1）の進行ソースを選択。7オプション：LFO、T1、T2、T3、12、23、1213。デフォルト：1（T1）。"}});
        m.entries.push_back({"Length", {"Global pattern length (1-48 steps, default: 32, snap). Shared across all rhythm tracks and CV sequencers. Clamped to 1-48 in process.", "全域 Pattern 長度（1-48 步，預設：32，Snap）。所有節奏軌道和 CV Sequencer 共用。", "グローバルパターン長（1-48ステップ、デフォルト：32、スナップ）。全リズムトラックとCVシーケンサーで共有。"}});
        m.entries.push_back({"Decay", {"Envelope decay parameter (0-1, default: 0.3). Non-linear mapping using power curve for natural-sounding decay. Shared across all tracks.", "Envelope Decay 參數（0-1，預設：0.3）。使用 Power Curve 的非線性映射。所有軌道共用。", "エンベロープディケイパラメータ（0-1、デフォルト：0.3）。パワーカーブの非線形マッピング。全トラック共有。"}});
        m.entries.push_back({"T1 Fill", {"Track 1 rhythm density (0-100%, default: 100%). Converted to integer hit count based on pattern length.", "Track 1 節奏密度（0-100%，預設：100%）。根據 Pattern 長度轉換為整數 Hit 數。", "トラック1リズム密度（0-100%、デフォルト：100%）。パターン長に基づき整数ヒット数に変換。"}});
        m.entries.push_back({"T1 Div/Mult", {"Track 1 clock division/multiplication (-3 to +3, default: 0 = 1x). Snap-enabled.", "Track 1 Clock Division/Multiplication（-3 到 +3，預設：0 = 1x）。Snap 啟用。", "トラック1クロック分割/倍増（-3から+3、デフォルト：0 = 1x）。スナップ有効。"}});
        m.entries.push_back({"T2 Fill", {"Track 2 rhythm density (0-100%, default: 100%).", "Track 2 節奏密度（0-100%，預設：100%）。", "トラック2リズム密度（0-100%、デフォルト：100%）。"}});
        m.entries.push_back({"T2 Div/Mult", {"Track 2 clock division/multiplication (-3 to +3, default: 0 = 1x). Snap-enabled.", "Track 2 Clock Division/Multiplication（-3 到 +3，預設：0 = 1x）。Snap 啟用。", "トラック2クロック分割/倍増（-3から+3、デフォルト：0 = 1x）。スナップ有効。"}});
        m.entries.push_back({"T3 Fill", {"Track 3 rhythm density (0-100%, default: 100%).", "Track 3 節奏密度（0-100%，預設：100%）。", "トラック3リズム密度（0-100%、デフォルト：100%）。"}});
        m.entries.push_back({"T3 Div/Mult", {"Track 3 clock division/multiplication (-3 to +3, default: 0 = 1x). Snap-enabled.", "Track 3 Clock Division/Multiplication（-3 到 +3，預設：0 = 1x）。Snap 啟用。", "トラック3クロック分割/倍増（-3から+3、デフォルト：0 = 1x）。スナップ有効。"}});
        m.entries.push_back({"K1", {"Voltage knob 1 (-10V to +10V, default: 0V)", "電壓旋鈕 1（-10V 至 +10V，預設：0V）", "電圧ノブ1（-10Vから+10V、デフォルト：0V）"}});
        m.entries.push_back({"K2", {"Voltage knob 2 (-10V to +10V, default: 2V)", "電壓旋鈕 2（-10V 至 +10V，預設：2V）", "電圧ノブ2（-10Vから+10V、デフォルト：2V）"}});
        m.entries.push_back({"K3", {"Voltage knob 3 (-10V to +10V, default: 4V)", "電壓旋鈕 3（-10V 至 +10V，預設：4V）", "電圧ノブ3（-10Vから+10V、デフォルト：4V）"}});
        m.entries.push_back({"K4", {"Voltage knob 4 (-10V to +10V, default: 6V)", "電壓旋鈕 4（-10V 至 +10V，預設：6V）", "電圧ノブ4（-10Vから+10V、デフォルト：6V）"}});
        m.entries.push_back({"K5", {"Voltage knob 5 (-10V to +10V, default: 8V)", "電壓旋鈕 5（-10V 至 +10V，預設：8V）", "電圧ノブ5（-10Vから+10V、デフォルト：8V）"}});
        m.entries.push_back({"Ch1 Mode", {"Pattern mode (0-5, default: 1 = Custom). 0: Sequential, 1: Custom, 2: Jump, 3: Rev Sequential, 4: Rev Custom, 5: Rev Jump.", "Pattern 模式（0-5，預設：1 = Custom）。0: Sequential、1: Custom、2: Jump、3: Rev Sequential、4: Rev Custom、5: Rev Jump。", "パターンモード（0-5、デフォルト：1 = Custom）。0: Sequential、1: Custom、2: Jump、3: Rev Sequential、4: Rev Custom、5: Rev Jump。"}});
        m.entries.push_back({"Density", {"Controls the number of active K knobs (0-1, default: 0.5 = 4 knobs). 0-0.2: 2, 0.2-0.4: 3, 0.4-0.6: 4, 0.6-1.0: 5.", "控制活動 K 旋鈕數量（0-1，預設：0.5 = 4 個）。0-0.2: 2 個、0.2-0.4: 3 個、0.4-0.6: 4 個、0.6-1.0: 5 個。", "アクティブKノブ数を制御（0-1、デフォルト：0.5 = 4個）。0-0.2: 2個、0.2-0.4: 3個、0.4-0.6: 4個、0.6-1.0: 5個。"}});
        m.entries.push_back({"Chaos", {"Randomization amount (0-100%, default: 0%). >50% selects knobs outside DENSITY range.", "隨機化量（0-100%，預設：0%）。>50% 時選擇 DENSITY 範圍外的旋鈕。", "ランダム化量（0-100%、デフォルト：0%）。>50%でDENSITY範囲外のノブを選択。"}});
        m.entries.push_back({"Sequential", {"Cycles K1-K5 in order, uses all 5 knobs regardless of DENSITY", "依序循環 K1-K5，無論 DENSITY 皆使用全部 5 個旋鈕", "K1-K5を順にサイクル、DENSITYに関わらず全5ノブを使用"}});
        m.entries.push_back({"Custom", {"User-defined pattern via right-click menu. Default: 32-step phasing pattern. DENSITY controls active knob count.", "透過右鍵選單自訂 Pattern。預設：32 步移相 Pattern。DENSITY 控制活動旋鈕數。", "右クリックメニューでユーザー定義パターン。デフォルト：32ステップフェージングパターン。DENSITYがアクティブノブ数を制御。"}});
        m.entries.push_back({"Jump", {"Fixed pattern K1->K3->K5->K2->K4", "固定 Pattern K1->K3->K5->K2->K4", "固定パターンK1->K3->K5->K2->K4"}});
        m.entries.push_back({"Rev Sequential / Rev Custom / Rev Jump", {"Reverse variants of the above modes", "上述模式的反向變體", "上記モードの逆順バリアント"}});
        m.entries.push_back({"Ch2 Clock Source", {"Clock source for Ch2 (0-6, default: 2 = T2). Same options as Ch1.", "Ch2 的 Clock 來源（0-6，預設：2 = T2）。與 Ch1 相同選項。", "Ch2のクロックソース（0-6、デフォルト：2 = T2）。Ch1と同じオプション。"}});
        m.entries.push_back({"Ch2 Mode", {"Pattern mode for Ch2 (0-5, default: 1 = Custom). Same modes as Ch1.", "Ch2 的 Pattern 模式（0-5，預設：1 = Custom）。與 Ch1 相同模式。", "Ch2のパターンモード（0-5、デフォルト：1 = Custom）。Ch1と同じモード。"}});
        m.entries.push_back({"Ch2 Density", {"Active knob count for Ch2 (0-1, default: 0.5).", "Ch2 的活動旋鈕數（0-1，預設：0.5）。", "Ch2のアクティブノブ数（0-1、デフォルト：0.5）。"}});
        m.entries.push_back({"Ch2 CVD Time/Attenuation", {"CV delay time/attenuation for Ch2 (0-1, default: 0). When Ch2 CV input is connected, attenuates the external CV signal.", "Ch2 的 CV Delay 時間/衰減（0-1，預設：0）。當 Ch2 CV 輸入連接時，衰減外部 CV 訊號。", "Ch2のCVディレイ時間/アッテネーション（0-1、デフォルト：0）。Ch2 CV入力接続時に外部CV信号をアッテネート。"}});
        m.entries.push_back({"Ch2 Step Delay", {"Step delay for Ch2 (0-5, default: 0, snap). Delays Ch2 output by N steps, creating canon/echo effects.", "Ch2 的步進延遲（0-5，預設：0，Snap）。將 Ch2 輸出延遲 N 步，產生卡農/迴音效果。", "Ch2のステップディレイ（0-5、デフォルト：0、スナップ）。Ch2出力をNステップ遅延、カノン/エコー効果を生成。"}});
        m.entries.push_back({"Ch3 Clock Source", {"Clock source for Ch3 (0-6, default: 3 = T3). Same options as Ch1.", "Ch3 的 Clock 來源（0-6，預設：3 = T3）。與 Ch1 相同選項。", "Ch3のクロックソース（0-6、デフォルト：3 = T3）。Ch1と同じオプション。"}});
        m.entries.push_back({"Ch3 Mode", {"Pattern mode for Ch3 (0-5, default: 1 = Custom). Same modes as Ch1.", "Ch3 的 Pattern 模式（0-5，預設：1 = Custom）。與 Ch1 相同模式。", "Ch3のパターンモード（0-5、デフォルト：1 = Custom）。Ch1と同じモード。"}});
        m.entries.push_back({"Ch3 Density", {"Active knob count for Ch3 (0-1, default: 0.5).", "Ch3 的活動旋鈕數（0-1，預設：0.5）。", "Ch3のアクティブノブ数（0-1、デフォルト：0.5）。"}});
        m.entries.push_back({"Ch3 CVD Time/Attenuation", {"CV delay time/attenuation for Ch3 (0-1, default: 0). When Ch3 CV input is connected, attenuates the external CV signal.", "Ch3 的 CV Delay 時間/衰減（0-1，預設：0）。當 Ch3 CV 輸入連接時，衰減外部 CV 訊號。", "Ch3のCVディレイ時間/アッテネーション（0-1、デフォルト：0）。Ch3 CV入力接続時に外部CV信号をアッテネート。"}});
        m.entries.push_back({"Ch3 Step Delay", {"Step delay for Ch3 (0-5, default: 0, snap). Delays Ch3 output by N steps.", "Ch3 的步進延遲（0-5，預設：0，Snap）。將 Ch3 輸出延遲 N 步。", "Ch3のステップディレイ（0-5、デフォルト：0、スナップ）。Ch3出力をNステップ遅延。"}});
        m.entries.push_back({"Clock", {"Master clock output (1ms pulse, post-swing)", "主 Clock 輸出（1ms Pulse，Swing 後）", "マスタークロック出力（1msパルス、スウィング後）"}});
        m.entries.push_back({"Reset", {"Reset output pulse (generated on manual/auto reset)", "Reset 輸出 Pulse（手動/自動 Reset 時產生）", "リセット出力パルス（手動/自動リセット時に生成）"}});
        m.entries.push_back({"T1 Trigger", {"Track 1 envelope output (0-10V)", "Track 1 Envelope 輸出（0-10V）", "トラック1エンベロープ出力（0-10V）"}});
        m.entries.push_back({"T2 Trigger", {"Track 2 envelope output (0-10V)", "Track 2 Envelope 輸出（0-10V）", "トラック2エンベロープ出力（0-10V）"}});
        m.entries.push_back({"T3 Trigger", {"Track 3 envelope output (0-10V)", "Track 3 Envelope 輸出（0-10V）", "トラック3エンベロープ出力（0-10V）"}});
        m.entries.push_back({"Chain 1+2", {"Chain 1+2 envelope output (0-10V)", "Chain 1+2 Envelope 輸出（0-10V）", "Chain 1+2エンベロープ出力（0-10V）"}});
        m.entries.push_back({"Chain 2+3", {"Chain 2+3 envelope output (0-10V)", "Chain 2+3 Envelope 輸出（0-10V）", "Chain 2+3エンベロープ出力（0-10V）"}});
        m.entries.push_back({"Chain 1+2+3", {"Chain 1+2+1+3 envelope output (0-10V)", "Chain 1+2+1+3 Envelope 輸出（0-10V）", "Chain 1+2+1+3エンベロープ出力（0-10V）"}});
        m.entries.push_back({"CV", {"Ch1 CV sequencer voltage output (-10V to +10V)", "Ch1 CV Sequencer 電壓輸出（-10V 至 +10V）", "Ch1 CVシーケンサー電圧出力（-10Vから+10V）"}});
        m.entries.push_back({"Trigger", {"Ch1 trigger output (fires when CV value changes)", "Ch1 Trigger 輸出（CV 值變更時觸發）", "Ch1トリガー出力（CV値変更時に発火）"}});
        m.entries.push_back({"Panel Theme", {"Light / Dark panel appearance", "Light / Dark 面板外觀", "Light / Darkパネル外観"}});
        m.entries.push_back({"Attack Time", {"Envelope attack time (0.5-20ms, default: 6ms). Affects all three tracks.", "Envelope Attack 時間（0.5-20ms，預設：6ms）。影響所有三軌。", "エンベロープアタックタイム（0.5-20ms、デフォルト：6ms）。全3トラックに影響。"}});
        m.entries.push_back({"Track Shift", {"Euclidean pattern rotation for each track (0-4 steps)", "每軌 Euclidean Pattern 旋轉（0-4 步）", "各トラックのユークリッドパターン回転（0-4ステップ）"}});
        m.entries.push_back({"Custom Pattern", {"Define knob sequence for Custom mode. Enter a string of numbers 1-5 (e.g., \"12312345\"). Each number selects which K knob to use for that step.", "定義 Custom 模式的旋鈕序列。輸入 1-5 的數字字串（例如「12312345」）。每個數字選擇該步驟使用的 K 旋鈕。", "Customモード用のノブシーケンスを定義。1-5の数字文字列を入力（例：「12312345」）。各数字はそのステップで使用するKノブを選択。"}});
        data["MADDYPlus"] = std::move(m);
    }
    // NIGOQ (68 entries)
    {
        ModuleHelpData m;
        m.name = "NIGOQ";
        m.description = {"Buchla-inspired complex oscillator with parallel modulation architecture, waveshaping, and integrated AD envelope", "Buchla 風格 Complex Oscillator，平行調變架構、Waveshaping 與整合 AD Envelope", "Buchla風コンプレックスオシレーター、並列モジュレーション構造、ウェーブシェイピング、統合ADエンベロープ"};
        m.entries.push_back({"MOD Oscillator", {"Runs independently and provides modulation signals", "獨立運行並提供調變訊號", "Runs independently and provides modulation signals"}});
        m.entries.push_back({"FINAL Signal Path", {"FINAL Oscillator → Wavefolding → Rectification → LPF → VCA → Output", "FINAL Oscillator → Wavefolding → Rectification → LPF → VCA → Output", "FINAL Oscillator → Wavefolding → Rectification → LPF → VCA → Output"}});
        m.entries.push_back({"Modulation Routing", {"MOD simultaneously controls FM amount, TM (timbre/fold depth), and RECT modulation", "MOD simultaneously controls FM amount, TM (timbre/fold depth), and RECT modulation", "MOD simultaneously controls FM amount, TM (timbre/fold depth), and RECT modulation"}});
        m.entries.push_back({"SYNC", {"When enabled, MOD oscillator resets its phase when FINAL completes a cycle, locking MOD to FINAL's frequency for harmonic timbres", "Off = 獨立振盪器。Soft = 溫和相位校正。Hard = 完整相位重置。Sync 將 MOD 鎖定到 FINAL 的週期。", "Off = 独立オシレーター。Soft = 穏やかな位相補正。Hard = 完全な位相リセット。SyncはMODをFINALのサイクルにロック。"}});
        m.entries.push_back({"BASS", {"Adds unprocessed sine wave from FINAL to the output for low-end weight", "將乾淨的正弦波（FINAL 的音高）混入輸出。在複雜諧波下增加基頻的份量。柔和飽和防止刺耳的削波。", "クリーンなサイン波（FINALのピッチ）を出力にブレンド。複雑なハーモニクスの下に基音の重みを追加。ソフトサチュレーションで刺激的なクリッピングを防止。"}});
        m.entries.push_back({"MOD FREQ", {"Frequency from 0.001Hz (slow LFO) to 6kHz (audio rate). Use as LFO for evolving textures or at audio rate for FM timbres.", "頻率從 0.001Hz（慢速 LFO）到 6kHz（音頻速率）。作為 LFO 可創造演化的紋理，音頻速率則用於 FM 音色。", "0.001Hz（スローLFO）から6kHz（オーディオレート）。LFOとして進化するテクスチャ、オーディオレートではFMティンバーに。"}});
        m.entries.push_back({"WAVE", {"Smoothly morphs through waveforms. Turn clockwise: Sine → Triangle → Sawtooth → Pulse (wide to narrow).", "平滑變形的波形。順時針旋轉：Sine → Triangle → Sawtooth → Pulse（寬到窄）。", "スムーズにモーフする波形。時計回りに：サイン → 三角 → ノコギリ → パルス（広から狭）。"}});
        m.entries.push_back({"MOD 1V/OCT", {"Pitch tracking input. Connect to play MOD at musical intervals.", "音高追蹤輸入。連接後可以音樂音程演奏 MOD。", "ピッチトラッキング入力。接続するとMODを音楽的インターバルで演奏可能。"}});
        m.entries.push_back({"MOD FM IN", {"External signal to modulate MOD's frequency. Attenuator controls depth.", "外部訊號調變 MOD 的頻率。衰減器控制深度。", "MODの周波数をモジュレートする外部信号。アッテヌエーターで深度を制御。"}});
        m.entries.push_back({"EXT MOD IN", {"Replace internal MOD with any external signal for custom modulation sources.", "以任何外部訊號取代內部 MOD，用於自訂調變源。", "任意の外部信号で内部MODを置換。カスタムモジュレーションソースに。"}});
        m.entries.push_back({"FINAL FREQ", {"Main pitch from 20Hz to 8kHz. Default knob position 0.3 (~121Hz).", "主音高從 20Hz 到 8kHz。預設旋鈕位置 0.3（約 121Hz）。", "メインピッチ20Hzから8kHz。デフォルトノブ位置0.3（約121Hz）。"}});
        m.entries.push_back({"FINAL 1V/OCT", {"Primary pitch input for playing melodies.", "FINAL 振盪器 1V/Oct 音高追蹤。", "FINALオシレーター1V/Octピッチトラッキング。"}});
        m.entries.push_back({"FINAL FM IN", {"External FM source with attenuator. Independent from internal MOD→FINAL FM.", "外部 FM 源含衰減器。獨立於內部 MOD→FINAL FM。", "アッテヌエーター付き外部FMソース。内部MOD→FINAL FMとは独立。"}});
        m.entries.push_back({"EXT FINAL IN", {"Replace internal FINAL with external audio. Useful for processing other sound sources through the waveshaping chain.", "以外部音頻取代內部 FINAL。適合將其他聲音源送入 Waveshaping 處理鏈。", "外部オーディオで内部FINALを置換。他のサウンドソースをウェーブシェイピングチェーンに通すのに便利。"}});
        m.entries.push_back({"SYNC", {"Off = independent oscillators. Soft = gentle phase correction. Hard = full phase reset. Sync locks MOD to FINAL's cycle.", "Off = 獨立振盪器。Soft = 溫和相位校正。Hard = 完整相位重置。Sync 將 MOD 鎖定到 FINAL 的週期。", "Off = 独立オシレーター。Soft = 穏やかな位相補正。Hard = 完全な位相リセット。SyncはMODをFINALのサイクルにロック。"}});
        m.entries.push_back({"FM", {"Linear FM Index (0-4, default: 0.2). Controls how much MOD affects FINAL's pitch. Through-zero FM allows the frequency to go negative, creating the characteristic metallic, bell-like timbres of FM synthesis.", "Linear FM Index（0-4，預設: 0.2）。控制 MOD 對 FINAL 音高的影響程度。Through-zero FM 允許頻率變為負值，創造 FM 合成特有的金屬、鐘聲般的音色。", "リニアFMインデックス（0-4、デフォルト: 0.2）。MODがFINALのピッチに与える影響を制御。スルーゼロFMは周波数が負になることを許容し、FM合成特有のメタリックでベルのようなティンバーを生成。"}});
        m.entries.push_back({"FM CV", {"External control of FM depth with attenuator.", "外部控制 FM 深度，含衰減器。", "アッテヌエーター付きFM深度の外部制御。"}});
        m.entries.push_back({"FOLD", {"Wavefolding adds harmonics by \"folding\" the waveform back on itself when it exceeds a threshold. Higher settings create increasingly complex, buzzy overtones.", "Wavefolding 透過將波形超過閾值的部分「折回」來增加諧波。較高設定創造越來越複雜、嗡嗡作響的泛音。", "ウェーブフォールディングは波形が閾値を超えると「折り返す」ことでハーモニクスを追加。高い設定では複雑でバジーな倍音を生成。"}});
        m.entries.push_back({"TM (Timbre Modulation)", {"MOD signal dynamically controls the fold amount. Creates evolving, animated timbres as MOD sweeps the fold depth.", "MOD signal dynamically controls the fold amount. Creates evolving, animated timbres as MOD sweeps the fold depth.", "MOD signal dynamically controls the fold amount. Creates evolving, animated timbres as MOD sweeps the fold depth."}});
        m.entries.push_back({"RECTIFY", {"Asymmetric rectification progressively removes negative portions of the waveform. At 50%, negative values are halved. At 100%, full half-wave rectification. Adds odd harmonics and changes the tonal character. Automatic DC blocking prevents speaker damage.", "非對稱整流漸進地移除波形的負值部分。50% 時負值減半，100% 時完整半波整流。增加奇次諧波並改變音色特性。自動 DC 阻隔防止喇叭損壞。", "非対称レクティファイは波形の負の部分を徐々に除去。50%で負値が半減、100%で完全半波整流。奇数次ハーモニクスを追加しトーンキャラクターを変化。自動DCブロッキングでスピーカー損傷を防止。"}});
        m.entries.push_back({"LPF", {"12dB/octave lowpass filter. Range 10Hz to 20kHz. Tames harsh harmonics from waveshaping or creates muffled textures.", "12dB/octave 低通濾波器。範圍 10Hz 到 20kHz。馴服 Waveshaping 的刺耳諧波或創造悶濁的質感。", "12dB/オクターブローパスフィルター。範囲10Hzから20kHz。ウェーブシェイピングの刺激的なハーモニクスを抑えるか、こもったテクスチャを作成。"}});
        m.entries.push_back({"LPF CV", {"External cutoff control for filter sweeps.", "外部截止頻率控制，用於濾波器掃頻。", "フィルタースイープ用外部カットオフ制御。"}});
        m.entries.push_back({"BASS", {"Blends clean sine wave (at FINAL's pitch) into the output. Adds fundamental weight underneath complex harmonics. Soft saturation prevents harsh clipping.", "將乾淨的正弦波（FINAL 的音高）混入輸出。在複雜諧波下增加基頻的份量。柔和飽和防止刺耳的削波。", "クリーンなサイン波（FINALのピッチ）を出力にブレンド。複雑なハーモニクスの下に基音の重みを追加。ソフトサチュレーションで刺激的なクリッピングを防止。"}});
        m.entries.push_back({"DECAY", {"AD envelope with fixed 10ms attack for punchy transients. First half of knob: short decays (0-300ms). Second half: longer decays (300ms-3s). At maximum: Drone mode keeps the envelope fully open.", "AD Envelope 具有固定 10ms 起音，產生有力的瞬態。旋鈕前半：短衰減（0-300ms）。後半：長衰減（300ms-3s）。最大值：Drone 模式保持 Envelope 完全開啟。", "パンチの効いたトランジェント用に10ms固定アタックのADエンベロープ。ノブ前半：短いディケイ（0-300ms）。後半：長いディケイ（300ms-3s）。最大値：ドローンモードでエンベロープを完全に開放維持。"}});
        m.entries.push_back({"TRIG", {"Trigger input. Any signal crossing the threshold starts the envelope. Retriggerable for repeated notes.", "觸發輸入。任何超過閾值的訊號啟動 Envelope。可重複觸發。", "トリガー入力。閾値を超える信号でエンベロープを開始。リトリガー可能。"}});
        m.entries.push_back({"Modulation Signal", {"Raw modulation oscillator output (0-10V). Useful for modulating other modules.", "原始 MOD 振盪器輸出（0-10V）。適合調變其他模組。", "生MODオシレーター出力（0-10V）。他モジュールの変調に便利。"}});
        m.entries.push_back({"Final Sine", {"Clean sine wave at FINAL's pitch (±5V). Unaffected by waveshaping.", "FINAL 音高的乾淨正弦波（±5V）。不受 Waveshaping 影響。", "FINALピッチのクリーンサイン波（±5V）。ウェーブシェイピング影響なし。"}});
        m.entries.push_back({"Final Output", {"Main output with all processing applied (±5V).", "套用所有處理的主輸出（±5V）。", "全処理適用後のメイン出力（±5V）。"}});
        m.entries.push_back({"Panel Theme", {"Light / Dark panel appearance", "Light / Dark 面板外觀", "Light / Dark パネル外観"}});
        m.entries.push_back({"2x Oversample", {"Toggle oversampling. ON reduces aliasing, OFF saves CPU.", "切換 Oversampling。ON 減少混疊，OFF 節省 CPU。", "オーバーサンプリング切替。ONでエイリアシング削減、OFFでCPU節約。"}});
        m.entries.push_back({"Randomize Amount", {"How much parameters change when randomizing (0-100%).", "隨機化時參數變化的幅度（0-100%）。", "ランダム化時のパラメータ変化量（0-100%）。"}});
        m.entries.push_back({"Randomize Glide Time", {"How smoothly parameters morph during randomization.", "隨機化時參數變化的平滑程度。", "ランダム化時のパラメータ変化の滑らかさ。"}});
        m.entries.push_back({"Exclude FINAL_FREQ from Random", {"Preserve pitch when randomizing (default: ON).", "隨機化時保留音高（預設：ON）。", "ランダム化時にピッチを保持（デフォルト：ON）。"}});
        m.entries.push_back({"Exclude DECAY from Random", {"Preserve envelope time when randomizing.", "隨機化時保留 Envelope 時間。", "ランダム化時にエンベロープ時間を保持。"}});
        m.entries.push_back({"Modulation Frequency", {"MOD oscillator frequency 0.001Hz-6kHz (exponential). Default knob: 0.25 (~8Hz).", "MOD 振盪器頻率 0.001Hz-6kHz（指數）。預設旋鈕: 0.25（約 8Hz）。", "MODオシレーター周波数 0.001Hz-6kHz（指数）。デフォルトノブ: 0.25（約8Hz）。"}});
        m.entries.push_back({"Final Frequency", {"FINAL oscillator pitch 20Hz-8kHz (exponential). Default knob: 0.3 (~121Hz).", "FINAL 振盪器音高 20Hz-8kHz（指數）。預設旋鈕: 0.3（約 121Hz）。", "FINALオシレーターピッチ 20Hz-8kHz（指数）。デフォルトノブ: 0.3（約121Hz）。"}});
        m.entries.push_back({"LPF Cutoff", {"12dB/oct lowpass filter cutoff 10Hz-20kHz (exponential). Default: ~3kHz.", "12dB/oct 低通濾波器截止 10Hz-20kHz（指數）。預設: 約 3kHz。", "12dB/octローパスフィルターカットオフ 10Hz-20kHz（指数）。デフォルト: 約3kHz。"}});
        m.entries.push_back({"Rectify Amount", {"Asymmetric rectification 0-100% (default: 15%). Removes negative waveform portions.", "非對稱整流 0-100%（預設: 15%）。移除波形負值部分。", "非対称レクティファイ 0-100%（デフォルト: 15%）。波形の負の部分を除去。"}});
        m.entries.push_back({"Wavefolding", {"Wavefolding amount 0-100% (default: 25%). Adds harmonics by folding waveform.", "Wavefolding 量 0-100%（預設: 25%）。透過折疊波形增加諧波。", "ウェーブフォールディング量 0-100%（デフォルト: 25%）。波形を折り返してハーモニクスを追加。"}});
        m.entries.push_back({"Modulation Wave Shape", {"MOD waveform morph. Sine-Triangle-Saw-Pulse(wide to narrow). Default: 0.15.", "MOD 波形變形。Sine-Triangle-Saw-Pulse（寬至窄）。預設: 0.15。", "MOD波形モーフ。Sine-Triangle-Saw-Pulse（広から狭）。デフォルト: 0.15。"}});
        m.entries.push_back({"FM CV Attenuator", {"Attenuator for FM Amount CV input 0-100% (default: 70%).", "FM Amount CV 輸入衰減器 0-100%（預設: 70%）。", "FM Amount CV入力アッテヌエーター 0-100%（デフォルト: 70%）。"}});
        m.entries.push_back({"TM CV Attenuator", {"Attenuator for TM (Timbre Mod) CV input 0-100% (default: 70%).", "TM（Timbre Mod）CV 輸入衰減器 0-100%（預設: 70%）。", "TM（ティンバーモジュレーション）CV入力アッテヌエーター 0-100%（デフォルト: 70%）。"}});
        m.entries.push_back({"RECT CV Attenuator", {"Attenuator for Rectify CV input 0-100% (default: 70%).", "Rectify CV 輸入衰減器 0-100%（預設: 70%）。", "レクティファイCV入力アッテヌエーター 0-100%（デフォルト: 70%）。"}});
        m.entries.push_back({"Mod FM Attenuator", {"Attenuator for MOD FM external input 0-100% (default: 0%).", "MOD FM 外部輸入衰減器 0-100%（預設: 0%）。", "MOD FM外部入力アッテヌエーター 0-100%（デフォルト: 0%）。"}});
        m.entries.push_back({"Final FM Attenuator", {"Attenuator for FINAL FM external input 0-100% (default: 0%).", "FINAL FM 外部輸入衰減器 0-100%（預設: 0%）。", "FINAL FM外部入力アッテヌエーター 0-100%（デフォルト: 0%）。"}});
        m.entries.push_back({"Decay Time", {"AD envelope decay. First half: 0-300ms, second half: 300ms-3s, max: Drone mode. Default: 0.73 (~1.3s).", "AD Envelope 衰減。前半: 0-300ms，後半: 300ms-3s，最大: Drone 模式。預設: 0.73（約 1.3s）。", "ADエンベロープディケイ。前半: 0-300ms、後半: 300ms-3s、最大: ドローンモード。デフォルト: 0.73（約1.3s）。"}});
        m.entries.push_back({"Bass/Sine Mix", {"Clean sine wave blend into output 0-100% (default: 30%). Adds fundamental weight.", "乾淨正弦波混入輸出 0-100%（預設: 30%）。增加基頻份量。", "クリーンサイン波の出力ブレンド 0-100%（デフォルト: 30%）。基音の重み追加。"}});
        m.entries.push_back({"Linear FM Index", {"FM depth 0-4 (display multiplier). Through-zero FM. Default: 0.05.", "FM 深度 0-4（顯示乘數）。Through-zero FM。預設: 0.05。", "FM深度 0-4（表示乗数）。スルーゼロFM。デフォルト: 0.05。"}});
        m.entries.push_back({"TM Amount", {"Timbre modulation depth 0-100% (default: 50%). MOD dynamically controls fold amount.", "Timbre Modulation 深度 0-100%（預設: 50%）。MOD 動態控制折疊量。", "ティンバーモジュレーション深度 0-100%（デフォルト: 50%）。MODがフォールド量を動的制御。"}});
        m.entries.push_back({"RECT Mod Amount", {"Rectification modulation by MOD 0-100% (default: 20%).", "MOD 對整流的調變量 0-100%（預設: 20%）。", "MODによるレクティファイモジュレーション量 0-100%（デフォルト: 20%）。"}});
        m.entries.push_back({"Sync Mode", {"Off / Soft / Hard. Syncs MOD phase to FINAL cycle.", "Off / Soft / Hard。將 MOD 相位同步到 FINAL 週期。", "Off / Soft / Hard。MOD位相をFINALサイクルに同期。"}});
        m.entries.push_back({"Trigger", {"Trigger input to start AD envelope. Retriggerable.", "觸發輸入，啟動 AD Envelope。可重複觸發。", "ADエンベロープ開始用トリガー入力。リトリガー可能。"}});
        m.entries.push_back({"Modulation Wave CV", {"CV input for MOD waveform shape.", "MOD 波形形狀 CV 輸入。", "MOD波形シェイプCV入力。"}});
        m.entries.push_back({"External Modulation Input", {"Replace internal MOD with external signal.", "以外部訊號取代內部 MOD。", "外部信号で内部MODを置換。"}});
        m.entries.push_back({"External Final Input", {"Replace internal FINAL with external audio for waveshaping processing.", "以外部音頻取代內部 FINAL，進行 Waveshaping 處理。", "外部オーディオで内部FINALを置換、ウェーブシェイピング処理用。"}});
        m.entries.push_back({"LPF Cutoff CV", {"CV modulation for lowpass filter cutoff.", "低通濾波器截止 CV 調變。", "ローパスフィルターカットオフCV変調。"}});
        m.entries.push_back({"Rectify CV", {"CV modulation for rectification amount.", "整流量 CV 調變。", "レクティファイ量CV変調。"}});
        m.entries.push_back({"FM Amount CV", {"CV modulation for FM depth.", "FM 深度 CV 調變。", "FM深度CV変調。"}});
        m.entries.push_back({"Harmonics CV", {"CV modulation for wavefolding amount.", "Wavefolding 量 CV 調變。", "ウェーブフォールディング量CV変調。"}});
        m.entries.push_back({"Fold Amount CV", {"CV modulation for TM (timbre modulation) depth.", "TM（Timbre Modulation）深度 CV 調變。", "TM（ティンバーモジュレーション）深度CV変調。"}});
        m.entries.push_back({"RECT Mod Amount CV", {"CV modulation for rectification modulation amount.", "整流調變量 CV 調變。", "レクティファイモジュレーション量CV変調。"}});
        m.entries.push_back({"Modulation FM", {"External FM signal for MOD oscillator frequency modulation.", "MOD 振盪器頻率調變的外部 FM 訊號。", "MODオシレーター周波数変調用外部FM信号。"}});
        m.entries.push_back({"Modulation 1V/Oct", {"1V/Oct pitch tracking for MOD oscillator.", "MOD 振盪器 1V/Oct 音高追蹤。", "MODオシレーター1V/Octピッチトラッキング。"}});
        m.entries.push_back({"Final FM", {"External FM signal for FINAL oscillator frequency modulation.", "FINAL 振盪器頻率調變的外部 FM 訊號。", "FINALオシレーター周波数変調用外部FM信号。"}});
        m.entries.push_back({"Final 1V/Oct", {"1V/Oct pitch tracking for FINAL oscillator.", "FINAL 振盪器 1V/Oct 音高追蹤。", "FINALオシレーター1V/Octピッチトラッキング。"}});
        m.entries.push_back({"Modulation Signal", {"Raw MOD oscillator output (0-10V). For modulating other modules.", "原始 MOD 振盪器輸出（0-10V）。適合調變其他模組。", "生MODオシレーター出力（0-10V）。他モジュールの変調に便利。"}});
        m.entries.push_back({"Final Sine", {"Clean sine at FINAL pitch (+-5V). Unaffected by waveshaping.", "FINAL 音高的乾淨正弦波（±5V）。不受 Waveshaping 影響。", "FINALピッチのクリーンサイン波（±5V）。ウェーブシェイピング影響なし。"}});
        m.entries.push_back({"Final Output", {"Main output with all processing applied (+-5V).", "套用所有處理的主輸出（±5V）。", "全処理適用後のメイン出力（±5V）。"}});
        data["NIGOQ"] = std::move(m);
    }
    // Obserfour (45 entries)
    {
        ModuleHelpData m;
        m.name = "Obserfour";
        m.description = {"Compact 4-track oscilloscope with 8 inputs grouped into overlapping display pairs for signal comparison", "精簡 4 軌 Oscilloscope，8 個輸入分組為重疊顯示配對供訊號比較", "信号比較用に重複表示ペアにグループ化された8入力付きコンパクト4トラックオシロスコープ"};
        m.entries.push_back({"Display Size", {"120 × 300 pixels, divided into 4 horizontal tracks", "120 × 300 pixels, divided into 4 horizontal tracks", "120 × 300 pixels, divided into 4 horizontal tracks"}});
        m.entries.push_back({"Track Height", {"75 pixels per track (300 ÷ 4)", "75 pixels per track (300 ÷ 4)", "75 pixels per track (300 ÷ 4)"}});
        m.entries.push_back({"Buffer Size", {"256 samples per track (SCOPE_BUFFER_SIZE constant)", "256 samples per track (SCOPE_BUFFER_SIZE constant)", "256 samples per track (SCOPE_BUFFER_SIZE constant)"}});
        m.entries.push_back({"Sample Storage", {"Min/max structure captures signal extremes for accurate representation at any zoom level", "Min/max structure captures signal extremes for accurate representation at any zoom level", "Min/max structure captures signal extremes for accurate representation at any zoom level"}});
        m.entries.push_back({"Rendering", {"Anti-aliased lines at 1.5px stroke width, round line caps", "Anti-aliased lines at 1.5px stroke width, round line caps", "Anti-aliased lines at 1.5px stroke width, round line caps"}});
        m.entries.push_back({"Range", {"5ms to 50,000ms (50s) per screen width", "5ms to 50,000ms (50s) per screen width", "5ms to 50,000ms (50s) per screen width"}});
        m.entries.push_back({"Scaling", {"Base-2 logarithmic curve from 50,000ms to 5ms", "Base-2 logarithmic curve from 50,000ms to 5ms", "Base-2 logarithmic curve from 50,000ms to 5ms"}});
        m.entries.push_back({"Frame Calculation", {"Samples per buffer entry derived from time delta and sample rate", "Samples per buffer entry derived from time delta and sample rate", "Samples per buffer entry derived from time delta and sample rate"}});
        m.entries.push_back({"Interaction", {"Click and drag vertically on display area to adjust time scale", "Click and drag vertically on display area to adjust time scale", "Click and drag vertically on display area to adjust time scale"}});
        m.entries.push_back({"Hidden Knob", {"Uses HiddenTimeKnobObserver widget covering display area", "Uses HiddenTimeKnobObserver widget covering display area", "Uses HiddenTimeKnobObserver widget covering display area"}});
        m.entries.push_back({"Trigger Source", {"First connected input (lowest numbered with cable attached)", "First connected input (lowest numbered with cable attached)", "First connected input (lowest numbered with cable attached)"}});
        m.entries.push_back({"Toggle Button", {"Clickable LED switches between triggered and free-running modes", "Clickable LED switches between triggered and free-running modes", "Clickable LED switches between triggered and free-running modes"}});
        m.entries.push_back({"Triggered (White LED)", {"Waits for rising edge using rescale(voltage, 0, 0.001, 0, 1) before capturing", "Waits for rising edge using rescale(voltage, 0, 0.001, 0, 1) before capturing", "Waits for rising edge using rescale(voltage, 0, 0.001, 0, 1) before capturing"}});
        m.entries.push_back({"Free-Running (Pink LED)", {"Continuous waveform display, buffer fills then refreshes", "Continuous waveform display, buffer fills then refreshes", "Continuous waveform display, buffer fills then refreshes"}});
        m.entries.push_back({"Schmitt Trigger", {"Uses rescale(voltage, 0, 0.001, 0, 1) for edge detection with hysteresis", "使用 rescale(voltage, 0, 0.001, 0, 1) 進行帶磁滯的邊緣偵測", "Uses rescale(voltage, 0, 0.001, 0, 1) for edge detection with hysteresis"}});
        m.entries.push_back({"Holdoff", {"After trigger, fills complete buffer (256 samples) before waiting for next trigger", "Trigger 後填滿完整 Buffer（256 Samples）才等待下一次 Trigger", "After trigger, fills complete buffer (256 samples) before waiting for next trigger"}});
        m.entries.push_back({"Min/Max Tracking", {"Each frame tracks both minimum and maximum voltage for accurate peak display", "Each frame tracks both minimum and maximum voltage for accurate peak display", "Each frame tracks both minimum and maximum voltage for accurate peak display"}});
        m.entries.push_back({"Sample Decimation", {"At longer time bases, multiple samples are accumulated per buffer entry", "較長 Time Base 時，多個 Samples 累積至每 Buffer 項目", "At longer time bases, multiple samples are accumulated per buffer entry"}});
        m.entries.push_back({"Y-axis Mapping", {"±10V scaled to display height, 0V centered", "±10V scaled to display height, 0V centered", "±10V scaled to display height, 0V centered"}});
        m.entries.push_back({"Grid Lines", {"Horizontal track dividers and center lines drawn at 0V per track", "Horizontal track dividers and center lines drawn at 0V per track", "Horizontal track dividers and center lines drawn at 0V per track"}});
        m.entries.push_back({"Implementation", {"Reads cable color from PortWidget via APP->scene->rack->getTopCable()", "Reads cable color from PortWidget via APP->scene->rack->getTopCable()", "Reads cable color from PortWidget via APP->scene->rack->getTopCable()"}});
        m.entries.push_back({"Default Color", {"White if no cable connected or color unavailable", "White if no cable connected or color unavailable", "White if no cable connected or color unavailable"}});
        m.entries.push_back({"Per-Input Colors", {"Each of the 8 inputs has independent color matching", "Each of the 8 inputs has independent color matching", "Each of the 8 inputs has independent color matching"}});
        m.entries.push_back({"Visual Benefit", {"Easy identification of which waveform corresponds to which signal source", "Easy identification of which waveform corresponds to which signal source", "Easy identification of which waveform corresponds to which signal source"}});
        m.entries.push_back({"Track 1", {"Top row input 1 (leftmost). Overlaid on display track 1.", "上排輸入 1（最左）。顯示於 Oscilloscope Track 1。", "上段入力1（最左）。オシロスコープトラック1に表示。"}});
        m.entries.push_back({"Track 2", {"Top row input 2. Overlaid on display track 2.", "上排輸入 2。顯示於 Oscilloscope Track 2。", "上段入力2。オシロスコープトラック2に表示。"}});
        m.entries.push_back({"Track 3", {"Top row input 3. Overlaid on display track 3.", "上排輸入 3。顯示於 Oscilloscope Track 3。", "上段入力3。オシロスコープトラック3に表示。"}});
        m.entries.push_back({"Track 4", {"Top row input 4 (rightmost). Overlaid on display track 4.", "上排輸入 4（最右）。顯示於 Oscilloscope Track 4。", "上段入力4（最右）。オシロスコープトラック4に表示。"}});
        m.entries.push_back({"Track 5", {"Bottom row input 1 (leftmost). Overlaid on display track 1 with Track 1.", "下排輸入 1（最左）。與 Track 1 疊加於 Oscilloscope Track 1。", "下段入力1（最左）。Track 1とオシロスコープトラック1に重畳。"}});
        m.entries.push_back({"Track 6", {"Bottom row input 2. Overlaid on display track 2 with Track 2.", "下排輸入 2。與 Track 2 疊加於 Oscilloscope Track 2。", "下段入力2。Track 2とオシロスコープトラック2に重畳。"}});
        m.entries.push_back({"Track 7", {"Bottom row input 3. Overlaid on display track 3 with Track 3.", "下排輸入 3。與 Track 3 疊加於 Oscilloscope Track 3。", "下段入力3。Track 3とオシロスコープトラック3に重畳。"}});
        m.entries.push_back({"Track 8", {"Bottom row input 4 (rightmost). Overlaid on display track 4 with Track 4.", "下排輸入 4（最右）。與 Track 4 疊加於 Oscilloscope Track 4。", "下段入力4（最右）。Track 4とオシロスコープトラック4に重畳。"}});
        m.entries.push_back({"Time", {"Hidden time base control (click and drag on display). Range: 5ms to 50,000ms per screen width, logarithmic scaling.", "隱藏 Time Base 控制（在顯示上點擊拖曳）。範圍：每畫面 5ms 至 50,000ms，對數縮放。", "隠しタイムベース制御（ディスプレイをクリック＆ドラッグ）。範囲：画面幅あたり5ms〜50,000ms、対数スケーリング。"}});
        m.entries.push_back({"Trigger", {"Click LED to toggle trigger mode. Enabled (white LED) = waits for rising edge; Disabled (pink LED) = free-running display.", "Trigger 模式切換按鈕。Enabled（白色 LED）= 等待上升邊緣；Disabled（粉色 LED）= 自由運行顯示。預設：Disabled。", "トリガーモード切替ボタン。Enabled（白色LED）= 立ち上がりエッジ待機；Disabled（ピンク色LED）= フリーランディスプレイ。デフォルト：Disabled。"}});
        m.entries.push_back({"Polyphonic", {"Displays channel 0 only for polyphonic inputs", "Polyphonic 輸入僅顯示 Channel 0", "Displays channel 0 only for polyphonic inputs"}});
        m.entries.push_back({"Time", {"Hidden time base control (click and drag on display). Range: 5ms to 50,000ms per screen width, logarithmic scaling.", "隱藏 Time Base 控制（在顯示上點擊拖曳）。範圍：每畫面 5ms 至 50,000ms，對數縮放。", "隠しタイムベース制御（ディスプレイをクリック＆ドラッグ）。範囲：画面幅あたり5ms〜50,000ms、対数スケーリング。"}});
        m.entries.push_back({"Trigger", {"Toggle trigger mode button. Enabled (white LED) = waits for rising edge; Disabled (pink LED) = free-running display. Default: Disabled.", "Trigger 模式切換按鈕。Enabled（白色 LED）= 等待上升邊緣；Disabled（粉色 LED）= 自由運行顯示。預設：Disabled。", "トリガーモード切替ボタン。Enabled（白色LED）= 立ち上がりエッジ待機；Disabled（ピンク色LED）= フリーランディスプレイ。デフォルト：Disabled。"}});
        m.entries.push_back({"Track 1", {"Top row input 1 (leftmost). Displayed on oscilloscope track 1.", "上排輸入 1（最左）。顯示於 Oscilloscope Track 1。", "上段入力1（最左）。オシロスコープトラック1に表示。"}});
        m.entries.push_back({"Track 2", {"Top row input 2. Displayed on oscilloscope track 2.", "上排輸入 2。顯示於 Oscilloscope Track 2。", "上段入力2。オシロスコープトラック2に表示。"}});
        m.entries.push_back({"Track 3", {"Top row input 3. Displayed on oscilloscope track 3.", "上排輸入 3。顯示於 Oscilloscope Track 3。", "上段入力3。オシロスコープトラック3に表示。"}});
        m.entries.push_back({"Track 4", {"Top row input 4 (rightmost). Displayed on oscilloscope track 4.", "上排輸入 4（最右）。顯示於 Oscilloscope Track 4。", "上段入力4（最右）。オシロスコープトラック4に表示。"}});
        m.entries.push_back({"Track 5", {"Bottom row input 1 (leftmost). Overlaid on oscilloscope track 1 with Track 1.", "下排輸入 1（最左）。與 Track 1 疊加於 Oscilloscope Track 1。", "下段入力1（最左）。Track 1とオシロスコープトラック1に重畳。"}});
        m.entries.push_back({"Track 6", {"Bottom row input 2. Overlaid on oscilloscope track 2 with Track 2.", "下排輸入 2。與 Track 2 疊加於 Oscilloscope Track 2。", "下段入力2。Track 2とオシロスコープトラック2に重畳。"}});
        m.entries.push_back({"Track 7", {"Bottom row input 3. Overlaid on oscilloscope track 3 with Track 3.", "下排輸入 3。與 Track 3 疊加於 Oscilloscope Track 3。", "下段入力3。Track 3とオシロスコープトラック3に重畳。"}});
        m.entries.push_back({"Track 8", {"Bottom row input 4 (rightmost). Overlaid on oscilloscope track 4 with Track 4.", "下排輸入 4（最右）。與 Track 4 疊加於 Oscilloscope Track 4。", "下段入力4（最右）。Track 4とオシロスコープトラック4に重畳。"}});
        data["Obserfour"] = std::move(m);
    }
    // Observer (36 entries)
    {
        ModuleHelpData m;
        m.name = "Observer";
        m.description = {"8-channel oscilloscope with color-coded waveforms matching cable colors and adjustable time base", "8 通道 Oscilloscope，Waveform 顏色對應 Cable 顏色，可調整 Time Base", "ケーブル色に対応したカラーコード波形表示と調整可能なタイムベースを備えた8チャンネルオシロスコープ"};
        m.entries.push_back({"Buffer Size", {"256 samples per track (SCOPE_BUFFER_SIZE constant)", "256 samples per track (SCOPE_BUFFER_SIZE constant)", "256 samples per track (SCOPE_BUFFER_SIZE constant)"}});
        m.entries.push_back({"Waveform Rendering", {"Anti-aliased line drawing with 1.5px stroke width", "Anti-aliased line drawing with 1.5px stroke width", "Anti-aliased line drawing with 1.5px stroke width"}});
        m.entries.push_back({"Y-axis Scaling", {"±10V mapped to display height with -0.05 multiplier (voltage × -0.05 + 0.5)", "±10V mapped to display height with -0.05 multiplier (voltage × -0.05 + 0.5)", "±10V mapped to display height with -0.05 multiplier (voltage × -0.05 + 0.5)"}});
        m.entries.push_back({"Color Source", {"Reads cable color from input port connection via PortWidget API", "Reads cable color from input port connection via PortWidget API", "Reads cable color from input port connection via PortWidget API"}});
        m.entries.push_back({"Frame Rate", {"Waveform updates at display refresh rate (typically 60Hz)", "Waveform updates at display refresh rate (typically 60Hz)", "Waveform updates at display refresh rate (typically 60Hz)"}});
        m.entries.push_back({"Range", {"5ms to 50,000ms (50s) per screen width", "5ms to 50,000ms (50s) per screen width", "5ms to 50,000ms (50s) per screen width"}});
        m.entries.push_back({"Scaling", {"Base-2 logarithmic curve from 5ms to 50,000ms", "Base-2 logarithmic curve from 5ms to 50,000ms", "Base-2 logarithmic curve from 5ms to 50,000ms"}});
        m.entries.push_back({"Samples per Buffer", {"Derived from time base and sample rate", "Derived from time base and sample rate", "Derived from time base and sample rate"}});
        m.entries.push_back({"Interaction", {"Click and drag vertically on display to adjust time base", "Click and drag vertically on display to adjust time base", "Click and drag vertically on display to adjust time base"}});
        m.entries.push_back({"Trigger Source", {"First connected input (lowest numbered track with cable)", "First connected input (lowest numbered track with cable)", "First connected input (lowest numbered track with cable)"}});
        m.entries.push_back({"Trigger Mode Toggle", {"Click LED to switch between free-running and triggered", "Click LED to switch between free-running and triggered", "Click LED to switch between free-running and triggered"}});
        m.entries.push_back({"Triggered (White LED)", {"Waits for rising edge using rescale(voltage, 0, 0.001, 0, 1) with Schmitt trigger", "Waits for rising edge using rescale(voltage, 0, 0.001, 0, 1) with Schmitt trigger", "Waits for rising edge using rescale(voltage, 0, 0.001, 0, 1) with Schmitt trigger"}});
        m.entries.push_back({"Free-Running (Pink LED)", {"Continuous waveform display without synchronization", "Continuous waveform display without synchronization", "Continuous waveform display without synchronization"}});
        m.entries.push_back({"Threshold", {"Schmitt trigger with rescale from 0V-0.001V to 0-1 range (triggers when voltage rises above 0.001V)", "Schmitt trigger with rescale from 0V-0.001V to 0-1 range (triggers when voltage rises above 0.001V)", "Schmitt trigger with rescale from 0V-0.001V to 0-1 range (triggers when voltage rises above 0.001V)"}});
        m.entries.push_back({"Holdoff", {"Buffer refills completely after each trigger before waiting for next", "各 Trigger 後 Buffer 完全填滿才等待下一次", "Buffer refills completely after each trigger before waiting for next"}});
        m.entries.push_back({"Input Handling", {"Reads first channel of polyphonic inputs (channel 0)", "Reads first channel of polyphonic inputs (channel 0)", "Reads first channel of polyphonic inputs (channel 0)"}});
        m.entries.push_back({"Sample Decimation", {"At longer time bases, samples are decimated to fit buffer", "較長 Time Base 時，Samples 經抽取以適應 Buffer", "At longer time bases, samples are decimated to fit buffer"}});
        m.entries.push_back({"Buffer Write", {"Circular buffer with frame index tracking", "Circular buffer with frame index tracking", "Circular buffer with frame index tracking"}});
        m.entries.push_back({"Display Mapping", {"Buffer index 0-255 mapped to display X coordinates", "Buffer index 0-255 mapped to display X coordinates", "Buffer index 0-255 mapped to display X coordinates"}});
        m.entries.push_back({"Track 1", {"Top row input 1 (leftmost). Waveform color matches connected cable.", "上排輸入 1（最左）。Waveform 顏色對應連接 Cable。", "上段入力1（最左）。波形色は接続ケーブル色に対応。"}});
        m.entries.push_back({"Track 2", {"Top row input 2. Waveform color matches connected cable.", "上排輸入 2。Waveform 顏色對應連接 Cable。", "上段入力2。波形色は接続ケーブル色に対応。"}});
        m.entries.push_back({"Track 3", {"Top row input 3. Waveform color matches connected cable.", "上排輸入 3。Waveform 顏色對應連接 Cable。", "上段入力3。波形色は接続ケーブル色に対応。"}});
        m.entries.push_back({"Track 4", {"Top row input 4 (rightmost). Waveform color matches connected cable.", "上排輸入 4（最右）。Waveform 顏色對應連接 Cable。", "上段入力4（最右）。波形色は接続ケーブル色に対応。"}});
        m.entries.push_back({"Track 5", {"Bottom row input 1 (leftmost). Waveform color matches connected cable.", "下排輸入 1（最左）。Waveform 顏色對應連接 Cable。", "下段入力1（最左）。波形色は接続ケーブル色に対応。"}});
        m.entries.push_back({"Track 6", {"Bottom row input 2. Waveform color matches connected cable.", "下排輸入 2。Waveform 顏色對應連接 Cable。", "下段入力2。波形色は接続ケーブル色に対応。"}});
        m.entries.push_back({"Track 7", {"Bottom row input 3. Waveform color matches connected cable.", "下排輸入 3。Waveform 顏色對應連接 Cable。", "下段入力3。波形色は接続ケーブル色に対応。"}});
        m.entries.push_back({"Track 8", {"Bottom row input 4 (rightmost). Waveform color matches connected cable.", "下排輸入 4（最右）。Waveform 顏色對應連接 Cable。", "下段入力4（最右）。波形色は接続ケーブル色に対応。"}});
        m.entries.push_back({"Time", {"Hidden time base control (click and drag on display). Range: 5ms to 50,000ms per screen width, logarithmic scaling.", "隱藏 Time Base 控制（在顯示上點擊拖曳）。範圍：每畫面 5ms 至 50,000ms，對數縮放。", "隠しタイムベース制御（ディスプレイをクリック＆ドラッグ）。範囲：画面幅あたり5ms〜50,000ms、対数スケーリング。"}});
        m.entries.push_back({"Trigger", {"Click LED to toggle trigger mode. Enabled (white LED) = waits for rising edge; Disabled (pink LED) = free-running display.", "點擊 LED 切換 Trigger 模式。Enabled（白色 LED）= 等待上升邊緣；Disabled（粉色 LED）= 自由運行顯示。", "LEDをクリックしてトリガーモードを切替。Enabled（白色LED）= 立ち上がりエッジ待機；Disabled（ピンク色LED）= フリーランディスプレイ。"}});
        m.entries.push_back({"Cable Color Matching", {"Waveform color automatically matches the connected cable color using VCV's cable color API", "Waveform color automatically matches the connected cable color using VCV's cable color API", "Waveform color automatically matches the connected cable color using VCV's cable color API"}});
        m.entries.push_back({"Grid Lines", {"Horizontal center line at 0V, vertical divisions at regular intervals", "Horizontal center line at 0V, vertical divisions at regular intervals", "Horizontal center line at 0V, vertical divisions at regular intervals"}});
        m.entries.push_back({"Trigger LED", {"Top-right indicator showing trigger mode (white = triggered, pink = free-running)", "右上指示器顯示 Trigger Mode（白色 = Triggered，粉色 = 自由運行）", "Top-right indicator showing trigger mode (white = triggered, pink = free-running)"}});
        m.entries.push_back({"Dark Background", {"High contrast display optimized for waveform visibility", "High contrast display optimized for waveform visibility", "High contrast display optimized for waveform visibility"}});
        m.entries.push_back({"Based on", {"VCV Scope module architecture", "VCV Scope module architecture", "VCV Scope module architecture"}});
        m.entries.push_back({"Key Difference", {"Multiple simultaneous tracks with automatic cable color matching", "Multiple simultaneous tracks with automatic cable color matching", "Multiple simultaneous tracks with automatic cable color matching"}});
        m.entries.push_back({"Performance", {"Optimized for minimal CPU usage with frame-rate-limited display updates", "Optimized for minimal CPU usage with frame-rate-limited display updates", "Optimized for minimal CPU usage with frame-rate-limited display updates"}});
        data["Observer"] = std::move(m);
    }
    // Ovomorph (32 entries)
    {
        ModuleHelpData m;
        m.name = "Ovomorph";
        m.description = {"Chaos-modulated stereo reverb with Lorenz attractor modulation and sample-and-hold output", "Chaos 調變 Stereo Reverb，具備 Lorenz Attractor 調變與 Sample-and-Hold 輸出", "ローレンツアトラクター変調とサンプル&ホールド出力を備えたカオス変調ステレオリバーブ"};
        m.entries.push_back({"Room", {"Room size (0-100%, default: 50%). Affects early reflection tap positions. Scale: 0.3-1.7×", "空間大小（0-100%，預設：50%）。影響早期反射 Tap 位置。縮放：0.3-1.7×", "ルームサイズ（0-100%、デフォルト：50%）。初期反射タップ位置に影響。スケール：0.3-1.7×"}});
        m.entries.push_back({"Tone", {"Damping coefficient (0-100%, default: 40%). Low = dark, warm sound. High = bright, open sound.", "Damping 係數（0-100%，預設：40%）。低 = 暗、溫暖的聲音。高 = 亮、開放的聲音。", "ダンピング係数（0-100%、デフォルト：40%）。低=暗く暖かいサウンド。高=明るく開放的なサウンド。"}});
        m.entries.push_back({"Decay", {"Feedback amount (0-100%, default: 60%). At maximum, near-infinite reverb tails (10+ seconds).", "Feedback 量（0-100%，預設：60%）。最大時，近乎無限 Reverb 尾部（10+ 秒）。", "フィードバック量（0-100%、デフォルト：60%）。最大時、ほぼ無限のリバーブテール（10秒以上）。"}});
        m.entries.push_back({"Mix", {"Wet/dry balance (0-100%, default: 50%). CV adds ±10%.", "Wet/Dry 平衡（0-100%，預設：50%）。CV 增加 ±10%。", "ウェット/ドライバランス（0-100%、デフォルト：50%）。CVで±10%追加。"}});
        m.entries.push_back({"Algorithm", {"Freeverb-style with 8 parallel comb filters + 4 series allpass diffusers", "Freeverb-style with 8 parallel comb filters + 4 series allpass diffusers", "Freeverb-style with 8 parallel comb filters + 4 series allpass diffusers"}});
        m.entries.push_back({"Comb Filters (L)", {"1557, 1617, 1491, 1422 samples (~30-34ms at 48kHz)", "1557, 1617, 1491, 1422 samples (~30-34ms at 48kHz)", "1557, 1617, 1491, 1422 samples (~30-34ms at 48kHz)"}});
        m.entries.push_back({"Comb Filters (R)", {"1277, 1356, 1188, 1116 samples (~23-28ms at 48kHz)", "1277, 1356, 1188, 1116 samples (~23-28ms at 48kHz)", "1277, 1356, 1188, 1116 samples (~23-28ms at 48kHz)"}});
        m.entries.push_back({"Allpass Sizes", {"556, 441, 341, 225 samples", "556, 441, 341, 225 samples", "556, 441, 341, 225 samples"}});
        m.entries.push_back({"Highpass Filter", {"100Hz cutoff removes sub-bass buildup", "100Hz 截止頻率移除低頻累積", "100Hz cutoff removes sub-bass buildup"}});
        m.entries.push_back({"Algorithm", {"Lorenz strange attractor differential equations", "Lorenz strange attractor differential equations", "Lorenz strange attractor differential equations"}});
        m.entries.push_back({"Chaos", {"Modulation depth (0-100%, default: 0%). Controls chaos effects on reverb parameters.", "原始 Chaos 訊號 0-5V（Polyphonic）", "生カオス信号0-5V（ポリフォニック）"}});
        m.entries.push_back({"Rate", {"Chaos evolution speed (0.01-2.0x, default: 0.5x). Higher = faster parameter changes.", "Chaos 演化速度（0.01-2.0x，預設：0.5x）。越高 = 參數變化越快。", "カオス進化速度（0.01-2.0x、デフォルト：0.5x）。高いほどパラメータ変化が速い。"}});
        m.entries.push_back({"Chaos Effects", {"Feedback ±0.5, room offset ±50-70 samples", "Feedback ±0.5, room offset ±50-70 samples", "Feedback ±0.5, room offset ±50-70 samples"}});
        m.entries.push_back({"S&H Output", {"Stepped chaos signal sampled at RATE × 10 Hz", "Stepped chaos signal sampled at RATE × 10 Hz", "Stepped chaos signal sampled at RATE × 10 Hz"}});
        m.entries.push_back({"Range", {"0-5V CV output", "0-5V CV output", "0-5V CV output"}});
        m.entries.push_back({"Use", {"External modulation source with quantized, stepped behavior", "External modulation source with quantized, stepped behavior", "External modulation source with quantized, stepped behavior"}});
        m.entries.push_back({"Detection", {"When placed directly right of YAMANOTE, auto-connects to send/return", "When placed directly right of YAMANOTE, auto-connects to send/return", "When placed directly right of YAMANOTE, auto-connects to send/return"}});
        m.entries.push_back({"Channel Selection", {"Uses Channel A if single effect, Channel B if part of chain", "Uses Channel A if single effect, Channel B if part of chain", "Uses Channel A if single effect, Channel B if part of chain"}});
        m.entries.push_back({"Chain Support", {"Multiple effects (Runner, Facehugger, Ovomorph) auto-distribute across channels", "Multiple effects (Runner, Facehugger, Ovomorph) auto-distribute across channels", "Multiple effects (Runner, Facehugger, Ovomorph) auto-distribute across channels"}});
        m.entries.push_back({"L/R Audio", {"Stereo input (R normalizes to L if disconnected)", "Stereo 處理輸出（Polyphonic）", "ステレオ処理出力（ポリフォニック）"}});
        m.entries.push_back({"ROOM CV", {"Room size modulation", "Room Size 調變", "ルームサイズ変調"}});
        m.entries.push_back({"TONE CV", {"Damping modulation", "Damping 調變", "ダンピング変調"}});
        m.entries.push_back({"DECAY CV", {"Decay time modulation", "Decay 時間調變", "ディケイタイム変調"}});
        m.entries.push_back({"MIX CV", {"Wet/dry modulation", "Wet/Dry 調變", "ウェット/ドライ変調"}});
        m.entries.push_back({"CHAOS CV", {"Chaos depth modulation", "Chaos 深度調變", "カオス深度変調"}});
        m.entries.push_back({"RATE CV", {"Chaos speed modulation", "Chaos 速度調變", "カオス速度変調"}});
        m.entries.push_back({"L/R Audio", {"Stereo processed output (polyphonic)", "Stereo 處理輸出（Polyphonic）", "ステレオ処理出力（ポリフォニック）"}});
        m.entries.push_back({"Chaos", {"Raw chaos signal 0-5V (polyphonic)", "原始 Chaos 訊號 0-5V（Polyphonic）", "生カオス信号0-5V（ポリフォニック）"}});
        m.entries.push_back({"S&H", {"Sample-and-hold chaos 0-5V (polyphonic)", "Sample-and-Hold Chaos 0-5V（Polyphonic）", "サンプル&ホールドカオス0-5V（ポリフォニック）"}});
        m.entries.push_back({"Left Audio", {"Stereo audio left channel input/output. Input feeds the reverb processor; output delivers the processed signal.", "立體聲左聲道 Audio 輸入/輸出。輸入饋送 Reverb 處理器；輸出傳遞處理後的訊號。", "ステレオオーディオ左チャンネル入出力。入力はリバーブプロセッサに送り、出力は処理済み信号を出力。"}});
        m.entries.push_back({"Right Audio", {"Stereo audio right channel input/output. Input feeds the reverb processor; output delivers the processed signal.", "立體聲右聲道 Audio 輸入/輸出。輸入饋送 Reverb 處理器；輸出傳遞處理後的訊號。", "ステレオオーディオ右チャンネル入出力。入力はリバーブプロセッサに送り、出力は処理済み信号を出力。"}});
        m.entries.push_back({"Sample & Hold CV", {"Sample & Hold output from Chaos section. Samples random values at Chaos Rate, with slew limiting.", "Chaos 區段的 Sample & Hold 輸出。依 Chaos Rate 取樣隨機值，帶有 Slew 限制。", "Chaosセクションのサンプル＆ホールド出力。Chaos Rateでランダム値をサンプリングし、スルーリミッティング付き。"}});
        data["Ovomorph"] = std::move(m);
    }
    // PPaTTTerning (47 entries)
    {
        ModuleHelpData m;
        m.name = "PPaTTTerning";
        m.description = {"Minimalist CV sequencer with 5 voltage knobs, 3 pattern styles including custom pattern support, density control, and integrated shift register delay", "極簡 CV Sequencer，具備 5 個電壓旋鈕、3 種 Pattern 風格（含自訂 Pattern）、Density 控制與整合 Shift Register Delay", "5つの電圧ノブ、カスタムパターン対応3つのパターンスタイル、密度制御、統合シフトレジスターディレイを備えたミニマリストCVシーケンサー"};
        m.entries.push_back({"Range 0.0-0.2", {"2 primary knobs (K1-K2)", "2 primary knobs (K1-K2)", "2 primary knobs (K1-K2)"}});
        m.entries.push_back({"Range 0.2-0.4", {"3 primary knobs (K1-K3)", "3 primary knobs (K1-K3)", "3 primary knobs (K1-K3)"}});
        m.entries.push_back({"Range 0.4-0.6", {"4 primary knobs (K1-K4)", "4 primary knobs (K1-K4)", "4 primary knobs (K1-K4)"}});
        m.entries.push_back({"Range 0.6-1.0", {"5 primary knobs (K1-K5)", "5 primary knobs (K1-K5)", "5 primary knobs (K1-K5)"}});
        m.entries.push_back({"High Density Bonus", {"Above 0.8, additional pattern variations every clamp(length/8, 3, 8) steps", "Above 0.8, additional pattern variations every clamp(length/8, 3, 8) steps", "Above 0.8, additional pattern variations every clamp(length/8, 3, 8) steps"}});
        m.entries.push_back({"Unused Knobs", {"Inserted at regular intervals even when not primary", "Inserted at regular intervals even when not primary", "Inserted at regular intervals even when not primary"}});
        m.entries.push_back({"Sequential (Red LED)", {"Simple cycle through primary knobs, wrapping at pattern length", "Simple cycle through primary knobs, wrapping at pattern length", "Simple cycle through primary knobs, wrapping at pattern length"}});
        m.entries.push_back({"Custom (Green LED)", {"User-defined pattern editable via right-click menu. Default is a 32-step Steve Reich-inspired motif. Enter knob numbers 1-5 to define your own pattern.", "User-defined pattern editable via right-click menu. Default is a 32-step Steve Reich-inspired motif. Enter knob numbers 1-5 to define your own pattern.", "User-defined pattern editable via right-click menu. Default is a 32-step Steve Reich-inspired motif. Enter knob numbers 1-5 to define your own pattern."}});
        m.entries.push_back({"Jump (Blue LED)", {"Alternates between distant knobs using pattern [0,2,4,1,3]. Creates wider melodic intervals.", "Alternates between distant knobs using pattern [0,2,4,1,3]. Creates wider melodic intervals.", "Alternates between distant knobs using pattern [0,2,4,1,3]. Creates wider melodic intervals."}});
        m.entries.push_back({"Access", {"Right-click menu → Custom Pattern section", "Right-click menu → Custom Pattern section", "Right-click menu → Custom Pattern section"}});
        m.entries.push_back({"Format", {"Enter numbers 1-5 representing knobs (e.g., \"12312345\")", "Enter numbers 1-5 representing knobs (e.g., \"12312345\")", "Enter numbers 1-5 representing knobs (e.g., \"12312345\")"}});
        m.entries.push_back({"Default", {"32-step phasing pattern [1,2,3,1,2,3,4,5,4,5,1,2,3,1,2,3,4,5,4,5,2,4,3,5,1,3,2,4,1,5,3,2]", "32-step phasing pattern [1,2,3,1,2,3,4,5,4,5,1,2,3,1,2,3,4,5,4,5,2,4,3,5,1,3,2,4,1,5,3,2]", "32-step phasing pattern [1,2,3,1,2,3,4,5,4,5,1,2,3,1,2,3,4,5,4,5,2,4,3,5,1,3,2,4,1,5,3,2]"}});
        m.entries.push_back({"Density Interaction", {"Pattern values are clamped to available knobs based on Density setting", "Pattern values are clamped to available knobs based on Density setting", "Pattern values are clamped to available knobs based on Density setting"}});
        m.entries.push_back({"Step Randomization", {"Randomizes up to 50% of steps based on chaos amount", "Randomizes up to 50% of steps based on chaos amount", "Randomizes up to 50% of steps based on chaos amount"}});
        m.entries.push_back({"High Chaos (&gt;50%)", {"Randomly selects knobs outside the density range, creating unexpected variations", "Randomly selects knobs outside the density range, creating unexpected variations", "Randomly selects knobs outside the density range, creating unexpected variations"}});
        m.entries.push_back({"Result", {"Higher chaos creates less predictable, more generative sequences", "Higher chaos creates less predictable, more generative sequences", "Higher chaos creates less predictable, more generative sequences"}});
        m.entries.push_back({"Range", {"-10V to +10V each", "-10V to +10V each", "-10V to +10V each"}});
        m.entries.push_back({"Defaults", {"K1=0V, K2=2V, K3=4V, K4=6V, K5=8V (ascending pattern)", "K1=0V, K2=2V, K3=4V, K4=6V, K5=8V (ascending pattern)", "K1=0V, K2=2V, K3=4V, K4=6V, K5=8V (ascending pattern)"}});
        m.entries.push_back({"Use Case", {"Set to scale notes for melodic sequences, or arbitrary values for modulation", "Set to scale notes for melodic sequences, or arbitrary values for modulation", "Set to scale notes for melodic sequences, or arbitrary values for modulation"}});
        m.entries.push_back({"TRIG Output", {"Generates 10ms pulse when CV value changes from previous step", "Generates 10ms pulse when CV value changes from previous step", "Generates 10ms pulse when CV value changes from previous step"}});
        m.entries.push_back({"TRIG2 Output", {"Generates 10ms pulse when CV2 value changes", "Generates 10ms pulse when CV2 value changes", "Generates 10ms pulse when CV2 value changes"}});
        m.entries.push_back({"No Trigger", {"If consecutive steps output the same voltage, no trigger fires", "If consecutive steps output the same voltage, no trigger fires", "If consecutive steps output the same voltage, no trigger fires"}});
        m.entries.push_back({"T2.DLY (Shift Register)", {"0-5 step delay. Stores last 8 CV values in circular buffer. LED brightness indicates delay amount.", "0-5 step delay. Stores last 8 CV values in circular buffer. LED brightness indicates delay amount.", "0-5 step delay. Stores last 8 CV values in circular buffer. LED brightness indicates delay amount."}});
        m.entries.push_back({"CVD (Time Delay)", {"0-1000ms continuous delay. Buffer size: 192,000 samples (~4.3s at 44.1kHz).", "0-1000ms continuous delay. Buffer size: 192,000 samples (~4.3s at 44.1kHz).", "0-1000ms continuous delay. Buffer size: 192,000 samples (~4.3s at 44.1kHz)."}});
        m.entries.push_back({"CV Modulation", {"When CVD CV input is connected, knob acts as attenuator for CV. Formula: (CV/10V) × knob × 1000 = delay time in ms.", "連接 CVD CV 輸入時，旋鈕作為 CV 衰減器。公式: (CV/10V) × knob × 1000 = Delay 時間 (ms)。", "When CVD CV input is connected, knob acts as attenuator for CV. Formula: (CV/10V) × knob × 1000 = delay time in ms."}});
        m.entries.push_back({"Combined Delay", {"CV2 = CVD_delay(shift_register[step - T2.DLY])", "CV2 = CVD_delay(shift_register[step - T2.DLY])", "CV2 = CVD_delay(shift_register[step - T2.DLY])"}});
        m.entries.push_back({"CV OUT", {"Main sequence output (current knob voltage)", "主序列輸出（目前旋鈕電壓）", "メインシーケンス出力（現在のノブ電圧）"}});
        m.entries.push_back({"TRIG", {"Trigger on CV change (10ms pulse)", "CV 變化時 Trigger（10ms Pulse）", "CV変化時トリガー（10msパルス）"}});
        m.entries.push_back({"CV2", {"Delayed sequence (shift register + CVD)", "延遲 CV 序列輸出。對主序列先施加 Shift Register Delay 再施加 CVD Time Delay。", "遅延CVシーケンス出力。メインシーケンスにシフトレジスターディレイ後、CVDタイムディレイを適用。"}});
        m.entries.push_back({"TRIG2", {"Trigger on CV2 change (10ms pulse)", "CV2 變化時 Trigger（10ms Pulse）", "CV2変化時トリガー（10msパルス）"}});
        m.entries.push_back({"K1", {"Voltage knob 1. Range -10V to +10V, default 0V.", "電壓旋鈕 1。範圍 -10V 至 +10V，預設 0V。", "電圧ノブ1。範囲-10V〜+10V、デフォルト0V。"}});
        m.entries.push_back({"K2", {"Voltage knob 2. Range -10V to +10V, default 2V.", "電壓旋鈕 2。範圍 -10V 至 +10V，預設 2V。", "電圧ノブ2。範囲-10V〜+10V、デフォルト2V。"}});
        m.entries.push_back({"K3", {"Voltage knob 3. Range -10V to +10V, default 4V.", "電壓旋鈕 3。範圍 -10V 至 +10V，預設 4V。", "電圧ノブ3。範囲-10V〜+10V、デフォルト4V。"}});
        m.entries.push_back({"K4", {"Voltage knob 4. Range -10V to +10V, default 6V.", "電壓旋鈕 4。範圍 -10V 至 +10V，預設 6V。", "電圧ノブ4。範囲-10V〜+10V、デフォルト6V。"}});
        m.entries.push_back({"K5", {"Voltage knob 5. Range -10V to +10V, default 8V.", "電壓旋鈕 5。範圍 -10V 至 +10V，預設 8V。", "電圧ノブ5。範囲-10V〜+10V、デフォルト8V。"}});
        m.entries.push_back({"Mode", {"Pattern style selector. Sequential (Red), Custom (Green), Jump (Blue).", "Pattern 風格選擇器。Sequential（紅）、Custom（綠）、Jump（藍）。", "パターンスタイルセレクター。Sequential（赤）、Custom（緑）、Jump（青）。"}});
        m.entries.push_back({"Density", {"Controls active knob count and sequence length (8-48 steps).", "控制使用旋鈕數量與序列長度（8-48 步）。", "使用ノブ数とシーケンス長（8-48ステップ）を制御。"}});
        m.entries.push_back({"Chaos", {"Randomization amount 0-100%. Higher values create less predictable sequences.", "隨機化量 0-100%。較高值產生較不可預測的序列。", "ランダム化量0-100%。高値はより予測不可能なシーケンスを生成。"}});
        m.entries.push_back({"CVD Time/Attenuation", {"CVD delay time 0-1000ms. Acts as attenuator when CV input is connected.", "CVD Delay 時間 0-1000ms。連接 CV 輸入時作為衰減器。", "CVDディレイ時間0-1000ms。CV入力接続時はアッテネーターとして機能。"}});
        m.entries.push_back({"Delay", {"Shift register delay 0-5 steps for Track 2.", "Track 2 的 Shift Register Delay 0-5 步。", "トラック2のシフトレジスターディレイ0-5ステップ。"}});
        m.entries.push_back({"Clock", {"Clock input. Rising edge advances the step counter.", "Clock 輸入。上升邊緣推進 Step Counter。", "クロック入力。立ち上がりエッジでステップカウンターを進行。"}});
        m.entries.push_back({"Reset", {"Reset input. Rising edge resets step counter to 0.", "Reset 輸入。上升邊緣將 Step Counter 重置為 0。", "リセット入力。立ち上がりエッジでステップカウンターを0にリセット。"}});
        m.entries.push_back({"CVD Time CV", {"CV input for CVD delay time modulation (0-10V).", "CVD Delay 時間調變 CV 輸入（0-10V）。", "CVDディレイ時間変調用CV入力（0-10V）。"}});
        m.entries.push_back({"CV", {"Main CV sequence output (current knob voltage).", "主 CV 序列輸出（目前旋鈕電壓）。", "メインCVシーケンス出力（現在のノブ電圧）。"}});
        m.entries.push_back({"Trigger", {"Trigger output. 10ms pulse when CV value changes from previous step.", "Trigger 輸出。當 CV 值與前一步不同時產生 10ms Pulse。", "トリガー出力。CV値が前ステップから変化すると10msパルスを生成。"}});
        m.entries.push_back({"CV2", {"Delayed CV sequence output. Applies shift register delay then CVD time delay to the main sequence.", "延遲 CV 序列輸出。對主序列先施加 Shift Register Delay 再施加 CVD Time Delay。", "遅延CVシーケンス出力。メインシーケンスにシフトレジスターディレイ後、CVDタイムディレイを適用。"}});
        m.entries.push_back({"Trigger 2", {"Trigger output for CV2. 10ms pulse when delayed CV value changes.", "CV2 的 Trigger 輸出。延遲 CV 值變化時產生 10ms Pulse。", "CV2のトリガー出力。遅延CV値が変化すると10msパルスを生成。"}});
        data["PPaTTTerning"] = std::move(m);
    }
    // Pinpple (54 entries)
    {
        ModuleHelpData m;
        m.name = "Pinpple";
        m.description = {"Ripples-inspired percussion synthesizer using band pass filtering with randomized frequency/decay and integrated LPG modulation", "Ripples 風格 Percussion 合成器，使用 Band Pass 濾波與隨機化頻率/Decay，以及整合 LPG Modulation", "Ripples風パーカッションシンセサイザー、バンドパスフィルタリングとランダム化された周波数/ディケイ、統合LPG変調を使用"};
        m.entries.push_back({"Architecture", {"Second-order sections (SOS) IIR filter cascade with 3x oversampling", "Second-order sections (SOS) IIR filter cascade with 3x oversampling", "Second-order sections (SOS) IIR filter cascade with 3x oversampling"}});
        m.entries.push_back({"Filter Type", {"4-pole OTA ladder filter with self-modulation feedback", "4-pole OTA ladder filter with self-modulation feedback", "4-pole OTA ladder filter with self-modulation feedback"}});
        m.entries.push_back({"Antialiasing", {"6-section cascaded SOS filters for up/down conversion", "6-section cascaded SOS filters for up/down conversion", "6-section cascaded SOS filters for up/down conversion"}});
        m.entries.push_back({"RC Filter Constants", {"Based on Ripples hardware (33kΩ cell resistance, 560pF capacitors)", "Based on Ripples hardware (33kΩ cell resistance, 560pF capacitors)", "Based on Ripples hardware (33kΩ cell resistance, 560pF capacitors)"}});
        m.entries.push_back({"OTA VCA", {"Temperature-compensated (40°C nominal) with rational polynomial approximation", "溫度補償（標稱 40°C），含有理多項式近似", "温度補償（公称40°C）、有理多項式近似付き"}});
        m.entries.push_back({"Self-Modulation", {"0.01 coefficient adds subtle harmonic character", "0.01 coefficient adds subtle harmonic character", "0.01 coefficient adds subtle harmonic character"}});
        m.entries.push_back({"Integration", {"Runge-Kutta 2 (RK2) numerical integration for filter state", "Runge-Kutta 2 (RK2) numerical integration for filter state", "Runge-Kutta 2 (RK2) numerical integration for filter state"}});
        m.entries.push_back({"Pink Noise", {"Voss-McCartney algorithm with 8-octave summation. Normalized by /0.816 factor.", "Voss-McCartney 演算法，含 8 個八度總和。以 /0.816 係數正規化。", "Voss-McCartney algorithm with 8-octave summation. Normalized by /0.816 factor."}});
        m.entries.push_back({"Blue Noise", {"Differentiated pink noise (current - previous sample). Normalized by /0.705 factor.", "微分 Pink Noise（目前 - 前一 Sample）。以 /0.705 係數正規化。", "Differentiated pink noise (current - previous sample). Normalized by /0.705 factor."}});
        m.entries.push_back({"Noise Gain", {"Normalized baseline, pink ×0.8, blue ×1.5", "正規化基線，Pink ×0.8，Blue ×1.5", "Normalized baseline, pink ×0.8, blue ×1.5"}});
        m.entries.push_back({"LPG IN MIX", {"Crossfades Pink (0) → External (0.5) → Blue (1.0)", "Noise 訊號源交叉混合: Pink (0) → 外部 (0.5) → Blue (1.0)。", "ノイズソースクロスフェード: ピンク (0) → 外部 (0.5) → ブルー (1.0)。"}});
        m.entries.push_back({"Trigger Detection", {"Schmitt trigger on TRIG IN, generates 2ms internal pulse", "Schmitt trigger on TRIG IN, generates 2ms internal pulse", "Schmitt trigger on TRIG IN, generates 2ms internal pulse"}});
        m.entries.push_back({"LPG Envelope", {"Attack phase (6ms default) + exponential decay phase", "Attack 階段（預設 6ms）+ 指數 Decay 階段", "Attack phase (6ms default) + exponential decay phase"}});
        m.entries.push_back({"Decay Range", {"10ms to 510ms based on resonance parameter", "10ms to 510ms based on resonance parameter", "10ms to 510ms based on resonance parameter"}});
        m.entries.push_back({"Cutoff Sweep", {"200Hz to 18.2kHz, envelope-controlled", "200Hz to 18.2kHz, envelope-controlled", "200Hz to 18.2kHz, envelope-controlled"}});
        m.entries.push_back({"Filter Type", {"Biquad lowpass with Q = 0.707 (Butterworth)", "Biquad lowpass with Q = 0.707 (Butterworth)", "Biquad lowpass with Q = 0.707 (Butterworth)"}});
        m.entries.push_back({"VCA Amount", {"FM AMT parameter controls LPG output level into BPF", "FM AMT parameter controls LPG output level into BPF", "FM AMT parameter controls LPG output level into BPF"}});
        m.entries.push_back({"Attack Time", {"Configurable 0.5-20ms via right-click menu (default: 6ms)", "Configurable 0.5-20ms via right-click menu (default: 6ms)", "Configurable 0.5-20ms via right-click menu (default: 6ms)"}});
        m.entries.push_back({"Behavior", {"Attack-only envelope; ramps from 0 to 1 and holds at peak (no decay phase)", "Attack-only envelope; ramps from 0 to 1 and holds at peak (no decay phase)", "Attack-only envelope; ramps from 0 to 1 and holds at peak (no decay phase)"}});
        m.entries.push_back({"Application", {"Multiplies final BPF output for amplitude shaping", "Multiplies final BPF output for amplitude shaping", "Multiplies final BPF output for amplitude shaping"}});
        m.entries.push_back({"Trigger Event", {"Each trigger generates new random offsets", "Each trigger generates new random offsets", "Each trigger generates new random offsets"}});
        m.entries.push_back({"Distribution", {"Gaussian (normal) with σ = 0.00006", "Gaussian (normal) with σ = 0.00006", "Gaussian (normal) with σ = 0.00006"}});
        m.entries.push_back({"Frequency Offset", {"Added to FREQ parameter before filter processing", "Added to FREQ parameter before filter processing", "Added to FREQ parameter before filter processing"}});
        m.entries.push_back({"Decay Offset", {"Added to DECAY parameter", "Added to DECAY parameter", "Added to DECAY parameter"}});
        m.entries.push_back({"Visual Feedback", {"PinppleRandomizedKnob class displays offset-adjusted position", "PinppleRandomizedKnob class displays offset-adjusted position", "PinppleRandomizedKnob class displays offset-adjusted position"}});
        m.entries.push_back({"Knob Display", {"Visual offset shows randomization effect on knob position", "Visual offset shows randomization effect on knob position", "Visual offset shows randomization effect on knob position"}});
        m.entries.push_back({"MUTE Button", {"Toggle output mute with red LED indicator", "Toggle output mute with red LED indicator", "Toggle output mute with red LED indicator"}});
        m.entries.push_back({"VOLUME", {"Output gain 0-100% (default: 100%)", "輸出 Gain 0-100%（預設: 100%）。", "出力ゲイン 0-100%（デフォルト: 100%）。"}});
        m.entries.push_back({"FREQ", {"BPF center frequency 20Hz-20kHz (log scale). Default: 261Hz (C4). CV: 1V/octave with attenuverter.", "BPF 中心頻率 20Hz-20kHz（對數刻度）。預設: 261Hz (C4)。CV: 1V/octave 含 Attenuverter。", "BPF中心周波数 20Hz-20kHz（対数スケール）。デフォルト: 261Hz (C4)。CV: 1V/octaveアッテヌバーター付き。"}});
        m.entries.push_back({"DECAY", {"Envelope decay time 0-100%. Also controls LPG decay. CV input with attenuverter.", "Envelope Decay 時間 0-100%。同時控制 LPG Decay 範圍 10-510ms。", "エンベロープディケイ時間 0-100%。LPGディケイ範囲 10-510msも制御。"}});
        m.entries.push_back({"FM AMT", {"FM modulation depth 0-100%. Controls LPG signal level into BPF. CV input with attenuverter.", "FM Modulation 深度 0-100%。控制 LPG 訊號至 BPF 的電平。CV 輸入含 Attenuverter。", "FM変調深度 0-100%。BPFへのLPG信号レベルを制御。CVアッテヌバーター付き入力。"}});
        m.entries.push_back({"LPG IN MIX", {"Noise source selection: Pink (left) → External (center) → Blue (right)", "Noise 訊號源交叉混合: Pink (0) → 外部 (0.5) → Blue (1.0)。", "ノイズソースクロスフェード: ピンク (0) → 外部 (0.5) → ブルー (1.0)。"}});
        m.entries.push_back({"TRIG IN", {"Trigger input for envelope and randomization (Schmitt trigger detection)", "Envelope 與隨機化的 Trigger 輸入（Schmitt Trigger 偵測）", "エンベロープとランダム化用トリガー入力（シュミットトリガー検出）"}});
        m.entries.push_back({"FREQ CV", {"1V/octave frequency CV input with attenuverter", "1V/octave 頻率 CV 輸入，含 Attenuverter", "1V/octave周波数CVアッテヌバーター付き入力"}});
        m.entries.push_back({"DECAY CV", {"Decay modulation ±10V with attenuverter", "Decay Modulation 輸入（+-10V，含 Attenuverter）。", "ディケイ変調入力（+-10V、アッテヌバーター付き）。"}});
        m.entries.push_back({"FM IN", {"External audio input for FM modulation (replaces noise when LPG IN MIX centered)", "FM Modulation 的外部 Audio 輸入（LPG IN MIX 置中時取代 Noise）", "FM変調用外部オーディオ入力（LPG IN MIX中央時にノイズを置換）"}});
        m.entries.push_back({"FM AMT CV", {"FM amount modulation ±10V with attenuverter", "FM 量 Modulation 輸入（+-10V，含 Attenuverter）。", "FM量変調入力（+-10V、アッテヌバーター付き）。"}});
        m.entries.push_back({"OUTPUT", {"Main audio output (monophonic). BPF output × VCA envelope × Volume × Mute state.", "主 Audio 輸出（單音）。BPF 輸出 × VCA Envelope × Volume × Mute 狀態。", "メインオーディオ出力（モノフォニック）。BPF出力 × VCAエンベロープ × Volume × ミュート状態。"}});
        m.entries.push_back({"Frequency", {"BPF center frequency 20Hz-20kHz (log scale). Default: 261Hz (C4). CV: 1V/octave.", "BPF 中心頻率 20Hz-20kHz（對數刻度）。預設: 261Hz (C4)。CV: 1V/octave。", "BPF中心周波数 20Hz-20kHz（対数スケール）。デフォルト: 261Hz (C4)。CV: 1V/octave。"}});
        m.entries.push_back({"Decay", {"Envelope decay time 0-100%. Also controls LPG decay range 10-510ms.", "Envelope Decay 時間 0-100%。同時控制 LPG Decay 範圍 10-510ms。", "エンベロープディケイ時間 0-100%。LPGディケイ範囲 10-510msも制御。"}});
        m.entries.push_back({"FM Amount", {"FM modulation depth 0-100%. Controls LPG signal level into BPF.", "FM Modulation 深度 0-100%。控制 LPG 訊號至 BPF 的電平。", "FM変調深度 0-100%。BPFへのLPG信号レベルを制御。"}});
        m.entries.push_back({"Freq CV Attenuverter", {"Attenuverter for Frequency CV input (-1.0 to +1.0).", "頻率 CV 輸入的 Attenuverter（-1.0 至 +1.0）。", "周波数CV入力用アッテヌバーター（-1.0〜+1.0）。"}});
        m.entries.push_back({"Decay CV Attenuverter", {"Attenuverter for Decay CV input (-1.0 to +1.0).", "Decay CV 輸入的 Attenuverter（-1.0 至 +1.0）。", "ディケイCV入力用アッテヌバーター（-1.0〜+1.0）。"}});
        m.entries.push_back({"FM AMT CV Attenuverter", {"Attenuverter for FM AMT CV input (-1.0 to +1.0).", "FM AMT CV 輸入的 Attenuverter（-1.0 至 +1.0）。", "FM AMT CV入力用アッテヌバーター（-1.0〜+1.0）。"}});
        m.entries.push_back({"Mute", {"Toggle output mute with red LED indicator.", "切換輸出 Mute，含紅色 LED 指示。", "赤色LEDインジケーター付き出力ミュート切替。"}});
        m.entries.push_back({"Volume", {"Output gain 0-100% (default: 100%).", "輸出 Gain 0-100%（預設: 100%）。", "出力ゲイン 0-100%（デフォルト: 100%）。"}});
        m.entries.push_back({"LPG IN MIX", {"Noise source crossfade: Pink (0) to External (0.5) to Blue (1.0).", "Noise 訊號源交叉混合: Pink (0) → 外部 (0.5) → Blue (1.0)。", "ノイズソースクロスフェード: ピンク (0) → 外部 (0.5) → ブルー (1.0)。"}});
        m.entries.push_back({"FM", {"External audio input for FM modulation (replaces noise when LPG IN MIX centered).", "FM Modulation 的外部 Audio 輸入（LPG IN MIX 置中時取代 Noise）。", "FM変調用外部オーディオ入力（LPG IN MIX中央時にノイズを置換）。"}});
        m.entries.push_back({"1V/Oct Frequency CV", {"1V/octave frequency CV input with attenuverter.", "1V/octave 頻率 CV 輸入，含 Attenuverter。", "1V/octave周波数CVアッテヌバーター付き入力。"}});
        m.entries.push_back({"Decay CV", {"Decay modulation input (+-10V with attenuverter).", "Decay Modulation 輸入（+-10V，含 Attenuverter）。", "ディケイ変調入力（+-10V、アッテヌバーター付き）。"}});
        m.entries.push_back({"Trigger", {"Trigger input for envelope and randomization (Schmitt trigger detection).", "Envelope 與隨機化的 Trigger 輸入（Schmitt Trigger 偵測）。", "エンベロープとランダム化用トリガー入力（シュミットトリガー検出）。"}});
        m.entries.push_back({"FM AMT CV", {"FM amount modulation input (+-10V with attenuverter).", "FM 量 Modulation 輸入（+-10V，含 Attenuverter）。", "FM量変調入力（+-10V、アッテヌバーター付き）。"}});
        m.entries.push_back({"Audio", {"Main audio output (monophonic). BPF output x VCA envelope x Volume x Mute state.", "主 Audio 輸出（單音）。BPF 輸出 x VCA Envelope x Volume x Mute 狀態。", "メインオーディオ出力（モノフォニック）。BPF出力 x VCAエンベロープ x Volume x ミュート状態。"}});
        m.entries.push_back({"VCA Attack Time", {"Slider to adjust VCA envelope attack (0.5-20ms, default: 6ms)", "調整 VCA Envelope Attack 的滑桿（0.5-20ms，預設: 6ms）", "VCAエンベロープアタック調整用スライダー（0.5-20ms、デフォルト: 6ms）"}});
        data["Pinpple"] = std::move(m);
    }
    // Portal (38 entries)
    {
        ModuleHelpData m;
        m.name = "Portal";
        m.description = {"Poly crossfader for UniRhythm with 3-band isolator, tube drive, DJ-style cue monitoring, and selectable crossfader curve", "UniRhythm 專用 Poly Crossfader，具備 3 頻段 Isolator、Tube Drive、DJ 風格 Cue 監聽與可選 Crossfader 曲線", "3バンドアイソレーター、チューブドライブ、DJスタイルキューモニタリング、選択可能なクロスフェーダーカーブを備えたUniRhythm用ポリクロスフェーダー"};
        m.entries.push_back({"Channels 0-3", {"Timeline (Audio, Gate, Pitch, VelEnv)", "Timeline (Audio, Gate, Pitch, VelEnv)", "Timeline (Audio, Gate, Pitch, VelEnv)"}});
        m.entries.push_back({"Channels 4-7", {"Foundation (Audio, Gate, Pitch, VelEnv)", "Foundation (Audio, Gate, Pitch, VelEnv)", "Foundation (Audio, Gate, Pitch, VelEnv)"}});
        m.entries.push_back({"Channels 8-11", {"Groove (Audio, Gate, Pitch, VelEnv)", "Groove (Audio, Gate, Pitch, VelEnv)", "Groove (Audio, Gate, Pitch, VelEnv)"}});
        m.entries.push_back({"Channels 12-15", {"Lead (Audio, Gate, Pitch, VelEnv)", "Lead (Audio, Gate, Pitch, VelEnv)", "Lead (Audio, Gate, Pitch, VelEnv)"}});
        m.entries.push_back({"XFADE Slider", {"Horizontal slider. A (left) to B (right). Default: center (50%).", "Horizontal slider. A (left) to B (right). Default: center (50%).", "Horizontal slider. A (left) to B (right). Default: center (50%)."}});
        m.entries.push_back({"Cross CV", {"CV input for crossfader position. ±10V maps to ±1.0 position offset (coefficient 0.1).", "Crossfader 位置 CV 調變（±10V，係數 0.1）。", "クロスフェーダー位置CV変調（±10V、係数0.1）。"}});
        m.entries.push_back({"Linear Curve", {"Straight gain crossfade. A = 1-pos, B = pos.", "Straight gain crossfade. A = 1-pos, B = pos.", "Straight gain crossfade. A = 1-pos, B = pos."}});
        m.entries.push_back({"Equal Power (default)", {"Constant power crossfade using sine/cosine. No dip at center.", "Constant power crossfade using sine/cosine. No dip at center.", "Constant power crossfade using sine/cosine. No dip at center."}});
        m.entries.push_back({"Cut Curve", {"DJ-style hard cut with overlap zone. A stays at full until 40%, B stays at full after 60%.", "DJ-style hard cut with overlap zone. A stays at full until 40%, B stays at full after 60%.", "DJ-style hard cut with overlap zone. A stays at full until 40%, B stays at full after 60%."}});
        m.entries.push_back({"LOW", {"Low band gain (below 250Hz). Bipolar: -1 = kill, 0 = unity, +1 = +12dB boost. Quadratic cut for musical feel.", "低頻增益（250Hz 以下）。雙極: -1 = Kill、0 = Unity、+1 = +12dB Boost。二次曲線 Cut 提供音樂感。", "低域ゲイン（250Hz以下）。バイポーラ: -1 = Kill、0 = Unity、+1 = +12dBブースト。二次曲線カットで音楽的な操作感。"}});
        m.entries.push_back({"MID", {"Mid band gain (250Hz-4kHz). Same bipolar control.", "中頻增益（250Hz-4kHz）。相同雙極控制。", "中域ゲイン（250Hz-4kHz）。同じバイポーラコントロール。"}});
        m.entries.push_back({"HIGH", {"High band gain (above 4kHz). Same bipolar control.", "高頻增益（4kHz 以上）。相同雙極控制。", "高域ゲイン（4kHz以上）。同じバイポーラコントロール。"}});
        m.entries.push_back({"Filter Type", {"Linkwitz-Riley 4th order (cascaded 2nd order Butterworth) for phase-coherent crossover.", "Linkwitz-Riley 4th order (cascaded 2nd order Butterworth) for phase-coherent crossover.", "Linkwitz-Riley 4th order (cascaded 2nd order Butterworth) for phase-coherent crossover."}});
        m.entries.push_back({"DRIVE", {"Master tube drive (0-100%). Asymmetric waveshaping: soft saturation on positive, harder on negative for even harmonics.", "Master Tube Drive（0-100%）。非對稱波形整形：正半週柔和飽和、負半週較硬，產生偶次諧波。", "マスターチューブドライブ（0-100%）。非対称ウェーブシェーピング：正側ソフトサチュレーション、負側ハードで偶数倍音を生成。"}});
        m.entries.push_back({"Makeup Gain", {"Automatic gain compensation to maintain perceived volume.", "自動增益補償以維持感知音量。", "Automatic gain compensation to maintain perceived volume."}});
        m.entries.push_back({"DC Blocker", {"1st order high-pass at ~10Hz removes DC offset from saturation.", "1 階高通約 10Hz，消除 Saturation 產生的 DC 偏移。", "1st order high-pass at ~10Hz removes DC offset from saturation."}});
        m.entries.push_back({"CUE Button", {"Toggle between A and B. Displays current selection as colored text.", "Toggle between A and B. Displays current selection as colored text.", "Toggle between A and B. Displays current selection as colored text."}});
        m.entries.push_back({"CUE L/R", {"Dedicated stereo output for headphone monitoring. Outputs raw audio from selected deck (A or B) without crossfader, isolator, or drive processing.", "Stereo Cue 監聽輸出（原始所選 Deck Audio）。", "ステレオキューモニタリング出力（選択デッキの生オーディオ）。"}});
        m.entries.push_back({"Use Case", {"Pre-listen to the incoming deck before crossfading, just like a DJ mixer.", "Pre-listen to the incoming deck before crossfading, just like a DJ mixer.", "Pre-listen to the incoming deck before crossfading, just like a DJ mixer."}});
        m.entries.push_back({"Gate", {"Crossfaded gate signal per role (Timeline, Foundation, Groove, Lead).", "各角色 Crossfade 後的 Gate 訊號（Timeline、Foundation、Groove、Lead）。", "ロール別クロスフェード済みゲート信号（Timeline、Foundation、Groove、Lead）。"}});
        m.entries.push_back({"Pitch", {"Crossfaded pitch CV per role (1V/Oct).", "各角色 Crossfade 後的 Pitch CV（1V/Oct）。", "ロール別クロスフェード済みピッチCV（1V/Oct）。"}});
        m.entries.push_back({"Vel/Env", {"Crossfaded velocity envelope CV per role.", "各角色 Crossfade 後的 Velocity Envelope CV。", "ロール別クロスフェード済みベロシティエンベロープCV。"}});
        m.entries.push_back({"Layout", {"Upper group (Timeline + Groove), Lower group (Foundation + Lead).", "Upper group (Timeline + Groove), Lower group (Foundation + Lead).", "Upper group (Timeline + Groove), Lower group (Foundation + Lead)."}});
        m.entries.push_back({"Poly in A", {"16-channel polyphonic input from UniRhythm Deck A.", "來自 UniRhythm Deck A 的 16 聲道 Polyphonic 輸入。", "UniRhythmデッキAからの16チャンネルポリフォニック入力。"}});
        m.entries.push_back({"Poly in B", {"16-channel polyphonic input from UniRhythm Deck B.", "來自 UniRhythm Deck B 的 16 聲道 Polyphonic 輸入。", "UniRhythmデッキBからの16チャンネルポリフォニック入力。"}});
        m.entries.push_back({"Cross CV", {"Crossfader position CV modulation (±10V, coefficient 0.1).", "Crossfader 位置 CV 調變（±10V，係數 0.1）。", "クロスフェーダー位置CV変調（±10V、係数0.1）。"}});
        m.entries.push_back({"Master L/R", {"Stereo master output with crossfade, isolator, drive, and soft clip applied.", "Stereo Master 輸出，含 Crossfade、Isolator、Drive 與 Soft Clip。", "ステレオマスター出力（クロスフェード、アイソレーター、ドライブ、ソフトクリップ適用済み）。"}});
        m.entries.push_back({"CUE L/R", {"Stereo cue monitoring output (raw selected deck audio).", "Stereo Cue 監聽輸出（原始所選 Deck Audio）。", "ステレオキューモニタリング出力（選択デッキの生オーディオ）。"}});
        m.entries.push_back({"Per-Role Gate/Pitch/VelEnv", {"4 roles x 3 CV types = 12 crossfaded CV outputs.", "4 roles x 3 CV types = 12 crossfaded CV outputs.", "4 roles x 3 CV types = 12 crossfaded CV outputs."}});
        m.entries.push_back({"Crossfader", {"DJ-style crossfader between Deck A and Deck B. 0%=full A, 100%=full B. CV modulated via Crossfader CV input.", "DJ 風格交叉混合器，Deck A 與 Deck B 之間。0%=完全 A，100%=完全 B。可透過 Crossfader CV 輸入進行 CV 調變。", "DJスタイルのクロスフェーダー。Deck AとDeck Bの間を制御。0%=完全A、100%=完全B。Crossfader CV入力でCV変調可能。"}});
        m.entries.push_back({"Cue A/B", {"Cue routing switch. Selects which deck (A or B) is sent to the Cue output for monitoring.", "Cue 路由切換。選擇將哪個 Deck（A 或 B）送至 Cue 輸出以供監聽。", "キュールーティングスイッチ。モニタリング用にどのデッキ（AまたはB）をキュー出力に送るかを選択。"}});
        m.entries.push_back({"Deck A (16ch poly)", {"16-channel polyphonic input for Deck A. Channels are decoded into individual track signals.", "Deck A 的 16 聲道 Polyphonic 輸入。各聲道解碼為獨立的軌道訊號。", "Deck Aの16チャンネルポリフォニック入力。チャンネルは個別のトラック信号にデコードされます。"}});
        m.entries.push_back({"Deck B (16ch poly)", {"16-channel polyphonic input for Deck B. Channels are decoded into individual track signals.", "Deck B 的 16 聲道 Polyphonic 輸入。各聲道解碼為獨立的軌道訊號。", "Deck Bの16チャンネルポリフォニック入力。チャンネルは個別のトラック信号にデコードされます。"}});
        m.entries.push_back({"Crossfader CV", {"CV input for crossfader position modulation (±10V, coefficient 0.1, adds offset to crossfader position).", "Crossfader 位置調變的 CV 輸入（±10V，係數 0.1，加至 Crossfader 位置偏移）。", "クロスフェーダー位置変調用CV入力（±10V、係数0.1、クロスフェーダー位置にオフセットを加算）。"}});
        m.entries.push_back({"Master L", {"Master left channel output. Main stereo mix after crossfader, isolator, drive, and soft clip.", "Master 左聲道輸出。經過 Crossfader、Isolator、Drive 與 Soft Clip 後的主立體聲混音。", "マスター左チャンネル出力。クロスフェーダー、アイソレーター、ドライブ、ソフトクリップ後のメインステレオミックス。"}});
        m.entries.push_back({"Master R", {"Master right channel output. Main stereo mix after crossfader, isolator, drive, and soft clip.", "Master 右聲道輸出。經過 Crossfader、Isolator、Drive 與 Soft Clip 後的主立體聲混音。", "マスター右チャンネル出力。クロスフェーダー、アイソレーター、ドライブ、ソフトクリップ後のメインステレオミックス。"}});
        m.entries.push_back({"Cue L", {"Cue left channel output for headphone monitoring.", "Cue 左聲道輸出，用於耳機監聽。", "ヘッドフォンモニタリング用キュー左チャンネル出力。"}});
        m.entries.push_back({"Cue R", {"Cue right channel output for headphone monitoring.", "Cue 右聲道輸出，用於耳機監聽。", "ヘッドフォンモニタリング用キュー右チャンネル出力。"}});
        data["Portal"] = std::move(m);
    }
    // Pyramid (41 entries)
    {
        ModuleHelpData m;
        m.name = "Pyramid";
        m.description = {"3D spatial panner with X/Y/Z position control, 8 speaker outputs, and full CV modulation for all axes", "3D 空間 Panner，具備 X/Y/Z 位置控制、8 聲道 Speaker Output 與所有軸向的 CV 調變", "X/Y/Z位置制御、8スピーカー出力、全軸CV変調を備えた3D空間パンナー"};
        m.entries.push_back({"Speaker Positions", {"8 corners at coordinates (±1.0, ±1.0, ±1.0)", "8 corners at coordinates (±1.0, ±1.0, ±1.0)", "8 corners at coordinates (±1.0, ±1.0, ±1.0)"}});
        m.entries.push_back({"Distance Calculation", {"Speaker gain is based on distance from source to each speaker position in 3D space", "Speaker gain is based on distance from source to each speaker position in 3D space", "Speaker gain is based on distance from source to each speaker position in 3D space"}});
        m.entries.push_back({"Gain Response", {"Closer speakers receive more signal; gain decreases non-linearly with distance for natural spatial perception", "Closer speakers receive more signal; gain decreases non-linearly with distance for natural spatial perception", "Closer speakers receive more signal; gain decreases non-linearly with distance for natural spatial perception"}});
        m.entries.push_back({"Edge Fade-Out", {"When source position exceeds ±0.8 on any axis, opposite speakers are smoothly faded to prevent artifacts", "When source position exceeds ±0.8 on any axis, opposite speakers are smoothly faded to prevent artifacts", "When source position exceeds ±0.8 on any axis, opposite speakers are smoothly faded to prevent artifacts"}});
        m.entries.push_back({"Normalization", {"Total output power is kept constant regardless of source position", "Total output power is kept constant regardless of source position", "Total output power is kept constant regardless of source position"}});
        m.entries.push_back({"X Position", {"Left-right positioning (-1.0 to +1.0, default: 0). CV adds ±10V × 0.2 = ±2.0 range, clamped to ±1.0.", "左右定位（-1.0 至 +1.0，預設: 0）。CV 增加 ±10V × 0.2 = ±2.0 範圍，限制在 ±1.0。", "左右定位（-1.0〜+1.0、デフォルト: 0）。CVは ±10V × 0.2 = ±2.0 の範囲を追加、±1.0 でクランプ。"}});
        m.entries.push_back({"Y Position", {"Front-back positioning (-1.0 to +1.0, default: 0). Front speakers at Y=-1, back at Y=+1.", "前後定位（-1.0 至 +1.0，預設: 0）。前方 Speaker 位於 Y=-1，後方位於 Y=+1。", "前後定位（-1.0〜+1.0、デフォルト: 0）。前方スピーカーは Y=-1、後方は Y=+1。"}});
        m.entries.push_back({"Z Position", {"Up-down positioning (-1.0 to +1.0, default: 0). Lower speakers at Z=-1, upper at Z=+1.", "上下定位（-1.0 至 +1.0，預設: 0）。下層 Speaker 位於 Z=-1，上層位於 Z=+1。", "上下定位（-1.0〜+1.0、デフォルト: 0）。下層スピーカーは Z=-1、上層は Z=+1。"}});
        m.entries.push_back({"Topology", {"2-stage cascaded biquad (24dB/octave slope)", "2-stage cascaded biquad (24dB/octave slope)", "2-stage cascaded biquad (24dB/octave slope)"}});
        m.entries.push_back({"Q Factor", {"Fixed at 0.707 (Butterworth response)", "Fixed at 0.707 (Butterworth response)", "Fixed at 0.707 (Butterworth response)"}});
        m.entries.push_back({"Smoothing", {"Filter parameter smoothed with coefficient 0.002 to prevent zipper noise", "Filter parameter smoothed with coefficient 0.002 to prevent zipper noise", "Filter parameter smoothed with coefficient 0.002 to prevent zipper noise"}});
        m.entries.push_back({"Negative Values (Lowpass)", {"Frequency from 20Hz (filter=-1.0) to 22kHz (filter=0)", "Frequency from 20Hz (filter=-1.0) to 22kHz (filter=0)", "Frequency from 20Hz (filter=-1.0) to 22kHz (filter=0)"}});
        m.entries.push_back({"Positive Values (Highpass)", {"Frequency from 10Hz (filter=0) to 8kHz (filter=+1.0)", "Frequency from 10Hz (filter=0) to 8kHz (filter=+1.0)", "Frequency from 10Hz (filter=0) to 8kHz (filter=+1.0)"}});
        m.entries.push_back({"Deadband", {"Filter bypassed when |filter| < 0.001", "Filter bypassed when |filter| < 0.001", "Filter bypassed when |filter| < 0.001"}});
        m.entries.push_back({"Level", {"Channel volume (0.0 to 1.0, default: 0.7). Applied before filtering.", "通道音量（0.0 至 1.0，預設: 0.7）。在 Filter 前套用。", "チャンネル音量（0.0〜1.0、デフォルト: 0.7）。フィルタリング前に適用。"}});
        m.entries.push_back({"Send", {"Effect send level (0.0 to 1.0, default: 0). Output is post-level unless Send Pre-Level is enabled.", "效果器 Send Output / Send Level（0-100%，預設: 0%）", "エフェクトセンド出力 / センドレベル（0-100%、デフォルト: 0%）"}});
        m.entries.push_back({"Projection", {"30° isometric view of the speaker cube", "30° isometric view of the speaker cube", "30° isometric view of the speaker cube"}});
        m.entries.push_back({"Speaker Indicators", {"Numbered circles (1-8) at cube corners scaled to 60% of actual positions", "Numbered circles (1-8) at cube corners scaled to 60% of actual positions", "Numbered circles (1-8) at cube corners scaled to 60% of actual positions"}});
        m.entries.push_back({"Source Indicator", {"Yellow filled circle at current XYZ position", "Yellow filled circle at current XYZ position", "Yellow filled circle at current XYZ position"}});
        m.entries.push_back({"Cube Edges", {"12 edge lines connecting all speaker positions", "12 edge lines connecting all speaker positions", "12 edge lines connecting all speaker positions"}});
        m.entries.push_back({"Audio", {"Mono audio input (±10V)", "Mono Audio 輸入（±10V）", "モノオーディオ入力（±10V）"}});
        m.entries.push_back({"X CV", {"X position modulation (±10V × 0.2 = ±2.0 offset, clamped to ±1.0)", "X 位置調變（±10V × 0.2 = ±2.0 偏移，限制在 ±1.0）", "X位置変調（±10V × 0.2 = ±2.0 オフセット、±1.0でクランプ）"}});
        m.entries.push_back({"Y CV", {"Y position modulation (±10V × 0.2 = ±2.0 offset, clamped to ±1.0)", "Y 位置調變（±10V × 0.2 = ±2.0 偏移，限制在 ±1.0）", "Y位置変調（±10V × 0.2 = ±2.0 オフセット、±1.0でクランプ）"}});
        m.entries.push_back({"Z CV", {"Z position modulation (±10V × 0.2 = ±2.0 offset, clamped to ±1.0)", "Z 位置調變（±10V × 0.2 = ±2.0 偏移，限制在 ±1.0）", "Z位置変調（±10V × 0.2 = ±2.0 オフセット、±1.0でクランプ）"}});
        m.entries.push_back({"Filter CV", {"Filter modulation (±10V × 0.2 = ±2.0 offset, clamped to ±1.0)", "Filter 調變（±10V × 0.2 = ±2.0 偏移，限制在 ±1.0）", "フィルター変調（±10V × 0.2 = ±2.0 オフセット、±1.0でクランプ）"}});
        m.entries.push_back({"Filter", {"Bipolar filter control (-1.0 to +1.0, default: 0). Negative = lowpass, positive = highpass.", "Bipolar Filter 控制（-1.0 至 +1.0，預設: 0）。負值 = Lowpass，正值 = Highpass。", "バイポーラフィルター制御（-1.0〜+1.0、デフォルト: 0）。負=ローパス、正=ハイパス。"}});
        m.entries.push_back({"Send", {"Effect send output / send level (0-100%, default: 0%)", "效果器 Send Output / Send Level（0-100%，預設: 0%）", "エフェクトセンド出力 / センドレベル（0-100%、デフォルト: 0%）"}});
        m.entries.push_back({"Return L", {"Left return input, mixed into even outputs weighted by VBAP gains", "Left Return 輸入，依 VBAP Gain 權重混入偶數 Output", "左リターン入力、VBAPゲインで重み付けされて偶数出力にミックス"}});
        m.entries.push_back({"Return R", {"Right return input, mixed into odd outputs weighted by VBAP gains", "Right Return 輸入，依 VBAP Gain 權重混入奇數 Output", "右リターン入力、VBAPゲインで重み付けされて奇数出力にミックス"}});
        m.entries.push_back({"1", {"Output 1 - FL Upper speaker", "Output 1 - FL Upper Speaker", "出力1 - FL Upperスピーカー"}});
        m.entries.push_back({"2", {"Output 2 - FR Upper speaker", "Output 2 - FR Upper Speaker", "出力2 - FR Upperスピーカー"}});
        m.entries.push_back({"3", {"Output 3 - BL Upper speaker", "Output 3 - BL Upper Speaker", "出力3 - BL Upperスピーカー"}});
        m.entries.push_back({"4", {"Output 4 - BR Upper speaker", "Output 4 - BR Upper Speaker", "出力4 - BR Upperスピーカー"}});
        m.entries.push_back({"5", {"Output 5 - FL Lower speaker", "Output 5 - FL Lower Speaker", "出力5 - FL Lowerスピーカー"}});
        m.entries.push_back({"6", {"Output 6 - FR Lower speaker", "Output 6 - FR Lower Speaker", "出力6 - FR Lowerスピーカー"}});
        m.entries.push_back({"7", {"Output 7 - BL Lower speaker", "Output 7 - BL Lower Speaker", "出力7 - BL Lowerスピーカー"}});
        m.entries.push_back({"8", {"Output 8 - BR Lower speaker", "Output 8 - BR Lower Speaker", "出力8 - BR Lowerスピーカー"}});
        m.entries.push_back({"DECAPyramid", {"8-channel 3D mixer (same VBAP algorithm, multiple sources)", "8 聲道 3D Mixer（相同 VBAP 演算法，多音源）", "8チャンネル3Dミキサー（同じVBAPアルゴリズム、複数ソース）"}});
        m.entries.push_back({"KEN", {"8-to-2 binaural processor (converts 8-channel output to headphones)", "8 至 2 Binaural Processor（將 8 聲道 Output 轉換為耳機輸出）", "8→2バイノーラルプロセッサー（8チャンネル出力をヘッドフォン用に変換）"}});
        m.entries.push_back({"Panel Theme", {"Light / Dark panel appearance", "Light / Dark 面板外觀", "Light / Dark パネル外観"}});
        m.entries.push_back({"Send Pre-Level", {"When enabled, send output taps signal before level fader. Default: OFF (post-fader).", "啟用時，Send Output 在 Level Fader 前取訊號。預設: OFF（Post-Fader）。", "有効時、センド出力はレベルフェーダー前から信号をタップ。デフォルト: OFF（ポストフェーダー）。"}});
        data["Pyramid"] = std::move(m);
    }
    // QQ (43 entries)
    {
        ModuleHelpData m;
        m.name = "Q_Q";
        m.description = {"Triple decay envelope generator with adjustable S-curve shaping and real-time 3-track oscilloscope display", "三軌 Decay Envelope 生成器，具備可調 S-curve Shaping 與即時 3 軌 Oscilloscope 顯示", "調整可能なS-curveシェイピングとリアルタイム3トラックオシロスコープ表示を備えたトリプルディケイエンベロープ生成器"};
        m.entries.push_back({"Implementation", {"Uses smoothDecayEnvelope() with dual-k interpolation for natural envelope shapes", "Uses smoothDecayEnvelope() with dual-k interpolation for natural envelope shapes", "Uses smoothDecayEnvelope() with dual-k interpolation for natural envelope shapes"}});
        m.entries.push_back({"Front K", {"Controls initial curve steepness, ranging -0.9 to -0.4", "控制初始曲線陡峭度，範圍 -0.9 至 -0.4", "初期カーブ急峻さを制御、範囲 -0.9〜-0.4"}});
        m.entries.push_back({"Back K", {"Controls tail curve, ranging -1.0 to 0.6", "控制尾端曲線，範圍 -1.0 至 0.6", "テールカーブを制御、範囲 -1.0〜0.6"}});
        m.entries.push_back({"K Crossfade", {"Linear interpolation from frontK to backK as envelope progresses", "隨著 Envelope 進展從 frontK 至 backK 線性內插", "Linear interpolation from frontK to backK as envelope progresses"}});
        m.entries.push_back({"Shape Behavior", {"Weighted interpolation between target and current, with k varying through decay", "Weighted interpolation between target and current, with k varying through decay", "Weighted interpolation between target and current, with k varying through decay"}});
        m.entries.push_back({"At Shape 0", {"Linear decay (frontK ≈ -0.9, backK ≈ -1.0)", "Linear decay (frontK ≈ -0.9, backK ≈ -1.0)", "Linear decay (frontK ≈ -0.9, backK ≈ -1.0)"}});
        m.entries.push_back({"At Shape 1", {"Exponential decay with long tail (frontK ≈ -0.4, backK ≈ 0.6)", "Exponential decay with long tail (frontK ≈ -0.4, backK ≈ 0.6)", "Exponential decay with long tail (frontK ≈ -0.4, backK ≈ 0.6)"}});
        m.entries.push_back({"Track 3 Trigger", {"Rising edge detection with Schmitt trigger (low: 0.1V, high: 2.0V).", "Track 3 Envelope 的 Trigger 輸入。", "トラック3エンベロープのトリガー入力。"}});
        m.entries.push_back({"Track 3 Decay Time", {"Decay time 0.01-2.0 seconds (default: 1.0s, unit: s)", "Decay 時間 0.01-2.0 秒（預設：1.0s，單位：s）", "ディケイ時間0.01-2.0秒（デフォルト：1.0s、単位：s）"}});
        m.entries.push_back({"Track 3 Shape", {"Envelope curve shape 0 to 0.99 (default: 0.5). Lower values create faster initial decay, higher values create longer tails with slower initial decay.", "Track 3 的 Envelope 形狀（0-0.99）。控制 Decay 階段的曲線。0=線性，越高=越指數。", "トラック3のエンベロープシェイプ（0-0.99）。ディケイフェーズのカーブを制御。0=リニア、高値=よりエクスポネンシャル。"}});
        m.entries.push_back({"Track 3 Decay CV", {"Decay time CV modulation input (±10V), additive and scaled by attenuator", "Track 3 Decay 時間調變的 CV 輸入。由 Track 3 Decay CV Amount 縮放。", "トラック3ディケイ時間変調用CV入力。Track 3 Decay CV Amountでスケーリング。"}});
        m.entries.push_back({"Track 3 Decay CV Amount", {"Hidden small knob (0-100%, default: 50%) controlling CV depth", "Track 3 Decay 時間調變的 CV 衰減量（0-100%）。預設 50%。", "トラック3ディケイ時間変調用CVアッテネーター（0-100%）。デフォルト50%。"}});
        m.entries.push_back({"Attack Time", {"Configurable 1.0-20ms per track via right-click menu", "Configurable 1.0-20ms per track via right-click menu", "Configurable 1.0-20ms per track via right-click menu"}});
        m.entries.push_back({"Default", {"1.0ms (nearly instantaneous)", "1.0ms (nearly instantaneous)", "1.0ms (nearly instantaneous)"}});
        m.entries.push_back({"Algorithm", {"Linear ramp from current value (or 0 if not retriggering) to 10V", "Linear ramp from current value (or 0 if not retriggering) to 10V", "Linear ramp from current value (or 0 if not retriggering) to 10V"}});
        m.entries.push_back({"Menu Options", {"1.0ms, 1ms, 2ms, 5ms, 10ms, 20ms", "1.0ms, 1ms, 2ms, 5ms, 10ms, 20ms", "1.0ms, 1ms, 2ms, 5ms, 10ms, 20ms"}});
        m.entries.push_back({"Off (Default)", {"New trigger resets envelope to 0V, then attack phase begins", "New trigger resets envelope to 0V, then attack phase begins", "New trigger resets envelope to 0V, then attack phase begins"}});
        m.entries.push_back({"On", {"New trigger starts attack from current envelope value, preventing amplitude discontinuities", "新 Trigger 從當前 Envelope 值開始 Attack，防止振幅不連續", "新トリガーは現在のエンベロープ値からアタック開始、振幅不連続を防止"}});
        m.entries.push_back({"Use Case", {"Retrigger mode creates smoother overlapping envelopes for fast trigger sequences", "Retrigger mode creates smoother overlapping envelopes for fast trigger sequences", "Retrigger mode creates smoother overlapping envelopes for fast trigger sequences"}});
        m.entries.push_back({"Buffer Size", {"128 samples per track (BUFFER_SIZE constant)", "每軌 128 Samples（BUFFER_SIZE 常數）", "128 samples per track (BUFFER_SIZE constant)"}});
        m.entries.push_back({"Color Matching", {"Each waveform adopts the color of its connected input cable via PortWidget API", "Each waveform adopts the color of its connected input cable via PortWidget API", "Each waveform adopts the color of its connected input cable via PortWidget API"}});
        m.entries.push_back({"Time", {"Adjustable via hidden time parameter (click and drag on display)", "可透過隱藏時間參數調整（在顯示上點擊並拖曳）", "隠し時間パラメータで調整可能（ディスプレイをクリック＆ドラッグ）"}});
        m.entries.push_back({"Y-axis", {"0-10V mapped to display height", "0-10V mapped to display height", "0-10V mapped to display height"}});
        m.entries.push_back({"Rendering", {"Anti-aliased line drawing at display refresh rate", "Anti-aliased line drawing at display refresh rate", "Anti-aliased line drawing at display refresh rate"}});
        m.entries.push_back({"Track 3 Envelope", {"Individual track envelope outputs (0-10V unipolar)", "Track 3 的 Envelope 輸出（0-10V）。", "トラック3のエンベロープ出力（0-10V）。"}});
        m.entries.push_back({"Polyphonic", {"Each output is monophonic (1 channel)", "每個輸出為單音（1 Channel）", "Each output is monophonic (1 channel)"}});
        m.entries.push_back({"Track 1/2/3 Attack Time", {"Select attack duration per track (1.0ms, 1ms, 2ms, 5ms, 10ms, 20ms)", "選擇各軌 Attack 持續時間（1.0ms、1ms、2ms、5ms、10ms、20ms）", "トラック別アタック期間選択（1.0ms、1ms、2ms、5ms、10ms、20ms）"}});
        m.entries.push_back({"Retrigger", {"Enable/disable retriggering from current value (global setting)", "啟用/停用從當前值重觸發（全域設定）", "現在値からのリトリガーを有効/無効化（グローバル設定）"}});
        m.entries.push_back({"Track 1 Shape", {"Envelope shape for Track 1 (0-0.99). Controls the curve of the decay phase. 0=linear, higher=more exponential.", "Track 1 的 Envelope 形狀（0-0.99）。控制 Decay 階段的曲線。0=線性，越高=越指數。", "トラック1のエンベロープシェイプ（0-0.99）。ディケイフェーズのカーブを制御。0=リニア、高値=よりエクスポネンシャル。"}});
        m.entries.push_back({"Track 2 Shape", {"Envelope shape for Track 2 (0-0.99). Controls the curve of the decay phase. 0=linear, higher=more exponential.", "Track 2 的 Envelope 形狀（0-0.99）。控制 Decay 階段的曲線。0=線性，越高=越指數。", "トラック2のエンベロープシェイプ（0-0.99）。ディケイフェーズのカーブを制御。0=リニア、高値=よりエクスポネンシャル。"}});
        m.entries.push_back({"Track 1 Decay CV Amount", {"CV attenuator for Track 1 decay time modulation (0-100%). Default 50%.", "Track 1 Decay 時間調變的 CV 衰減量（0-100%）。預設 50%。", "トラック1ディケイ時間変調用CVアッテネーター（0-100%）。デフォルト50%。"}});
        m.entries.push_back({"Track 2 Decay CV Amount", {"CV attenuator for Track 2 decay time modulation (0-100%). Default 50%.", "Track 2 Decay 時間調變的 CV 衰減量（0-100%）。預設 50%。", "トラック2ディケイ時間変調用CVアッテネーター（0-100%）。デフォルト50%。"}});
        m.entries.push_back({"Track 1 Trigger", {"Trigger input for Track 1 envelope.", "Track 1 Envelope 的 Trigger 輸入。", "トラック1エンベロープのトリガー入力。"}});
        m.entries.push_back({"Track 2 Trigger", {"Trigger input for Track 2 envelope.", "Track 2 Envelope 的 Trigger 輸入。", "トラック2エンベロープのトリガー入力。"}});
        m.entries.push_back({"Track 1 Decay CV", {"CV input for Track 1 decay time modulation. Scaled by Track 1 Decay CV Amount.", "Track 1 Decay 時間調變的 CV 輸入。由 Track 1 Decay CV Amount 縮放。", "トラック1ディケイ時間変調用CV入力。Track 1 Decay CV Amountでスケーリング。"}});
        m.entries.push_back({"Track 2 Decay CV", {"CV input for Track 2 decay time modulation. Scaled by Track 2 Decay CV Amount.", "Track 2 Decay 時間調變的 CV 輸入。由 Track 2 Decay CV Amount 縮放。", "トラック2ディケイ時間変調用CV入力。Track 2 Decay CV Amountでスケーリング。"}});
        m.entries.push_back({"Track 1 Envelope", {"Envelope output for Track 1 (0-10V).", "Track 1 的 Envelope 輸出（0-10V）。", "トラック1のエンベロープ出力（0-10V）。"}});
        m.entries.push_back({"Track 2 Envelope", {"Envelope output for Track 2 (0-10V).", "Track 2 的 Envelope 輸出（0-10V）。", "トラック2のエンベロープ出力（0-10V）。"}});
        m.entries.push_back({"Track 3 Shape", {"Envelope shape for Track 3 (0-0.99). Controls the curve of the decay phase. 0=linear, higher=more exponential.", "Track 3 的 Envelope 形狀（0-0.99）。控制 Decay 階段的曲線。0=線性，越高=越指數。", "トラック3のエンベロープシェイプ（0-0.99）。ディケイフェーズのカーブを制御。0=リニア、高値=よりエクスポネンシャル。"}});
        m.entries.push_back({"Track 3 Decay CV Amount", {"CV attenuator for Track 3 decay time modulation (0-100%). Default 50%.", "Track 3 Decay 時間調變的 CV 衰減量（0-100%）。預設 50%。", "トラック3ディケイ時間変調用CVアッテネーター（0-100%）。デフォルト50%。"}});
        m.entries.push_back({"Track 3 Trigger", {"Trigger input for Track 3 envelope.", "Track 3 Envelope 的 Trigger 輸入。", "トラック3エンベロープのトリガー入力。"}});
        m.entries.push_back({"Track 3 Decay CV", {"CV input for Track 3 decay time modulation. Scaled by Track 3 Decay CV Amount.", "Track 3 Decay 時間調變的 CV 輸入。由 Track 3 Decay CV Amount 縮放。", "トラック3ディケイ時間変調用CV入力。Track 3 Decay CV Amountでスケーリング。"}});
        m.entries.push_back({"Track 3 Envelope", {"Envelope output for Track 3 (0-10V).", "Track 3 的 Envelope 輸出（0-10V）。", "トラック3のエンベロープ出力（0-10V）。"}});
        data["QQ"] = std::move(m);
    }
    // Quantizer (36 entries)
    {
        ModuleHelpData m;
        m.name = "Quanti2er";
        m.description = {"24-note (2-octave) chromatic quantizer with per-note microtune control, 28 world tuning presets, and 3 directional presets for ascending/descending scale differences", "24 音（2 八度）Chromatic Quantizer，具備各音符 Microtune 控制、28 種世界調音預設，以及 3 種方向性預設（上行/下行音階差異）", "24音（2オクターブ）クロマチッククオンタイザー。ノート別マイクロチューン制御、28種の世界調律プリセット、3種の方向性プリセット（上行/下行差異）を搭載"};
        m.entries.push_back({"24 Notes", {"Two complete octaves for independent upper/lower octave tuning", "Two complete octaves for independent upper/lower octave tuning", "Two complete octaves for independent upper/lower octave tuning"}});
        m.entries.push_back({"24 Microtune Sliders", {"Per-note tuning (±50 cents) for both octaves", "Per-note tuning (±50 cents) for both octaves", "Per-note tuning (±50 cents) for both octaves"}});
        m.entries.push_back({"2-Oct Presets", {"Marked with (2-Oct) - Arabic Maqam, Turkish Makam, Persian Dastgah, Indian Raga, Japanese scales", "Marked with (2-Oct) - Arabic Maqam, Turkish Makam, Persian Dastgah, Indian Raga, Japanese scales", "Marked with (2-Oct) - Arabic Maqam, Turkish Makam, Persian Dastgah, Indian Raga, Japanese scales"}});
        m.entries.push_back({"Single-Octave Presets", {"Automatically mirror to both octaves", "Automatically mirror to both octaves", "Automatically mirror to both octaves"}});
        m.entries.push_back({"Algorithm", {"Compares current note with previous note", "Compares current note with previous note", "Compares current note with previous note"}});
        m.entries.push_back({"Threshold", {"±0.5 semitones to avoid quantization jitter", "±0.5 semitones to avoid quantization jitter", "±0.5 semitones to avoid quantization jitter"}});
        m.entries.push_back({"Directional Presets (↑↓)", {"Turkish Rast, Arabic Hijaz, Miyako-bushi", "Turkish Rast, Arabic Hijaz, Miyako-bushi", "Turkish Rast, Arabic Hijaz, Miyako-bushi"}});
        m.entries.push_back({"Usage", {"Ascending melodies use upper octave tuning, descending use lower octave", "Ascending melodies use upper octave tuning, descending use lower octave", "Ascending melodies use upper octave tuning, descending use lower octave"}});
        m.entries.push_back({"Scale", {"Pitch multiplier (0-2, default: 1). Values 1 expand", "Pitch 倍數（0-2，預設: 1）。值 1 擴展", "ピッチ乗数（0-2、デフォルト: 1）。値1で拡張"}});
        m.entries.push_back({"Pre-offset", {"Pre-quantization pitch offset (±12 semitones displayed, raw ±1V)", "Quantize 前 Pitch Offset（顯示 ±12 半音，原始 ±1V）", "プリクオンタイズピッチオフセット（表示±12半音、生値±1V）"}});
        m.entries.push_back({"Microtune", {"Per-note tuning sliders (±50 cents) for all 24 notes across 2 octaves", "各音符調音滑桿（±50 Cent），涵蓋 2 八度共 24 音", "ノート別チューニングスライダー（±50セント）、2オクターブ全24音"}});
        m.entries.push_back({"Western (4)", {"Equal Temperament, Just Intonation, Pythagorean, Quarter-tone", "Equal Temperament, Just Intonation, Pythagorean, Quarter-tone", "Equal Temperament, Just Intonation, Pythagorean, Quarter-tone"}});
        m.entries.push_back({"Arabic Maqam (6)", {"Rast, Bayati, Hijaz, Saba, Nahawand, Kurd - all 2-Oct", "Rast, Bayati, Hijaz, Saba, Nahawand, Kurd - all 2-Oct", "Rast, Bayati, Hijaz, Saba, Nahawand, Kurd - all 2-Oct"}});
        m.entries.push_back({"Turkish Makam (4)", {"Rast, Ussak, Hicaz, Segah - all 2-Oct", "Rast, Ussak, Hicaz, Segah - all 2-Oct", "Rast, Ussak, Hicaz, Segah - all 2-Oct"}});
        m.entries.push_back({"Persian (2)", {"Dastgah Shur, Dastgah Segah - all 2-Oct", "Dastgah Shur, Dastgah Segah - all 2-Oct", "Dastgah Shur, Dastgah Segah - all 2-Oct"}});
        m.entries.push_back({"Indian (4)", {"Shruti, Raga Bhairav, Yaman, Bhairavi - all 2-Oct", "Shruti, Raga Bhairav, Yaman, Bhairavi - all 2-Oct", "Shruti, Raga Bhairav, Yaman, Bhairavi - all 2-Oct"}});
        m.entries.push_back({"Japanese (4)", {"Gagaku, In Scale, Yo Scale, Ryukyu - all 2-Oct", "Gagaku, In Scale, Yo Scale, Ryukyu - all 2-Oct", "Gagaku, In Scale, Yo Scale, Ryukyu - all 2-Oct"}});
        m.entries.push_back({"Southeast Asian (3)", {"Slendro, Pelog, Thai 7-TET", "Slendro, Pelog, Thai 7-TET", "Slendro, Pelog, Thai 7-TET"}});
        m.entries.push_back({"Chinese (1)", {"Pentatonic", "Pentatonic", "Pentatonic"}});
        m.entries.push_back({"Turkish Rast (↑↓)", {"F# ascending, F natural descending", "上行用 F#，下行用 F", "上行でF#、下行でFナチュラル"}});
        m.entries.push_back({"Arabic Hijaz (↑↓)", {"Different intervals for melodic direction", "旋律方向有不同音程", "メロディ方向で異なる音程"}});
        m.entries.push_back({"Miyako-bushi (↑↓)", {"6th degree changes with direction (D↑, C↓)", "6th degree changes with direction (D↑, C↓)", "6th degree changes with direction (D↑, C↓)"}});
        m.entries.push_back({"Chromatic", {"All 24 notes enabled", "全部 24 音啟用", "全24音有効"}});
        m.entries.push_back({"Major/Minor", {"Ionian, Aeolian modes", "Ionian、Aeolian 調式", "イオニアン、エオリアンモード"}});
        m.entries.push_back({"Pentatonic", {"Major, Minor (5 notes)", "大調、小調（5 音）", "メジャー、マイナー（5音）"}});
        m.entries.push_back({"Modes", {"Dorian, Phrygian, Lydian, Mixolydian, Locrian", "Dorian、Phrygian、Lydian、Mixolydian、Locrian", "ドリアン、フリジアン、リディアン、ミクソリディアン、ロクリアン"}});
        m.entries.push_back({"Triads", {"Major (C-E-G), Minor (C-Eb-G)", "Major (C-E-G)、Minor (C-Eb-G)", "Major (C-E-G)、Minor (C-Eb-G)"}});
        m.entries.push_back({"World", {"Blues, Arabic, Japanese, Whole Tone", "Blues、Arabic、Japanese、Whole Tone", "ブルース、アラビック、ジャパニーズ、ホールトーン"}});
        m.entries.push_back({"CV1", {"Pitch CV input 1 (polyphonic, 16 ch)", "Pitch CV 輸入 1（Polyphonic，16 通道）", "ピッチCV入力1（ポリフォニック、16ch）"}});
        m.entries.push_back({"CV2", {"Pitch CV input 2 (polyphonic, 16 ch)", "Pitch CV 輸入 2（Polyphonic，16 通道）", "ピッチCV入力2（ポリフォニック、16ch）"}});
        m.entries.push_back({"CV3", {"Pitch CV input 3 (polyphonic, 16 ch)", "Pitch CV 輸入 3（Polyphonic，16 通道）", "ピッチCV入力3（ポリフォニック、16ch）"}});
        m.entries.push_back({"Pitch", {"Quantized output 1", "Quantized 輸出 1", "クオンタイズ出力1"}});
        m.entries.push_back({"Pitch 2", {"Quantized output 2", "Quantized 輸出 2", "クオンタイズ出力2"}});
        m.entries.push_back({"Pitch 3", {"Quantized output 3", "Quantized 輸出 3", "クオンタイズ出力3"}});
        m.entries.push_back({"Scale CV", {"Modulates pitch multiplier", "調變 Pitch 倍數", "ピッチ乗数を変調"}});
        m.entries.push_back({"Offset CV", {"Modulates pre-quantization offset", "調變 Quantize 前 Offset", "プリクオンタイズオフセットを変調"}});
        data["Quantizer"] = std::move(m);
    }
    // Runner (27 entries)
    {
        ModuleHelpData m;
        m.name = "Runner";
        m.description = {"Chaos-modulated stereo delay with Lorenz attractor modulation and sample-and-hold output", "Chaos 調變 Stereo Delay，具備 Lorenz Attractor 調變與 Sample-and-Hold 輸出", "ローレンツアトラクター変調とサンプル&ホールド出力を備えたカオス変調ステレオディレイ"};
        m.entries.push_back({"Time L", {"Left channel delay time (1ms-2s, default: 250ms). CV adds ±200ms.", "左聲道 Delay 時間（1ms-2s，預設：250ms）。CV 增加 ±200ms。", "左チャンネルディレイタイム（1ms-2s、デフォルト：250ms）。CVで±200ms追加。"}});
        m.entries.push_back({"Time R", {"Right channel delay time (1ms-2s, default: 250ms). CV adds ±200ms.", "右聲道 Delay 時間（1ms-2s，預設：250ms）。CV 增加 ±200ms。", "右チャンネルディレイタイム（1ms-2s、デフォルト：250ms）。CVで±200ms追加。"}});
        m.entries.push_back({"Feedback", {"Feedback amount (0-95%, default: 30%). CV adds ±10%.", "Feedback 量（0-95%，預設：30%）。CV 增加 ±10%。", "フィードバック量（0-95%、デフォルト：30%）。CVで±10%追加。"}});
        m.entries.push_back({"Mix", {"Wet/dry balance (0-100%, default: 50%). CV adds ±10%.", "Wet/Dry 平衡（0-100%，預設：50%）。CV 增加 ±10%。", "ウェット/ドライバランス（0-100%、デフォルト：50%）。CVで±10%追加。"}});
        m.entries.push_back({"Algorithm", {"Lorenz strange attractor differential equations", "Lorenz strange attractor differential equations", "Lorenz strange attractor differential equations"}});
        m.entries.push_back({"Chaos", {"Modulation depth (0-100%, default: 0%). Controls how much chaos affects delay time and feedback.", "調變深度（0-100%，預設：0%）。控制 Chaos 對 Delay 時間和 Feedback 的影響程度。", "変調深度（0-100%、デフォルト：0%）。カオスがディレイタイムとフィードバックに与える影響を制御。"}});
        m.entries.push_back({"Rate", {"Chaos evolution speed (0.01-2.0x, default: 0.5x). Higher = faster parameter changes.", "Chaos 演化速度（0.01-2.0x，預設：0.5x）。越高 = 參數變化越快。", "カオス進化速度（0.01-2.0x、デフォルト：0.5x）。高いほどパラメータ変化が速い。"}});
        m.entries.push_back({"Effect on Delay", {"Time ±100ms, Feedback ±10%", "Time ±100ms, Feedback ±10%", "Time ±100ms, Feedback ±10%"}});
        m.entries.push_back({"S&H Output", {"Stepped chaos signal sampled at RATE × 10 Hz", "Stepped chaos signal sampled at RATE × 10 Hz", "Stepped chaos signal sampled at RATE × 10 Hz"}});
        m.entries.push_back({"Range", {"0-5V CV output", "0-5V CV output", "0-5V CV output"}});
        m.entries.push_back({"Use", {"External modulation source with quantized, stepped behavior", "External modulation source with quantized, stepped behavior", "External modulation source with quantized, stepped behavior"}});
        m.entries.push_back({"Detection", {"When placed directly right of YAMANOTE, auto-connects to send/return", "When placed directly right of YAMANOTE, auto-connects to send/return", "When placed directly right of YAMANOTE, auto-connects to send/return"}});
        m.entries.push_back({"Channel Selection", {"Uses Channel A if single effect, Channel B if part of chain", "Uses Channel A if single effect, Channel B if part of chain", "Uses Channel A if single effect, Channel B if part of chain"}});
        m.entries.push_back({"Chain Support", {"Multiple effects (Runner, Facehugger, Ovomorph) auto-distribute across channels", "Multiple effects (Runner, Facehugger, Ovomorph) auto-distribute across channels", "Multiple effects (Runner, Facehugger, Ovomorph) auto-distribute across channels"}});
        m.entries.push_back({"Left Audio", {"Left stereo input", "左聲道處理輸出（Polyphonic）", "左処理出力（ポリフォニック）"}});
        m.entries.push_back({"Right Audio", {"Right stereo input (normalizes to Left if disconnected)", "右聲道處理輸出（Polyphonic）", "右処理出力（ポリフォニック）"}});
        m.entries.push_back({"Time L CV", {"Left delay time modulation", "左 Delay 時間調變", "左ディレイタイム変調"}});
        m.entries.push_back({"Time R CV", {"Right delay time modulation", "右 Delay 時間調變", "右ディレイタイム変調"}});
        m.entries.push_back({"Feedback CV", {"Feedback modulation", "Feedback 調變", "フィードバック変調"}});
        m.entries.push_back({"Mix CV", {"Wet/dry modulation", "Wet/Dry 調變", "ウェット/ドライ変調"}});
        m.entries.push_back({"Chaos CV", {"Chaos depth modulation", "原始 Chaos 訊號 0-5V（Polyphonic）", "生カオス信号0-5V（ポリフォニック）"}});
        m.entries.push_back({"Rate CV", {"Chaos speed modulation", "Chaos 速度調變", "カオス速度変調"}});
        m.entries.push_back({"Left Audio", {"Left processed output (polyphonic)", "左聲道處理輸出（Polyphonic）", "左処理出力（ポリフォニック）"}});
        m.entries.push_back({"Right Audio", {"Right processed output (polyphonic)", "右聲道處理輸出（Polyphonic）", "右処理出力（ポリフォニック）"}});
        m.entries.push_back({"Chaos CV", {"Raw chaos signal 0-5V (polyphonic)", "原始 Chaos 訊號 0-5V（Polyphonic）", "生カオス信号0-5V（ポリフォニック）"}});
        m.entries.push_back({"Sample &amp; Hold CV", {"Sample-and-hold chaos 0-5V (polyphonic)", "Sample-and-Hold Chaos 0-5V（Polyphonic）", "サンプル&ホールドカオス0-5V（ポリフォニック）"}});
        m.entries.push_back({"Sample & Hold CV", {"Sample & Hold output from Chaos section. Samples random values at Chaos Rate, with slew limiting.", "Chaos 區段的 Sample & Hold 輸出。依 Chaos Rate 取樣隨機值，帶有 Slew 限制。", "Chaosセクションのサンプル＆ホールド出力。Chaos Rateでランダム値をサンプリングし、スルーリミッティング付き。"}});
        data["Runner"] = std::move(m);
    }
    // Runshow (44 entries)
    {
        ModuleHelpData m;
        m.name = "Runshow";
        m.description = {"Performance timer with real-time clock, bar/beat counter, dual interval timers, and four variable-length bar sequencers", "演出計時器，具備即時 Clock、Bar/Beat 計數器、雙 Interval Timer 與四個可變長度 Bar Sequencer", "リアルタイムクロック、バー/ビートカウンター、デュアルインターバルタイマー、4つの可変長バーシーケンサーを備えたパフォーマンスタイマー"};
        m.entries.push_back({"Start/Stop", {"Toggle via button or CV. Starts/stops elapsed time counting.", "Start/Stop CV 觸發輸入。", "Start/Stop CVトリガー入力。"}});
        m.entries.push_back({"Reset", {"Resets all counters (clockCount, currentBar, quarter_notes, eighth_notes, sixteenth_notes)", "重設 CV 觸發輸入。", "リセットCVトリガー入力。"}});
        m.entries.push_back({"Clock Input", {"External clock assumed to be 16th notes. 4 clocks = 1 beat, 16 clocks = 1 bar default.", "外部 Clock 假定為 16 分音符。預設 4 個 Clock = 1 Beat，16 個 Clock = 1 Bar。", "外部クロックは16分音符と想定。デフォルトで4クロック = 1ビート、16クロック = 1バー。"}});
        m.entries.push_back({"5 Base Waveforms", {"Attack (ramp up, instant drop), Triangle (symmetric), Decay (instant rise, ramp down), Sine (half-wave), Square (sustain at 10V)", "Attack (ramp up, instant drop), Triangle (symmetric), Decay (instant rise, ramp down), Sine (half-wave), Square (sustain at 10V)", "Attack (ramp up, instant drop), Triangle (symmetric), Decay (instant rise, ramp down), Sine (half-wave), Square (sustain at 10V)"}});
        m.entries.push_back({"Attack", {"Linear ramp from 0V to 10V over 99% of phase, then drops to 0V", "0V 到 10V 線性上升佔 99% Phase，然後下降至 0V", "0Vから10Vへフェーズの99%で線形上昇、その後0Vに降下"}});
        m.entries.push_back({"Triangle", {"Rises from 0V to 10V in first half, falls back to 0V in second half", "前半段從 0V 上升至 10V，後半段下降回 0V", "前半で0Vから10Vへ上昇、後半で0Vへ降下"}});
        m.entries.push_back({"Decay", {"Starts at 10V, then linear ramp down to 0V over 99% of phase", "從 10V 開始，線性下降至 0V 佔 99% Phase", "10Vで開始、フェーズの99%で0Vへ線形降下"}});
        m.entries.push_back({"Sine", {"Half-wave sine shape, 0-10V unipolar", "半波 Sine 形狀，0-10V 單極性", "半波サイン形状、0-10Vユニポーラ"}});
        m.entries.push_back({"Square", {"Sustains at 10V for entire duration", "整個持續時間維持於 10V", "全持続時間10Vを維持"}});
        m.entries.push_back({"Morphing", {"Linear interpolation between adjacent waveforms. Morph 0-1: Attack↔Triangle, 1-2: Triangle↔Decay, 2-3: Decay↔Sine, 3-4: Sine↔Square", "相鄰 Waveform 間的線性插值。Morph 0-1: Attack↔Triangle，1-2: Triangle↔Decay，2-3: Decay↔Sine，3-4: Sine↔Square", "隣接波形間の線形補間。Morph 0-1: Attack↔Triangle、1-2: Triangle↔Decay、2-3: Decay↔Sine、3-4: Sine↔Square"}});
        m.entries.push_back({"Pulse Width", {"1-99%, determines waveform duration as percentage of interval", "1-99%，決定 Waveform 持續時間佔 Interval 的百分比", "1-99%、インターバルに対する波形持続時間の割合を決定"}});
        m.entries.push_back({"Waveform", {"0-4 snapped value selecting/morphing between shapes", "選擇 Waveform 形狀 0-4（Snap）。Attack、Triangle、Decay、Sine、Square。", "波形シェイプ選択 0-4（スナップ）。Attack、Triangle、Decay、Sine、Square。"}});
        m.entries.push_back({"Bar 1-4", {"Each bar length 1-16 clocks (integer snap). Total cycle = sum of all 4 bar lengths.", "各 Bar 長度 1-16 個 Clock（整數對齊）。總循環 = 所有 4 個 Bar 長度之總和。", "各バー長1-16クロック（整数スナップ）。総サイクル = 全4バー長の合計。"}});
        m.entries.push_back({"Timer Max", {"Adjustable maximum for the 5Min progress bar (1-60 minutes, default: 5). Draggable yellow line on first bar.", "5Min 進度條的最大分鐘數 1-60（整數對齊，預設: 5）。黃色線可拖曳。", "5Minプログレスバーの最大分数 1-60（整数スナップ、デフォルト: 5）。黄色ラインをドラッグ可能。"}});
        m.entries.push_back({"5Min Timer", {"Triggers every 5 minutes until 30 minutes elapsed (5 triggers total)", "每 5 分鐘觸發 Waveform，直到 30 分鐘（0-10V）。", "30分経過まで5分毎に波形トリガー（0-10V）。"}});
        m.entries.push_back({"1Min Timer", {"Triggers every 1 minute until 15 minutes elapsed (15 triggers total)", "每 1 分鐘觸發 Waveform，直到 15 分鐘（0-10V）。", "15分経過まで1分毎に波形トリガー（0-10V）。"}});
        m.entries.push_back({"Duration", {"Waveform length equals pulse width percentage of interval", "Waveform 長度等於 Pulse Width 百分比乘以 Interval", "波形長はパルス幅パーセンテージとインターバルの積"}});
        m.entries.push_back({"Phase", {"Advances proportionally each sample, resets when complete", "每個 Sample 按比例遞進，完成時重置", "各サンプルで比例進行、完了時リセット"}});
        m.entries.push_back({"Cycle Position", {"Current position wraps within total cycle length", "Current position wraps within total cycle length", "Current position wraps within total cycle length"}});
        m.entries.push_back({"Bar Detection", {"Compares clocksInCycle against cumulative bar lengths", "Compares clocksInCycle against cumulative bar lengths", "Compares clocksInCycle against cumulative bar lengths"}});
        m.entries.push_back({"Transition Trigger", {"Waveform triggered when currentBarInCycle changes", "Waveform triggered when currentBarInCycle changes", "Waveform triggered when currentBarInCycle changes"}});
        m.entries.push_back({"Clock Interval Tracking", {"Measures time between clocks for adaptive pulse width", "Measures time between clocks for adaptive pulse width", "Measures time between clocks for adaptive pulse width"}});
        m.entries.push_back({"Time Code", {"M:SS:CC format. Minutes limited to 0-999 (3 digits).", "M:SS:CC 格式。分鐘限制為 0-999（3 位數）。", "M:SS:CC format. Minutes limited to 0-999 (3 digits)."}});
        m.entries.push_back({"Bar:Beat:Tick", {"BBB:B:T format. Beat = (clockCount/4) % 4 + 1, Tick = clockCount % 4 + 1", "BBB:B:T 格式。Beat = (clockCount/4) % 4 + 1，Tick = clockCount % 4 + 1", "BBB:B:T形式。Beat = (clockCount/4) % 4 + 1、Tick = clockCount % 4 + 1"}});
        m.entries.push_back({"Beat LED", {"Full brightness on beat, then fades smoothly", "Beat 時全亮度，然後平滑淡出", "ビート時フル輝度、その後スムーズにフェードアウト"}});
        m.entries.push_back({"6 Progress Bars", {"5m (6 segments, max adjustable via Timer Max 1-60 min), 1m (15 segments), Bar 1-4 (4 segments each)", "5m (6 segments, max adjustable via Timer Max 1-60 min), 1m (15 segments), Bar 1-4 (4 segments each)", "5m (6 segments, max adjustable via Timer Max 1-60 min), 1m (15 segments), Bar 1-4 (4 segments each)"}});
        m.entries.push_back({"Reset", {"Reset button. Resets all counters and timers to zero.", "重設 CV 觸發輸入。", "リセットCVトリガー入力。"}});
        m.entries.push_back({"Start/Stop", {"Toggle button. Starts or stops elapsed time counting.", "Start/Stop CV 觸發輸入。", "Start/Stop CVトリガー入力。"}});
        m.entries.push_back({"Pulse Width (Bar %)", {"Waveform duration as percentage of interval 1-99% (default: 10%).", "Waveform 持續時間佔 Interval 的百分比 1-99%（預設: 10%）。", "インターバルに対する波形持続時間の割合 1-99%（デフォルト: 10%）。"}});
        m.entries.push_back({"Waveform", {"Selects waveform shape 0-4 (snap). Attack, Triangle, Decay, Sine, Square.", "選擇 Waveform 形狀 0-4（Snap）。Attack、Triangle、Decay、Sine、Square。", "波形シェイプ選択 0-4（スナップ）。Attack、Triangle、Decay、Sine、Square。"}});
        m.entries.push_back({"Bar 1 Length", {"Bar 1 clock count 1-16 (integer snap, default: 16).", "Bar 1 Clock 數量 1-16（整數對齊，預設: 16）。", "バー1クロック数 1-16（整数スナップ、デフォルト: 16）。"}});
        m.entries.push_back({"Bar 2 Length", {"Bar 2 clock count 1-16 (integer snap, default: 16).", "Bar 2 Clock 數量 1-16（整數對齊，預設: 16）。", "バー2クロック数 1-16（整数スナップ、デフォルト: 16）。"}});
        m.entries.push_back({"Bar 3 Length", {"Bar 3 clock count 1-16 (integer snap, default: 16).", "Bar 3 Clock 數量 1-16（整數對齊，預設: 16）。", "バー3クロック数 1-16（整数スナップ、デフォルト: 16）。"}});
        m.entries.push_back({"Bar 4 Length", {"Bar 4 clock count 1-16 (integer snap, default: 16).", "Bar 4 Clock 數量 1-16（整數對齊，預設: 16）。", "バー4クロック数 1-16（整数スナップ、デフォルト: 16）。"}});
        m.entries.push_back({"Timer Max", {"Maximum minutes for 5Min progress bar 1-60 (integer snap, default: 5). Draggable yellow line.", "5Min 進度條的最大分鐘數 1-60（整數對齊，預設: 5）。黃色線可拖曳。", "5Minプログレスバーの最大分数 1-60（整数スナップ、デフォルト: 5）。黄色ラインをドラッグ可能。"}});
        m.entries.push_back({"Clock", {"External clock input (assumed 16th notes). 4 clocks = 1 beat.", "外部 Clock 輸入（假定為 16 分音符）。4 個 Clock = 1 Beat。", "外部クロック入力（16分音符と想定）。4クロック = 1ビート。"}});
        m.entries.push_back({"Reset", {"Reset CV trigger input.", "重設 CV 觸發輸入。", "リセットCVトリガー入力。"}});
        m.entries.push_back({"Start/Stop", {"Start/Stop CV trigger input.", "Start/Stop CV 觸發輸入。", "Start/Stop CVトリガー入力。"}});
        m.entries.push_back({"5Min Timer", {"Triggers waveform every 5 minutes until 30 minutes elapsed (0-10V).", "每 5 分鐘觸發 Waveform，直到 30 分鐘（0-10V）。", "30分経過まで5分毎に波形トリガー（0-10V）。"}});
        m.entries.push_back({"1Min Timer", {"Triggers waveform every 1 minute until 15 minutes elapsed (0-10V).", "每 1 分鐘觸發 Waveform，直到 15 分鐘（0-10V）。", "15分経過まで1分毎に波形トリガー（0-10V）。"}});
        m.entries.push_back({"Bar 1 Timer", {"Waveform output triggered at start of Bar 1 (0-10V).", "Bar 1 開始時觸發的 Waveform 輸出（0-10V）。", "バー1開始時にトリガーされる波形出力（0-10V）。"}});
        m.entries.push_back({"Bar 2 Timer", {"Waveform output triggered at start of Bar 2 (0-10V).", "Bar 2 開始時觸發的 Waveform 輸出（0-10V）。", "バー2開始時にトリガーされる波形出力（0-10V）。"}});
        m.entries.push_back({"Bar 3 Timer", {"Waveform output triggered at start of Bar 3 (0-10V).", "Bar 3 開始時觸發的 Waveform 輸出（0-10V）。", "バー3開始時にトリガーされる波形出力（0-10V）。"}});
        m.entries.push_back({"Bar 4 Timer", {"Waveform output triggered at start of Bar 4 (0-10V).", "Bar 4 開始時觸發的 Waveform 輸出（0-10V）。", "バー4開始時にトリガーされる波形出力（0-10V）。"}});
        data["Runshow"] = std::move(m);
    }
    // SHINJUKU (39 entries)
    {
        ModuleHelpData m;
        m.name = "SHINJUKU";
        m.description = {"8-track stereo mixer with 12-band master EQ, sidechain ducking, and cross-module solo, Tokyo Marunouchi line themed", "8 軌 Stereo Mixer，具備 12-band Master EQ、Sidechain Ducking 與跨模組 Solo，東京丸之內線主題", "12バンドマスターEQ、サイドチェインダッキング、クロスモジュールソロを備えた8トラックステレオミキサー、東京丸ノ内線テーマ"};
        m.entries.push_back({"Bands", {"12 frequency bands (full spectrum coverage)", "12 frequency bands (full spectrum coverage)", "12 frequency bands (full spectrum coverage)"}});
        m.entries.push_back({"Frequencies", {"31Hz, 63Hz, 125Hz, 250Hz, 500Hz, 1kHz, 2kHz, 3.15kHz, 5kHz, 8kHz, 12.5kHz, 16kHz", "31Hz, 63Hz, 125Hz, 250Hz, 500Hz, 1kHz, 2kHz, 3.15kHz, 5kHz, 8kHz, 12.5kHz, 16kHz", "31Hz, 63Hz, 125Hz, 250Hz, 500Hz, 1kHz, 2kHz, 3.15kHz, 5kHz, 8kHz, 12.5kHz, 16kHz"}});
        m.entries.push_back({"Gain Range", {"-12dB to +12dB per band", "-12dB to +12dB per band", "-12dB to +12dB per band"}});
        m.entries.push_back({"Filter Type", {"Biquad peak EQ (Audio EQ Cookbook)", "Biquad peak EQ (Audio EQ Cookbook)", "Biquad peak EQ (Audio EQ Cookbook)"}});
        m.entries.push_back({"Q Factor", {"Fixed at 1.41 (approximately 1 octave bandwidth)", "Fixed at 1.41 (approximately 1 octave bandwidth)", "Fixed at 1.41 (approximately 1 octave bandwidth)"}});
        m.entries.push_back({"Controls", {"Vertical faders with center detent at 0dB", "Vertical faders with center detent at 0dB", "Vertical faders with center detent at 0dB"}});
        m.entries.push_back({"Processing", {"Applied to summed stereo output before final limiting", "Applied to summed stereo output before final limiting", "Applied to summed stereo output before final limiting"}});
        m.entries.push_back({"INPUT L/R", {"Stereo audio input (polyphonic). If only L connected, a 20ms delayed copy is sent to R (Haas effect stereo widening).", "Stereo Audio 輸入（Polyphonic）。若僅連接 L，以 20ms 延遲複製至 R（Haas Effect Stereo Widening）。", "ステレオオーディオ入力（ポリフォニック）。Lのみ接続時、20ms遅延コピーをRに送出（Haasエフェクトステレオワイドニング）。"}});
        m.entries.push_back({"LEVEL Knob", {"Track gain 0-200% (default: 100%)", "軌道 Gain 0-200%（預設: 100%）", "Track gain 0-200% (default: 100%)"}});
        m.entries.push_back({"LEVEL CV", {"Gain modulation input (\\u00b110V adds \\u00b1100% to knob value, final gain clamped 0.0-2.0)", "聲道音量 CV 輸入。", "チャンネル音量のCV入力。"}});
        m.entries.push_back({"DUCK Knob", {"Sidechain ducking depth 0-100%", "Sidechain Ducking 深度 0-100%", "Sidechain ducking depth 0-100%"}});
        m.entries.push_back({"DUCK Input", {"Sidechain control input (expects envelope 0-10V)", "Sidechain 控制輸入（預期 Envelope 0-10V）", "Sidechain control input (expects envelope 0-10V)"}});
        m.entries.push_back({"MUTE Button", {"Toggle track mute with red LED indicator", "切換軌道 Mute，紅色 LED 指示", "Toggle track mute with red LED indicator"}});
        m.entries.push_back({"SOLO Button", {"Toggle track solo with green LED indicator", "切換軌道 Solo，綠色 LED 指示", "Toggle track solo with green LED indicator"}});
        m.entries.push_back({"MUTE/SOLO Triggers", {"External trigger inputs for mute/solo toggle", "Mute/Solo 切換的外部 Trigger 輸入", "External trigger inputs for mute/solo toggle"}});
        m.entries.push_back({"Chain Detection", {"Solo state is shared across adjacent U8, ALEXANDERPLATZ, and SHINJUKU modules", "Solo state is shared across adjacent U8, ALEXANDERPLATZ, and SHINJUKU modules", "Solo state is shared across adjacent U8, ALEXANDERPLATZ, and SHINJUKU modules"}});
        m.entries.push_back({"Exclusive Solo", {"Hold solo button for 400ms to activate exclusive mode (cancels all other solos in chain)", "長按 Solo 按鈕 400ms 啟用 Exclusive 模式（取消 Chain 中所有其他 Solo）", "Hold solo button for 400ms to activate exclusive mode (cancels all other solos in chain)"}});
        m.entries.push_back({"Visual Feedback", {"Mute LED lights up when track is silenced by another track's solo", "Mute LED lights up when track is silenced by another track's solo", "Mute LED lights up when track is silenced by another track's solo"}});
        m.entries.push_back({"CHAIN L/R", {"Input from previous mixer module in chain", "來自 Chain 中前一個 Mixer 模組的輸入", "チェーン内の前のミキサーモジュールからの入力"}});
        m.entries.push_back({"MIX L/R", {"Final stereo output (track sum + chain + EQ processed)", "最終 Stereo 輸出（軌道加總 + Chain + EQ 處理）", "最終ステレオ出力（トラック合計 + チェーン + EQ処理）"}});
        m.entries.push_back({"Auto-Patch", {"Automatically connects to adjacent U8, YAMANOTE, ALEXANDERPLATZ, or SHINJUKU", "自動連接至相鄰的 U8、YAMANOTE、ALEXANDERPLATZ 或 SHINJUKU", "Automatically connects to adjacent U8, YAMANOTE, ALEXANDERPLATZ, or SHINJUKU"}});
        m.entries.push_back({"Cable Color", {"Red (#F62F36) matching Tokyo Marunouchi line", "Red (#F62F36) matching Tokyo Marunouchi line", "Red (#F62F36) matching Tokyo Marunouchi line"}});
        m.entries.push_back({"Display", {"Horizontal stereo VU meter per track", "Horizontal stereo VU meter per track", "Horizontal stereo VU meter per track"}});
        m.entries.push_back({"Range", {"-36dB to +6dB", "-36dB to +6dB", "-36dB to +6dB"}});
        m.entries.push_back({"Color", {"Green to red gradient, red zone above 0dB", "Green to red gradient, red zone above 0dB", "Green to red gradient, red zone above 0dB"}});
        m.entries.push_back({"Chain Left", {"Left channel chain input for daisy-chaining multiple mixer modules.", "左聲道 Chain 輸入，用於串接多個混音器模組。", "複数のミキサーモジュールをデイジーチェーン接続するための左チャンネルチェイン入力。"}});
        m.entries.push_back({"Chain Right", {"Right channel chain input for daisy-chaining multiple mixer modules.", "右聲道 Chain 輸入，用於串接多個混音器模組。", "複数のミキサーモジュールをデイジーチェーン接続するための右チャンネルチェイン入力。"}});
        m.entries.push_back({"Mix Left", {"Main stereo mix left output. Sum of all 8 tracks, 12-band EQ, and chain input.", "主立體聲混音左輸出。所有 8 軌、12 頻段 EQ 與 Chain 輸入的總和。", "メインステレオミックス左出力。全8トラック、12バンドEQ、チェイン入力の合計。"}});
        m.entries.push_back({"Mix Right", {"Main stereo mix right output. Sum of all 8 tracks, 12-band EQ, and chain input.", "主立體聲混音右輸出。所有 8 軌、12 頻段 EQ 與 Chain 輸入的總和。", "メインステレオミックス右出力。全8トラック、12バンドEQ、チェイン入力の合計。"}});
        m.entries.push_back({"Left", {"Per-channel stereo audio input (left channel).", "各聲道立體聲音訊輸入（左聲道）。", "チャンネル別ステレオオーディオ入力（左チャンネル）。"}});
        m.entries.push_back({"Right", {"Per-channel stereo audio input (right channel).", "各聲道立體聲音訊輸入（右聲道）。", "チャンネル別ステレオオーディオ入力（右チャンネル）。"}});
        m.entries.push_back({"Level", {"Per-channel volume control (0-200%, default 100%).", "各聲道音量控制（0-200%，預設 100%）。", "チャンネル別音量コントロール（0-200%、デフォルト100%）。"}});
        m.entries.push_back({"Duck", {"Sidechain duck amount (0-100%).", "Sidechain Ducking 量（0-100%）。", "サイドチェインダック量（0-100%）。"}});
        m.entries.push_back({"Mute", {"Toggle channel mute.", "切換聲道靜音。", "チャンネルミュート切替。"}});
        m.entries.push_back({"Solo", {"Toggle channel solo (exclusive mode available in right-click menu).", "切換聲道 Solo（右鍵選單可啟用 Exclusive 模式）。", "チャンネルソロ切替（右クリックメニューでエクスクルーシブモード利用可能）。"}});
        m.entries.push_back({"Level CV", {"CV input for channel volume control.", "聲道音量 CV 輸入。", "チャンネル音量のCV入力。"}});
        m.entries.push_back({"Mute Trigger", {"Trigger input to toggle mute state.", "切換靜音狀態的 Trigger 輸入。", "ミュート状態を切り替えるトリガー入力。"}});
        m.entries.push_back({"Solo Trigger", {"Trigger input to toggle solo state.", "切換 Solo 狀態的 Trigger 輸入。", "ソロ状態を切り替えるトリガー入力。"}});
        m.entries.push_back({"Master EQ", {"12-band parametric EQ (31/63/125/250/500/1K/2K/3K/5K/8K/12K/16K Hz), \\u00b112dB per band.", "12 頻段參數型 EQ（31/63/125/250/500/1K/2K/3K/5K/8K/12K/16K Hz），每頻段 \\u00b112dB。", "12バンドパラメトリックEQ（31/63/125/250/500/1K/2K/3K/5K/8K/12K/16K Hz）、バンドあたり\\u00b112dB。"}});
        data["SHINJUKU"] = std::move(m);
    }
    // SongMode (56 entries)
    {
        ModuleHelpData m;
        m.name = "SONG MODE";
        m.description = {"8-input sequential switch with clock-based length control and crossfade support", "8 輸入順序切換器，具備 Clock 長度控制與 Crossfade 支援", "8入力シーケンシャルスイッチ、クロックベース長さ制御とクロスフェード対応"};
        m.entries.push_back({"Sequence Field", {"Text input defining playback order. Default: \"12345678\"", "Text input defining playback order. Default: \"12345678\"", "Text input defining playback order. Default: \"12345678\""}});
        m.entries.push_back({"Formats", {"Direct (\"12345678\"), space-separated (\"1 2 3\"), comma-separated (\"1,2,3\"), range (\"1-4\" expands to 1,2,3,4)", "Direct (\"12345678\"), space-separated (\"1 2 3\"), comma-separated (\"1,2,3\"), range (\"1-4\" expands to 1,2,3,4)", "Direct (\"12345678\"), space-separated (\"1 2 3\"), comma-separated (\"1,2,3\"), range (\"1-4\" expands to 1,2,3,4)"}});
        m.entries.push_back({"Numbers", {"1-8 corresponding to inputs IN 1 through IN 8", "1-8 corresponding to inputs IN 1 through IN 8", "1-8 corresponding to inputs IN 1 through IN 8"}});
        m.entries.push_back({"Loop", {"Sequence repeats indefinitely after completion", "序列完成後無限循環", "Sequence repeats indefinitely after completion"}});
        m.entries.push_back({"Length 1-8", {"Clock cycles per input (1-64, default: 4). Determines how many clocks each input plays before advancing.", "每個輸入的 Clock 週期數（1-64，預設：4）。決定每個輸入播放多少 Clock 後切換。", "入力毎のクロックサイクル数（1-64、デフォルト：4）。各入力が切り替わるまでのクロック数を決定。"}});
        m.entries.push_back({"Snap", {"Values snap to integers for precise timing", "數值自動對齊整數以確保精確時序", "Values snap to integers for precise timing"}});
        m.entries.push_back({"Learn", {"Activates learn mode for that input (red LED)", "啟動該輸入的 Learn 模式（紅色 LED）", "その入力のLearnモードを有効化（赤LED）"}});
        m.entries.push_back({"Behavior", {"Counts incoming clocks while active. Press again to set length to counted value.", "Counts incoming clocks while active. Press again to set length to counted value.", "Counts incoming clocks while active. Press again to set length to counted value."}});
        m.entries.push_back({"Use Case", {"Match input length to external loop length automatically", "Match input length to external loop length automatically", "Match input length to external loop length automatically"}});
        m.entries.push_back({"Fade Clock", {"Start crossfade N clocks before input ends (0-16, default: 0). If 0, no fade occurs.", "在輸入結束前 N 個 Clock 開始 Crossfade（0-16，預設 0）。", "入力終了Nクロック前にクロスフェード開始（0-16、デフォルト0）。"}});
        m.entries.push_back({"Fade Time", {"Crossfade duration in milliseconds (0-1000, default: 100)", "Crossfade 持續時間（0-1000ms，預設 100）。", "クロスフェード持続時間（0-1000ms、デフォルト100）。"}});
        m.entries.push_back({"Behavior", {"Linear crossfade between current and next input signals", "Linear crossfade between current and next input signals", "Linear crossfade between current and next input signals"}});
        m.entries.push_back({"Clock", {"Clock input for advancing the sequence", "Clock 輸入用於推進序列。", "シーケンス進行用クロック入力。"}});
        m.entries.push_back({"Reset", {"Reset to beginning of sequence", "重置至序列開頭。", "シーケンス先頭にリセット。"}});
        m.entries.push_back({"Input 1-8", {"Eight audio/CV inputs (polyphonic)", "8 組 Audio/CV 輸入（Polyphonic）", "8系統Audio/CV入力（ポリフォニック）"}});
        m.entries.push_back({"Main", {"Routed output signal (active input with optional crossfade, polyphonic, matches maximum input channel count)", "路由輸出訊號（當前活動輸入，可選 Crossfade，Polyphonic）。", "ルーティング出力信号（アクティブ入力、クロスフェード可、ポリフォニック）。"}});
        m.entries.push_back({"Trigger 1-8", {"Trigger output when corresponding input becomes active", "對應輸入變為活動時的 Trigger 輸出", "対応する入力がアクティブになった時のトリガー出力"}});
        m.entries.push_back({"Green LED", {"Shows currently active input", "Shows currently active input", "Shows currently active input"}});
        m.entries.push_back({"Red LED", {"Shows learn mode is active for that input", "Shows learn mode is active for that input", "Shows learn mode is active for that input"}});
        m.entries.push_back({"Length 1", {"Clock cycles for Input 1 (1-64, default 4).", "Input 1 的 Clock 週期數（1-64，預設 4）。", "Input 1のクロックサイクル数（1-64、デフォルト4）。"}});
        m.entries.push_back({"Length 2", {"Clock cycles for Input 2 (1-64, default 4).", "Input 2 的 Clock 週期數（1-64，預設 4）。", "Input 2のクロックサイクル数（1-64、デフォルト4）。"}});
        m.entries.push_back({"Length 3", {"Clock cycles for Input 3 (1-64, default 4).", "Input 3 的 Clock 週期數（1-64，預設 4）。", "Input 3のクロックサイクル数（1-64、デフォルト4）。"}});
        m.entries.push_back({"Length 4", {"Clock cycles for Input 4 (1-64, default 4).", "Input 4 的 Clock 週期數（1-64，預設 4）。", "Input 4のクロックサイクル数（1-64、デフォルト4）。"}});
        m.entries.push_back({"Length 5", {"Clock cycles for Input 5 (1-64, default 4).", "Input 5 的 Clock 週期數（1-64，預設 4）。", "Input 5のクロックサイクル数（1-64、デフォルト4）。"}});
        m.entries.push_back({"Length 6", {"Clock cycles for Input 6 (1-64, default 4).", "Input 6 的 Clock 週期數（1-64，預設 4）。", "Input 6のクロックサイクル数（1-64、デフォルト4）。"}});
        m.entries.push_back({"Length 7", {"Clock cycles for Input 7 (1-64, default 4).", "Input 7 的 Clock 週期數（1-64，預設 4）。", "Input 7のクロックサイクル数（1-64、デフォルト4）。"}});
        m.entries.push_back({"Length 8", {"Clock cycles for Input 8 (1-64, default 4).", "Input 8 的 Clock 週期數（1-64，預設 4）。", "Input 8のクロックサイクル数（1-64、デフォルト4）。"}});
        m.entries.push_back({"Learn 1", {"Learn mode for Input 1. Counts clocks to auto-set length.", "Input 1 的 Learn 模式。計數 Clock 以自動設定長度。", "Input 1のLearnモード。クロックをカウントして長さを自動設定。"}});
        m.entries.push_back({"Learn 2", {"Learn mode for Input 2. Counts clocks to auto-set length.", "Input 2 的 Learn 模式。計數 Clock 以自動設定長度。", "Input 2のLearnモード。クロックをカウントして長さを自動設定。"}});
        m.entries.push_back({"Learn 3", {"Learn mode for Input 3. Counts clocks to auto-set length.", "Input 3 的 Learn 模式。計數 Clock 以自動設定長度。", "Input 3のLearnモード。クロックをカウントして長さを自動設定。"}});
        m.entries.push_back({"Learn 4", {"Learn mode for Input 4. Counts clocks to auto-set length.", "Input 4 的 Learn 模式。計數 Clock 以自動設定長度。", "Input 4のLearnモード。クロックをカウントして長さを自動設定。"}});
        m.entries.push_back({"Learn 5", {"Learn mode for Input 5. Counts clocks to auto-set length.", "Input 5 的 Learn 模式。計數 Clock 以自動設定長度。", "Input 5のLearnモード。クロックをカウントして長さを自動設定。"}});
        m.entries.push_back({"Learn 6", {"Learn mode for Input 6. Counts clocks to auto-set length.", "Input 6 的 Learn 模式。計數 Clock 以自動設定長度。", "Input 6のLearnモード。クロックをカウントして長さを自動設定。"}});
        m.entries.push_back({"Learn 7", {"Learn mode for Input 7. Counts clocks to auto-set length.", "Input 7 的 Learn 模式。計數 Clock 以自動設定長度。", "Input 7のLearnモード。クロックをカウントして長さを自動設定。"}});
        m.entries.push_back({"Learn 8", {"Learn mode for Input 8. Counts clocks to auto-set length.", "Input 8 的 Learn 模式。計數 Clock 以自動設定長度。", "Input 8のLearnモード。クロックをカウントして長さを自動設定。"}});
        m.entries.push_back({"Fade Clock", {"Start crossfade N clocks before input ends (0-16, default 0).", "在輸入結束前 N 個 Clock 開始 Crossfade（0-16，預設 0）。", "入力終了Nクロック前にクロスフェード開始（0-16、デフォルト0）。"}});
        m.entries.push_back({"Fade Time", {"Crossfade duration in milliseconds (0-1000, default 100).", "Crossfade 持續時間（0-1000ms，預設 100）。", "クロスフェード持続時間（0-1000ms、デフォルト100）。"}});
        m.entries.push_back({"Clock", {"Clock input for advancing the sequence.", "Clock 輸入用於推進序列。", "シーケンス進行用クロック入力。"}});
        m.entries.push_back({"Reset", {"Reset to beginning of sequence.", "重置至序列開頭。", "シーケンス先頭にリセット。"}});
        m.entries.push_back({"Input 1", {"Audio/CV input 1 (polyphonic).", "Audio/CV 輸入 1（Polyphonic）。", "Audio/CV入力1（ポリフォニック）。"}});
        m.entries.push_back({"Input 2", {"Audio/CV input 2 (polyphonic).", "Audio/CV 輸入 2（Polyphonic）。", "Audio/CV入力2（ポリフォニック）。"}});
        m.entries.push_back({"Input 3", {"Audio/CV input 3 (polyphonic).", "Audio/CV 輸入 3（Polyphonic）。", "Audio/CV入力3（ポリフォニック）。"}});
        m.entries.push_back({"Input 4", {"Audio/CV input 4 (polyphonic).", "Audio/CV 輸入 4（Polyphonic）。", "Audio/CV入力4（ポリフォニック）。"}});
        m.entries.push_back({"Input 5", {"Audio/CV input 5 (polyphonic).", "Audio/CV 輸入 5（Polyphonic）。", "Audio/CV入力5（ポリフォニック）。"}});
        m.entries.push_back({"Input 6", {"Audio/CV input 6 (polyphonic).", "Audio/CV 輸入 6（Polyphonic）。", "Audio/CV入力6（ポリフォニック）。"}});
        m.entries.push_back({"Input 7", {"Audio/CV input 7 (polyphonic).", "Audio/CV 輸入 7（Polyphonic）。", "Audio/CV入力7（ポリフォニック）。"}});
        m.entries.push_back({"Input 8", {"Audio/CV input 8 (polyphonic).", "Audio/CV 輸入 8（Polyphonic）。", "Audio/CV入力8（ポリフォニック）。"}});
        m.entries.push_back({"Main", {"Routed output signal (active input with optional crossfade, polyphonic).", "路由輸出訊號（當前活動輸入，可選 Crossfade，Polyphonic）。", "ルーティング出力信号（アクティブ入力、クロスフェード可、ポリフォニック）。"}});
        m.entries.push_back({"Trigger 1", {"Trigger output when Input 1 becomes active.", "Input 1 變為活動時的 Trigger 輸出。", "Input 1がアクティブになった時のトリガー出力。"}});
        m.entries.push_back({"Trigger 2", {"Trigger output when Input 2 becomes active.", "Input 2 變為活動時的 Trigger 輸出。", "Input 2がアクティブになった時のトリガー出力。"}});
        m.entries.push_back({"Trigger 3", {"Trigger output when Input 3 becomes active.", "Input 3 變為活動時的 Trigger 輸出。", "Input 3がアクティブになった時のトリガー出力。"}});
        m.entries.push_back({"Trigger 4", {"Trigger output when Input 4 becomes active.", "Input 4 變為活動時的 Trigger 輸出。", "Input 4がアクティブになった時のトリガー出力。"}});
        m.entries.push_back({"Trigger 5", {"Trigger output when Input 5 becomes active.", "Input 5 變為活動時的 Trigger 輸出。", "Input 5がアクティブになった時のトリガー出力。"}});
        m.entries.push_back({"Trigger 6", {"Trigger output when Input 6 becomes active.", "Input 6 變為活動時的 Trigger 輸出。", "Input 6がアクティブになった時のトリガー出力。"}});
        m.entries.push_back({"Trigger 7", {"Trigger output when Input 7 becomes active.", "Input 7 變為活動時的 Trigger 輸出。", "Input 7がアクティブになった時のトリガー出力。"}});
        m.entries.push_back({"Trigger 8", {"Trigger output when Input 8 becomes active.", "Input 8 變為活動時的 Trigger 輸出。", "Input 8がアクティブになった時のトリガー出力。"}});
        data["SongMode"] = std::move(m);
    }
    // SwingLFO (41 entries)
    {
        ModuleHelpData m;
        m.name = "Swing LFO";
        m.description = {"Dual-phase LFO with adjustable swing groove, morphable waveform shapes, and variable pulse width for rhythmic modulation", "雙相位 LFO，具備可調 Swing Groove、可變形波形與可變 Pulse 寬度，用於 Rhythmic Modulation", "調整可能なスウィンググルーブ、モーフィング波形、可変パルス幅を備えたデュアルフェーズLFO。リズミック変調用"};
        m.entries.push_back({"Range", {"0.125Hz to 128Hz (exponential scaling)", "0.125Hz to 128Hz (exponential scaling)", "0.125Hz to 128Hz (exponential scaling)"}});
        m.entries.push_back({"Default", {"Approximately 3Hz", "Approximately 3Hz", "Approximately 3Hz"}});
        m.entries.push_back({"CV", {"1V/octave response with attenuverter", "±10V 含 Attenuverter", "±10V（アッテヌバーター付き）"}});
        m.entries.push_back({"Range", {"0.0 to 1.0 (adjusts phase offset from 180° to 90°)", "0.0 to 1.0 (adjusts phase offset from 180° to 90°)", "0.0 to 1.0 (adjusts phase offset from 180° to 90°)"}});
        m.entries.push_back({"At 0%", {"Second phase is 180° ahead (opposite phase)", "Second phase is 180° ahead (opposite phase)", "Second phase is 180° ahead (opposite phase)"}});
        m.entries.push_back({"At 100%", {"Second phase is 90° ahead (quarter cycle)", "Second phase is 90° ahead (quarter cycle)", "Second phase is 90° ahead (quarter cycle)"}});
        m.entries.push_back({"Default", {"Approximately 153°, providing a classic swing feel", "Approximately 153°, providing a classic swing feel", "Approximately 153°, providing a classic swing feel"}});
        m.entries.push_back({"CV", {"±10V with attenuverter", "±10V 含 Attenuverter", "±10V（アッテヌバーター付き）"}});
        m.entries.push_back({"Range 0.0-0.5", {"Morphs from falling ramp to triangle", "Morphs from falling ramp to triangle", "Morphs from falling ramp to triangle"}});
        m.entries.push_back({"At 0", {"Falling ramp waveform", "Falling ramp waveform", "Falling ramp waveform"}});
        m.entries.push_back({"At 0.5", {"Triangle waveform", "Triangle waveform", "Triangle waveform"}});
        m.entries.push_back({"Range 0.5-1.0", {"Morphs from triangle to rising sawtooth", "Morphs from triangle to rising sawtooth", "Morphs from triangle to rising sawtooth"}});
        m.entries.push_back({"At 1.0", {"Rising sawtooth waveform", "Rising sawtooth waveform", "Rising sawtooth waveform"}});
        m.entries.push_back({"Transition", {"Smooth crossfade between shapes", "Smooth crossfade between shapes", "Smooth crossfade between shapes"}});
        m.entries.push_back({"Duty Cycle", {"Controlled by SHAPE parameter", "由 SHAPE 參數控制", "Controlled by SHAPE parameter"}});
        m.entries.push_back({"At Shape 0", {"1% duty cycle (very narrow pulse)", "1% duty cycle (very narrow pulse)", "1% duty cycle (very narrow pulse)"}});
        m.entries.push_back({"At Shape 1", {"30% duty cycle", "30% duty cycle", "30% duty cycle"}});
        m.entries.push_back({"Output", {"10V during pulse, 0V otherwise", "10V during pulse, 0V otherwise", "10V during pulse, 0V otherwise"}});
        m.entries.push_back({"Range", {"0.0 to 1.0", "0.0 to 1.0", "0.0 to 1.0"}});
        m.entries.push_back({"At 0", {"Only main phase output", "Only main phase output", "Only main phase output"}});
        m.entries.push_back({"At 0.5", {"Equal blend of both phases", "Equal blend of both phases", "Equal blend of both phases"}});
        m.entries.push_back({"At 1", {"Only swing phase output", "Only swing phase output", "Only swing phase output"}});
        m.entries.push_back({"Default", {"0.384 (slightly favoring main phase)", "0.384 (slightly favoring main phase)", "0.384 (slightly favoring main phase)"}});
        m.entries.push_back({"Threshold", {"2.0V rising edge", "2.0V rising edge", "2.0V rising edge"}});
        m.entries.push_back({"Action", {"Resets main phase to 0, second phase to calculated offset", "Resets main phase to 0, second phase to calculated offset", "Resets main phase to 0, second phase to calculated offset"}});
        m.entries.push_back({"Use Case", {"Sync to external clock or sequencer for tempo-locked swing", "Sync to external clock or sequencer for tempo-locked swing", "Sync to external clock or sequencer for tempo-locked swing"}});
        m.entries.push_back({"Saw Wave", {"Shaped waveform (0-10V unipolar). Morphs through falling ramp → triangle → rising saw.", "造型波形（0-10V 單極）。變形範圍：下降 Ramp → Triangle → 上升 Saw。", "シェイプされた波形（0-10V ユニポーラ）。モーフィング範囲: 下降ランプ → 三角波 → 上昇ノコギリ波。"}});
        m.entries.push_back({"Pulse Wave", {"Variable width pulse (0-10V unipolar). Width controlled by SHAPE parameter.", "可變寬度 Pulse（0-10V 單極）。寬度由 SHAPE 參數控制。", "可変幅パルス（0-10V ユニポーラ）。幅はSHAPEパラメータで制御。"}});
        m.entries.push_back({"Frequency", {"LFO rate 0.125Hz-128Hz (exponential, base 2). Default ~3Hz. CV: 1V/octave.", "LFO 速率 0.125Hz-128Hz（指數，base 2）。預設約 3Hz。CV: 1V/octave。", "LFOレート 0.125Hz-128Hz（指数、base 2）。デフォルト約3Hz。CV: 1V/octave。"}});
        m.entries.push_back({"Swing", {"Phase offset 180°-90° (0.0-1.0). Default ~153°. Display unit: degrees.", "相位偏移 180°-90°（0.0-1.0）。預設約 153°。顯示單位：度。", "位相オフセット 180°-90°（0.0-1.0）。デフォルト約153°。表示単位: 度。"}});
        m.entries.push_back({"Shape", {"Waveform morphing 0-100%. SAW: ramp→tri→saw. PULSE: 1-30% duty cycle.", "波形 Morphing 0-100%。SAW: ramp→tri→saw。PULSE: 1-30% Duty Cycle。", "波形モーフィング 0-100%。SAW: ランプ→三角波→ノコギリ波。PULSE: 1-30%デューティサイクル。"}});
        m.entries.push_back({"Mix", {"Crossfade main/swing phase (0.0-1.0). Default 0.384.", "主相位/Swing 相位交叉混合（0.0-1.0）。預設 0.384。", "メイン/スウィングフェーズクロスフェード（0.0-1.0）。デフォルト0.384。"}});
        m.entries.push_back({"Freq CV Attenuverter", {"Attenuverter for Frequency CV input (-1.0 to +1.0).", "頻率 CV 輸入的 Attenuverter（-1.0 至 +1.0）。", "周波数CV入力用アッテヌバーター（-1.0〜+1.0）。"}});
        m.entries.push_back({"Swing CV Attenuverter", {"Attenuverter for Swing CV input (-1.0 to +1.0).", "Swing CV 輸入的 Attenuverter（-1.0 至 +1.0）。", "スウィングCV入力用アッテヌバーター（-1.0〜+1.0）。"}});
        m.entries.push_back({"Shape CV Attenuverter", {"Attenuverter for Shape CV input (-1.0 to +1.0).", "Shape CV 輸入的 Attenuverter（-1.0 至 +1.0）。", "シェイプCV入力用アッテヌバーター（-1.0〜+1.0）。"}});
        m.entries.push_back({"Mix CV Attenuverter", {"Attenuverter for Mix CV input (-1.0 to +1.0).", "Mix CV 輸入的 Attenuverter（-1.0 至 +1.0）。", "ミックスCV入力用アッテヌバーター（-1.0〜+1.0）。"}});
        m.entries.push_back({"Frequency CV", {"1V/octave frequency modulation input.", "1V/octave 頻率 Modulation 輸入。", "1V/octave周波数変調入力。"}});
        m.entries.push_back({"Swing CV", {"Swing amount modulation input (±10V).", "Swing 量 Modulation 輸入（±10V）。", "スウィング量変調入力（±10V）。"}});
        m.entries.push_back({"Shape CV", {"Shape modulation input (±10V).", "Shape Modulation 輸入（±10V）。", "シェイプ変調入力（±10V）。"}});
        m.entries.push_back({"Reset", {"Reset trigger input. 2.0V rising edge threshold.", "Reset Trigger 輸入。2.0V 上升緣閾值。", "リセットトリガー入力。2.0V立ち上がりエッジ閾値。"}});
        m.entries.push_back({"Mix CV", {"Mix modulation input (±10V).", "Mix Modulation 輸入（±10V）。", "ミックス変調入力（±10V）。"}});
        data["SwingLFO"] = std::move(m);
    }
    // TWNC (70 entries)
    {
        ModuleHelpData m;
        m.name = "TWNC";
        m.description = {"Dual-track Euclidean drum machine with built-in FM synthesis and accent system", "雙軌 Euclidean Drum Machine，具備內建 FM 合成與 Accent 系統", "内蔵FM合成とアクセントシステムを備えたデュアルトラックユークリッドドラムマシン"};
        m.entries.push_back({"Distribution", {"Bjorklund algorithm distributes hits as evenly as possible across the pattern length", "Bjorklund algorithm distributes hits as evenly as possible across the pattern length", "Bjorklund algorithm distributes hits as evenly as possible across the pattern length"}});
        m.entries.push_back({"Rotation", {"Pattern shift for offset control", "Pattern shift for offset control", "Pattern shift for offset control"}});
        m.entries.push_back({"Length", {"1-32 steps (default: 32)", "1-32 steps (default: 32)", "1-32 steps (default: 32)"}});
        m.entries.push_back({"Fill", {"0-100% converted to integer step count", "活躍步進百分比（0-100%，預設：25%）", "アクティブステップ割合（0-100%、デフォルト：25%）"}});
        m.entries.push_back({"FILL", {"Active steps percentage (0-100%, default: 84.5%)", "活躍步進百分比（0-100%，預設：25%）", "アクティブステップ割合（0-100%、デフォルト：25%）"}});
        m.entries.push_back({"FREQ", {"Oscillator frequency (20Hz-20kHz logarithmic, default: ~36.7Hz)", "Hi-hat 頻率（20Hz-20kHz，預設：~20kHz）", "ハイハット周波数（20Hz-20kHz、デフォルト：~20kHz）"}});
        m.entries.push_back({"FM", {"FM amount (0-1, default: 0.65). Envelope modulates pitch exponentially for classic FM drum character", "FM 量（0-1，預設：0.65）。Envelope 以指數方式調變音高，產生經典 FM Drum 音色", "FM量（0-1、デフォルト：0.65）。エンベロープが指数的にピッチを変調し、クラシックなFMドラムキャラクターを実現"}});
        m.entries.push_back({"NOISE", {"Pink/blue noise mix (0-1, default: 0.152). Blue noise is derived from pink noise via differentiation", "Noise FM 調變（0-1，預設：0.714）。<0.5 時為 Pink，≥0.5 時為 Blue", "ノイズFM変調（0-1、デフォルト：0.714）。<0.5でPink、≥0.5でBlue"}});
        m.entries.push_back({"ACCENT", {"Quarter-note VCA shift (1-7 steps, default: 1)", "四分音符 VCA 位移（1-7 步，預設：1）", "四分音符VCAシフト（1-7ステップ、デフォルト：1）"}});
        m.entries.push_back({"DELAY", {"Main VCA decay time (0.01-2s, default: 0.396s)", "主 VCA Decay 時間（0.01-2s，預設：0.396s）", "メインVCAディケイ時間（0.01-2s、デフォルト：0.396s）"}});
        m.entries.push_back({"DECAY", {"Track envelope decay (0.01-2s, default: 0.720s). FM envelope uses half this value (decayParam * 0.5), VCA envelope uses full value.", "可變 S 曲線與 smoothstep 過渡，產生自然的衰減聲音", "エンベロープディケイ（0.01-2s、デフォルト：0.131s）。VCAエンベロープはこの値の半分を使用（decayParam * 0.5）。"}});
        m.entries.push_back({"SHAPE", {"Envelope curve (0-0.99, default: 0)", "Envelope 曲線（0-0.99，預設：0.055）", "エンベロープカーブ（0-0.99、デフォルト：0.055）"}});
        m.entries.push_back({"SHIFT", {"Pattern rotation offset (0-7 steps, default: 2)", "Pattern 旋轉偏移（0-7 步，預設：2）", "パターンローテーションオフセット（0-7ステップ、デフォルト：2）"}});
        m.entries.push_back({"FILL", {"Active steps percentage (0-100%, default: 25%)", "活躍步進百分比（0-100%，預設：25%）", "アクティブステップ割合（0-100%、デフォルト：25%）"}});
        m.entries.push_back({"D/M", {"Clock div/mult - 0:1/2x, 1:1x, 2:1.5x, 3:2x, 4:3x (default: 1x)", "Clock 除/乘 - 0:1/2x, 1:1x, 2:1.5x, 3:2x, 4:3x（預設：1x）", "クロック除/乗 - 0:1/2x, 1:1x, 2:1.5x, 3:2x, 4:3x（デフォルト：1x）"}});
        m.entries.push_back({"NOISE", {"Noise FM modulation (0-1, default: 0.714). Pink when <0.5, blue when ≥0.5", "Noise FM 調變（0-1，預設：0.714）。<0.5 時為 Pink，≥0.5 時為 Blue", "ノイズFM変調（0-1、デフォルト：0.714）。<0.5でPink、≥0.5でBlue"}});
        m.entries.push_back({"FREQ", {"Hi-hat frequency (20Hz-20kHz, default: ~20kHz)", "Hi-hat 頻率（20Hz-20kHz，預設：~20kHz）", "ハイハット周波数（20Hz-20kHz、デフォルト：~20kHz）"}});
        m.entries.push_back({"DECAY", {"Envelope decay (0.01-2s, default: 0.131s). VCA envelope uses half this value (decayParam * 0.5).", "可變 S 曲線與 smoothstep 過渡，產生自然的衰減聲音", "エンベロープディケイ（0.01-2s、デフォルト：0.131s）。VCAエンベロープはこの値の半分を使用（decayParam * 0.5）。"}});
        m.entries.push_back({"SHAPE", {"Envelope curve (0-0.99, default: 0.055)", "Envelope 曲線（0-0.99，預設：0.055）", "エンベロープカーブ（0-0.99、デフォルト：0.055）"}});
        m.entries.push_back({"Attack", {"Fixed 1ms linear ramp", "固定 1ms 線性斜坡", "Fixed 1ms linear ramp"}});
        m.entries.push_back({"Decay", {"Variable S-curve with smoothstep transition for natural-sounding decay", "可變 S 曲線與 smoothstep 過渡，產生自然的衰減聲音", "エンベロープディケイ（0.01-2s、デフォルト：0.131s）。VCAエンベロープはこの値の半分を使用（decayParam * 0.5）。"}});
        m.entries.push_back({"Shape Control", {"Adjusts the curvature of both attack and decay segments", "Adjusts the curvature of both attack and decay segments", "Adjusts the curvature of both attack and decay segments"}});
        m.entries.push_back({"Transition", {"Smooth blending between front and back envelope curves for percussive character", "Smooth blending between front and back envelope curves for percussive character", "Smooth blending between front and back envelope curves for percussive character"}});
        m.entries.push_back({"Factor", {"3x oversampling", "3x oversampling", "3x oversampling"}});
        m.entries.push_back({"Filter", {"6-stage cascaded Second-Order Sections (SOS)", "6-stage cascaded Second-Order Sections (SOS)", "6-stage cascaded Second-Order Sections (SOS)"}});
        m.entries.push_back({"Coefficients", {"Pre-computed for 48kHz × 3 operation", "Pre-computed for 48kHz × 3 operation", "Pre-computed for 48kHz × 3 operation"}});
        m.entries.push_back({"Processing", {"Separate up/down filters for clean aliasing rejection", "Separate up/down filters for clean aliasing rejection", "Separate up/down filters for clean aliasing rejection"}});
        m.entries.push_back({"Algorithm", {"Voss-McCartney with 6 quality stages", "Voss-McCartney with 6 quality stages", "Voss-McCartney with 6 quality stages"}});
        m.entries.push_back({"Frame counting", {"XOR-based bit detection for efficient computation", "XOR-based bit detection for efficient computation", "XOR-based bit detection for efficient computation"}});
        m.entries.push_back({"Normalization", {"Scaled to appropriate output level for mixing with oscillator signal", "Scaled to appropriate output level for mixing with oscillator signal", "Scaled to appropriate output level for mixing with oscillator signal"}});
        m.entries.push_back({"CLK", {"Global clock (Schmitt trigger)", "全域 Clock（Schmitt Trigger）", "グローバルクロック（シュミットトリガー）"}});
        m.entries.push_back({"RESET", {"Reset sequence to step 0", "Reset Trigger 輸入，重置所有軌道至步進 0。", "リセットトリガー入力、全トラックをステップ0にリセット。"}});
        m.entries.push_back({"Manual Reset", {"Button to manually reset sequence (same as RESET input)", "手動重置按鈕。", "手動リセットボタン。"}});
        m.entries.push_back({"D.F", {"Drum frequency CV (±10V, 1V/oct)", "Drum 頻率 CV（±10V，1V/oct）", "ドラム周波数CV（±10V、1V/oct）"}});
        m.entries.push_back({"D.D", {"Drum decay CV (±10V, scaled /10)", "Drum Decay CV（±10V，縮放 /10）", "ドラムディケイCV（±10V、スケール/10）"}});
        m.entries.push_back({"H.F", {"Hats frequency CV (±10V, 1V/oct)", "Hats 頻率 CV（±10V，1V/oct）", "ハット周波数CV（±10V、1V/oct）"}});
        m.entries.push_back({"H.D", {"Hats decay CV (±10V, scaled /10)", "Hats Decay CV（±10V，縮放 /10）", "ハットディケイCV（±10V、スケール/10）"}});
        m.entries.push_back({"TRACK 1", {"Drum audio (scaled by envelope × mainVCA × 1.4)", "Drum Audio（由 Envelope × mainVCA × 1.4 縮放）", "ドラムオーディオ（Envelope × mainVCA × 1.4でスケール）"}});
        m.entries.push_back({"TRACK 2", {"Hi-hat audio (scaled by envelope × 0.7)", "Hi-hat Audio（由 Envelope × 0.7 縮放）", "ハイハットオーディオ（Envelope × 0.7でスケール）"}});
        m.entries.push_back({"VCA ENV", {"Main accent envelope (0-10V)", "主 Accent Envelope（0-10V）", "メインアクセントエンベロープ（0-10V）"}});
        m.entries.push_back({"DRUM FM ENV", {"Track 1 FM envelope (0-10V)", "Track 1 FM Envelope（0-10V）", "トラック1 FMエンベロープ（0-10V）"}});
        m.entries.push_back({"HATS VCA ENV", {"Track 2 VCA envelope (0-10V)", "Track 2 VCA Envelope（0-10V）", "トラック2 VCAエンベロープ（0-10V）"}});
        m.entries.push_back({"Global Clock", {"Clock input (Schmitt trigger, 0.1V/2V thresholds).", "Clock 輸入（Schmitt Trigger，閾值 0.1V/2V）。", "クロック入力（シュミットトリガー、閾値0.1V/2V）。"}});
        m.entries.push_back({"Reset", {"Reset trigger input, resets all tracks to step 0.", "Reset Trigger 輸入，重置所有軌道至步進 0。", "リセットトリガー入力、全トラックをステップ0にリセット。"}});
        m.entries.push_back({"Global Length", {"Sequence length (1-32 steps, default: 32, snap).", "序列長度（1-32 步，預設：32，Snap）。", "シーケンス長（1-32ステップ、デフォルト：32、スナップ）。"}});
        m.entries.push_back({"Manual Reset", {"Button to manually reset all tracks.", "手動重置按鈕。", "手動リセットボタン。"}});
        m.entries.push_back({"Track 1 Fill", {"Drum track rhythm density (0-100%, default: 84.5%).", "Drum 軌道節奏密度（0-100%，預設：84.5%）。", "ドラムトラックリズム密度（0-100%、デフォルト：84.5%）。"}});
        m.entries.push_back({"Track 1 Frequency", {"Drum oscillator frequency (20Hz-20kHz logarithmic, default: ~36.7Hz).", "Drum Oscillator 頻率（20Hz-20kHz 對數，預設：~36.7Hz）。", "ドラムオシレーター周波数（20Hz-20kHz対数、デフォルト：~36.7Hz）。"}});
        m.entries.push_back({"Track 1 FM Amount", {"FM modulation depth (0-1, default: 0.65).", "FM 調變深度（0-1，預設：0.65）。", "FM変調深度（0-1、デフォルト：0.65）。"}});
        m.entries.push_back({"Track 1 Noise Mix", {"Pink/blue noise mix (0-1, default: 0.152).", "Pink/Blue Noise 混合（0-1，預設：0.152）。", "Pink/Blueノイズミックス（0-1、デフォルト：0.152）。"}});
        m.entries.push_back({"VCA Shift", {"Quarter-note accent shift (1-7 steps, default: 1, snap).", "四分音符 Accent Shift（1-7 步，預設：1，Snap）。", "四分音符アクセントシフト（1-7ステップ、デフォルト：1、スナップ）。"}});
        m.entries.push_back({"VCA Decay", {"Main VCA envelope decay time (0.01-2s, default: 0.396s).", "主 VCA Envelope Decay 時間（0.01-2s，預設：0.396s）。", "メインVCAエンベロープディケイ時間（0.01-2s、デフォルト：0.396s）。"}});
        m.entries.push_back({"Track 1 Decay", {"Drum envelope decay (0.01-2s, default: 0.720s).", "Drum Envelope Decay（0.01-2s，預設：0.720s）。", "ドラムエンベロープディケイ（0.01-2s、デフォルト：0.720s）。"}});
        m.entries.push_back({"Track 1 Shape", {"Drum envelope curve shape (0-0.99, default: 0).", "Drum Envelope 曲線形狀（0-0.99，預設：0）。", "ドラムエンベロープカーブ形状（0-0.99、デフォルト：0）。"}});
        m.entries.push_back({"Track 2 Shift", {"HATs pattern rotation offset (0-7 steps, default: 2, snap).", "HATs Pattern 旋轉偏移（0-7 步，預設：2，Snap）。", "HATsパターンローテーションオフセット（0-7ステップ、デフォルト：2、スナップ）。"}});
        m.entries.push_back({"Track 2 Fill", {"HATs rhythm density (0-100%, default: 25%).", "HATs 節奏密度（0-100%，預設：25%）。", "HATsリズム密度（0-100%、デフォルト：25%）。"}});
        m.entries.push_back({"Track 2 Div/Mult", {"HATs clock div/mult (0-4, default: 1x, snap).", "HATs Clock Div/Mult（0-4，預設：1x，Snap）。", "HATsクロックDiv/Mult（0-4、デフォルト：1x、スナップ）。"}});
        m.entries.push_back({"Track 2 Frequency", {"Hi-hat oscillator frequency (20Hz-20kHz, default: ~20kHz).", "Hi-hat Oscillator 頻率（20Hz-20kHz，預設：~20kHz）。", "ハイハットオシレーター周波数（20Hz-20kHz、デフォルト：~20kHz）。"}});
        m.entries.push_back({"Track 2 Decay", {"HATs envelope decay (0.01-2s, default: 0.131s).", "HATs Envelope Decay（0.01-2s，預設：0.131s）。", "HATsエンベロープディケイ（0.01-2s、デフォルト：0.131s）。"}});
        m.entries.push_back({"Track 2 Shape", {"HATs envelope curve (0-0.99, default: 0.055).", "HATs Envelope 曲線（0-0.99，預設：0.055）。", "HATsエンベロープカーブ（0-0.99、デフォルト：0.055）。"}});
        m.entries.push_back({"Track 2 Noise FM", {"HATs noise FM amount (0-1, default: 0.714).", "HATs Noise FM 量（0-1，預設：0.714）。", "HATsノイズFM量（0-1、デフォルト：0.714）。"}});
        m.entries.push_back({"Drum Frequency CV", {"Drum frequency CV input (1V/oct).", "Drum 頻率 CV 輸入（1V/oct）。", "ドラム周波数CV入力（1V/oct）。"}});
        m.entries.push_back({"Drum Decay CV", {"Drum decay CV input (scaled /10).", "Drum Decay CV 輸入（縮放 /10）。", "ドラムディケイCV入力（スケール/10）。"}});
        m.entries.push_back({"Hats Frequency CV", {"Hats frequency CV input (1V/oct).", "Hats 頻率 CV 輸入（1V/oct）。", "ハット周波数CV入力（1V/oct）。"}});
        m.entries.push_back({"Hats Decay CV", {"Hats decay CV input (scaled /10).", "Hats Decay CV 輸入（縮放 /10）。", "ハットディケイCV入力（スケール/10）。"}});
        m.entries.push_back({"Track 1 Audio", {"Drum audio output (envelope x mainVCA x 1.4).", "Drum Audio 輸出（Envelope x mainVCA x 1.4）。", "ドラムオーディオ出力（Envelope x mainVCA x 1.4）。"}});
        m.entries.push_back({"Track 2 Audio", {"Hi-hat audio output (envelope x 0.7).", "Hi-hat Audio 輸出（Envelope x 0.7）。", "ハイハットオーディオ出力（Envelope x 0.7）。"}});
        m.entries.push_back({"Accent VCA Envelope", {"Main accent VCA envelope output (0-10V).", "主 Accent VCA Envelope 輸出（0-10V）。", "メインアクセントVCAエンベロープ出力（0-10V）。"}});
        m.entries.push_back({"Track 1 FM Envelope", {"Track 1 FM envelope output (0-10V).", "Track 1 FM Envelope 輸出（0-10V）。", "トラック1 FMエンベロープ出力（0-10V）。"}});
        m.entries.push_back({"Track 2 VCA Envelope", {"Track 2 VCA envelope output (0-10V).", "Track 2 VCA Envelope 輸出（0-10V）。", "トラック2 VCAエンベロープ出力（0-10V）。"}});
        data["TWNC"] = std::move(m);
    }
    // TWNC2 (50 entries)
    {
        ModuleHelpData m;
        m.name = "TWNC 2";
        m.description = {"Three-track drum voice module with BD/SN/HH synthesis, 10-bit quantization, and sidechain ducking", "三軌 Drum Voice 模組，具備 BD/SN/HH 合成、10-bit 量化與 Sidechain Ducking", "BD/SN/HH合成、10ビット量子化、サイドチェーンダッキングを備えた3トラックドラムボイスモジュール"};
        m.entries.push_back({"Kick Volume", {"Output volume (0-1, default: 1.0)", "輸出音量（0-1，預設：1.0）", "出力音量（0-1、デフォルト：1.0）"}});
        m.entries.push_back({"Kick Envelope CV", {"Envelope CV input (0-10V). VCA uses square root response for punchy character", "Envelope CV 輸入（0-10V）。VCA 使用平方根響應提供沖擊感", "エンベロープCV入力（0-10V）。VCAは平方根レスポンスでパンチのあるキャラクター"}});
        m.entries.push_back({"Kick Accent CV", {"Accent CV input (0-10V). Also drives sidechain ducking", "Accent CV 輸入（0-10V）。同時驅動 Sidechain Ducking", "アクセントCV入力（0-10V）。サイドチェーンダッキングも駆動"}});
        m.entries.push_back({"Kick Frequency", {"Frequency (24-500Hz logarithmic, default: ~24Hz)", "頻率（24-500Hz 對數，預設：~24Hz）", "周波数（24-500Hz対数、デフォルト：~24Hz）"}});
        m.entries.push_back({"Kick FM Amount", {"FM amount (0-1, default: 0.157). Internally scaled 20x. Envelope squared for pitch sweep", "FM 量（0-1，預設：0.157）。內部縮放 20 倍。Envelope 平方用於音高掃描", "FM量（0-1、デフォルト：0.157）。内部で20倍スケール。ピッチスイープ用にエンベロープを二乗"}});
        m.entries.push_back({"Kick Punch Amount", {"Saturation amount (0-1, default: 0.168). Soft-clipping saturation for warm distortion", "飽和量（0-1，預設：0.168）。軟削波飽和產生溫暖失真", "サチュレーション量（0-1、デフォルト：0.168）。ソフトクリッピングサチュレーションで温かい歪み"}});
        m.entries.push_back({"Kick Frequency CV", {"Frequency CV input (±10V, scaled /10)", "頻率 CV 輸入（±10V，縮放 /10）", "周波数CV入力（±10V、スケール/10）"}});
        m.entries.push_back({"Kick FM CV", {"FM CV input (±10V, scaled /10)", "FM CV 輸入（±10V，縮放 /10）", "FM CV入力（±10V、スケール/10）"}});
        m.entries.push_back({"Kick Punch CV", {"Punch CV input (±10V, scaled /10)", "Punch CV 輸入（±10V，縮放 /10）", "パンチCV入力（±10V、スケール/10）"}});
        m.entries.push_back({"Snare Volume", {"Output volume (0-1, default: 1.0)", "輸出音量（0-1，預設：1.0）", "出力音量（0-1、デフォルト：1.0）"}});
        m.entries.push_back({"Snare Envelope CV", {"Envelope CV input (0-10V). VCA uses square root response", "Envelope CV 輸入（0-10V）。VCA 使用平方根響應", "エンベロープCV入力（0-10V）。VCAは平方根レスポンス使用"}});
        m.entries.push_back({"Snare Frequency", {"Body frequency (100-300Hz logarithmic, default: ~120Hz)", "Body 頻率（100-300Hz 對數，預設：~120Hz）", "ボディ周波数（100-300Hz対数、デフォルト：~120Hz）"}});
        m.entries.push_back({"Snare Noise Tone", {"Noise filter tone (0-1, default: 0.717). Filter freq = bodyFreq*5 + tone*5000 + envCV*2000", "Noise Filter 音色（0-1，預設：0.717）。Filter 頻率 = bodyFreq*5 + tone*5000 + envCV*2000", "ノイズフィルター音色（0-1、デフォルト：0.717）。フィルター周波数 = bodyFreq*5 + tone*5000 + envCV*2000"}});
        m.entries.push_back({"Snare Noise Mix", {"Body/noise crossfade (0-1, default: 0.288). CV modulation available", "Body/Noise Crossfade（0-1，預設：0.288）。可接 CV 調變", "ボディ/ノイズクロスフェード（0-1、デフォルト：0.288）。CV変調可能"}});
        m.entries.push_back({"Snare Noise Mix CV", {"Noise mix CV input (±10V, scaled /10)", "Noise Mix CV 輸入（±10V，縮放 /10）", "ノイズミックスCV入力（±10V、スケール/10）"}});
        m.entries.push_back({"Body", {"Sine oscillator at 75% amplitude", "Sine Oscillator，振幅 75%", "Sine oscillator at 75% amplitude"}});
        m.entries.push_back({"Noise", {"White noise through 2-pole biquad bandpass, 4x gain boost", "White Noise 通過 2-pole Biquad Bandpass，4 倍增益提升", "White noise through 2-pole biquad bandpass, 4x gain boost"}});
        m.entries.push_back({"Type", {"2-pole biquad bandpass with optional 3rd pole for high Q", "2-pole biquad bandpass with optional 3rd pole for high Q", "2-pole biquad bandpass with optional 3rd pole for high Q"}});
        m.entries.push_back({"Q behavior", {"At high Q values, additional filtering stage blends in for sharper resonance", "At high Q values, additional filtering stage blends in for sharper resonance", "At high Q values, additional filtering stage blends in for sharper resonance"}});
        m.entries.push_back({"Frequency range", {"20Hz to 45% of sample rate", "20Hz to 45% of sample rate", "20Hz to 45% of sample rate"}});
        m.entries.push_back({"Hats Volume", {"Output volume (0-1, default: 1.0)", "輸出音量（0-1，預設：1.0）", "出力音量（0-1、デフォルト：1.0）"}});
        m.entries.push_back({"Hats Envelope CV", {"Envelope CV input (0-10V)", "Envelope CV 輸入（0-10V）", "エンベロープCV入力（0-10V）"}});
        m.entries.push_back({"Hats Tone", {"Base frequency (0-1, default: 0.965). Range: 1000-5500 Hz", "基礎頻率（0-1，預設：0.965）。範圍：1000-5500 Hz", "基本周波数（0-1、デフォルト：0.965）。範囲：1000-5500 Hz"}});
        m.entries.push_back({"Hats Decay", {"Envelope decay shape (0-1, default: 0). Higher values add noise component (noiseAmount = decay * 0.8). CV input available via Hats Decay CV (±10V, scaled /10)", "Envelope Decay 形狀（0-1，預設：0）。較高值加入 Noise 成分（noiseAmount = decay * 0.8）。CV 輸入透過 Hats Decay CV 可用（±10V，縮放 /10）", "エンベロープディケイ形状（0-1、デフォルト：0）。高い値でノイズ成分追加（noiseAmount = decay * 0.8）。Hats Decay CV経由でCV入力対応（±10V、スケール/10）"}});
        m.entries.push_back({"Hats Decay CV", {"Decay CV input (±10V, scaled /10)", "Decay CV 輸入（±10V，縮放 /10）", "ディケイCV入力（±10V、スケール/10）"}});
        m.entries.push_back({"VCA curve", {"Variable decay exponent (2.0 - decay*1.5), creating sharper/softer envelope shapes", "Variable decay exponent (2.0 - decay*1.5), creating sharper/softer envelope shapes", "Variable decay exponent (2.0 - decay*1.5), creating sharper/softer envelope shapes"}});
        m.entries.push_back({"Engine", {"6 detuned triangle oscillators (FM synthesis approach)", "6 detuned triangle oscillators (FM synthesis approach)", "6 detuned triangle oscillators (FM synthesis approach)"}});
        m.entries.push_back({"Frequency offsets", {"+100, +250, +400, +550, +600, +1000 Hz from base", "+100, +250, +400, +550, +600, +1000 Hz from base", "+100, +250, +400, +550, +600, +1000 Hz from base"}});
        m.entries.push_back({"Resolution", {"1024 levels (10-bit)", "1024 levels (10-bit)", "1024 levels (10-bit)"}});
        m.entries.push_back({"Behavior", {"Signal rounded to discrete amplitude levels", "Signal rounded to discrete amplitude levels", "Signal rounded to discrete amplitude levels"}});
        m.entries.push_back({"Applied to", {"BD, SN, HH individual outputs and mix", "BD, SN, HH individual outputs and mix", "BD, SN, HH individual outputs and mix"}});
        m.entries.push_back({"Character", {"Adds subtle lo-fi grit reminiscent of vintage samplers", "Adds subtle lo-fi grit reminiscent of vintage samplers", "Adds subtle lo-fi grit reminiscent of vintage samplers"}});
        m.entries.push_back({"Duck Amount", {"Ducking amount (0-1, default: 0)", "Ducking 量（0-1，預設：0）", "ダッキング量（0-1、デフォルト：0）"}});
        m.entries.push_back({"Source", {"Kick accent CV input", "Kick accent CV input", "Kick accent CV input"}});
        m.entries.push_back({"Behavior", {"Kick accent attenuates other signals based on duck amount (internally scaled 3x: sidechainCV = 1 - accentCV * duckAmount * 3)", "Kick accent attenuates other signals based on duck amount (internally scaled 3x: sidechainCV = 1 - accentCV * duckAmount * 3)", "Kick accent attenuates other signals based on duck amount (internally scaled 3x: sidechainCV = 1 - accentCV * duckAmount * 3)"}});
        m.entries.push_back({"Affects", {"Snare (full ducking via 0.02 + sidechain*0.98), external input (full ducking), hi-hat (reduced: 0.8 + sidechain*0.2)", "Snare (full ducking via 0.02 + sidechain*0.98), external input (full ducking), hi-hat (reduced: 0.8 + sidechain*0.2)", "Snare (full ducking via 0.02 + sidechain*0.98), external input (full ducking), hi-hat (reduced: 0.8 + sidechain*0.2)"}});
        m.entries.push_back({"Max delay", {"1440 samples (~30ms @ 48kHz)", "1440 samples (~30ms @ 48kHz)", "1440 samples (~30ms @ 48kHz)"}});
        m.entries.push_back({"HH spread", {"Fixed 20ms delay on right channel", "右聲道固定 20ms Delay", "Fixed 20ms delay on right channel"}});
        m.entries.push_back({"Implementation", {"Circular buffer delay line", "Circular buffer delay line", "Circular buffer delay line"}});
        m.entries.push_back({"Threshold", {"±5V", "±5V", "±5V"}});
        m.entries.push_back({"Behavior", {"Smooth saturation above threshold for warm limiting", "Smooth saturation above threshold for warm limiting", "Smooth saturation above threshold for warm limiting"}});
        m.entries.push_back({"Applied to", {"L/R mix outputs only", "L/R mix outputs only", "L/R mix outputs only"}});
        m.entries.push_back({"External Input", {"External audio input, affected by sidechain ducking", "外部音訊輸入，受 Sidechain Ducking 影響", "外部オーディオ入力、サイドチェーンダッキングの影響を受ける"}});
        m.entries.push_back({"Kick Audio", {"Bass drum output (10-bit quantized)", "Bass Drum 輸出（10-bit 量化）", "バスドラム出力（10ビット量子化）"}});
        m.entries.push_back({"Snare Audio", {"Snare drum output (10-bit quantized)", "Snare Drum 輸出（10-bit 量化）", "スネアドラム出力（10ビット量子化）"}});
        m.entries.push_back({"Hats Audio 1", {"Hi-hat output (10-bit quantized)", "Hi-hat 輸出（10-bit 量化）", "ハイハット出力（10ビット量子化）"}});
        m.entries.push_back({"Mix Output L", {"Mix with HH direct", "Mix 含 HH 直接訊號", "HH直接信号入りミックス"}});
        m.entries.push_back({"Mix Output R", {"Mix with HH delayed 20ms", "Mix 含 HH 延遲 20ms", "HH 20msディレイ入りミックス"}});
        m.entries.push_back({"TWNC", {"Provides Euclidean rhythm envelopes via VCA ENV, DRUM FM ENV, HATS VCA ENV outputs", "透過 VCA ENV、DRUM FM ENV、HATS VCA ENV 輸出提供 Euclidean Rhythm Envelope", "VCA ENV、DRUM FM ENV、HATS VCA ENV出力経由でユークリッドリズムエンベロープを提供"}});
        m.entries.push_back({"TWNC Light", {"Provides envelope outputs in 4HP form factor", "提供 4HP 形式的 Envelope 輸出", "4HPフォームファクターでエンベロープ出力を提供"}});
        data["TWNC2"] = std::move(m);
    }
    // TWNCLight (53 entries)
    {
        ModuleHelpData m;
        m.name = "TWNC Light";
        m.description = {"Streamlined dual-track Euclidean rhythm generator with envelope outputs, ideal for minimal setups", "精簡雙軌 Euclidean Rhythm 生成器，具備 Envelope 輸出，適合極簡設置", "エンベロープ出力を備えたコンパクトデュアルトラックユークリッドリズム生成器、ミニマルセットアップに最適"};
        m.entries.push_back({"Distribution", {"Bjorklund via floor-based spacing, evenly distributing fills across pattern length. Euclidean shift is always 0 (rotation not applied to pattern).", "Bjorklund via floor-based spacing, evenly distributing fills across pattern length. Euclidean shift is always 0 (rotation not applied to pattern).", "Bjorklund via floor-based spacing, evenly distributing fills across pattern length. Euclidean shift is always 0 (rotation not applied to pattern)."}});
        m.entries.push_back({"Auto-reset", {"Sequence resets every 32 clock pulses", "Sequence resets every 32 clock pulses", "Sequence resets every 32 clock pulses"}});
        m.entries.push_back({"Length", {"1-32 steps (default: 16)", "1-32 steps (default: 16)", "1-32 steps (default: 16)"}});
        m.entries.push_back({"CLK", {"Clock input (Schmitt trigger, 0.1V low / 2V high threshold)", "Clock 輸入（Schmitt Trigger，0.1V 低 / 2V 高閾值）", "クロック入力（シュミットトリガー、0.1V低/2V高閾値）"}});
        m.entries.push_back({"LEN", {"Sequence length (1-32 steps, default: 16, snap)", "序列長度（1-32 步，預設：16，Snap）", "シーケンス長（1-32ステップ、デフォルト：16、スナップ）"}});
        m.entries.push_back({"FILL", {"Rhythm density (0-100%, default: 66.6%)", "節奏密度（0-100%，預設：100%）", "リズム密度（0-100%、デフォルト：100%）"}});
        m.entries.push_back({"ACCNT", {"Quarter-note accent shift (1-7 steps, default: 1). Triggers main VCA envelope. Accent fires when clock step matches (shift-1) mod 4. Track 1 always runs at 1x clock (no div/mult).", "四分音符 Accent Shift（1-7 步，預設：1）。觸發主 VCA Envelope。Accent 在 Clock Step 匹配 (shift-1) mod 4 時觸發。Track 1 固定以 1x Clock 運行（無 Div/Mult）。", "四分音符アクセントシフト（1-7ステップ、デフォルト：1）。メインVCAエンベロープをトリガー。アクセントはクロックステップが(shift-1) mod 4に一致した時に発火。Track 1は常に1xクロックで動作（除/乗なし）。"}});
        m.entries.push_back({"A.DEC", {"Accent envelope decay (0.01-2s, default: 0.549s)", "Accent Envelope Decay（0.01-2s，預設：0.549s）", "アクセントエンベロープディケイ（0.01-2s、デフォルト：0.549s）"}});
        m.entries.push_back({"DECAY", {"Track 1 envelope decay (0.01-2s, default: 0.3s). FM envelope uses half this value (decayParam * 0.5), VCA envelope uses full value.", "可變 S 曲線與 Smoothstep 過渡（與 TWNC 相同）", "エンベロープディケイ（0.01-2s、デフォルト：0.094s）。VCAエンベロープはこの値の半分（decayParam * 0.5）を使用。"}});
        m.entries.push_back({"SHAPE", {"Envelope curve (0-0.99, default: 0.5)", "Envelope 曲線（0-0.99，預設：0.5）", "エンベロープカーブ（0-0.99、デフォルト：0.5）"}});
        m.entries.push_back({"D.D", {"Decay CV input (±10V, scaled /10)", "Decay CV 輸入（±10V，縮放 /10）", "ディケイCV入力（±10V、スケール/10）"}});
        m.entries.push_back({"FILL", {"Rhythm density (0-100%, default: 100%)", "節奏密度（0-100%，預設：100%）", "リズム密度（0-100%、デフォルト：100%）"}});
        m.entries.push_back({"SHIFT", {"Clock delay from accent (1-4 steps, default: 3). Delays HATs start relative to accent", "相對 Accent 的 Clock Delay（1-4 步，預設：3）。延遲 HATs 相對於 Accent 的起始", "アクセントからのクロックディレイ（1-4ステップ、デフォルト：3）。アクセントに対してHATs開始を遅延"}});
        m.entries.push_back({"D/M", {"Clock div/mult (-3 to +3, default: -3 = 1/4x)", "Clock Div/Mult（-3 到 +3，預設：-3 = 1/4x）", "クロック除/乗（-3から+3、デフォルト：-3 = 1/4x）"}});
        m.entries.push_back({"DECAY", {"Envelope decay (0.01-2s, default: 0.094s). VCA envelope uses half this value (decayParam * 0.5).", "可變 S 曲線與 Smoothstep 過渡（與 TWNC 相同）", "エンベロープディケイ（0.01-2s、デフォルト：0.094s）。VCAエンベロープはこの値の半分（decayParam * 0.5）を使用。"}});
        m.entries.push_back({"SHAPE", {"Envelope curve (0-0.99, default: 0.5)", "Envelope 曲線（0-0.99，預設：0.5）", "エンベロープカーブ（0-0.99、デフォルト：0.5）"}});
        m.entries.push_back({"H.D", {"Decay CV input (±10V, scaled /10)", "Decay CV 輸入（±10V，縮放 /10）", "ディケイCV入力（±10V、スケール/10）"}});
        m.entries.push_back({"Attack", {"Fixed 1ms linear ramp", "固定 1ms 線性斜坡", "Fixed 1ms linear ramp"}});
        m.entries.push_back({"Decay", {"Variable S-curve with smoothstep transition (same as TWNC)", "可變 S 曲線與 Smoothstep 過渡（與 TWNC 相同）", "エンベロープディケイ（0.01-2s、デフォルト：0.094s）。VCAエンベロープはこの値の半分（decayParam * 0.5）を使用。"}});
        m.entries.push_back({"Shape calculation", {"frontK ranges -0.9 to -0.4, backK ranges -1.0 to 0.6 based on shape parameter", "frontK ranges -0.9 to -0.4, backK ranges -1.0 to 0.6 based on shape parameter", "frontK ranges -0.9 to -0.4, backK ranges -1.0 to 0.6 based on shape parameter"}});
        m.entries.push_back({"Output formula", {"Weighted interpolation producing S-curve decay characteristic", "Weighted interpolation producing S-curve decay characteristic", "Weighted interpolation producing S-curve decay characteristic"}});
        m.entries.push_back({"Trigger pulse", {"30ms pulse for retriggering", "30ms Pulse 用於 Retriggering", "30ms pulse for retriggering"}});
        m.entries.push_back({"Division", {"Counts input clocks, triggers on Nth", "計數輸入 Clock，在第 N 個時觸發", "Counts input clocks, triggers on Nth"}});
        m.entries.push_back({"Multiplication", {"Generates N triggers per input clock period", "每個輸入 Clock 週期生成 N 個 Trigger", "Generates N triggers per input clock period"}});
        m.entries.push_back({"Gate width", {"50% of multiplied clock period (minimum 1ms)", "50% of multiplied clock period (minimum 1ms)", "50% of multiplied clock period (minimum 1ms)"}});
        m.entries.push_back({"Combined", {"Division applied first, then multiplication", "Division applied first, then multiplication", "Division applied first, then multiplication"}});
        m.entries.push_back({"SHIFT parameter", {"Delays HATs track start by N clock pulses after accent trigger", "Delays HATs track start by N clock pulses after accent trigger", "Delays HATs track start by N clock pulses after accent trigger"}});
        m.entries.push_back({"Behavior", {"When SHIFT=1, HATs starts immediately with accent. When SHIFT>1, waits SHIFT-1 clocks", "When SHIFT=1, HATs starts immediately with accent. When SHIFT>1, waits SHIFT-1 clocks", "When SHIFT=1, HATs starts immediately with accent. When SHIFT>1, waits SHIFT-1 clocks"}});
        m.entries.push_back({"Reset", {"Delay counter resets when SHIFT parameter changes", "Delay counter resets when SHIFT parameter changes", "Delay counter resets when SHIFT parameter changes"}});
        m.entries.push_back({"ENVs (top)", {"Main accent VCA envelope (0-10V)", "Main accent VCA envelope (0-10V)", "Main accent VCA envelope (0-10V)"}});
        m.entries.push_back({"ENVs (bottom-left)", {"Track 1 FM envelope (0-10V)", "Track 1 FM envelope (0-10V)", "Track 1 FM envelope (0-10V)"}});
        m.entries.push_back({"ENVs (bottom-right)", {"Track 2 VCA envelope (0-10V)", "Track 2 VCA envelope (0-10V)", "Track 2 VCA envelope (0-10V)"}});
        m.entries.push_back({"TWNC 2", {"Receives envelope outputs to drive BD/SN/HH synthesis", "接收 Envelope 輸出以驅動 BD/SN/HH 合成", "エンベロープ出力を受信してBD/SN/HH合成を駆動"}});
        m.entries.push_back({"Usage", {"Connect ENVs outputs to TWNC 2 ENV inputs for complete drum machine", "Connect ENVs outputs to TWNC 2 ENV inputs for complete drum machine", "Connect ENVs outputs to TWNC 2 ENV inputs for complete drum machine"}});
        m.entries.push_back({"Global Clock", {"Clock input (Schmitt trigger, 0.1V/2V thresholds).", "Clock 輸入（Schmitt Trigger，閾值 0.1V/2V）。", "クロック入力（シュミットトリガー、閾値0.1V/2V）。"}});
        m.entries.push_back({"Global Length", {"Sequence length (1-32 steps, default: 16, snap).", "序列長度（1-32 步，預設：16，Snap）。", "シーケンス長（1-32ステップ、デフォルト：16、スナップ）。"}});
        m.entries.push_back({"Track 1 Fill", {"Drum track rhythm density (0-100%, default: 66.6%).", "Drum 軌道節奏密度（0-100%，預設：66.6%）。", "ドラムトラックリズム密度（0-100%、デフォルト：66.6%）。"}});
        m.entries.push_back({"VCA Shift", {"Quarter-note accent shift (1-7 steps, default: 1, snap).", "四分音符 Accent Shift（1-7 步，預設：1，Snap）。", "四分音符アクセントシフト（1-7ステップ、デフォルト：1、スナップ）。"}});
        m.entries.push_back({"VCA Decay", {"Accent envelope decay time (0.01-2s, default: 0.549s).", "Accent Envelope Decay 時間（0.01-2s，預設：0.549s）。", "アクセントエンベロープディケイ時間（0.01-2s、デフォルト：0.549s）。"}});
        m.entries.push_back({"Track 1 Decay", {"Drum envelope decay (0.01-2s, default: 0.3s).", "Drum Envelope Decay（0.01-2s，預設：0.3s）。", "ドラムエンベロープディケイ（0.01-2s、デフォルト：0.3s）。"}});
        m.entries.push_back({"Track 1 Shape", {"Drum envelope curve (0-0.99, default: 0.5).", "Drum Envelope 曲線（0-0.99，預設：0.5）。", "ドラムエンベロープカーブ（0-0.99、デフォルト：0.5）。"}});
        m.entries.push_back({"Track 2 Fill", {"HATs rhythm density (0-100%, default: 100%).", "HATs 節奏密度（0-100%，預設：100%）。", "HATsリズム密度（0-100%、デフォルト：100%）。"}});
        m.entries.push_back({"Track 2 Shift", {"HATs clock delay from accent (1-4 steps, default: 3, snap).", "HATs 相對 Accent 的 Clock Delay（1-4 步，預設：3，Snap）。", "HATsアクセントからのクロックディレイ（1-4ステップ、デフォルト：3、スナップ）。"}});
        m.entries.push_back({"Track 2 Div/Mult", {"HATs clock div/mult (-3 to +3, default: -3 = 1/4x, snap).", "HATs Clock Div/Mult（-3 到 +3，預設：-3 = 1/4x，Snap）。", "HATsクロックDiv/Mult（-3から+3、デフォルト：-3 = 1/4x、スナップ）。"}});
        m.entries.push_back({"Track 2 Decay", {"HATs envelope decay (0.01-2s, default: 0.094s).", "HATs Envelope Decay（0.01-2s，預設：0.094s）。", "HATsエンベロープディケイ（0.01-2s、デフォルト：0.094s）。"}});
        m.entries.push_back({"Track 2 Shape", {"HATs envelope curve (0-0.99, default: 0.5).", "HATs Envelope 曲線（0-0.99，預設：0.5）。", "HATsエンベロープカーブ（0-0.99、デフォルト：0.5）。"}});
        m.entries.push_back({"Drum Frequency CV", {"Drum frequency CV input.", "Drum 頻率 CV 輸入。", "ドラム周波数CV入力。"}});
        m.entries.push_back({"Drum Decay CV", {"Drum decay CV input (scaled /10).", "Drum Decay CV 輸入（縮放 /10）。", "ドラムディケイCV入力（スケール/10）。"}});
        m.entries.push_back({"Hats Frequency CV", {"Hats frequency CV input.", "Hats 頻率 CV 輸入。", "ハット周波数CV入力。"}});
        m.entries.push_back({"Hats Decay CV", {"Hats decay CV input (scaled /10).", "Hats Decay CV 輸入（縮放 /10）。", "ハットディケイCV入力（スケール/10）。"}});
        m.entries.push_back({"Accent VCA Envelope", {"Main accent VCA envelope output (0-10V).", "主 Accent VCA Envelope 輸出（0-10V）。", "メインアクセントVCAエンベロープ出力（0-10V）。"}});
        m.entries.push_back({"Track 1 FM Envelope", {"Track 1 FM envelope output (0-10V).", "Track 1 FM Envelope 輸出（0-10V）。", "トラック1 FMエンベロープ出力（0-10V）。"}});
        m.entries.push_back({"Track 2 VCA Envelope", {"Track 2 VCA envelope output (0-10V).", "Track 2 VCA Envelope 輸出（0-10V）。", "トラック2 VCAエンベロープ出力（0-10V）。"}});
        data["TWNCLight"] = std::move(m);
    }
    // U8 (78 entries)
    {
        ModuleHelpData m;
        m.name = "U8";
        m.description = {"Stereo channel strip with automatic mono-to-stereo conversion, sidechain ducking, and chain bus integration", "Stereo Channel Strip，具備自動 Mono-to-Stereo 轉換、Sidechain Ducking 與 Chain Bus 整合", "自動モノ→ステレオ変換、サイドチェインダッキング、チェーンバス統合を備えたステレオチャンネルストリップ"};
        m.entries.push_back({"Detection", {"Automatic when only LEFT input is connected (RIGHT disconnected)", "Automatic when only LEFT input is connected (RIGHT disconnected)", "Automatic when only LEFT input is connected (RIGHT disconnected)"}});
        m.entries.push_back({"Delay Amount", {"Fixed 20ms delay on right channel", "Fixed 20ms delay on right channel", "Fixed 20ms delay on right channel"}});
        m.entries.push_back({"Buffer Size", {"2048 samples per polyphonic channel", "2048 samples per polyphonic channel", "2048 samples per polyphonic channel"}});
        m.entries.push_back({"Sample Calculation", {"Delay samples based on 20ms at current sample rate", "Delay samples based on 20ms at current sample rate", "Delay samples based on 20ms at current sample rate"}});
        m.entries.push_back({"Buffer Implementation", {"Circular buffer with read/write pointers", "Circular buffer with read/write pointers", "Circular buffer with read/write pointers"}});
        m.entries.push_back({"Purpose", {"Creates stereo width from mono source via Haas effect", "Creates stereo width from mono source via Haas effect", "Creates stereo width from mono source via Haas effect"}});
        m.entries.push_back({"When Stereo", {"Both inputs connected, delay is bypassed, signals pass through directly", "Both inputs connected, delay is bypassed, signals pass through directly", "Both inputs connected, delay is bypassed, signals pass through directly"}});
        m.entries.push_back({"Automatic Chain Connection", {"When U8 modules are placed adjacent to each other, chain cables are automatically created", "When U8 modules are placed adjacent to each other, chain cables are automatically created", "When U8 modules are placed adjacent to each other, chain cables are automatically created"}});
        m.entries.push_back({"Cross-Module Support", {"Works with U8, YAMANOTE, ALEXANDERPLATZ, and SHINJUKU modules on the right side", "Works with U8, YAMANOTE, ALEXANDERPLATZ, and SHINJUKU modules on the right side", "Works with U8, YAMANOTE, ALEXANDERPLATZ, and SHINJUKU modules on the right side"}});
        m.entries.push_back({"Cable Color", {"Yellow (#FFCC00) matching the Berlin U-Bahn U8 train color", "Yellow (#FFCC00) matching the Berlin U-Bahn U8 train color", "Yellow (#FFCC00) matching the Berlin U-Bahn U8 train color"}});
        m.entries.push_back({"Non-Destructive", {"Auto cables are only created if the target input is not already connected", "Auto cables are only created if the target input is not already connected", "Auto cables are only created if the target input is not already connected"}});
        m.entries.push_back({"Dynamic", {"Cables automatically update when modules are moved or removed", "Cables automatically update when modules are moved or removed", "Cables automatically update when modules are moved or removed"}});
        m.entries.push_back({"Knob Range", {"0.0 to 2.0 (0% to 200% gain)", "0.0 to 2.0 (0% to 200% gain)", "0.0 to 2.0 (0% to 200% gain)"}});
        m.entries.push_back({"Default", {"1.0 (unity gain)", "1.0 (unity gain)", "1.0 (unity gain)"}});
        m.entries.push_back({"CV Modulation", {"CV adds to knob value (final gain clamped 0-2.0)", "CV adds to knob value (final gain clamped 0-2.0)", "CV adds to knob value (final gain clamped 0-2.0)"}});
        m.entries.push_back({"CV Range", {"\\u00b110V (adds \\u00b1100% to knob value, final gain clamped 0-2.0)", "\\u00b110V (adds \\u00b1100% to knob value, final gain clamped 0-2.0)", "\\u00b110V (adds \\u00b1100% to knob value, final gain clamped 0-2.0)"}});
        m.entries.push_back({"Polyphonic", {"Same gain applied to all 16 channels", "Chain 訊號維持最多 16 個 Polyphonic 聲道", "Same gain applied to all 16 channels"}});
        m.entries.push_back({"Duck Input", {"Expects 0-10V envelope signal (typically from kick or bass)", "Expects 0-10V envelope signal (typically from kick or bass)", "Expects 0-10V envelope signal (typically from kick or bass)"}});
        m.entries.push_back({"Duck Amount", {"0.0 to 1.0 (controls attenuation depth)", "0.0 to 1.0 (controls attenuation depth)", "0.0 to 1.0 (controls attenuation depth)"}});
        m.entries.push_back({"Ducking Behavior", {"Duck CV reduces gain proportionally, scaled by duck amount with 3x multiplier", "Duck CV reduces gain proportionally, scaled by duck amount with 3x multiplier", "Duck CV reduces gain proportionally, scaled by duck amount with 3x multiplier"}});
        m.entries.push_back({"Maximum Attenuation", {"At duck=1.0 with 10V input, gain reduces to zero (full silence)", "At duck=1.0 with 10V input, gain reduces to zero (full silence)", "At duck=1.0 with 10V input, gain reduces to zero (full silence)"}});
        m.entries.push_back({"At Duck 0", {"No attenuation regardless of input", "No attenuation regardless of input", "No attenuation regardless of input"}});
        m.entries.push_back({"At Duck 1", {"Full 10V input causes complete silence", "Full 10V input causes complete silence", "Full 10V input causes complete silence"}});
        m.entries.push_back({"Typical Use", {"Feed kick drum envelope to duck pads or bass for cleaner mix", "Feed kick drum envelope to duck pads or bass for cleaner mix", "Feed kick drum envelope to duck pads or bass for cleaner mix"}});
        m.entries.push_back({"Button", {"Toggle mute on/off with LED indicator", "Toggle mute on/off with LED indicator", "Toggle mute on/off with LED indicator"}});
        m.entries.push_back({"Trigger Input", {"Rising edge (2.0V threshold) toggles mute state", "Rising edge (2.0V threshold) toggles mute state", "Rising edge (2.0V threshold) toggles mute state"}});
        m.entries.push_back({"LED Color", {"Red when muted, dark when unmuted", "Red when muted, dark when unmuted", "Red when muted, dark when unmuted"}});
        m.entries.push_back({"When Muted", {"Channel output is zeroed, chain signal passes through unaffected", "Channel output is zeroed, chain signal passes through unaffected", "Channel output is zeroed, chain signal passes through unaffected"}});
        m.entries.push_back({"Button", {"Toggle solo on/off with LED indicator", "Toggle solo on/off with LED indicator", "Toggle solo on/off with LED indicator"}});
        m.entries.push_back({"Trigger Input", {"Rising edge (2.0V threshold) toggles solo state", "Rising edge (2.0V threshold) toggles solo state", "Rising edge (2.0V threshold) toggles solo state"}});
        m.entries.push_back({"LED Color", {"Green when soloed", "Green when soloed", "Green when soloed"}});
        m.entries.push_back({"When Soloed", {"All other non-soloed channels in the chain are muted", "All other non-soloed channels in the chain are muted", "All other non-soloed channels in the chain are muted"}});
        m.entries.push_back({"Chain Detection", {"Solo state is shared across adjacent U8, ALEXANDERPLATZ, and SHINJUKU modules", "Solo state is shared across adjacent U8, ALEXANDERPLATZ, and SHINJUKU modules", "Solo state is shared across adjacent U8, ALEXANDERPLATZ, and SHINJUKU modules"}});
        m.entries.push_back({"Exclusive Solo", {"Hold solo button for 400ms to activate exclusive mode (cancels all other solos in chain)", "長按 Solo 按鈕 400ms 啟用 Exclusive 模式（取消 Chain 中所有其他 Solo）", "Hold solo button for 400ms to activate exclusive mode (cancels all other solos in chain)"}});
        m.entries.push_back({"Visual Feedback", {"Mute LED lights up when channel is silenced by another channel's solo", "Mute LED lights up when channel is silenced by another channel's solo", "Mute LED lights up when channel is silenced by another channel's solo"}});
        m.entries.push_back({"CHAIN L/R Inputs", {"Receives signal from previous U8 module in chain", "Receives signal from previous U8 module in chain", "Receives signal from previous U8 module in chain"}});
        m.entries.push_back({"Summation", {"Processed signal added to chain input", "Processed signal added to chain input", "Processed signal added to chain input"}});
        m.entries.push_back({"Polyphonic", {"Chain signals maintain up to 16 polyphonic channels", "Chain 訊號維持最多 16 個 Polyphonic 聲道", "Chain signals maintain up to 16 polyphonic channels"}});
        m.entries.push_back({"Cascading", {"Connect multiple U8 modules by linking OUT to next module's CHAIN", "Connect multiple U8 modules by linking OUT to next module's CHAIN", "Connect multiple U8 modules by linking OUT to next module's CHAIN"}});
        m.entries.push_back({"Bypass Mode", {"When module is bypassed, chain input passes directly to output", "When module is bypassed, chain input passes directly to output", "When module is bypassed, chain input passes directly to output"}});
        m.entries.push_back({"Channel Count", {"Supports up to 16 polyphonic channels", "Supports up to 16 polyphonic channels", "Supports up to 16 polyphonic channels"}});
        m.entries.push_back({"Channel Detection", {"Automatically detects input polyphony", "Automatically detects input polyphony", "Automatically detects input polyphony"}});
        m.entries.push_back({"Output Channels", {"Matches input channel count", "Matches input channel count", "Matches input channel count"}});
        m.entries.push_back({"Per-Channel Delay", {"Each polyphonic channel has independent 2048-sample delay buffer", "Each polyphonic channel has independent 2048-sample delay buffer", "Each polyphonic channel has independent 2048-sample delay buffer"}});
        m.entries.push_back({"Memory", {"Total delay buffer: 2048 × 16 × 2 (L/R) = 65,536 samples", "Total delay buffer: 2048 × 16 × 2 (L/R) = 65,536 samples", "Total delay buffer: 2048 × 16 × 2 (L/R) = 65,536 samples"}});
        m.entries.push_back({"LEVEL Knob", {"Channel gain 0-200%", "通道 Gain 0-200%", "Channel gain 0-200%"}});
        m.entries.push_back({"LEVEL CV", {"Gain modulation input (\\u00b110V)", "Level 調變 CV 輸入（\\u00b110V）。", "レベル変調CV入力（\\u00b110V）。"}});
        m.entries.push_back({"DUCK Knob", {"Sidechain ducking depth 0-100%", "Sidechain Ducking 深度 0-100%", "Sidechain ducking depth 0-100%"}});
        m.entries.push_back({"DUCK CV", {"Sidechain control input (expects envelope 0-10V)", "Sidechain 控制輸入（預期 Envelope 0-10V）", "サイドチェインコントロール入力（エンベロープ 0-10Vを想定）"}});
        m.entries.push_back({"MUTE Button", {"Toggle channel mute with LED", "切換通道 Mute，含 LED", "Toggle channel mute with LED"}});
        m.entries.push_back({"MUTE TRIG", {"External mute toggle trigger", "外部 Mute 切換 Trigger", "外部ミュート切替トリガー"}});
        m.entries.push_back({"SOLO Button", {"Toggle channel solo with LED. Hold 400ms for exclusive solo.", "切換通道 Solo，含 LED。長按 400ms 啟用 Exclusive Solo。", "LED付きチャンネルソロ切替。400ms長押しでエクスクルーシブソロ。"}});
        m.entries.push_back({"SOLO TRIG", {"External solo toggle trigger", "外部 Solo 切換 Trigger", "外部ソロ切替トリガー"}});
        m.entries.push_back({"Display", {"Dual horizontal bar meters (L/R) located below the input jacks", "Dual horizontal bar meters (L/R) located below the input jacks", "Dual horizontal bar meters (L/R) located below the input jacks"}});
        m.entries.push_back({"Metering Point", {"Pre-level, pre-mute input signal (does not include chain)", "Pre-level, pre-mute input signal (does not include chain)", "Pre-level, pre-mute input signal (does not include chain)"}});
        m.entries.push_back({"Reference", {"0 dB = 5V", "0 dB = 5V", "0 dB = 5V"}});
        m.entries.push_back({"Range", {"-36 dB to +6 dB", "-36 dB to +6 dB", "-36 dB to +6 dB"}});
        m.entries.push_back({"Ballistics", {"5ms attack, 300ms release (exponential smoothing)", "5ms attack, 300ms release (exponential smoothing)", "5ms attack, 300ms release (exponential smoothing)"}});
        m.entries.push_back({"Color", {"Green-to-red gradient, red highlight above 0 dB", "Green-to-red gradient, red highlight above 0 dB", "Green-to-red gradient, red highlight above 0 dB"}});
        m.entries.push_back({"Mono Mode", {"When only left input is connected, both meters show the same level", "When only left input is connected, both meters show the same level", "When only left input is connected, both meters show the same level"}});
        m.entries.push_back({"LEFT", {"Left audio input (polyphonic, up to 16 channels)", "Left Audio 輸入（Polyphonic，最多 16 聲道）", "左オーディオ入力（ポリフォニック、最大16チャンネル）"}});
        m.entries.push_back({"RIGHT", {"Right audio input (polyphonic). If disconnected, mono-to-stereo mode activates.", "Right Audio 輸入（Polyphonic）。若未連接，啟用 Mono-to-Stereo 模式。", "右オーディオ入力（ポリフォニック）。未接続時、モノ→ステレオモード起動。"}});
        m.entries.push_back({"CHAIN L", {"Left chain bus input from previous U8", "來自前一個 U8 的 Left Chain Bus 輸入", "前のU8からの左チェーンバス入力"}});
        m.entries.push_back({"CHAIN R", {"Right chain bus input from previous U8", "來自前一個 U8 的 Right Chain Bus 輸入", "前のU8からの右チェーンバス入力"}});
        m.entries.push_back({"OUT L", {"Processed left output + chain sum (polyphonic)", "處理後 Left Output + Chain 加總（Polyphonic）", "処理済み左出力 + チェーン合計（ポリフォニック）"}});
        m.entries.push_back({"OUT R", {"Processed right output + chain sum (polyphonic)", "處理後 Right Output + Chain 加總（Polyphonic）", "処理済み右出力 + チェーン合計（ポリフォニック）"}});
        m.entries.push_back({"Level", {"Channel gain knob. Range 0-200%, default 100% (unity gain).", "通道 Gain 旋鈕。範圍 0-200%，預設 100%（Unity Gain）。", "チャンネルゲインノブ。範囲0-200%、デフォルト100%（ユニティゲイン）。"}});
        m.entries.push_back({"Duck Level", {"Sidechain ducking depth. Range 0-100%, default 0%.", "Sidechain Ducking 深度。範圍 0-100%，預設 0%。", "サイドチェインダッキング深度。範囲0-100%、デフォルト0%。"}});
        m.entries.push_back({"Mute", {"Toggle mute button with red LED indicator.", "Mute 切換按鈕，含紅色 LED 指示器。", "ミュート切替ボタン、赤LEDインジケーター付き。"}});
        m.entries.push_back({"Solo", {"Toggle solo button with green LED. Hold 400ms for exclusive solo.", "Solo 切換按鈕，含綠色 LED。長按 400ms 啟用 Exclusive Solo。", "ソロ切替ボタン、緑LED付き。400ms長押しでエクスクルーシブソロ。"}});
        m.entries.push_back({"Left Audio", {"Left audio input (polyphonic, up to 16 channels).", "Left Audio 輸入（Polyphonic，最多 16 聲道）。", "左オーディオ入力（ポリフォニック、最大16チャンネル）。"}});
        m.entries.push_back({"Right Audio", {"Right audio input (polyphonic). Mono-to-stereo mode when disconnected.", "Right Audio 輸入（Polyphonic）。未連接時啟用 Mono-to-Stereo 模式。", "右オーディオ入力（ポリフォニック）。未接続時モノ→ステレオモード起動。"}});
        m.entries.push_back({"Duck Signal", {"Sidechain control input. Expects 0-10V envelope signal.", "Sidechain 控制輸入。預期 0-10V Envelope 訊號。", "サイドチェインコントロール入力。0-10Vエンベロープ信号を想定。"}});
        m.entries.push_back({"Level CV", {"Level modulation CV input (\\u00b110V).", "Level 調變 CV 輸入（\\u00b110V）。", "レベル変調CV入力（\\u00b110V）。"}});
        m.entries.push_back({"Mute Trigger", {"External mute toggle trigger input (2.0V threshold).", "外部 Mute 切換 Trigger 輸入（2.0V 閾值）。", "外部ミュート切替トリガー入力（2.0V閾値）。"}});
        m.entries.push_back({"Solo Trigger", {"External solo toggle trigger input (2.0V threshold).", "外部 Solo 切換 Trigger 輸入（2.0V 閾值）。", "外部ソロ切替トリガー入力（2.0V閾値）。"}});
        m.entries.push_back({"Chain Left", {"Left chain bus input from previous mixer module.", "來自前一個 Mixer 模組的 Left Chain Bus 輸入。", "前のミキサーモジュールからの左チェーンバス入力。"}});
        m.entries.push_back({"Chain Right", {"Right chain bus input from previous mixer module.", "來自前一個 Mixer 模組的 Right Chain Bus 輸入。", "前のミキサーモジュールからの右チェーンバス入力。"}});
        data["U8"] = std::move(m);
    }
    // UniRhythm (110 entries)
    {
        ModuleHelpData m;
        m.name = "Uni Rhythm";
        m.description = {"Compact 4-track rhythm generator with 10 world music styles, 8-voice drum synthesis, primary-priority merged outputs, master isolator and drive, and 16-channel poly output for Portal", "精簡 4 軌節奏生成器，具備 10 種世界音樂風格、8 聲 Drum Synthesis、Primary-Priority 合併輸出、Master Isolator + Drive，以及 16 聲道 Poly Output 供 Portal 使用", "10種のワールドミュージックスタイル、8ボイスドラム合成、プライマリプライオリティ統合出力、マスターアイソレーター+ドライブ、Portal用16チャンネルポリ出力を備えたコンパクト4トラックリズムジェネレーター"};
        m.entries.push_back({"STYLE", {"Rhythm style (0-9, snapped to integers). Each role can have a different style. CV: ±5V selects style.", "節奏風格（0-9，整數對齊）。各角色可選不同風格。CV: ±5V 選擇風格。", "リズムスタイル（0-9、整数スナップ）。各ロールで異なるスタイル選択可能。CV: ±5Vでスタイル選択。"}});
        m.entries.push_back({"DENSITY", {"Pattern density (0-100%). Foundation default 20%, Timeline 40%, others 50%. CV: ±5V modulation.", "Pattern 密度（0-100%）。Foundation 預設 20%、Timeline 40%、其他 50%。CV: ±5V 調變。", "パターン密度（0-100%）。Foundationデフォルト20%、Timeline 40%、その他50%。CV: ±5V変調。"}});
        m.entries.push_back({"LENGTH", {"Pattern length (4-32 steps, integer snap).", "Pattern 長度（4-32 步，整數對齊）。", "パターン長（4-32ステップ、整数スナップ）。"}});
        m.entries.push_back({"FREQ", {"Frequency modifier (-1 to +1 octave). CV: ±1V = ±0.2 octave.", "頻率修正（-1 至 +1 八度）。CV: ±1V = ±0.2 八度。", "周波数修正（-1～+1オクターブ）。CV: ±1V = ±0.2オクターブ。"}});
        m.entries.push_back({"DECAY", {"Decay multiplier (0.2x to 2.0x). CV: ±1V = ±0.18x (±5V = ±0.9x).", "Decay 倍數（0.2x 至 2.0x）。CV: ±1V = ±0.18x（±5V = ±0.9x）。", "ディケイ倍率（0.2x～2.0x）。CV: ±1V = ±0.18x（±5V = ±0.9x）。"}});
        m.entries.push_back({"MIX", {"Blend between internal synth (0%) and external audio (100%).", "內部合成（0%）與外部 Audio（100%）之間的混合。", "内部シンセ（0%）と外部オーディオ（100%）のブレンド。"}});
        m.entries.push_back({"EXT IN 1/2", {"Two external audio inputs per role, corresponding to voice 1 and voice 2.", "各角色兩個外部 Audio 輸入，分別對應 Voice 1 與 Voice 2。", "ロールごとに2つの外部オーディオ入力、ボイス1とボイス2に対応。"}});
        m.entries.push_back({"VCA Envelope", {"External audio volume responds to hit velocity with natural dynamics. Decay is faster for louder hits.", "外部 Audio 音量隨 Hit Velocity 變化。較大力度 Decay 較快。", "External audio volume responds to hit velocity with natural dynamics. Decay is faster for louder hits."}});
        m.entries.push_back({"MIX Knob", {"Crossfades between internal drum synthesis and external audio sources.", "Crossfades between internal drum synthesis and external audio sources.", "Crossfades between internal drum synthesis and external audio sources."}});
        m.entries.push_back({"Variation", {"Pattern variation amount (default 30%). Controls how much patterns evolve over time.", "Pattern 變化量（預設 30%）。控制 Pattern 隨時間演化的程度。", "パターンバリエーション量（デフォルト30%）。パターンの時間経過に伴う変化を制御。"}});
        m.entries.push_back({"Humanize", {"Humanize timing and velocity (default 50%). Adds organic feel to rhythms.", "時序與 Velocity Humanize（預設 50%）。為節奏添加有機感。", "タイミング・ベロシティヒューマナイズ（デフォルト50%）。リズムにオーガニック感を付加。"}});
        m.entries.push_back({"Swing", {"Swing amount (default 50%). Adjusts timing of off-beat notes.", "Swing 量（預設 50%）。調整 Off-beat 音符的時序。", "スウィング量（デフォルト50%）。オフビートノートのタイミングを調整。"}});
        m.entries.push_back({"Rest", {"Rest probability with CV input. Removes hits from patterns probabilistically.", "休止機率，帶 CV 輸入。以機率方式移除 Pattern 中的 Hit。", "休符確率、CV入力対応。確率的にパターンからヒットを除去。"}});
        m.entries.push_back({"Fill", {"Combined fill probability and intensity (default 30%). Pre-determined at bar start.", "結合 Fill 機率與強度（預設 30%）。在小節開始時預先決定。", "フィル確率+強度の統合パラメータ（デフォルト30%）。小節開始時に事前決定。"}});
        m.entries.push_back({"Articulation", {"Articulation amount. Enables Ghost, Accent, Rim, Flam, Drag, Buzz, Ruff articulations.", "Articulation 量。啟用 Ghost、Accent、Rim、Flam、Drag、Buzz、Ruff 演奏技法。", "アーティキュレーション量。Ghost、Accent、Rim、Flam、Drag、Buzz、Ruff技法を有効化。"}});
        m.entries.push_back({"Ghost Notes", {"Ghost note probability.", "Ghost Note 機率。", "ゴーストノート確率。"}});
        m.entries.push_back({"Accent", {"Accent probability.", "Accent 機率。", "アクセント確率。"}});
        m.entries.push_back({"Spread", {"Stereo spread for mix output (default 50%).", "Mix 輸出的 Stereo Spread（預設 50%）。", "ミックス出力のステレオスプレッド（デフォルト50%）。"}});
        m.entries.push_back({"Regenerate", {"Generate new patterns based on current settings.", "生成新 Pattern 的 Trigger 輸入。", "新パターン生成トリガー入力。"}});
        m.entries.push_back({"Reset", {"Reset all step counters to zero.", "Reset 輸入。將所有步數計數器歸零。", "リセット入力。全ステップカウンターをゼロに戻す。"}});
        m.entries.push_back({"Isolator Low", {"3-band isolator low band (250Hz crossover). Bipolar: -1 = kill, 0 = unity, +1 = +12dB boost.", "3 頻段 Isolator 低頻（250Hz 分頻點）。雙極: -1 = Kill、0 = Unity、+1 = +12dB Boost。", "3バンドアイソレーター低域（250Hzクロスオーバー）。バイポーラ: -1 = Kill、0 = Unity、+1 = +12dBブースト。"}});
        m.entries.push_back({"Isolator Mid", {"3-band isolator mid band (250Hz-4kHz). Same bipolar control.", "3 頻段 Isolator 中頻（250Hz-4kHz）。相同雙極控制。", "3バンドアイソレーター中域（250Hz-4kHz）。同じバイポーラコントロール。"}});
        m.entries.push_back({"Isolator High", {"3-band isolator high band (4kHz crossover). Same bipolar control.", "3 頻段 Isolator 高頻（4kHz 分頻點）。相同雙極控制。", "3バンドアイソレーター高域（4kHzクロスオーバー）。同じバイポーラコントロール。"}});
        m.entries.push_back({"Master Drive", {"Asymmetric tube saturation (0-100%). Creates warm even harmonics with DC blocker.", "非對稱 Tube Saturation（0-100%）。產生溫暖的偶次諧波，帶 DC Blocker。", "非対称チューブサチュレーション（0-100%）。DCブロッカー付きの暖かい偶数倍音を生成。"}});
        m.entries.push_back({"Audio", {"Sum of both voices per role (primary + secondary).", "各角色兩個 Voice 的音訊總和（Primary + Secondary）。", "ロールあたり両ボイスのオーディオ合計（プライマリ+セカンダリ）。"}});
        m.entries.push_back({"Gate", {"Merged gate output. When both voices trigger simultaneously, primary voice takes priority.", "合併的 Gate 輸出。兩個 Voice 同時觸發時，Primary Voice 優先。", "統合ゲート出力。両ボイスが同時トリガーの場合、プライマリボイスが優先。"}});
        m.entries.push_back({"Pitch CV", {"1V/Oct pitch output from the last triggered voice (primary priority). C4 = 0V.", "來自最後觸發 Voice 的 1V/Oct Pitch 輸出（Primary 優先）。C4 = 0V。", "最後にトリガーされたボイスの1V/Octピッチ出力（プライマリ優先）。C4 = 0V。"}});
        m.entries.push_back({"Vel/Env", {"Velocity envelope CV (0-10V) from the priority voice.", "來自優先 Voice 的 Velocity Envelope CV（0-10V）。", "優先ボイスのベロシティエンベロープCV（0-10V）。"}});
        m.entries.push_back({"CLK", {"External clock input. Required for pattern playback.", "外部 Clock 輸入。Pattern 播放所必需。", "外部クロック入力。パターン再生に必須。"}});
        m.entries.push_back({"RST", {"Reset input. Returns all step counters to zero.", "Reset 輸入。將所有步數計數器歸零。", "リセット入力。全ステップカウンターをゼロに戻す。"}});
        m.entries.push_back({"REGEN", {"Regenerate trigger input. Generates new patterns based on current settings.", "Regenerate 觸發輸入。依據目前設定生成新 Pattern。", "リジェネレートトリガー入力。現在の設定に基づいて新パターンを生成。"}});
        m.entries.push_back({"PPQN", {"1 (quarter), 2 (8th), or 4 (16th note) pulses per quarter. Set via right-click menu.", "1（四分音符）、2（八分音符）或 4（十六分音符）每四分音符脈衝數。透過右鍵選單設定。", "1（四分音符）、2（八分音符）、または4（十六分音符）。右クリックメニューで設定。"}});
        m.entries.push_back({"Per-Role Audio", {"Individual role audio output (sum of 2 voices).", "Individual role audio output (sum of 2 voices).", "Individual role audio output (sum of 2 voices)."}});
        m.entries.push_back({"Per-Role Gate", {"Merged trigger output per role.", "Merged trigger output per role.", "Merged trigger output per role."}});
        m.entries.push_back({"Per-Role Pitch", {"1V/Oct CV output per role.", "1V/Oct CV output per role.", "1V/Oct CV output per role."}});
        m.entries.push_back({"Per-Role Vel/Env", {"Velocity envelope CV output per role.", "Velocity envelope CV output per role.", "Velocity envelope CV output per role."}});
        m.entries.push_back({"MIX L/R", {"Stereo master mix with isolator, drive, and spread applied.", "Stereo Master Mix，含 Isolator、Drive 與 Spread。", "ステレオマスターミックス（アイソレーター、ドライブ、スプレッド適用済み）。"}});
        m.entries.push_back({"POLY", {"16-channel polyphonic output. 4 roles x 4 channels (Audio, Gate, Pitch, VelEnv). Designed for Portal.", "16 聲道 Polyphonic 輸出。4 角色 x 4 聲道（Audio、Gate、Pitch、VelEnv）。專為 Portal 設計。", "16チャンネルポリフォニック出力。4ロール×4チャンネル（Audio、Gate、Pitch、VelEnv）。Portal用に設計。"}});
        m.entries.push_back({"Timeline Style", {"Timeline role rhythm style (0-9, integer snap).", "Timeline 角色節奏風格（0-9，整數對齊）。", "Timelineロールリズムスタイル（0-9、整数スナップ）。"}});
        m.entries.push_back({"Timeline Density", {"Timeline role pattern density (0-100%, default 40%).", "Timeline 角色 Pattern 密度（0-100%，預設 40%）。", "Timelineロールパターン密度（0-100%、デフォルト40%）。"}});
        m.entries.push_back({"Timeline Length", {"Timeline role pattern length (4-32 steps, integer snap).", "Timeline 角色 Pattern 長度（4-32 步，整數對齊）。", "Timelineロールパターン長（4-32ステップ、整数スナップ）。"}});
        m.entries.push_back({"Timeline Freq", {"Timeline role frequency modifier (-1 to +1 octave).", "Timeline 角色頻率修正（-1 至 +1 八度）。", "Timelineロール周波数修正（-1～+1オクターブ）。"}});
        m.entries.push_back({"Timeline Decay", {"Timeline role decay multiplier (0.2x to 2.0x).", "Timeline 角色 Decay 倍數（0.2x 至 2.0x）。", "Timelineロールディケイ倍率（0.2x～2.0x）。"}});
        m.entries.push_back({"Timeline Mix (Int/Ext)", {"Timeline role internal synth (0%) to external audio (100%).", "Timeline 角色內部合成（0%）至外部 Audio（100%）。", "Timelineロール内部シンセ（0%）から外部オーディオ（100%）。"}});
        m.entries.push_back({"Foundation Style", {"Foundation role rhythm style (0-9, integer snap).", "Foundation 角色節奏風格（0-9，整數對齊）。", "Foundationロールリズムスタイル（0-9、整数スナップ）。"}});
        m.entries.push_back({"Foundation Density", {"Foundation role pattern density (0-100%, default 20%).", "Foundation 角色 Pattern 密度（0-100%，預設 20%）。", "Foundationロールパターン密度（0-100%、デフォルト20%）。"}});
        m.entries.push_back({"Foundation Length", {"Foundation role pattern length (4-32 steps, integer snap).", "Foundation 角色 Pattern 長度（4-32 步，整數對齊）。", "Foundationロールパターン長（4-32ステップ、整数スナップ）。"}});
        m.entries.push_back({"Foundation Freq", {"Foundation role frequency modifier (-1 to +1 octave).", "Foundation 角色頻率修正（-1 至 +1 八度）。", "Foundationロール周波数修正（-1～+1オクターブ）。"}});
        m.entries.push_back({"Foundation Decay", {"Foundation role decay multiplier (0.2x to 2.0x).", "Foundation 角色 Decay 倍數（0.2x 至 2.0x）。", "Foundationロールディケイ倍率（0.2x～2.0x）。"}});
        m.entries.push_back({"Foundation Mix (Int/Ext)", {"Foundation role internal synth (0%) to external audio (100%).", "Foundation 角色內部合成（0%）至外部 Audio（100%）。", "Foundationロール内部シンセ（0%）から外部オーディオ（100%）。"}});
        m.entries.push_back({"Groove Style", {"Groove role rhythm style (0-9, integer snap).", "Groove 角色節奏風格（0-9，整數對齊）。", "Grooveロールリズムスタイル（0-9、整数スナップ）。"}});
        m.entries.push_back({"Groove Density", {"Groove role pattern density (0-100%, default 50%).", "Groove 角色 Pattern 密度（0-100%，預設 50%）。", "Grooveロールパターン密度（0-100%、デフォルト50%）。"}});
        m.entries.push_back({"Groove Length", {"Groove role pattern length (4-32 steps, integer snap).", "Groove 角色 Pattern 長度（4-32 步，整數對齊）。", "Grooveロールパターン長（4-32ステップ、整数スナップ）。"}});
        m.entries.push_back({"Groove Freq", {"Groove role frequency modifier (-1 to +1 octave).", "Groove 角色頻率修正（-1 至 +1 八度）。", "Grooveロール周波数修正（-1～+1オクターブ）。"}});
        m.entries.push_back({"Groove Decay", {"Groove role decay multiplier (0.2x to 2.0x).", "Groove 角色 Decay 倍數（0.2x 至 2.0x）。", "Grooveロールディケイ倍率（0.2x～2.0x）。"}});
        m.entries.push_back({"Groove Mix (Int/Ext)", {"Groove role internal synth (0%) to external audio (100%).", "Groove 角色內部合成（0%）至外部 Audio（100%）。", "Grooveロール内部シンセ（0%）から外部オーディオ（100%）。"}});
        m.entries.push_back({"Lead Style", {"Lead role rhythm style (0-9, integer snap).", "Lead 角色節奏風格（0-9，整數對齊）。", "Leadロールリズムスタイル（0-9、整数スナップ）。"}});
        m.entries.push_back({"Lead Density", {"Lead role pattern density (0-100%, default 50%).", "Lead 角色 Pattern 密度（0-100%，預設 50%）。", "Leadロールパターン密度（0-100%、デフォルト50%）。"}});
        m.entries.push_back({"Lead Length", {"Lead role pattern length (4-32 steps, integer snap).", "Lead 角色 Pattern 長度（4-32 步，整數對齊）。", "Leadロールパターン長（4-32ステップ、整数スナップ）。"}});
        m.entries.push_back({"Lead Freq", {"Lead role frequency modifier (-1 to +1 octave).", "Lead 角色頻率修正（-1 至 +1 八度）。", "Leadロール周波数修正（-1～+1オクターブ）。"}});
        m.entries.push_back({"Lead Decay", {"Lead role decay multiplier (0.2x to 2.0x).", "Lead 角色 Decay 倍數（0.2x 至 2.0x）。", "Leadロールディケイ倍率（0.2x～2.0x）。"}});
        m.entries.push_back({"Lead Mix (Int/Ext)", {"Lead role internal synth (0%) to external audio (100%).", "Lead 角色內部合成（0%）至外部 Audio（100%）。", "Leadロール内部シンセ（0%）から外部オーディオ（100%）。"}});
        m.entries.push_back({"Clock", {"Clock input for pattern playback.", "Pattern 播放用 Clock 輸入。", "パターン再生用クロック入力。"}});
        m.entries.push_back({"Reset", {"Reset input. Returns all step counters to zero.", "Reset 輸入。將所有步數計數器歸零。", "リセット入力。全ステップカウンターをゼロに戻す。"}});
        m.entries.push_back({"Regenerate", {"Trigger input to generate new patterns.", "生成新 Pattern 的 Trigger 輸入。", "新パターン生成トリガー入力。"}});
        m.entries.push_back({"Rest CV", {"CV input for rest probability modulation.", "休止機率調變 CV 輸入。", "休符確率変調CV入力。"}});
        m.entries.push_back({"Fill Trigger", {"Trigger input for fill activation.", "Fill 啟動 Trigger 輸入。", "フィル発動トリガー入力。"}});
        m.entries.push_back({"Timeline Style CV", {"CV input for Timeline style modulation.", "Timeline 風格調變 CV 輸入。", "Timelineスタイル変調CV入力。"}});
        m.entries.push_back({"Timeline Density CV", {"CV input for Timeline density modulation.", "Timeline 密度調變 CV 輸入。", "Timeline密度変調CV入力。"}});
        m.entries.push_back({"Timeline Freq CV", {"CV input for Timeline frequency modulation.", "Timeline 頻率調變 CV 輸入。", "Timeline周波数変調CV入力。"}});
        m.entries.push_back({"Timeline Decay CV", {"CV input for Timeline decay modulation.", "Timeline Decay 調變 CV 輸入。", "Timelineディケイ変調CV入力。"}});
        m.entries.push_back({"Foundation Style CV", {"CV input for Foundation style modulation.", "Foundation 風格調變 CV 輸入。", "Foundationスタイル変調CV入力。"}});
        m.entries.push_back({"Foundation Density CV", {"CV input for Foundation density modulation.", "Foundation 密度調變 CV 輸入。", "Foundation密度変調CV入力。"}});
        m.entries.push_back({"Foundation Freq CV", {"CV input for Foundation frequency modulation.", "Foundation 頻率調變 CV 輸入。", "Foundation周波数変調CV入力。"}});
        m.entries.push_back({"Foundation Decay CV", {"CV input for Foundation decay modulation.", "Foundation Decay 調變 CV 輸入。", "Foundationディケイ変調CV入力。"}});
        m.entries.push_back({"Groove Style CV", {"CV input for Groove style modulation.", "Groove 風格調變 CV 輸入。", "Grooveスタイル変調CV入力。"}});
        m.entries.push_back({"Groove Density CV", {"CV input for Groove density modulation.", "Groove 密度調變 CV 輸入。", "Groove密度変調CV入力。"}});
        m.entries.push_back({"Groove Freq CV", {"CV input for Groove frequency modulation.", "Groove 頻率調變 CV 輸入。", "Groove周波数変調CV入力。"}});
        m.entries.push_back({"Groove Decay CV", {"CV input for Groove decay modulation.", "Groove Decay 調變 CV 輸入。", "Grooveディケイ変調CV入力。"}});
        m.entries.push_back({"Lead Style CV", {"CV input for Lead style modulation.", "Lead 風格調變 CV 輸入。", "Leadスタイル変調CV入力。"}});
        m.entries.push_back({"Lead Density CV", {"CV input for Lead density modulation.", "Lead 密度調變 CV 輸入。", "Lead密度変調CV入力。"}});
        m.entries.push_back({"Lead Freq CV", {"CV input for Lead frequency modulation.", "Lead 頻率調變 CV 輸入。", "Lead周波数変調CV入力。"}});
        m.entries.push_back({"Lead Decay CV", {"CV input for Lead decay modulation.", "Lead Decay 調變 CV 輸入。", "Leadディケイ変調CV入力。"}});
        m.entries.push_back({"Timeline Audio Input 1", {"External audio input 1 for Timeline role.", "Timeline 角色外部 Audio 輸入 1。", "Timelineロール外部オーディオ入力1。"}});
        m.entries.push_back({"Timeline Audio Input 2", {"External audio input 2 for Timeline role.", "Timeline 角色外部 Audio 輸入 2。", "Timelineロール外部オーディオ入力2。"}});
        m.entries.push_back({"Foundation Audio Input 1", {"External audio input 1 for Foundation role.", "Foundation 角色外部 Audio 輸入 1。", "Foundationロール外部オーディオ入力1。"}});
        m.entries.push_back({"Foundation Audio Input 2", {"External audio input 2 for Foundation role.", "Foundation 角色外部 Audio 輸入 2。", "Foundationロール外部オーディオ入力2。"}});
        m.entries.push_back({"Groove Audio Input 1", {"External audio input 1 for Groove role.", "Groove 角色外部 Audio 輸入 1。", "Grooveロール外部オーディオ入力1。"}});
        m.entries.push_back({"Groove Audio Input 2", {"External audio input 2 for Groove role.", "Groove 角色外部 Audio 輸入 2。", "Grooveロール外部オーディオ入力2。"}});
        m.entries.push_back({"Lead Audio Input 1", {"External audio input 1 for Lead role.", "Lead 角色外部 Audio 輸入 1。", "Leadロール外部オーディオ入力1。"}});
        m.entries.push_back({"Lead Audio Input 2", {"External audio input 2 for Lead role.", "Lead 角色外部 Audio 輸入 2。", "Leadロール外部オーディオ入力2。"}});
        m.entries.push_back({"Timeline Audio", {"Timeline role merged audio output (sum of 2 voices).", "Timeline 角色合併 Audio 輸出（2 Voice 總和）。", "Timelineロール統合オーディオ出力（2ボイス合計）。"}});
        m.entries.push_back({"Timeline Gate", {"Timeline role merged gate/trigger output (primary priority).", "Timeline 角色合併 Gate/Trigger 輸出（Primary 優先）。", "Timelineロール統合ゲート/トリガー出力（プライマリ優先）。"}});
        m.entries.push_back({"Timeline Pitch CV (1V/Oct, C4=0V)", {"Timeline role pitch CV output.", "Timeline 角色 Pitch CV 輸出。", "TimelineロールピッチCV出力。"}});
        m.entries.push_back({"Timeline Velocity Envelope", {"Timeline role velocity envelope CV output.", "Timeline 角色 Velocity Envelope CV 輸出。", "TimelineロールベロシティエンベロープCV出力。"}});
        m.entries.push_back({"Foundation Audio", {"Foundation role merged audio output (sum of 2 voices).", "Foundation 角色合併 Audio 輸出（2 Voice 總和）。", "Foundationロール統合オーディオ出力（2ボイス合計）。"}});
        m.entries.push_back({"Foundation Gate", {"Foundation role merged gate/trigger output (primary priority).", "Foundation 角色合併 Gate/Trigger 輸出（Primary 優先）。", "Foundationロール統合ゲート/トリガー出力（プライマリ優先）。"}});
        m.entries.push_back({"Foundation Pitch CV (1V/Oct, C4=0V)", {"Foundation role pitch CV output.", "Foundation 角色 Pitch CV 輸出。", "FoundationロールピッチCV出力。"}});
        m.entries.push_back({"Foundation Velocity Envelope", {"Foundation role velocity envelope CV output.", "Foundation 角色 Velocity Envelope CV 輸出。", "FoundationロールベロシティエンベロープCV出力。"}});
        m.entries.push_back({"Groove Audio", {"Groove role merged audio output (sum of 2 voices).", "Groove 角色合併 Audio 輸出（2 Voice 總和）。", "Grooveロール統合オーディオ出力（2ボイス合計）。"}});
        m.entries.push_back({"Groove Gate", {"Groove role merged gate/trigger output (primary priority).", "Groove 角色合併 Gate/Trigger 輸出（Primary 優先）。", "Grooveロール統合ゲート/トリガー出力（プライマリ優先）。"}});
        m.entries.push_back({"Groove Pitch CV (1V/Oct, C4=0V)", {"Groove role pitch CV output.", "Groove 角色 Pitch CV 輸出。", "GrooveロールピッチCV出力。"}});
        m.entries.push_back({"Groove Velocity Envelope", {"Groove role velocity envelope CV output.", "Groove 角色 Velocity Envelope CV 輸出。", "GrooveロールベロシティエンベロープCV出力。"}});
        m.entries.push_back({"Lead Audio", {"Lead role merged audio output (sum of 2 voices).", "Lead 角色合併 Audio 輸出（2 Voice 總和）。", "Leadロール統合オーディオ出力（2ボイス合計）。"}});
        m.entries.push_back({"Lead Gate", {"Lead role merged gate/trigger output (primary priority).", "Lead 角色合併 Gate/Trigger 輸出（Primary 優先）。", "Leadロール統合ゲート/トリガー出力（プライマリ優先）。"}});
        m.entries.push_back({"Lead Pitch CV (1V/Oct, C4=0V)", {"Lead role pitch CV output.", "Lead 角色 Pitch CV 輸出。", "LeadロールピッチCV出力。"}});
        m.entries.push_back({"Lead Velocity Envelope", {"Lead role velocity envelope CV output.", "Lead 角色 Velocity Envelope CV 輸出。", "LeadロールベロシティエンベロープCV出力。"}});
        m.entries.push_back({"Mix L", {"Left stereo master mix output (with isolator, drive, spread).", "左聲道 Stereo Master Mix 輸出（含 Isolator、Drive、Spread）。", "左ステレオマスターミックス出力（アイソレーター、ドライブ、スプレッド適用済み）。"}});
        m.entries.push_back({"Mix R", {"Right stereo master mix output (with isolator, drive, spread).", "右聲道 Stereo Master Mix 輸出（含 Isolator、Drive、Spread）。", "右ステレオマスターミックス出力（アイソレーター、ドライブ、スプレッド適用済み）。"}});
        m.entries.push_back({"Poly Out (16ch for Portal)", {"16-channel polyphonic output (4 roles x 4 channels).", "16 聲道 Polyphonic 輸出（4 角色 x 4 聲道）。", "16チャンネルポリフォニック出力（4ロール×4チャンネル）。"}});
        data["UniRhythm"] = std::move(m);
    }
    // UniversalRhythm (129 entries)
    {
        ModuleHelpData m;
        m.name = "Universal Rhythm";
        m.description = {"Cross-cultural rhythm generator featuring 10 world music styles, 4 rhythmic roles per style, and 8-voice polyphonic drum synthesis with per-role density and humanize controls", "跨文化節奏生成器，具備 10 種世界音樂風格、4 種節奏角色，以及 8 聲 Polyphonic Drum Synthesis", "10種のワールドミュージックスタイル、4つのリズムロール、8ボイスポリフォニックドラム合成を備えたクロスカルチャーリズム生成器"};
        m.entries.push_back({"West African", {"Gankogui 4.5kHz, Dununba 80Hz, Djembe 400-700Hz", "Gankogui 4.5kHz、Dununba 80Hz、Djembe 400-700Hz", "Gankogui 4.5kHz、Dununba 80Hz、Djembe 400-700Hz"}});
        m.entries.push_back({"Afro-Cuban", {"Clave 4kHz, Tumba 100Hz, Congas 350-550Hz, Timbales 3kHz", "Clave 4kHz、Tumba 100Hz、Congas 350-550Hz、Timbales 3kHz", "Clave 4kHz、Tumba 100Hz、Congas 350-550Hz、Timbales 3kHz"}});
        m.entries.push_back({"Brazilian", {"Agogô 3-4.5kHz, Surdo 55-80Hz, Tamborim 400Hz, Caixa 500Hz", "Agogô 3-4.5kHz、Surdo 55-80Hz、Tamborim 400Hz、Caixa 500Hz", "Agogô 3-4.5kHz、Surdo 55-80Hz、Tamborim 400Hz、Caixa 500Hz"}});
        m.entries.push_back({"Balkan", {"Rim 4kHz, Tapan 90-130Hz, Tarabuka 300-450Hz", "Rim 4kHz、Tapan 90-130Hz、Tarabuka 300-450Hz", "Rim 4kHz、Tapan 90-130Hz、Tarabuka 300-450Hz"}});
        m.entries.push_back({"Indian", {"Manjira 6-8kHz, Baya 65-90Hz, Daya 350-500Hz", "Manjira 6-8kHz、Baya 65-90Hz、Daya 350-500Hz", "Manjira 6-8kHz、Baya 65-90Hz、Daya 350-500Hz"}});
        m.entries.push_back({"Gamelan", {"Kenong 600-700Hz, Gong 90Hz, Bonang 800-1kHz, Gender 1.2kHz", "Kenong 600-700Hz、Gong 90Hz、Bonang 800-1kHz、Gender 1.2kHz", "Kenong 600-700Hz、Gong 90Hz、Bonang 800-1kHz、Gender 1.2kHz"}});
        m.entries.push_back({"Jazz", {"Ride 2.5-4.5kHz, Kick 50-80Hz, Snare 400-500Hz, HiHat 6-8kHz", "Ride 2.5-4.5kHz、Kick 50-80Hz、Snare 400-500Hz、HiHat 6-8kHz", "Ride 2.5-4.5kHz、Kick 50-80Hz、Snare 400-500Hz、HiHat 6-8kHz"}});
        m.entries.push_back({"Electronic", {"HiHat 9-12kHz, 808 Kick 45-60Hz, Clap 1.5-2.5kHz", "HiHat 9-12kHz、808 Kick 45-60Hz、Clap 1.5-2.5kHz", "HiHat 9-12kHz、808 Kick 45-60Hz、Clap 1.5-2.5kHz"}});
        m.entries.push_back({"Breakbeat", {"HiHat 8-10kHz, Kick 55-70Hz, Snare 2-2.5kHz", "HiHat 8-10kHz、Kick 55-70Hz、Snare 2-2.5kHz", "HiHat 8-10kHz、Kick 55-70Hz、Snare 2-2.5kHz"}});
        m.entries.push_back({"Techno", {"HiHat 10-12kHz, 909 Kick 42-55Hz, Clap 1.8kHz, Rim 3kHz", "HiHat 10-12kHz、909 Kick 42-55Hz、Clap 1.8kHz、Rim 3kHz", "HiHat 10-12kHz、909 Kick 42-55Hz、Clap 1.8kHz、Rim 3kHz"}});
        m.entries.push_back({"STYLE", {"Rhythm style (0-9, snapped to integers)", "節奏風格（0-9，對齊至整數）", "リズムスタイル（0-9、整数スナップ）"}});
        m.entries.push_back({"DENSITY", {"Pattern density. Foundation default 20%, Timeline 40%, others 50%", "Pattern 密度。Foundation 預設 20%、Timeline 40%、其他 50%", "パターン密度。Foundationデフォルト20%、Timeline 40%、その他50%"}});
        m.entries.push_back({"LENGTH", {"Pattern length (4-32 steps, integer snap)", "Pattern 長度（4-32 步，整數對齊）", "パターン長（4-32ステップ、整数スナップ）"}});
        m.entries.push_back({"FREQ", {"Frequency modifier (-1 to +1 octave). CV: ±1V = ±0.2 octave", "頻率修正（-1 至 +1 八度）。CV: ±1V = ±0.2 八度", "周波数修正（-1～+1オクターブ）。CV: ±1V = ±0.2オクターブ"}});
        m.entries.push_back({"DECAY", {"Decay multiplier (0.2x to 2.0x)", "Decay 倍數（0.2x 至 2.0x）", "ディケイ倍率（0.2x～2.0x）"}});
        m.entries.push_back({"MIX", {"Internal synth (0%) to external audio (100%)", "內部合成（0%）至外部 Audio（100%）", "内部シンセ（0%）から外部オーディオ（100%）"}});
        m.entries.push_back({"Velocity Response", {"External audio volume responds to hit velocity with natural-feeling dynamics", "External audio volume responds to hit velocity with natural-feeling dynamics", "External audio volume responds to hit velocity with natural-feeling dynamics"}});
        m.entries.push_back({"Decay Behavior", {"Envelope decays faster for louder hits, matching acoustic drum behavior", "Envelope decays faster for louder hits, matching acoustic drum behavior", "Envelope decays faster for louder hits, matching acoustic drum behavior"}});
        m.entries.push_back({"Application", {"Use external audio inputs to layer samples or external synth sounds with the rhythm patterns", "Use external audio inputs to layer samples or external synth sounds with the rhythm patterns", "Use external audio inputs to layer samples or external synth sounds with the rhythm patterns"}});
        m.entries.push_back({"Variation", {"Pattern variation (default 30%)", "Pattern 變化（預設 30%）", "パターンバリエーション（デフォルト30%）"}});
        m.entries.push_back({"Humanize", {"Humanize timing/velocity (default 50%)", "時序/Velocity Humanize（預設 50%）", "タイミング/ベロシティヒューマナイズ（デフォルト50%）"}});
        m.entries.push_back({"Swing", {"Swing amount (default 50%)", "Swing 量（預設 50%）", "スウィング量（デフォルト50%）"}});
        m.entries.push_back({"Rest", {"Rest probability with CV input", "休止機率，帶 CV 輸入", "休符確率、CV入力対応"}});
        m.entries.push_back({"Fill", {"Combined probability + intensity (default 30%)", "結合機率 + 強度（預設 30%）", "結合確率+強度（デフォルト30%）"}});
        m.entries.push_back({"Articulation", {"Articulation amount. Enables Ghost, Accent, Flam, Drag, Buzz, Ruff articulations.", "Articulation 量。啟用 Ghost、Accent、Flam、Drag、Buzz、Ruff 演奏技法。", "アーティキュレーション量。Ghost、Accent、Flam、Drag、Buzz、Ruff技法を有効化。"}});
        m.entries.push_back({"Ghost Notes", {"Ghost note probability.", "Ghost Note 機率。", "ゴーストノート確率。"}});
        m.entries.push_back({"Accent", {"Accent probability.", "Accent 機率。", "アクセント確率。"}});
        m.entries.push_back({"Spread", {"Stereo spread (default 50%)", "Stereo Spread（預設 50%）", "ステレオスプレッド（デフォルト50%）"}});
        m.entries.push_back({"Regenerate", {"Generate new patterns based on current settings.", "生成新 Pattern 的 Trigger 輸入。", "新パターン生成トリガー入力。"}});
        m.entries.push_back({"Reset", {"Reset all step counters to zero.", "Reset 輸入。將所有步數計數器歸零。", "リセット入力。全ステップカウンターをゼロに戻す。"}});
        m.entries.push_back({"Types", {"Normal, Ghost, Accent, Flam, Drag, Buzz, Ruff", "Normal, Ghost, Accent, Flam, Drag, Buzz, Ruff", "Normal, Ghost, Accent, Flam, Drag, Buzz, Ruff"}});
        m.entries.push_back({"Flam/Drag", {"Implemented via DelayedTrigger struct with sample-accurate timing", "透過 DelayedTrigger 結構實作，樣本精確時序", "DelayedTrigger構造体で実装、サンプル精度タイミング"}});
        m.entries.push_back({"Groove Templates", {"Auto, Straight, Swing, African, Latin, LaidBack, Pushed", "Auto, Straight, Swing, African, Latin, LaidBack, Pushed", "Auto, Straight, Swing, African, Latin, LaidBack, Pushed"}});
        m.entries.push_back({"Fill Types", {"Pre-determined at bar start, triggered at fillStartStep", "Pre-determined at bar start, triggered at fillStartStep", "Pre-determined at bar start, triggered at fillStartStep"}});
        m.entries.push_back({"PPQN Settings", {"1 (quarter), 2 (8th), or 4 (16th note) pulses per quarter", "1 (quarter), 2 (8th), or 4 (16th note) pulses per quarter", "1 (quarter), 2 (8th), or 4 (16th note) pulses per quarter"}});
        m.entries.push_back({"Per-Role Steps", {"Each role maintains independent step counter", "Each role maintains independent step counter", "Each role maintains independent step counter"}});
        m.entries.push_back({"Global Step", {"Shared counter for bar tracking and fill synchronization", "Shared counter for bar tracking and fill synchronization", "Shared counter for bar tracking and fill synchronization"}});
        m.entries.push_back({"MIX L/R", {"Stereo sum of all voices with spread", "所有 Voice 的 Stereo 混合（含 Spread）", "全ボイスのステレオミックス（スプレッド含む）"}});
        m.entries.push_back({"Per-Voice Audio", {"Individual voice audio outputs", "Individual voice audio outputs", "Individual voice audio outputs"}});
        m.entries.push_back({"Per-Voice Gate", {"Trigger outputs via PulseGenerator", "Trigger outputs via PulseGenerator", "Trigger outputs via PulseGenerator"}});
        m.entries.push_back({"Per-Voice Pitch CV", {"1V/Oct, C4=0V, derived from currentFreqs array", "1V/Oct, C4=0V, derived from currentFreqs array", "1V/Oct, C4=0V, derived from currentFreqs array"}});
        m.entries.push_back({"Per-Voice Velocity", {"Velocity CV (0-8V)", "Velocity CV (0-8V)", "Velocity CV (0-8V)"}});
        m.entries.push_back({"Timeline Style", {"Timeline role rhythm style (0-9, integer snap).", "Timeline 角色節奏風格（0-9，整數對齊）。", "Timelineロールリズムスタイル（0-9、整数スナップ）。"}});
        m.entries.push_back({"Timeline Density", {"Timeline role pattern density (0-100%, default 40%).", "Timeline 角色 Pattern 密度（0-100%，預設 40%）。", "Timelineロールパターン密度（0-100%、デフォルト40%）。"}});
        m.entries.push_back({"Timeline Length", {"Timeline role pattern length (4-32 steps, integer snap).", "Timeline 角色 Pattern 長度（4-32 步，整數對齊）。", "Timelineロールパターン長（4-32ステップ、整数スナップ）。"}});
        m.entries.push_back({"Timeline Freq", {"Timeline role frequency modifier (-1 to +1 octave).", "Timeline 角色頻率修正（-1 至 +1 八度）。", "Timelineロール周波数修正（-1～+1オクターブ）。"}});
        m.entries.push_back({"Timeline Decay", {"Timeline role decay multiplier (0.2x to 2.0x).", "Timeline 角色 Decay 倍數（0.2x 至 2.0x）。", "Timelineロールディケイ倍率（0.2x～2.0x）。"}});
        m.entries.push_back({"Timeline Mix (Int/Ext)", {"Timeline role internal synth (0%) to external audio (100%).", "Timeline 角色內部合成（0%）至外部 Audio（100%）。", "Timelineロール内部シンセ（0%）から外部オーディオ（100%）。"}});
        m.entries.push_back({"Foundation Style", {"Foundation role rhythm style (0-9, integer snap).", "Foundation 角色節奏風格（0-9，整數對齊）。", "Foundationロールリズムスタイル（0-9、整数スナップ）。"}});
        m.entries.push_back({"Foundation Density", {"Foundation role pattern density (0-100%, default 20%).", "Foundation 角色 Pattern 密度（0-100%，預設 20%）。", "Foundationロールパターン密度（0-100%、デフォルト20%）。"}});
        m.entries.push_back({"Foundation Length", {"Foundation role pattern length (4-32 steps, integer snap).", "Foundation 角色 Pattern 長度（4-32 步，整數對齊）。", "Foundationロールパターン長（4-32ステップ、整数スナップ）。"}});
        m.entries.push_back({"Foundation Freq", {"Foundation role frequency modifier (-1 to +1 octave).", "Foundation 角色頻率修正（-1 至 +1 八度）。", "Foundationロール周波数修正（-1～+1オクターブ）。"}});
        m.entries.push_back({"Foundation Decay", {"Foundation role decay multiplier (0.2x to 2.0x).", "Foundation 角色 Decay 倍數（0.2x 至 2.0x）。", "Foundationロールディケイ倍率（0.2x～2.0x）。"}});
        m.entries.push_back({"Foundation Mix (Int/Ext)", {"Foundation role internal synth (0%) to external audio (100%).", "Foundation 角色內部合成（0%）至外部 Audio（100%）。", "Foundationロール内部シンセ（0%）から外部オーディオ（100%）。"}});
        m.entries.push_back({"Groove Style", {"Groove role rhythm style (0-9, integer snap).", "Groove 角色節奏風格（0-9，整數對齊）。", "Grooveロールリズムスタイル（0-9、整数スナップ）。"}});
        m.entries.push_back({"Groove Density", {"Groove role pattern density (0-100%, default 50%).", "Groove 角色 Pattern 密度（0-100%，預設 50%）。", "Grooveロールパターン密度（0-100%、デフォルト50%）。"}});
        m.entries.push_back({"Groove Length", {"Groove role pattern length (4-32 steps, integer snap).", "Groove 角色 Pattern 長度（4-32 步，整數對齊）。", "Grooveロールパターン長（4-32ステップ、整数スナップ）。"}});
        m.entries.push_back({"Groove Freq", {"Groove role frequency modifier (-1 to +1 octave).", "Groove 角色頻率修正（-1 至 +1 八度）。", "Grooveロール周波数修正（-1～+1オクターブ）。"}});
        m.entries.push_back({"Groove Decay", {"Groove role decay multiplier (0.2x to 2.0x).", "Groove 角色 Decay 倍數（0.2x 至 2.0x）。", "Grooveロールディケイ倍率（0.2x～2.0x）。"}});
        m.entries.push_back({"Groove Mix (Int/Ext)", {"Groove role internal synth (0%) to external audio (100%).", "Groove 角色內部合成（0%）至外部 Audio（100%）。", "Grooveロール内部シンセ（0%）から外部オーディオ（100%）。"}});
        m.entries.push_back({"Lead Style", {"Lead role rhythm style (0-9, integer snap).", "Lead 角色節奏風格（0-9，整數對齊）。", "Leadロールリズムスタイル（0-9、整数スナップ）。"}});
        m.entries.push_back({"Lead Density", {"Lead role pattern density (0-100%, default 50%).", "Lead 角色 Pattern 密度（0-100%，預設 50%）。", "Leadロールパターン密度（0-100%、デフォルト50%）。"}});
        m.entries.push_back({"Lead Length", {"Lead role pattern length (4-32 steps, integer snap).", "Lead 角色 Pattern 長度（4-32 步，整數對齊）。", "Leadロールパターン長（4-32ステップ、整数スナップ）。"}});
        m.entries.push_back({"Lead Freq", {"Lead role frequency modifier (-1 to +1 octave).", "Lead 角色頻率修正（-1 至 +1 八度）。", "Leadロール周波数修正（-1～+1オクターブ）。"}});
        m.entries.push_back({"Lead Decay", {"Lead role decay multiplier (0.2x to 2.0x).", "Lead 角色 Decay 倍數（0.2x 至 2.0x）。", "Leadロールディケイ倍率（0.2x～2.0x）。"}});
        m.entries.push_back({"Lead Mix (Int/Ext)", {"Lead role internal synth (0%) to external audio (100%).", "Lead 角色內部合成（0%）至外部 Audio（100%）。", "Leadロール内部シンセ（0%）から外部オーディオ（100%）。"}});
        m.entries.push_back({"Clock", {"Clock input for pattern playback.", "Pattern 播放用 Clock 輸入。", "パターン再生用クロック入力。"}});
        m.entries.push_back({"Reset", {"Reset input. Returns all step counters to zero.", "Reset 輸入。將所有步數計數器歸零。", "リセット入力。全ステップカウンターをゼロに戻す。"}});
        m.entries.push_back({"Regenerate", {"Trigger input to generate new patterns.", "生成新 Pattern 的 Trigger 輸入。", "新パターン生成トリガー入力。"}});
        m.entries.push_back({"Rest CV", {"CV input for rest probability modulation.", "休止機率調變 CV 輸入。", "休符確率変調CV入力。"}});
        m.entries.push_back({"Fill Trigger", {"Trigger input for fill activation.", "Fill 啟動 Trigger 輸入。", "フィル発動トリガー入力。"}});
        m.entries.push_back({"Timeline Style CV", {"CV input for Timeline style modulation.", "Timeline 風格調變 CV 輸入。", "Timelineスタイル変調CV入力。"}});
        m.entries.push_back({"Timeline Density CV", {"CV input for Timeline density modulation.", "Timeline 密度調變 CV 輸入。", "Timeline密度変調CV入力。"}});
        m.entries.push_back({"Timeline Freq CV", {"CV input for Timeline frequency modulation.", "Timeline 頻率調變 CV 輸入。", "Timeline周波数変調CV入力。"}});
        m.entries.push_back({"Timeline Decay CV", {"CV input for Timeline decay modulation.", "Timeline Decay 調變 CV 輸入。", "Timelineディケイ変調CV入力。"}});
        m.entries.push_back({"Foundation Style CV", {"CV input for Foundation style modulation.", "Foundation 風格調變 CV 輸入。", "Foundationスタイル変調CV入力。"}});
        m.entries.push_back({"Foundation Density CV", {"CV input for Foundation density modulation.", "Foundation 密度調變 CV 輸入。", "Foundation密度変調CV入力。"}});
        m.entries.push_back({"Foundation Freq CV", {"CV input for Foundation frequency modulation.", "Foundation 頻率調變 CV 輸入。", "Foundation周波数変調CV入力。"}});
        m.entries.push_back({"Foundation Decay CV", {"CV input for Foundation decay modulation.", "Foundation Decay 調變 CV 輸入。", "Foundationディケイ変調CV入力。"}});
        m.entries.push_back({"Groove Style CV", {"CV input for Groove style modulation.", "Groove 風格調變 CV 輸入。", "Grooveスタイル変調CV入力。"}});
        m.entries.push_back({"Groove Density CV", {"CV input for Groove density modulation.", "Groove 密度調變 CV 輸入。", "Groove密度変調CV入力。"}});
        m.entries.push_back({"Groove Freq CV", {"CV input for Groove frequency modulation.", "Groove 頻率調變 CV 輸入。", "Groove周波数変調CV入力。"}});
        m.entries.push_back({"Groove Decay CV", {"CV input for Groove decay modulation.", "Groove Decay 調變 CV 輸入。", "Grooveディケイ変調CV入力。"}});
        m.entries.push_back({"Lead Style CV", {"CV input for Lead style modulation.", "Lead 風格調變 CV 輸入。", "Leadスタイル変調CV入力。"}});
        m.entries.push_back({"Lead Density CV", {"CV input for Lead density modulation.", "Lead 密度調變 CV 輸入。", "Lead密度変調CV入力。"}});
        m.entries.push_back({"Lead Freq CV", {"CV input for Lead frequency modulation.", "Lead 頻率調變 CV 輸入。", "Lead周波数変調CV入力。"}});
        m.entries.push_back({"Lead Decay CV", {"CV input for Lead decay modulation.", "Lead Decay 調變 CV 輸入。", "Leadディケイ変調CV入力。"}});
        m.entries.push_back({"Timeline Audio Input 1", {"External audio input 1 for Timeline role.", "Timeline 角色外部 Audio 輸入 1。", "Timelineロール外部オーディオ入力1。"}});
        m.entries.push_back({"Timeline Audio Input 2", {"External audio input 2 for Timeline role.", "Timeline 角色外部 Audio 輸入 2。", "Timelineロール外部オーディオ入力2。"}});
        m.entries.push_back({"Foundation Audio Input 1", {"External audio input 1 for Foundation role.", "Foundation 角色外部 Audio 輸入 1。", "Foundationロール外部オーディオ入力1。"}});
        m.entries.push_back({"Foundation Audio Input 2", {"External audio input 2 for Foundation role.", "Foundation 角色外部 Audio 輸入 2。", "Foundationロール外部オーディオ入力2。"}});
        m.entries.push_back({"Groove Audio Input 1", {"External audio input 1 for Groove role.", "Groove 角色外部 Audio 輸入 1。", "Grooveロール外部オーディオ入力1。"}});
        m.entries.push_back({"Groove Audio Input 2", {"External audio input 2 for Groove role.", "Groove 角色外部 Audio 輸入 2。", "Grooveロール外部オーディオ入力2。"}});
        m.entries.push_back({"Lead Audio Input 1", {"External audio input 1 for Lead role.", "Lead 角色外部 Audio 輸入 1。", "Leadロール外部オーディオ入力1。"}});
        m.entries.push_back({"Lead Audio Input 2", {"External audio input 2 for Lead role.", "Lead 角色外部 Audio 輸入 2。", "Leadロール外部オーディオ入力2。"}});
        m.entries.push_back({"Mix L", {"Left stereo mix output (all voices with spread).", "左聲道 Stereo Mix 輸出（所有 Voice 含 Spread）。", "左ステレオミックス出力（全ボイス、スプレッド含む）。"}});
        m.entries.push_back({"Mix R", {"Right stereo mix output (all voices with spread).", "右聲道 Stereo Mix 輸出（所有 Voice 含 Spread）。", "右ステレオミックス出力（全ボイス、スプレッド含む）。"}});
        m.entries.push_back({"Timeline 1 Audio", {"Timeline voice 1 audio output.", "Timeline Voice 1 Audio 輸出。", "Timelineボイス1オーディオ出力。"}});
        m.entries.push_back({"Timeline 1 Gate", {"Timeline voice 1 gate/trigger output.", "Timeline Voice 1 Gate/Trigger 輸出。", "Timelineボイス1ゲート/トリガー出力。"}});
        m.entries.push_back({"Timeline 1 Pitch CV (1V/Oct, C4=0V)", {"Timeline voice 1 pitch CV output.", "Timeline Voice 1 Pitch CV 輸出。", "Timelineボイス1ピッチCV出力。"}});
        m.entries.push_back({"Timeline 1 Velocity CV", {"Timeline voice 1 velocity CV output (0-8V).", "Timeline Voice 1 Velocity CV 輸出（0-8V）。", "Timelineボイス1ベロシティCV出力（0-8V）。"}});
        m.entries.push_back({"Timeline 2 Audio", {"Timeline voice 2 audio output.", "Timeline Voice 2 Audio 輸出。", "Timelineボイス2オーディオ出力。"}});
        m.entries.push_back({"Timeline 2 Gate", {"Timeline voice 2 gate/trigger output.", "Timeline Voice 2 Gate/Trigger 輸出。", "Timelineボイス2ゲート/トリガー出力。"}});
        m.entries.push_back({"Timeline 2 Pitch CV (1V/Oct, C4=0V)", {"Timeline voice 2 pitch CV output.", "Timeline Voice 2 Pitch CV 輸出。", "Timelineボイス2ピッチCV出力。"}});
        m.entries.push_back({"Timeline 2 Velocity CV", {"Timeline voice 2 velocity CV output (0-8V).", "Timeline Voice 2 Velocity CV 輸出（0-8V）。", "Timelineボイス2ベロシティCV出力（0-8V）。"}});
        m.entries.push_back({"Foundation 1 Audio", {"Foundation voice 1 audio output.", "Foundation Voice 1 Audio 輸出。", "Foundationボイス1オーディオ出力。"}});
        m.entries.push_back({"Foundation 1 Gate", {"Foundation voice 1 gate/trigger output.", "Foundation Voice 1 Gate/Trigger 輸出。", "Foundationボイス1ゲート/トリガー出力。"}});
        m.entries.push_back({"Foundation 1 Pitch CV (1V/Oct, C4=0V)", {"Foundation voice 1 pitch CV output.", "Foundation Voice 1 Pitch CV 輸出。", "Foundationボイス1ピッチCV出力。"}});
        m.entries.push_back({"Foundation 1 Velocity CV", {"Foundation voice 1 velocity CV output (0-8V).", "Foundation Voice 1 Velocity CV 輸出（0-8V）。", "Foundationボイス1ベロシティCV出力（0-8V）。"}});
        m.entries.push_back({"Foundation 2 Audio", {"Foundation voice 2 audio output.", "Foundation Voice 2 Audio 輸出。", "Foundationボイス2オーディオ出力。"}});
        m.entries.push_back({"Foundation 2 Gate", {"Foundation voice 2 gate/trigger output.", "Foundation Voice 2 Gate/Trigger 輸出。", "Foundationボイス2ゲート/トリガー出力。"}});
        m.entries.push_back({"Foundation 2 Pitch CV (1V/Oct, C4=0V)", {"Foundation voice 2 pitch CV output.", "Foundation Voice 2 Pitch CV 輸出。", "Foundationボイス2ピッチCV出力。"}});
        m.entries.push_back({"Foundation 2 Velocity CV", {"Foundation voice 2 velocity CV output (0-8V).", "Foundation Voice 2 Velocity CV 輸出（0-8V）。", "Foundationボイス2ベロシティCV出力（0-8V）。"}});
        m.entries.push_back({"Groove 1 Audio", {"Groove voice 1 audio output.", "Groove Voice 1 Audio 輸出。", "Grooveボイス1オーディオ出力。"}});
        m.entries.push_back({"Groove 1 Gate", {"Groove voice 1 gate/trigger output.", "Groove Voice 1 Gate/Trigger 輸出。", "Grooveボイス1ゲート/トリガー出力。"}});
        m.entries.push_back({"Groove 1 Pitch CV (1V/Oct, C4=0V)", {"Groove voice 1 pitch CV output.", "Groove Voice 1 Pitch CV 輸出。", "Grooveボイス1ピッチCV出力。"}});
        m.entries.push_back({"Groove 1 Velocity CV", {"Groove voice 1 velocity CV output (0-8V).", "Groove Voice 1 Velocity CV 輸出（0-8V）。", "Grooveボイス1ベロシティCV出力（0-8V）。"}});
        m.entries.push_back({"Groove 2 Audio", {"Groove voice 2 audio output.", "Groove Voice 2 Audio 輸出。", "Grooveボイス2オーディオ出力。"}});
        m.entries.push_back({"Groove 2 Gate", {"Groove voice 2 gate/trigger output.", "Groove Voice 2 Gate/Trigger 輸出。", "Grooveボイス2ゲート/トリガー出力。"}});
        m.entries.push_back({"Groove 2 Pitch CV (1V/Oct, C4=0V)", {"Groove voice 2 pitch CV output.", "Groove Voice 2 Pitch CV 輸出。", "Grooveボイス2ピッチCV出力。"}});
        m.entries.push_back({"Groove 2 Velocity CV", {"Groove voice 2 velocity CV output (0-8V).", "Groove Voice 2 Velocity CV 輸出（0-8V）。", "Grooveボイス2ベロシティCV出力（0-8V）。"}});
        m.entries.push_back({"Lead 1 Audio", {"Lead voice 1 audio output.", "Lead Voice 1 Audio 輸出。", "Leadボイス1オーディオ出力。"}});
        m.entries.push_back({"Lead 1 Gate", {"Lead voice 1 gate/trigger output.", "Lead Voice 1 Gate/Trigger 輸出。", "Leadボイス1ゲート/トリガー出力。"}});
        m.entries.push_back({"Lead 1 Pitch CV (1V/Oct, C4=0V)", {"Lead voice 1 pitch CV output.", "Lead Voice 1 Pitch CV 輸出。", "Leadボイス1ピッチCV出力。"}});
        m.entries.push_back({"Lead 1 Velocity CV", {"Lead voice 1 velocity CV output (0-8V).", "Lead Voice 1 Velocity CV 輸出（0-8V）。", "Leadボイス1ベロシティCV出力（0-8V）。"}});
        m.entries.push_back({"Lead 2 Audio", {"Lead voice 2 audio output.", "Lead Voice 2 Audio 輸出。", "Leadボイス2オーディオ出力。"}});
        m.entries.push_back({"Lead 2 Gate", {"Lead voice 2 gate/trigger output.", "Lead Voice 2 Gate/Trigger 輸出。", "Leadボイス2ゲート/トリガー出力。"}});
        m.entries.push_back({"Lead 2 Pitch CV (1V/Oct, C4=0V)", {"Lead voice 2 pitch CV output.", "Lead Voice 2 Pitch CV 輸出。", "Leadボイス2ピッチCV出力。"}});
        m.entries.push_back({"Lead 2 Velocity CV", {"Lead voice 2 velocity CV output (0-8V).", "Lead Voice 2 Velocity CV 輸出（0-8V）。", "Leadボイス2ベロシティCV出力（0-8V）。"}});
        data["UniversalRhythm"] = std::move(m);
    }
    // WeiiiDocumenta (86 entries)
    {
        ModuleHelpData m;
        m.name = "weiii documenta";
        m.description = {"Recording sampler emulating analog no-input mixer feedback with real-time transient slicing, 3-band EQ, send/return loop, soft limiter, and chaotic parameter morphing", "錄音 Sampler，模擬 No-input Mixer 回饋效果，具備即時 Transient 切片、3 段 EQ、Send/Return 迴路、Soft Limiter 與混沌參數變形", "ノーインプットミキサーのフィードバックを再現する録音サンプラー、リアルタイムトランジェントスライス、3バンドEQ、センド/リターンループ、ソフトリミッター、カオスパラメータモーフィング搭載"};
        m.entries.push_back({"REC", {"Toggle recording. Clears existing slices on start.", "切換錄音。開始時清除既有 Slice。", "録音切替。開始時に既存スライスをクリア。"}});
        m.entries.push_back({"Transient Detection", {"Triggers new slice when amplitude crosses threshold from below", "當振幅由下往上穿越閾值時觸發新 Slice", "振幅が閾値を下から上へ横切ると新しいスライスをトリガー"}});
        m.entries.push_back({"THRESHOLD", {"Detection threshold (0-10V, default 1V). Adjusting while not recording rescans existing buffer.", "偵測閾值（0-10V，預設 1V）。非錄音時調整會重新掃描既有 Buffer。", "検出閾値（0-10V、デフォルト1V）。非録音時の調整は既存バッファを再スキャン。"}});
        m.entries.push_back({"MIN SLICE", {"Minimum time between slices (0.001-1.0s, default 0.05s)", "Slice 間最小時間（0.001-1.0s，預設 0.05s）", "スライス間の最小時間（0.001-1.0s、デフォルト0.05s）"}});
        m.entries.push_back({"Slice Storage", {"Dynamic std::vector (unlimited slices). Each slice stores startSample, endSample, peakAmplitude.", "動態 std::vector（無限 Slice）。每個 Slice 儲存 startSample、endSample、peakAmplitude。", "動的std::vector（無制限スライス）。各スライスはstartSample、endSample、peakAmplitudeを保存。"}});
        m.entries.push_back({"Non-linear Mapping", {"Knob 0-1 maps to -8x to +8x with 1x at center", "Knob 0-1 maps to -8x to +8x with 1x at center", "Knob 0-1 maps to -8x to +8x with 1x at center"}});
        m.entries.push_back({"0-25%", {"Reverse playback from -8x to 0", "反向播放 -8x 至 0", "逆再生 -8x～0"}});
        m.entries.push_back({"25-50%", {"Forward playback from 0 to 1x", "正向播放 0 至 1x", "順再生 0～1x"}});
        m.entries.push_back({"50-100%", {"Forward playback from 1x to 8x", "正向播放 1x 至 8x", "順再生 1x～8x"}});
        m.entries.push_back({"Linear Interpolation", {"Sub-sample accuracy using playbackPhase for smooth playback at all speeds", "Sub-sample accuracy using playbackPhase for smooth playback at all speeds", "Sub-sample accuracy using playbackPhase for smooth playback at all speeds"}});
        m.entries.push_back({"POLY", {"1-8 voices (integer snap, CV modulatable)", "1-8 聲（整數對齊，可 CV 調變）", "1-8ボイス（整数スナップ、CVモジュレート可能）"}});
        m.entries.push_back({"Voice Structure", {"Each voice has independent sliceIndex, playbackPosition, playbackPhase, speedMultiplier", "Each voice has independent sliceIndex, playbackPosition, playbackPhase, speedMultiplier", "Each voice has independent sliceIndex, playbackPosition, playbackPhase, speedMultiplier"}});
        m.entries.push_back({"Speed Variation", {"Voices 1-7 get random speedMultiplier (0.5-2.0x)", "Voices 1-7 get random speedMultiplier (0.5-2.0x)", "Voices 1-7 get random speedMultiplier (0.5-2.0x)"}});
        m.entries.push_back({"Slice Switching", {"Voices randomly switch slices based on sliceChangeTimer (0.5-2.0s intervals)", "Voices randomly switch slices based on sliceChangeTimer (0.5-2.0s intervals)", "Voices randomly switch slices based on sliceChangeTimer (0.5-2.0s intervals)"}});
        m.entries.push_back({"Mix Output", {"Multi-voice output divided by numVoices to prevent clipping", "Multi-voice output divided by numVoices to prevent clipping", "Multi-voice output divided by numVoices to prevent clipping"}});
        m.entries.push_back({"Fade Time", {"0.1ms (SLICE_FADE_TIME_MS constant)", "0.1ms (SLICE_FADE_TIME_MS constant)", "0.1ms (SLICE_FADE_TIME_MS constant)"}});
        m.entries.push_back({"Fade Increment", {"Calculated from fade time and sample rate", "Calculated from fade time and sample rate", "Calculated from fade time and sample rate"}});
        m.entries.push_back({"Pending Slice", {"New slice stored in pendingSliceIndex during fade-out, applied after envelope reaches 0", "New slice stored in pendingSliceIndex during fade-out, applied after envelope reaches 0", "New slice stored in pendingSliceIndex during fade-out, applied after envelope reaches 0"}});
        m.entries.push_back({"Maximum Oscillator Frequency", {"~5kHz (limited by 0.1ms fade envelope)", "~5kHz (limited by 0.1ms fade envelope)", "~5kHz (limited by 0.1ms fade envelope)"}});
        m.entries.push_back({"Exponential Filter", {"Smoothly interpolates toward target with 0.5% step per sample", "Smoothly interpolates toward target with 0.5% step per sample", "Smoothly interpolates toward target with 0.5% step per sample"}});
        m.entries.push_back({"Time Constant", {"~5ms at 44.1kHz", "~5ms at 44.1kHz", "~5ms at 44.1kHz"}});
        m.entries.push_back({"Smoothed Parameters", {"Scan, Threshold, LoopEnd, FeedbackAmount, FeedbackDelay", "Scan, Threshold, LoopEnd, FeedbackAmount, FeedbackDelay", "Scan, Threshold, LoopEnd, FeedbackAmount, FeedbackDelay"}});
        m.entries.push_back({"No-Input Feedback", {"Output feeds back to input with analog-style saturation", "輸出回授至輸入，帶類比風格飽和", "Output feeds back to input with analog-style saturation"}});
        m.entries.push_back({"Saturation", {"Applies tanh soft-clipping scaled for unity gain at low levels", "Applies tanh soft-clipping scaled for unity gain at low levels", "Applies tanh soft-clipping scaled for unity gain at low levels"}});
        m.entries.push_back({"Mix", {"Feedback signal added to output scaled by feedback amount", "Feedback signal added to output scaled by feedback amount", "Feedback signal added to output scaled by feedback amount"}});
        m.entries.push_back({"States", {"IDLE, MORPHING, HOLDING, RETURNING", "IDLE, MORPHING, HOLDING, RETURNING", "IDLE, MORPHING, HOLDING, RETURNING"}});
        m.entries.push_back({"Curves", {"Linear, Smooth (S-curve easing), Exponential (fast attack, slow decay), Bezier (cubic)", "Linear, Smooth (S-curve easing), Exponential (fast attack, slow decay), Bezier (cubic)", "Linear, Smooth (S-curve easing), Exponential (fast attack, slow decay), Bezier (cubic)"}});
        m.entries.push_back({"Default Targets", {"EQ, Scan, Feedback, Speed enabled. Threshold, MinSlice, S&H disabled.", "EQ, Scan, Feedback, Speed enabled. Threshold, MinSlice, S&H disabled.", "EQ, Scan, Feedback, Speed enabled. Threshold, MinSlice, S&H disabled."}});
        m.entries.push_back({"Morph Time", {"1-20 seconds (configurable via right-click)", "1-20 seconds (configurable via right-click)", "1-20 seconds (configurable via right-click)"}});
        m.entries.push_back({"Send L/R", {"Outputs the current audio signal before EQ processing, within the feedback loop", "在 EQ 處理前輸出目前音訊訊號，位於 Feedback 迴路內", "EQ処理前の現在のオーディオ信号を出力（フィードバックループ内）"}});
        m.entries.push_back({"Return L/R", {"When connected, replaces the signal with external return. Allows inserting external effects into the feedback chain.", "連接時取代訊號為外部 Return。可將外部效果器插入 Feedback 鏈路。", "接続時は外部リターンで信号を置換。外部エフェクトをフィードバックチェーンに挿入可能。"}});
        m.entries.push_back({"Signal Chain Position", {"Send/Return sits after playback+feedback mixing, before EQ. The EQ'd output then feeds back into the next cycle.", "Send/Return sits after playback+feedback mixing, before EQ. The EQ'd output then feeds back into the next cycle.", "Send/Return sits after playback+feedback mixing, before EQ. The EQ'd output then feeds back into the next cycle."}});
        m.entries.push_back({"Threshold", {"-3dB (7.07V for 10V peak system)", "偵測閾值（0-10V，預設 1V）。非錄音時調整會重新掃描既有 Buffer。", "検出閾値（0-10V、デフォルト1V）。非録音時の調整は既存バッファを再スキャン。"}});
        m.entries.push_back({"Below Threshold", {"Signal passes through unchanged (linear)", "Signal passes through unchanged (linear)", "Signal passes through unchanged (linear)"}});
        m.entries.push_back({"Above Threshold", {"Soft knee compression with saturation amount 0.2", "Soft knee compression with saturation amount 0.2", "Soft knee compression with saturation amount 0.2"}});
        m.entries.push_back({"Position", {"Applied to main output after EQ, before final output stage", "Applied to main output after EQ, before final output stage", "Applied to main output after EQ, before final output stage"}});
        m.entries.push_back({"Low", {"80Hz shelf (Allen & Heath reference)", "80Hz Shelf（Allen & Heath 參考）", "80Hz Shelf（Allen & Heath参照）"}});
        m.entries.push_back({"Mid", {"2.5kHz peak", "2.5kHz Peak", "2.5kHz Peak"}});
        m.entries.push_back({"High", {"12kHz shelf", "12kHz Shelf", "12kHz Shelf"}});
        m.entries.push_back({"Range", {"±12dB per band", "±12dB per band", "±12dB per band"}});
        m.entries.push_back({"Implementation", {"TBiquadFilter (IIR)", "TBiquadFilter (IIR)", "TBiquadFilter (IIR)"}});
        m.entries.push_back({"Rate", {"Logarithmic mapping, range 0.01-100Hz (center = 1Hz)", "對數映射，範圍 0.01-100Hz（中心 = 1Hz）", "対数マッピング、範囲0.01-100Hz（中心=1Hz）"}});
        m.entries.push_back({"Slew", {"0-1 second slew time", "0-1 秒 Slew 時間", "0-1秒スルー時間"}});
        m.entries.push_back({"Amount", {"0-5x gain multiplier", "0-5x 增益倍數", "0-5xゲイン倍率"}});
        m.entries.push_back({"Output", {"±10V CV output", "±10V CV output", "±10V CV output"}});
        m.entries.push_back({"Slice Scan", {"Scan position through recorded slices (0-100%). CV modulatable.", "Slice 掃描位置（0-100%）。可 CV 調變。", "スライススキャン位置（0-100%）。CVモジュレート可能。"}});
        m.entries.push_back({"Scan CV Attenuverter", {"Attenuverter for Slice Scan CV input (-1.0 to +1.0).", "Slice Scan CV 輸入的 Attenuverter（-1.0 至 +1.0）。", "Slice Scan CV入力用アッテヌバーター（-1.0～+1.0）。"}});
        m.entries.push_back({"Slice Threshold", {"Transient detection threshold (0-10V, default 1V). Rescans buffer when adjusted outside recording.", "Transient 偵測閾值（0-10V，預設 1V）。非錄音時調整會重新掃描 Buffer。", "トランジェント検出閾値（0-10V、デフォルト1V）。非録音時の調整でバッファを再スキャン。"}});
        m.entries.push_back({"Min Slice Time", {"Minimum time between slices (0.001-1.0s, default 0.05s).", "Slice 間最小時間（0.001-1.0s，預設 0.05s）。", "スライス間の最小時間（0.001-1.0s、デフォルト0.05s）。"}});
        m.entries.push_back({"Loop End Point", {"Loop end position (0-100%, default 100%).", "Loop 結束位置（0-100%，預設 100%）。", "ループ終了位置（0-100%、デフォルト100%）。"}});
        m.entries.push_back({"Morph Random (Hold)", {"Hold button for chaotic parameter morphing. Randomizes enabled targets while held.", "混沌參數變形按鈕。按住時隨機化已啟用的目標參數。", "カオスパラメータモーフィングボタン。押し続けると有効なターゲットをランダム化。"}});
        m.entries.push_back({"Feedback Amount", {"No-input feedback level (0-100%). Adds tanh-saturated output back to input.", "No-input Feedback 量（0-100%）。以 tanh 飽和方式將輸出回授至輸入。", "ノーインプットフィードバック量（0-100%）。tanh飽和で出力を入力にフィードバック。"}});
        m.entries.push_back({"Feedback CV Attenuverter", {"Attenuverter for Feedback Amount CV input (-1.0 to +1.0).", "Feedback Amount CV 輸入的 Attenuverter（-1.0 至 +1.0）。", "Feedback Amount CV入力用アッテヌバーター（-1.0～+1.0）。"}});
        m.entries.push_back({"Low EQ (80Hz)", {"Low shelf EQ at 80Hz (±12dB).", "80Hz Low Shelf EQ（±12dB）。", "80Hz ローシェルフEQ（±12dB）。"}});
        m.entries.push_back({"Mid EQ (2.5kHz)", {"Mid peak EQ at 2.5kHz (±12dB).", "2.5kHz Mid Peak EQ（±12dB）。", "2.5kHz ミッドピークEQ（±12dB）。"}});
        m.entries.push_back({"High EQ (12kHz)", {"High shelf EQ at 12kHz (±12dB).", "12kHz High Shelf EQ（±12dB）。", "12kHz ハイシェルフEQ（±12dB）。"}});
        m.entries.push_back({"Playback Speed", {"Non-linear speed control. 0-25%: reverse -8x to 0. 25-50%: forward 0 to 1x. 50-100%: forward 1x to 8x.", "非線性速度控制。0-25%: 反向 -8x 至 0。25-50%: 正向 0 至 1x。50-100%: 正向 1x 至 8x。", "非線形速度コントロール。0-25%: 逆再生-8x～0。25-50%: 順再生0～1x。50-100%: 順再生1x～8x。"}});
        m.entries.push_back({"Polyphonic Voices", {"Number of playback voices (1-8, integer snap). CV modulatable.", "播放 Voice 數量（1-8，整數對齊）。可 CV 調變。", "再生ボイス数（1-8、整数スナップ）。CVモジュレート可能。"}});
        m.entries.push_back({"S&H Slew Time", {"Sample & Hold slew time (0-1s). Smooths CV transitions.", "Sample & Hold Slew 時間（0-1s）。平滑 CV 過渡。", "サンプル＆ホールドスルー時間（0-1s）。CVトランジションをスムーズ化。"}});
        m.entries.push_back({"S&H Gain", {"Sample & Hold gain multiplier (0-5x).", "Sample & Hold 增益倍數（0-5x）。", "サンプル＆ホールドゲイン倍率（0-5x）。"}});
        m.entries.push_back({"S&H Gain CV Attenuverter", {"Attenuverter for S&H Gain CV input (-1.0 to +1.0).", "S&H Gain CV 輸入的 Attenuverter（-1.0 至 +1.0）。", "S&H Gain CV入力用アッテヌバーター（-1.0～+1.0）。"}});
        m.entries.push_back({"S&H Sample Rate", {"Sample & Hold rate (0.01-100Hz, logarithmic). Center = 1Hz.", "Sample & Hold 取樣率（0.01-100Hz，對數映射）。中心 = 1Hz。", "サンプル＆ホールドレート（0.01-100Hz、対数）。中心=1Hz。"}});
        m.entries.push_back({"S&H Rate CV Attenuverter", {"Attenuverter for S&H Rate CV input (-1.0 to +1.0).", "S&H Rate CV 輸入的 Attenuverter（-1.0 至 +1.0）。", "S&H Rate CV入力用アッテヌバーター（-1.0～+1.0）。"}});
        m.entries.push_back({"Record", {"Toggle recording. Clears existing slices on start.", "切換錄音。開始時清除既有 Slice。", "録音切替。開始時に既存スライスをクリア。"}});
        m.entries.push_back({"Play/Loop (cycles: Loop → Play)", {"Toggle playback mode. Cycles between Loop and one-shot Play.", "切換播放模式。在 Loop 與單次 Play 之間循環。", "再生モード切替。ループとワンショット再生を切り替え。"}});
        m.entries.push_back({"Stop (hold 2 sec to Clear)", {"Stop playback. Hold for 2 seconds to clear buffer.", "停止播放。按住 2 秒清除 Buffer。", "再生停止。2秒長押しでバッファクリア。"}});
        m.entries.push_back({"Audio L", {"Left audio input for recording.", "左聲道 Audio 錄音輸入。", "左チャンネルオーディオ録音入力。"}});
        m.entries.push_back({"Audio R", {"Right audio input for recording.", "右聲道 Audio 錄音輸入。", "右チャンネルオーディオ録音入力。"}});
        m.entries.push_back({"Slice Scan CV", {"CV input for slice scan position modulation.", "Slice 掃描位置調變 CV 輸入。", "スライススキャン位置変調CV入力。"}});
        m.entries.push_back({"Threshold CV", {"CV input for threshold modulation.", "閾值調變 CV 輸入。", "閾値変調CV入力。"}});
        m.entries.push_back({"Feedback Amount CV", {"CV input for feedback amount modulation.", "Feedback Amount 調變 CV 輸入。", "フィードバック量変調CV入力。"}});
        m.entries.push_back({"Speed CV", {"CV input for playback speed modulation.", "播放速度調變 CV 輸入。", "再生速度変調CV入力。"}});
        m.entries.push_back({"Polyphonic CV", {"CV input for voice count modulation.", "Voice 數量調變 CV 輸入。", "ボイス数変調CV入力。"}});
        m.entries.push_back({"S&H Amount CV", {"CV input for S&H gain modulation.", "S&H Gain 調變 CV 輸入。", "S&Hゲイン変調CV入力。"}});
        m.entries.push_back({"S&H Rate CV", {"CV input for S&H rate modulation.", "S&H Rate 調變 CV 輸入。", "S&Hレート変調CV入力。"}});
        m.entries.push_back({"Return L", {"Left return input for external effects loop.", "左聲道 Return 輸入，用於外部效果器迴路。", "左チャンネルリターン入力（外部エフェクトループ用）。"}});
        m.entries.push_back({"Return R", {"Right return input for external effects loop.", "右聲道 Return 輸入，用於外部效果器迴路。", "右チャンネルリターン入力（外部エフェクトループ用）。"}});
        m.entries.push_back({"Rec Trigger", {"Trigger input to toggle recording.", "切換錄音的 Trigger 輸入。", "録音切替トリガー入力。"}});
        m.entries.push_back({"Play Trigger", {"Trigger input to toggle playback.", "切換播放的 Trigger 輸入。", "再生切替トリガー入力。"}});
        m.entries.push_back({"Clear Trigger", {"Trigger input to stop/clear.", "停止/清除的 Trigger 輸入。", "停止/クリアトリガー入力。"}});
        m.entries.push_back({"Morph Gate", {"Gate input for parameter morphing. Morphs while gate is high.", "參數變形 Gate 輸入。Gate 為高時進行變形。", "パラメータモーフィングゲート入力。ゲートがハイの間モーフィング実行。"}});
        m.entries.push_back({"Main L", {"Left main audio output (with EQ and soft limiter applied).", "左聲道 Main Audio 輸出（含 EQ 與 Soft Limiter）。", "左チャンネルメインオーディオ出力（EQ・ソフトリミッター適用済み）。"}});
        m.entries.push_back({"Main R", {"Right main audio output (with EQ and soft limiter applied).", "右聲道 Main Audio 輸出（含 EQ 與 Soft Limiter）。", "右チャンネルメインオーディオ出力（EQ・ソフトリミッター適用済み）。"}});
        m.entries.push_back({"S&H CV", {"Sample & Hold CV output (±10V).", "Sample & Hold CV 輸出（±10V）。", "サンプル＆ホールドCV出力（±10V）。"}});
        m.entries.push_back({"Send L", {"Left send output for external effects loop (pre-EQ).", "左聲道 Send 輸出，用於外部效果器迴路（EQ 前）。", "左チャンネルセンド出力（外部エフェクトループ用、EQ前）。"}});
        m.entries.push_back({"Send R", {"Right send output for external effects loop (pre-EQ).", "右聲道 Send 輸出，用於外部效果器迴路（EQ 前）。", "右チャンネルセンド出力（外部エフェクトループ用、EQ前）。"}});
        data["WeiiiDocumenta"] = std::move(m);
    }
    // YAMANOTE (77 entries)
    {
        ModuleHelpData m;
        m.name = "YAMANOTE";
        m.description = {"8-channel stereo mixer with dual aux send buses, stereo returns, and chain I/O for modular expansion", "8 聲道 Stereo Mixer，具備雙 Aux Send Bus、Stereo Return 與 Chain I/O 擴展", "デュアルAuxセンドバス、ステレオリターン、チェーンI/O拡張を備えた8チャンネルステレオミキサー"};
        m.entries.push_back({"Channel Detection", {"Automatically detects maximum polyphonic channel count across all inputs", "Automatically detects maximum polyphonic channel count across all inputs", "Automatically detects maximum polyphonic channel count across all inputs"}});
        m.entries.push_back({"Output Channels", {"All outputs match the maximum detected polyphonic count", "All outputs match the maximum detected polyphonic count", "All outputs match the maximum detected polyphonic count"}});
        m.entries.push_back({"Per-Channel Processing", {"Each polyphonic channel processed independently through send buses", "Each polyphonic channel processed independently through send buses", "Each polyphonic channel processed independently through send buses"}});
        m.entries.push_back({"Maximum Polyphony", {"16 channels (VCV Rack standard)", "16 channels (VCV Rack standard)", "16 channels (VCV Rack standard)"}});
        m.entries.push_back({"L Input", {"Left audio input (polyphonic). If polyphonic, each channel processed independently.", "左聲道輸入（Polyphonic）。若為 Polyphonic，各聲道獨立處理。", "左オーディオ入力（ポリフォニック）。ポリフォニックの場合、各チャンネルを独立処理。"}});
        m.entries.push_back({"R Input", {"Right audio input (polyphonic). If unconnected, Left input is copied to Right.", "右聲道輸入（Polyphonic）。若未連接，Left 輸入複製至 Right。", "右オーディオ入力（ポリフォニック）。未接続の場合、Left入力がRightにコピーされる。"}});
        m.entries.push_back({"Send A Knob", {"Send level to aux bus A (0.0-1.0, default: 0.0). Input signal scaled by knob position and added to bus A.", "至 Aux Bus A 的 Send Level（0.0-1.0，預設: 0.0）。輸入訊號依旋鈕位置縮放後加至 Bus A。", "Auxバス Aへのセンドレベル（0.0-1.0、デフォルト: 0.0）。入力信号をノブ位置でスケールしてバス Aに加算。"}});
        m.entries.push_back({"Send B Knob", {"Send level to aux bus B (0.0-1.0, default: 0.0). Input signal scaled by knob position and added to bus B.", "至 Aux Bus B 的 Send Level（0.0-1.0，預設: 0.0）。輸入訊號依旋鈕位置縮放後加至 Bus B。", "Auxバス Bへのセンドレベル（0.0-1.0、デフォルト: 0.0）。入力信号をノブ位置でスケールしてバス Bに加算。"}});
        m.entries.push_back({"Send A/B Calculation", {"Sum of all 8 channels weighted by their respective send levels, plus accumulated send signals from left-adjacent YAMANOTE via expander", "Sum of all 8 channels weighted by their respective send levels, plus accumulated send signals from left-adjacent YAMANOTE via expander", "Sum of all 8 channels weighted by their respective send levels, plus accumulated send signals from left-adjacent YAMANOTE via expander"}});
        m.entries.push_back({"Expander Accumulation", {"When multiple YAMANOTE modules are chained, send signals accumulate from left to right. The rightmost YAMANOTE outputs the combined send of all chained modules.", "When multiple YAMANOTE modules are chained, send signals accumulate from left to right. The rightmost YAMANOTE outputs the combined send of all chained modules.", "When multiple YAMANOTE modules are chained, send signals accumulate from left to right. The rightmost YAMANOTE outputs the combined send of all chained modules."}});
        m.entries.push_back({"Stereo Processing", {"Left and right processed independently through separate sum accumulators", "Left and right processed independently through separate sum accumulators", "Left and right processed independently through separate sum accumulators"}});
        m.entries.push_back({"Output Clamping", {"All send outputs are hard-clamped to ±10V to prevent clipping", "All send outputs are hard-clamped to ±10V to prevent clipping", "All send outputs are hard-clamped to ±10V to prevent clipping"}});
        m.entries.push_back({"Typical Use", {"Connect send outputs to reverb/delay inputs, return outputs back to Return inputs", "Connect send outputs to reverb/delay inputs, return outputs back to Return inputs", "Connect send outputs to reverb/delay inputs, return outputs back to Return inputs"}});
        m.entries.push_back({"SEND A L/R", {"Stereo aux send A outputs (polyphonic)", "Stereo Aux Send A 輸出（Polyphonic）", "ステレオAuxセンド A出力（ポリフォニック）"}});
        m.entries.push_back({"SEND B L/R", {"Stereo aux send B outputs (polyphonic)", "Stereo Aux Send B 輸出（Polyphonic）", "ステレオAuxセンド B出力（ポリフォニック）"}});
        m.entries.push_back({"RETURN A L/R", {"Stereo return A inputs from external effects (polyphonic)", "來自外部 Effect 的 Stereo Return A 輸入（Polyphonic）", "外部エフェクトからのステレオリターン A入力（ポリフォニック）"}});
        m.entries.push_back({"RETURN B L/R", {"Stereo return B inputs from external effects (polyphonic)", "來自外部 Effect 的 Stereo Return B 輸入（Polyphonic）", "外部エフェクトからのステレオリターン B入力（ポリフォニック）"}});
        m.entries.push_back({"CHAIN L/R", {"Chain input from previous mixer module (polyphonic). Used for cascading multiple YAMANOTE or U8 modules.", "來自前一個 Mixer 模組的 Chain 輸入（Polyphonic）。用於串聯多個 YAMANOTE 或 U8 模組。", "前のミキサーモジュールからのチェーン入力（ポリフォニック）。複数のYAMANOTEまたはU8モジュールのカスケード接続に使用。"}});
        m.entries.push_back({"MIX L/R", {"Final stereo mix output (polyphonic). Sums Return A, Return B, and Chain inputs. Clamped to ±10V.", "最終 Stereo Mix 輸出（Polyphonic）。匯總 Return A、Return B 與 Chain 輸入。限制在 ±10V。", "最終ステレオミックス出力（ポリフォニック）。リターン A、リターン B、チェーン入力を合計。±10Vにクランプ。"}});
        m.entries.push_back({"When Bypassed", {"Chain input passes directly to Mix output, maintaining signal flow", "Chain input passes directly to Mix output, maintaining signal flow", "Chain input passes directly to Mix output, maintaining signal flow"}});
        m.entries.push_back({"Send Outputs", {"Not affected by bypass (become silent)", "Not affected by bypass (become silent)", "Not affected by bypass (become silent)"}});
        m.entries.push_back({"Purpose", {"Allows temporarily removing effects from chain without breaking mixer cascade", "Allows temporarily removing effects from chain without breaking mixer cascade", "Allows temporarily removing effects from chain without breaking mixer cascade"}});
        m.entries.push_back({"Effects Send", {"SEND A → Reverb input → Reverb output → RETURN A", "SEND A → Reverb input → Reverb output → RETURN A", "SEND A → Reverb input → Reverb output → RETURN A"}});
        m.entries.push_back({"Parallel Effects", {"SEND B → Delay input → Delay output → RETURN B", "SEND B → Delay input → Delay output → RETURN B", "SEND B → Delay input → Delay output → RETURN B"}});
        m.entries.push_back({"Cascading", {"Connect MIX L/R to next mixer's CHAIN L/R", "Connect MIX L/R to next mixer's CHAIN L/R", "Connect MIX L/R to next mixer's CHAIN L/R"}});
        m.entries.push_back({"With U8", {"U8 handles dry channel levels, YAMANOTE handles send/return effects", "U8 handles dry channel levels, YAMANOTE handles send/return effects", "U8 handles dry channel levels, YAMANOTE handles send/return effects"}});
        m.entries.push_back({"Automatic Chain Connection", {"When YAMANOTE modules are placed adjacent to each other, next to U8, ALEXANDERPLATZ, or SHINJUKU modules, chain cables are automatically created", "When YAMANOTE modules are placed adjacent to each other, next to U8, ALEXANDERPLATZ, or SHINJUKU modules, chain cables are automatically created", "When YAMANOTE modules are placed adjacent to each other, next to U8, ALEXANDERPLATZ, or SHINJUKU modules, chain cables are automatically created"}});
        m.entries.push_back({"Cable Color", {"Green (#80C342) matching the JR Yamanote Line train color", "Green (#80C342) matching the JR Yamanote Line train color", "Green (#80C342) matching the JR Yamanote Line train color"}});
        m.entries.push_back({"Auto CH Input", {"When audio is connected to an adjacent U8, ALEXANDERPLATZ, or SHINJUKU module's input, the same source automatically connects to YAMANOTE's corresponding channel input", "When audio is connected to an adjacent U8, ALEXANDERPLATZ, or SHINJUKU module's input, the same source automatically connects to YAMANOTE's corresponding channel input", "When audio is connected to an adjacent U8, ALEXANDERPLATZ, or SHINJUKU module's input, the same source automatically connects to YAMANOTE's corresponding channel input"}});
        m.entries.push_back({"Channel Mapping", {"Tracks from chained modules are assigned from left to right. U8 provides 1 channel, ALEXANDERPLATZ provides 4 channels (tracks 3→0), SHINJUKU provides 8 channels (tracks 7→0). Multiple YAMANOTE modules share the channels with offset (first YAMANOTE handles leftmost channels, next handles subsequent channels).", "Tracks from chained modules are assigned from left to right. U8 provides 1 channel, ALEXANDERPLATZ provides 4 channels (tracks 3→0), SHINJUKU provides 8 channels (tracks 7→0). Multiple YAMANOTE modules share the channels with offset (first YAMANOTE handles leftmost channels, next handles subsequent channels).", "Tracks from chained modules are assigned from left to right. U8 provides 1 channel, ALEXANDERPLATZ provides 4 channels (tracks 3→0), SHINJUKU provides 8 channels (tracks 7→0). Multiple YAMANOTE modules share the channels with offset (first YAMANOTE handles leftmost channels, next handles subsequent channels)."}});
        m.entries.push_back({"Send/Return Auto-Hide", {"When another YAMANOTE is placed to the right, Send/Return ports and labels are automatically hidden (signals are accumulated via expander and output from the rightmost YAMANOTE)", "When another YAMANOTE is placed to the right, Send/Return ports and labels are automatically hidden (signals are accumulated via expander and output from the rightmost YAMANOTE)", "When another YAMANOTE is placed to the right, Send/Return ports and labels are automatically hidden (signals are accumulated via expander and output from the rightmost YAMANOTE)"}});
        m.entries.push_back({"Non-Destructive", {"Auto cables are only created if the target input is not already connected", "Auto cables are only created if the target input is not already connected", "Auto cables are only created if the target input is not already connected"}});
        m.entries.push_back({"Dynamic", {"Cables automatically update when modules are moved, removed, or input connections change", "Cables automatically update when modules are moved, removed, or input connections change", "Cables automatically update when modules are moved, removed, or input connections change"}});
        m.entries.push_back({"CH1 Send A", {"Channel 1 send level to aux bus A (0.0-1.0).", "聲道 1 至 Aux Bus A 的 Send Level（0.0-1.0）。", "チャンネル1のAuxバスAへのセンドレベル（0.0-1.0）。"}});
        m.entries.push_back({"CH1 Send B", {"Channel 1 send level to aux bus B (0.0-1.0).", "聲道 1 至 Aux Bus B 的 Send Level（0.0-1.0）。", "チャンネル1のAuxバスBへのセンドレベル（0.0-1.0）。"}});
        m.entries.push_back({"CH2 Send A", {"Channel 2 send level to aux bus A (0.0-1.0).", "聲道 2 至 Aux Bus A 的 Send Level（0.0-1.0）。", "チャンネル2のAuxバスAへのセンドレベル（0.0-1.0）。"}});
        m.entries.push_back({"CH2 Send B", {"Channel 2 send level to aux bus B (0.0-1.0).", "聲道 2 至 Aux Bus B 的 Send Level（0.0-1.0）。", "チャンネル2のAuxバスBへのセンドレベル（0.0-1.0）。"}});
        m.entries.push_back({"CH3 Send A", {"Channel 3 send level to aux bus A (0.0-1.0).", "聲道 3 至 Aux Bus A 的 Send Level（0.0-1.0）。", "チャンネル3のAuxバスAへのセンドレベル（0.0-1.0）。"}});
        m.entries.push_back({"CH3 Send B", {"Channel 3 send level to aux bus B (0.0-1.0).", "聲道 3 至 Aux Bus B 的 Send Level（0.0-1.0）。", "チャンネル3のAuxバスBへのセンドレベル（0.0-1.0）。"}});
        m.entries.push_back({"CH4 Send A", {"Channel 4 send level to aux bus A (0.0-1.0).", "聲道 4 至 Aux Bus A 的 Send Level（0.0-1.0）。", "チャンネル4のAuxバスAへのセンドレベル（0.0-1.0）。"}});
        m.entries.push_back({"CH4 Send B", {"Channel 4 send level to aux bus B (0.0-1.0).", "聲道 4 至 Aux Bus B 的 Send Level（0.0-1.0）。", "チャンネル4のAuxバスBへのセンドレベル（0.0-1.0）。"}});
        m.entries.push_back({"CH5 Send A", {"Channel 5 send level to aux bus A (0.0-1.0).", "聲道 5 至 Aux Bus A 的 Send Level（0.0-1.0）。", "チャンネル5のAuxバスAへのセンドレベル（0.0-1.0）。"}});
        m.entries.push_back({"CH5 Send B", {"Channel 5 send level to aux bus B (0.0-1.0).", "聲道 5 至 Aux Bus B 的 Send Level（0.0-1.0）。", "チャンネル5のAuxバスBへのセンドレベル（0.0-1.0）。"}});
        m.entries.push_back({"CH6 Send A", {"Channel 6 send level to aux bus A (0.0-1.0).", "聲道 6 至 Aux Bus A 的 Send Level（0.0-1.0）。", "チャンネル6のAuxバスAへのセンドレベル（0.0-1.0）。"}});
        m.entries.push_back({"CH6 Send B", {"Channel 6 send level to aux bus B (0.0-1.0).", "聲道 6 至 Aux Bus B 的 Send Level（0.0-1.0）。", "チャンネル6のAuxバスBへのセンドレベル（0.0-1.0）。"}});
        m.entries.push_back({"CH7 Send A", {"Channel 7 send level to aux bus A (0.0-1.0).", "聲道 7 至 Aux Bus A 的 Send Level（0.0-1.0）。", "チャンネル7のAuxバスAへのセンドレベル（0.0-1.0）。"}});
        m.entries.push_back({"CH7 Send B", {"Channel 7 send level to aux bus B (0.0-1.0).", "聲道 7 至 Aux Bus B 的 Send Level（0.0-1.0）。", "チャンネル7のAuxバスBへのセンドレベル（0.0-1.0）。"}});
        m.entries.push_back({"CH8 Send A", {"Channel 8 send level to aux bus A (0.0-1.0).", "聲道 8 至 Aux Bus A 的 Send Level（0.0-1.0）。", "チャンネル8のAuxバスAへのセンドレベル（0.0-1.0）。"}});
        m.entries.push_back({"CH8 Send B", {"Channel 8 send level to aux bus B (0.0-1.0).", "聲道 8 至 Aux Bus B 的 Send Level（0.0-1.0）。", "チャンネル8のAuxバスBへのセンドレベル（0.0-1.0）。"}});
        m.entries.push_back({"CH1 Left", {"Channel 1 left audio input (polyphonic).", "聲道 1 Left Audio 輸入（Polyphonic）。", "チャンネル1の左オーディオ入力（ポリフォニック）。"}});
        m.entries.push_back({"CH1 Right", {"Channel 1 right audio input (polyphonic). If unconnected, left is copied.", "聲道 1 Right Audio 輸入（Polyphonic）。未連接時複製 Left。", "チャンネル1の右オーディオ入力（ポリフォニック）。未接続時左をコピー。"}});
        m.entries.push_back({"CH2 Left", {"Channel 2 left audio input (polyphonic).", "聲道 2 Left Audio 輸入（Polyphonic）。", "チャンネル2の左オーディオ入力（ポリフォニック）。"}});
        m.entries.push_back({"CH2 Right", {"Channel 2 right audio input (polyphonic).", "聲道 2 Right Audio 輸入（Polyphonic）。", "チャンネル2の右オーディオ入力（ポリフォニック）。"}});
        m.entries.push_back({"CH3 Left", {"Channel 3 left audio input (polyphonic).", "聲道 3 Left Audio 輸入（Polyphonic）。", "チャンネル3の左オーディオ入力（ポリフォニック）。"}});
        m.entries.push_back({"CH3 Right", {"Channel 3 right audio input (polyphonic).", "聲道 3 Right Audio 輸入（Polyphonic）。", "チャンネル3の右オーディオ入力（ポリフォニック）。"}});
        m.entries.push_back({"CH4 Left", {"Channel 4 left audio input (polyphonic).", "聲道 4 Left Audio 輸入（Polyphonic）。", "チャンネル4の左オーディオ入力（ポリフォニック）。"}});
        m.entries.push_back({"CH4 Right", {"Channel 4 right audio input (polyphonic).", "聲道 4 Right Audio 輸入（Polyphonic）。", "チャンネル4の右オーディオ入力（ポリフォニック）。"}});
        m.entries.push_back({"CH5 Left", {"Channel 5 left audio input (polyphonic).", "聲道 5 Left Audio 輸入（Polyphonic）。", "チャンネル5の左オーディオ入力（ポリフォニック）。"}});
        m.entries.push_back({"CH5 Right", {"Channel 5 right audio input (polyphonic).", "聲道 5 Right Audio 輸入（Polyphonic）。", "チャンネル5の右オーディオ入力（ポリフォニック）。"}});
        m.entries.push_back({"CH6 Left", {"Channel 6 left audio input (polyphonic).", "聲道 6 Left Audio 輸入（Polyphonic）。", "チャンネル6の左オーディオ入力（ポリフォニック）。"}});
        m.entries.push_back({"CH6 Right", {"Channel 6 right audio input (polyphonic).", "聲道 6 Right Audio 輸入（Polyphonic）。", "チャンネル6の右オーディオ入力（ポリフォニック）。"}});
        m.entries.push_back({"CH7 Left", {"Channel 7 left audio input (polyphonic).", "聲道 7 Left Audio 輸入（Polyphonic）。", "チャンネル7の左オーディオ入力（ポリフォニック）。"}});
        m.entries.push_back({"CH7 Right", {"Channel 7 right audio input (polyphonic).", "聲道 7 Right Audio 輸入（Polyphonic）。", "チャンネル7の右オーディオ入力（ポリフォニック）。"}});
        m.entries.push_back({"CH8 Left", {"Channel 8 left audio input (polyphonic).", "聲道 8 Left Audio 輸入（Polyphonic）。", "チャンネル8の左オーディオ入力（ポリフォニック）。"}});
        m.entries.push_back({"CH8 Right", {"Channel 8 right audio input (polyphonic).", "聲道 8 Right Audio 輸入（Polyphonic）。", "チャンネル8の右オーディオ入力（ポリフォニック）。"}});
        m.entries.push_back({"Chain Left", {"Left chain bus input from previous mixer module (polyphonic).", "來自前一個 Mixer 模組的 Left Chain Bus 輸入（Polyphonic）。", "前のミキサーモジュールからの左チェーンバス入力（ポリフォニック）。"}});
        m.entries.push_back({"Chain Right", {"Right chain bus input from previous mixer module (polyphonic).", "來自前一個 Mixer 模組的 Right Chain Bus 輸入（Polyphonic）。", "前のミキサーモジュールからの右チェーンバス入力（ポリフォニック）。"}});
        m.entries.push_back({"Return A Left", {"Stereo return A left input from external effects (polyphonic).", "來自外部 Effect 的 Stereo Return A Left 輸入（Polyphonic）。", "外部エフェクトからのステレオリターンA左入力（ポリフォニック）。"}});
        m.entries.push_back({"Return A Right", {"Stereo return A right input from external effects (polyphonic).", "來自外部 Effect 的 Stereo Return A Right 輸入（Polyphonic）。", "外部エフェクトからのステレオリターンA右入力（ポリフォニック）。"}});
        m.entries.push_back({"Return B Left", {"Stereo return B left input from external effects (polyphonic).", "來自外部 Effect 的 Stereo Return B Left 輸入（Polyphonic）。", "外部エフェクトからのステレオリターンB左入力（ポリフォニック）。"}});
        m.entries.push_back({"Return B Right", {"Stereo return B right input from external effects (polyphonic).", "來自外部 Effect 的 Stereo Return B Right 輸入（Polyphonic）。", "外部エフェクトからのステレオリターンB右入力（ポリフォニック）。"}});
        m.entries.push_back({"Send A Left", {"Stereo aux send A left output (polyphonic).", "Stereo Aux Send A Left 輸出（Polyphonic）。", "ステレオAuxセンドA左出力（ポリフォニック）。"}});
        m.entries.push_back({"Send A Right", {"Stereo aux send A right output (polyphonic).", "Stereo Aux Send A Right 輸出（Polyphonic）。", "ステレオAuxセンドA右出力（ポリフォニック）。"}});
        m.entries.push_back({"Send B Left", {"Stereo aux send B left output (polyphonic).", "Stereo Aux Send B Left 輸出（Polyphonic）。", "ステレオAuxセンドB左出力（ポリフォニック）。"}});
        m.entries.push_back({"Send B Right", {"Stereo aux send B right output (polyphonic).", "Stereo Aux Send B Right 輸出（Polyphonic）。", "ステレオAuxセンドB右出力（ポリフォニック）。"}});
        m.entries.push_back({"Mix Left", {"Final stereo mix left output (polyphonic). Sums returns and chain.", "最終 Stereo Mix Left 輸出（Polyphonic）。匯總 Return 與 Chain。", "最終ステレオミックス左出力（ポリフォニック）。リターンとチェーンを合計。"}});
        m.entries.push_back({"Mix Right", {"Final stereo mix right output (polyphonic). Sums returns and chain.", "最終 Stereo Mix Right 輸出（Polyphonic）。匯總 Return 與 Chain。", "最終ステレオミックス右出力（ポリフォニック）。リターンとチェーンを合計。"}});
        data["YAMANOTE"] = std::move(m);
    }
    // theKICK (27 entries)
    {
        ModuleHelpData m;
        m.name = "theKICK";
        m.description = {"Kick drum synthesizer with pitch sweep, 4-mode waveshaper, sample-based phase modulation, self-feedback FM, 4-pole LPF, accent input, and 2x oversampling", "Kick Drum Synthesizer，具備 Pitch Sweep、4 模式 Waveshaper、Sample Phase Modulation、Self-feedback FM、4-pole LPF、Accent 輸入與 2x Oversampling", "ピッチスイープ、4モードウェーブシェイパー、サンプルベース位相変調、セルフフィードバックFM、4ポールLPF、アクセント入力、2xオーバーサンプリング搭載キックドラムシンセサイザー"};
        m.entries.push_back({"PITCH", {"Base oscillator frequency, 20–200 Hz (default 47 Hz). CV input applies V/Oct scaling.", "Oscillator 基頻，20–200 Hz（預設 47 Hz）。CV 輸入以 V/Oct 方式縮放。", "オシレーター基本周波数、20–200 Hz（デフォルト47 Hz）。CV入力はV/Octスケーリング。"}});
        m.entries.push_back({"SWEEP", {"Pitch envelope amount added to base frequency, 0–500 Hz (default 260 Hz). Decays exponentially from trigger. CV input adds ±50 Hz per volt.", "加至基頻的 Pitch Envelope 量，0–500 Hz（預設 260 Hz）。從 Trigger 起指數衰減。CV 輸入每伏特 ±50 Hz。", "基本周波数に加算されるピッチエンベロープ量、0–500 Hz（デフォルト260 Hz）。トリガーから指数減衰。CV入力は1Vあたり±50 Hz。"}});
        m.entries.push_back({"BEND", {"Controls how fast the pitch sweep decays. Higher values produce a faster, snappier sweep. Range 0.5–4.0 (default 0.88). CV input adds ±0.35 per volt.", "控制 Pitch Sweep 衰減速度。數值越高衰減越快。範圍 0.5–4.0（預設 0.88）。CV 輸入每伏特 ±0.35。", "ピッチスイープの減衰速度を制御。高い値ほど速くシャープなスイープ。範囲0.5–4.0（デフォルト0.88）。CV入力は1Vあたり±0.35。"}});
        m.entries.push_back({"DECAY", {"Amplitude envelope decay time, 10–1000 ms (default 136 ms). Exponential decay shape. CV input adds ±100 ms per volt.", "振幅 Envelope Decay 時間，10–1000 ms（預設 136 ms）。指數 Decay 形狀。CV 輸入每伏特 ±100 ms。", "振幅エンベロープディケイ時間、10–1000 ms（デフォルト136 ms）。指数ディケイ形状。CV入力は1Vあたり±100 ms。"}});
        m.entries.push_back({"FOLD", {"Post-filter drive intensity, 0–10 (default 0.3). Below 0.01 the drive is bypassed. Uses tanh saturation with adjustable gain. Labeled DRIVE on the panel. CV input adds ±1 per volt.", "Post-filter Drive 強度，0–10（預設 0.3）。低於 0.01 時 Drive 被旁通。使用可調增益的 tanh Saturation。面板標籤為 DRIVE。CV 輸入每伏特 ±1。", "ポストフィルタードライブ強度、0–10（デフォルト0.3）。0.01未満でドライブはバイパス。調整可能ゲインのtanhサチュレーションを使用。パネル上のラベルはDRIVE。CV入力は1Vあたり±1。"}});
        m.entries.push_back({"MODE LED", {"Click to cycle through four waveshaper types. Right-click to select directly. Amber — PM: Phase Modulation (classic FM synthesis). Modulates oscillator phase for harmonic complexity.", "點按循環切換四種 Waveshaper 類型。右鍵可直接選擇。 琥珀色 — PM: Phase Modulation（經典 FM 合成）。調變 Oscillator 相位產生豐富 Harmonic。", "クリックで4種のウェーブシェイパータイプを順次切替。右クリックで直接選択。 アンバー — PM: Phase Modulation（クラシックFM合成）。オシレーター位相を変調しハーモニックの複雑さを生成。"}});
        m.entries.push_back({"RM", {"Ring Modulation. Multiplies signal with a secondary oscillator for metallic timbres.", "Ring Modulation。與次要 Oscillator 相乘產生金屬質感音色。", "Ring Modulation。信号を副オシレーターと乗算しメタリックな音色を生成。"}});
        m.entries.push_back({"AM", {"Amplitude Modulation. Modulates signal amplitude for sidebands and tremolo effects.", "Amplitude Modulation。調變訊號振幅產生邊帶與顫音效果。", "Amplitude Modulation。信号振幅を変調しサイドバンドとトレモロ効果を生成。"}});
        m.entries.push_back({"SYNC", {"Hard Sync. Resets oscillator phase on zero crossings for aggressive harmonic textures.", "Hard Sync。於零交越點重置 Oscillator 相位，產生激進 Harmonic 質感。", "Hard Sync。ゼロクロッシングでオシレーター位相をリセットしアグレッシブなハーモニックテクスチャを生成。"}});
        m.entries.push_back({"SAMPLE", {"Phase modulation amount from loaded WAV sample, 0–10 (default 0). The sample is read at the oscillator frequency and its value modulates the sine phase. The FM index follows the pitch envelope, decaying with BEND. Has no effect when no sample is loaded. CV input adds ±1 per volt.", "載入 WAV Sample 的 Phase Modulation 量，0–10（預設 0）。Sample 以 Oscillator 頻率讀取，其值調變 Sine Phase。FM Index 跟隨 Pitch Envelope，隨 BEND 衰減。未載入 Sample 時無效果。CV 輸入每伏特 ±1。", "読み込んだWAVサンプルからの位相変調量、0–10（デフォルト0）。サンプルはオシレーター周波数で読み取り、その値でサイン位相を変調。FMインデックスはピッチエンベロープに追従し、BENDに従い減衰。サンプル未読み込み時は効果なし。CV入力は1Vあたり±1。"}});
        m.entries.push_back({"LOAD button", {"Click to open a file dialog for loading a WAV sample. When loaded, displays the scrolling filename. Right-click to clear. The WAV is resampled to a 1024-point table.", "Click to open a file dialog for loading a WAV sample. When loaded, displays the scrolling filename. Right-click to clear. The WAV is resampled to a 1024-point table.", "Click to open a file dialog for loading a WAV sample. When loaded, displays the scrolling filename. Right-click to clear. The WAV is resampled to a 1024-point table."}});
        m.entries.push_back({"FEEDBACK", {"Self-feedback phase modulation amount, 0–1.0 (default 0). Uses the average of the previous two output samples fed back into the oscillator phase, following the Yamaha DX approach. Low values add subtle harmonics; higher values produce chaotic, metallic tones. CV input adds ±0.1 per volt.", "Self-feedback Phase Modulation 量，0–1.0（預設 0）。使用前兩個輸出 Sample 的平均值回饋至 Oscillator 相位，遵循 Yamaha DX 方式。低值加入微妙 Harmonic；高值產生混沌的金屬音色。CV 輸入每伏特 ±0.1。", "セルフフィードバック位相変調量、0–1.0（デフォルト0）。前2サンプルの出力平均をオシレーター位相にフィードバック（ヤマハDX方式）。低値は微妙なハーモニクスを追加、高値はカオスで金属的な音色を生成。CV入力は1Vあたり±0.1。"}});
        m.entries.push_back({"TONE", {"Low-pass filter cutoff, mapped logarithmically from 40 Hz to 20 kHz (knob range 0–10, default 10 = fully open). 4-pole cascaded one-pole filters with tan() frequency warping provide 24 dB/oct rolloff. CV input adds ±1 per volt.", "Low-pass Filter 截止頻率，對數映射從 40 Hz 至 20 kHz（旋鈕範圍 0–10，預設 10 = 全開）。4-pole 串接 One-pole Filter 含 tan() 頻率 Warping，提供 24 dB/oct Rolloff。CV 輸入每伏特 ±1。", "ローパスフィルターカットオフ、40 Hz–20 kHzの対数マッピング（ノブ範囲0–10、デフォルト10 = 全開）。tan()周波数ワーピング付き4ポールカスケードワンポールフィルターで24 dB/octロールオフ。CV入力は1Vあたり±1。"}});
        m.entries.push_back({"TRIG", {"Trigger input. Schmitt trigger detection (0.1V low, 2V high). Resets all envelopes, oscillator phase, and filter state.", "Trigger 輸入。Schmitt Trigger 偵測（0.1V 低、2V 高）。重置所有 Envelope、Oscillator 相位與 Filter 狀態。", "トリガー入力。シュミットトリガー検出（0.1V低、2V高）。全エンベロープ、オシレーター位相、フィルター状態をリセット。"}});
        m.entries.push_back({"ACCENT", {"Velocity-style input (0–10V). Voltage is sampled at the moment of trigger and scaled to 0–1, then multiplied with the output amplitude. When disconnected, defaults to 1.0 (full level).", "Velocity 風格輸入（0–10V）。Trigger 瞬間取樣電壓，縮放至 0–1 後乘以輸出振幅。未連接時預設 1.0（全音量）。", "ベロシティスタイル入力（0–10V）。トリガーの瞬間に電圧をサンプリングし0–1にスケーリング、出力振幅に乗算。未接続時はデフォルト1.0（フルレベル）。"}});
        m.entries.push_back({"PITCH CV", {"V/Oct pitch modulation. 1V doubles the frequency.", "V/Oct Pitch 調變。1V 使頻率加倍。", "V/Octピッチ変調。1Vで周波数が2倍。"}});
        m.entries.push_back({"SWEEP CV", {"Sweep amount modulation (±50 Hz/V).", "Sweep 量調變（±50 Hz/V）。", "スイープ量変調（±50 Hz/V）。"}});
        m.entries.push_back({"BEND CV", {"Bend curve modulation (±0.35/V).", "Bend 曲線調變（±0.35/V）。", "ベンドカーブ変調（±0.35/V）。"}});
        m.entries.push_back({"DECAY CV", {"Decay time modulation (±100 ms/V).", "Decay 時間調變（±100 ms/V）。", "ディケイ時間変調（±100 ms/V）。"}});
        m.entries.push_back({"FOLD CV", {"Waveshaper drive modulation (±1/V).", "Waveshaper Drive 調變（±1/V）。", "ウェーブシェイパードライブ変調（±1/V）。"}});
        m.entries.push_back({"SAMPLE CV", {"Sample PM amount modulation (±1/V).", "Sample PM 量調變（±1/V）。", "サンプルPM量変調（±1/V）。"}});
        m.entries.push_back({"FB CV", {"Feedback amount modulation (±0.1/V).", "Feedback 量調變（±0.1/V）。", "フィードバック量変調（±0.1/V）。"}});
        m.entries.push_back({"TONE CV", {"Filter cutoff modulation (±1/V).", "Filter 截止頻率調變（±1/V）。", "フィルターカットオフ変調（±1/V）。"}});
        m.entries.push_back({"OUT", {"Kick drum audio output (±5V peak).", "Kick Drum Audio 輸出（±5V Peak）。", "キックドラムオーディオ出力（±5Vピーク）。"}});
        m.entries.push_back({"2x Oversample", {"Toggle 2x oversampling. When enabled, the DSP core runs at double the sample rate and is downsampled via SST HalfRateFilter. Reduces aliasing from waveshaping and feedback at the cost of higher CPU usage.", "切換 2x Oversampling。啟用時 DSP 核心以雙倍 Sample Rate 運行，透過 SST HalfRateFilter 降採樣。減少 Waveshaping 與 Feedback 的混疊，CPU 使用量較高。", "2xオーバーサンプリングの切替。有効時はDSPコアがダブルサンプルレートで動作し、SST HalfRateFilterでダウンサンプリング。ウェーブシェイピングとフィードバックのエイリアシングを低減、CPU使用量は増加。"}});
        m.entries.push_back({"Panel theme and contrast", {"Standard MADZINE theme settings.", "標準 MADZINE 主題設定。", "標準MADZINEテーマ設定。"}});
        m.entries.push_back({"FM Mode", {"FM interaction mode selector. Click to cycle through 4 modes: PM (phase modulation), RM (ring modulation), AM (amplitude modulation), SYNC (hard sync).", "FM 交互模式選擇器。點擊循環 4 種模式：PM（相位調變）、RM（環形調變）、AM（振幅調變）、SYNC（硬同步）。", "FM相互作用モードセレクター。クリックで4モードを切替: PM（位相変調）、RM（リング変調）、AM（振幅変調）、SYNC（ハードシンク）。"}});
        data["theKICK"] = std::move(m);
    }
    return data;
}
